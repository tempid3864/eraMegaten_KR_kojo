;-------------------------------------------------
;마이ルド구상
;おっとり？　原型を留めてないです。許してください。
;オンナ、처녀、겁쟁이、꿋꿋함、프라이드높음、자제심、호기심、회복빠름、불결무시、매력。
;-------------------------------------------------

;-------------------------------------------------
;eraMegaten전용 구상 템플릿
;4972와 X2를 캐릭터 번호로 치환하고, Y1을 조수의 캐릭터 번호로 치환해주세요
;단, 캐릭터번호가 한자리인 캐릭터의 경우, 4972, Y1은 1, X2는 01과 같이 바꿔주세요
;그리고 커맨드 내의 적당한 조건 아래의 PRINTFORMW의 뒤에 반각 공백을 두고 대사를 추가해주세요
;구상을 추가하지 않는 커맨드는 무기입하셔도 상관없습니다만, CFLAG:XXX의 앞에『;』를 붙여서 주석처리를 하시면 더욱 좋습니다
;엑스트라 구문 등, 주석처리가 되어 있는 구문을 사용할 때는,『;』를 제거해 주석처리를 해제할 필요가 있습니다
;마지막으로 read me를 건드려 보시는 것도 추천드립니다
;-------------------------------------------------

;-------------------------------------------------
;범용 구상의 작성에 사용하는 경우는, 4972→캐릭터번호가 아니라,
;K4972→PUB회화타입번호　로 변경해서 사용해주세요
;-------------------------------------------------
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	Module		:EVENT_K4972_ASSI.ERB
;	Facility	:회화타입：조수時구상
;
;	Modification Data:
;	Edit	Date			Author					Reason
;	001		20XX/XX/XX		Jガン					新規作成
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;ライセンスについて
;	・基本項目(許可する項目を"○"に、許可しない項目を"×"に変更してください。)
;	+----+----+-------------------------------+
;	|番号|許可|ライセンス内容                 |
;	+----+----+-------------------------------+
;	|   1| ○ | 処理上のバグ修正              |
;	+----+----+-------------------------------+
;	|   2| ○ | 口上の誤字・脱字の修正        |
;	+----+----+-------------------------------+
;	|   3| ○ | 口上まとめへの収録            |
;	+----+----+-------------------------------+
;	|   4| ○ | 口上の新規追加                |
;	+----+----+-------------------------------+
;	|   5| ○ | 既存口上の改変                |
;	+----+----+-------------------------------+
;	|   6| ○ | 改変した口上の自由な再配布    |
;	+----+----+-------------------------------+
;	|   7| ○ | 口上内容の転載                |
;	+----+----+-------------------------------+

;--------------------------------------------------
;구상셀렉트 기능：구상설명
;--------------------------------------------------
;이 함수의 주석처리를 해제하면, 구상의 선택시에 선택지로 후보에 오릅니다
;이미 구상이 있는 캐릭터나 회화타입의 다른 버전의 구상을 작성할 때 이용해주세요
;이 때, 이 함수를 포함한 이 파일 내의 함수명 전체에 _임의의1-49(범용구상이면 1-9)의 숫자를 추가해주세요
;例： EVENTTRAIN_K4972 → EVENTTRAIN_K4972_12
;--------------------------------------------------
;@셀렉트구상설명_K4972,ARG = 0
;SIF ARG == 1
;	RETURN 0
;;구상명
;PRINTFORML ここに구상の명칭を記述してください
;;구상설명문
;PRINTFORML    ここに구상の簡가운な설명や注意点などを記述してください

;--------------------------------------------------
;EVENTTRAIN 관련（4972을 캐릭터번호로 치환）
;조교 개시시의 대화 CFLAG 201～219를 사용
;--------------------------------------------------
@EVENTTRAIN_K4972
;--------------------------------------------------
;엑스트라모드 구문　CFLAG:220을 사용
;네 를 선택시 첫조교시 구상설정용 소질로 변경 가능합니다
;아래에 처녀화할 경우의 구문을 샘플로 올려두겠습니다
;--------------------------------------------------
;IF CFLAG:220 == 0
;	PRINTL 処女モードに移行しますか？
;	PRINTL [0] はい
;	PRINTL [1] いいえ
;	$INPUT_LOOP
;	INPUT
;	IF RESULT == 0
;		PRINTFORMW ―処女モードに素質、能力を変更します―
;		TALENT:TARGET:0 = 1
;		ABL:TARGET:4 = 0
;		EXP:TARGET:0 = 0
;	ELSEIF RESULT == 1
;		PRINTFORMW ―通常モードで調教を開始します―
;	ELSE
;		GOTO INPUT_LOOP
;	ENDIF
;	CFLAG:220 = 1
;ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4972을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTTRAIN_PY1_K4972
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL EVENTTRAIN_PY1_K4972
;		RETURN 0
;	ENDIF
;ENDIF
;--------------------------------------------------
;간이 조수구상 발동시에 첫조교시 구상을 스킵（Y1을 조수의 캐릭터번호로 한다）
;여러 명이 있을 경우는, SIF NO:ASSI == Y1 || NO:ASSI == Z1으로 추가해 나간다
;--------------------------------------------------
;IF CFLAG:201 == 0 && ASSI > 0
;	SIF NO:ASSI == Y1
;		CFLAG:201 =1
;ENDIF
;-------------------------------------------------
;조교 개시시（1회만） CFLAG201
;-------------------------------------------------
DRAWLINE
;첫조교시
IF CFLAG:201 == 0 && TALENT:연모 == 0
	CFLAG:201 = 1
	PRINTFORMW %CALLNAME%を조교部屋に呼び出し、鍵を掛けた。
	PRINTFORMW 「え…、조교…？」
	PRINTFORMW 「そういうのって악마とか、攫ってきた子にするんじゃないの？」
	PRINTFORMW 「……」
	PRINTFORMW 「…はぁ。%CSTR:이인칭%にとっては、%CSTR:일인칭%も攫われてきた子なんですね…」
	PRINTFORMW 「嫌だけど… でも痛くされるよりは気持ちよくされたほうがいいかな…」
	RETURN 0
;굴복각인Lv1
ELSEIF MARK:2 == 1
	PRINTFORMW 「악마に売られた女の子達を見たことあるけど、あれは酷かったわ」
	PRINTFORMW 「鎖に繋がれて、朝から晩までかわるがわる犯されて、짐승みたいに扱われるの」
	PRINTFORMW 「注がれたMAGのせいか、凄く可愛くてドキッとしちゃったけど」
	PRINTFORMW 「%CSTR:일인칭%もああなっちゃうの…？」
	CFLAG:201 = 2
	RETURN 0
;굴복각인Lv2
ELSEIF MARK:2 == 2
	PRINTFORMW 「%CSTR:일인칭%の同期、みんな악마との섹스に嵌って破滅したの」
	PRINTFORMW 「%CSTR:일인칭%は겁쟁이だから、逃げるように別部署に入ったんだけど…」
	PRINTFORMW 「%CSTR:이인칭%は大丈남편なのね。むしろ破滅させる側ね」
	PRINTFORMW 「まずいのに捕まっちゃったかな…」
	CFLAG:201 = 3
	RETURN 0
;굴복각인Lv3
ELSEIF CFLAG:201 < 4 && MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	PRINTFORMW 「%CSTR:일인칭%の体。조교が嫌じゃなくなってる…」
	PRINTFORMW 「%CSTR:일인칭%の中のいやらしい%CSTR:일인칭%が、%CSTR:이인칭%に굴복しろって囁くの」
	PRINTFORMW 「だから帰って。もう%CSTR:일인칭%を気持ちよくしないで」
	PRINTFORMW 「……ううん。嘘。今日も可愛がってください……」
;	CFLAG:201 = 4
	RETURN 0
;함락
ELSEIF CFLAG:201 < 5 && (TALENT:3 || TALENT:4 || TALENT:5)
	;연모
	IF TALENT:3
		IF CFLAG:미조교함락 == 1
			;조교하지 않고 연모를 단 경우
			PRINTFORMW 
		ELSE
			PRINTFORMW 
		ENDIF
	;음란
	ELSEIF TALENT:4
			PRINTFORMW 「%CSTR:일인칭%、悟っちゃったの」
			PRINTFORMW 「今後一生、何をしても、섹스の喜びを超えるものなんて無いんだって」
			PRINTFORMW 「嫌な記憶も不安も、みんなイっちゃえば忘れられるの」
			PRINTFORMW 「女の子に生まれてよかったの… 寝ても깨달음めてもいやらしいことを考えているの」
			PRINTFORMW 「%CSTR:이인칭%に犯されたり、악마に犯されたり…」
			PRINTFORMW 「可愛い子を犯したり、정액を浴びたり、음란と罵られたりしたいの」
			PRINTFORMW 「そしてね、%CSTR:이인칭%に最高の쾌락をあげたいの。一緒にずっと쾌락に溺れていたいの…」
	;복종
	ELSEIF TALENT:5
		PRINTFORMW 
	ENDIF
	CFLAG:201 = 5
	RETURN 0
ENDIF

;-------------------------------------------------
;간이 조수구상（Y1을 조수의 캐릭터번호로 치환, 4972을 캐릭터번호로 변경한다) CFLAG203
;말할 캐릭터를 늘릴 경우, 구문을 복사＆붙여넣기 해, CFLAG의 수를 204, 205로 늘린다
;-------------------------------------------------
;조수의 유무를 체크（없을 경우는 2번째 이후로 건너뛴다）
IF ASSI < 0 
	CALL EVENTTRAIN_SECOND_K4972
;조수
;ELSEIF NO:ASSI == Y1
	;처음
;	IF CFLAG:203 == 0
		;함락완료＋함락시 첫 구상이 발생완료
;		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && CFLAG:201 >= 5
;			PRINTFORMW 
		;그 이외
;		ELSE
;			PRINTFORMW 
;		ENDIF
;		CFLAG:203 = 1
	;2번째 이후
;	ELSEIF CFLAG:203 < 2 || FLAG:7 ==2
		;함락완료
;		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
;			PRINTFORMW 
		;그 이외
;		ELSE
;			PRINTFORMW 
;		ENDIF
;		CFLAG:203 = 2
;	ENDIF
;구상이 있는 조수가 없을 경우, 통상의 2번째 이후의 구상으로 건너뛴다
ELSE
	CALL EVENTTRAIN_SECOND_K4972
ENDIF

;-------------------------------------------------
;조교 개시시（2번째 이후, 4972을 캐릭터번호로 변경한다）
;-------------------------------------------------
@EVENTTRAIN_SECOND_K4972
#DIM GYOU

FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:202 < 4 || FLAG:7 == 2)
	PRINTFORML 
	CFLAG:202 = 4
;굴복각인Lv0
ELSEIF MARK:2 == 0 && (CFLAG:202 < 1 || FLAG:7 == 2)
	PRINTFORML 
	CFLAG:202 = 1
;굴복각인Lv1
ELSEIF MARK:2 == 1 && (CFLAG:202 < 2 || FLAG:7 == 2)
	PRINTFORML 
	CFLAG:202 = 2
;굴복각인Lv2
ELSEIF MARK:2 == 2 && (CFLAG:202 < 3 || FLAG:7 == 2)
	PRINTFORML 
	CFLAG:202 = 3
;굴복각인Lv3＋함락소질없음
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0) && (CFLAG:202 < 4 || FLAG:7 == 2)
	PRINTFORML 
	CFLAG:202 = 4
;함락완료
ELSEIF CFLAG:202 < 5 || FLAG:7 == 2
	;연모, 친애
	IF TALENT:3 || TALENT:6
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			LOCALS:0 = 
		ELSEIF RAND:2 == 0
			LOCALS:0 = 
		ELSE
			LOCALS:0 = 
		ENDIF
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			LOCALS:0 = 
		ELSEIF RAND:2 == 0
			LOCALS:0 = 
		ELSE
			LOCALS:0 = 
		ENDIF
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			LOCALS:0 = 
		ELSEIF RAND:2 == 0
			LOCALS:0 = 
		ELSE
			LOCALS:0 = 
		ENDIF
	ENDIF
	;그 외, 공통
	IF LOCALS:0 == ""
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			PRINTFORML 
		ELSEIF RAND:2 == 0
			PRINTFORML 
		ELSE
			PRINTFORML 
		ENDIF
	ENDIF
	CFLAG:202 = 5
ENDIF
 
FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM 
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:GYOU == 0
	PRINTW
RETURN 0

;-------------------------------------------------
;EVENTEND 관련（4972을 캐릭터번호로 치환）
;조교 종료시의 대사
;-------------------------------------------------
@EVENTEND_K4972
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4972을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTEND_PY1_K4972
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	SIF NO:ASSI == Y1
;	CALL EVENTEND_PY1_K4972
;	RETURN 0
;ENDIF
;--------------------------------------------------
;조교 종료시의 대사
;--------------------------------------------------
DRAWLINE
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:217 < 1 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:217 = 1
;굴복각인Lv1 이하
ELSEIF MARK:2 <= 1 && (CFLAG:219 < 1 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:219 = 1
;굴복각인Lv2
ELSEIF MARK:2 == 2
	PRINTFORMW 
	CFLAG:219 = 2
;굴복각인Lv3＋함락소질없음
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	PRINTFORMW 
	CFLAG:219 = 3
;함락완료＋체력 500미만
ELSEIF BASE:0 < 500 && (CFLAG:218 < 1 || FLAG:7 == 2)
	;연모, 친애
	IF TALENT:3 || TALENT:6
		PRINTFORMW 
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		PRINTFORMW 
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		PRINTFORMW 
	ENDIF
	CFLAG:218 = 1
;함락완료
ELSEIF CFLAG:219 < 4 || FLAG:7 == 2
	;연모, 친애
	IF TALENT:3 || TALENT:6
		PRINTFORMW 
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		PRINTFORMW 
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		PRINTFORMW 
	ENDIF
	CFLAG:219 = 4
ENDIF
RETURN 0

;--------------------------------------------------
;@KOJO_MESSAGE_COM 관련（4972을 캐릭터번호로 치환）
;커맨드 실행시에 출력됩니다
;--------------------------------------------------
@KOJO_MESSAGE_COM_K4972
#DIM GYOU
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL TRAIN_MESSAGE
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4972을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_COM_PY1_K4972
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_COM_PY1_K4972
;		RETURN 0
;	ENDIF
;ENDIF
 
;-------------------------------------------------
;LOCALS
;-------------------------------------------------

FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT
;-------------------------------------------------
;커맨드 실행시의 대사 CFLAG 300～500
;-------------------------------------------------
SELECTCASE SELECTCOM
;=================================================
;애무계 커맨드
;=================================================
;-------------------------------------------------
;0,애무
;-------------------------------------------------
	CASE 0
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;처음
			IF CFLAG:300 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:300 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:300 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:300 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:300 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:300 = 3
				ENDIF
				;그 이외
				IF (CFLAG:300 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:300 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
;-------------------------------------------------
;1,커닐링구스
;-------------------------------------------------
	CASE 1
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:301 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:301 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:301 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:301 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:301 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:301 = 3
			ENDIF
			;그 이외
			IF (CFLAG:301 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:301 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;2,펠라한다
;-------------------------------------------------
	CASE 2
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:302 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:302 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:302 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:302 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:302 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:302 = 3
			ENDIF
			;그 이외
			IF (CFLAG:302 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:302 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;3,손가락삽입
;-------------------------------------------------
	CASE 3
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:303 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:303 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:303 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:303 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:303 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:303 = 3
			ENDIF
			;그 이외
			IF (CFLAG:303 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:303 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;4,애널핥기
;-------------------------------------------------
	CASE 4
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:304 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:304 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:304 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:304 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:304 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:304 = 3
			ENDIF
			;그 이외
			IF (CFLAG:304 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:304 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;5,애널애무
;-------------------------------------------------
	CASE 5
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;처음
			IF CFLAG:305 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:305 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:305 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:305 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:305 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:305 = 3
				ENDIF
				;그 이외
				IF (CFLAG:305 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:305 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
;-------------------------------------------------
;6,가슴주무르기
;-------------------------------------------------
	CASE 6
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;처음
			IF CFLAG:306 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:306 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:306 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:306 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:306 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:306 = 3
				ENDIF
				;그 이외
				IF (CFLAG:306 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:306 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:306 == 1
				CFLAG:306 = 0
			K = 306
			GOTO 가슴系共通
		ENDIF
;-------------------------------------------------
;7,유두자극
;-------------------------------------------------
	CASE 7
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;처음
			IF CFLAG:307 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:307 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:307 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:307 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:307 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:307 = 3
				ENDIF
				;그 이외
				IF (CFLAG:307 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:307 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:307 == 1
				CFLAG:307 = 0
			K = 307
			GOTO 가슴系共通
		ENDIF
;-------------------------------------------------
;8,가슴애무
;-------------------------------------------------
	CASE 8
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;처음
			IF CFLAG:308 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:308 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:308 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:308 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:308 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:308 = 3
				ENDIF
				;그 이외
				IF (CFLAG:308 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:308 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:308 == 1
				CFLAG:308 = 0
			K = 308
			GOTO 가슴系共通
		ENDIF
;-------------------------------------------------
;9,조개젖히기
;-------------------------------------------------
	CASE 9
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:309 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:309 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:309 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:309 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:309 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:309 = 3
			ENDIF
			;그 이외
			IF (CFLAG:309 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:309 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;10,가슴자위
;-------------------------------------------------
	CASE 10
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:310 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:310 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:310 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:310 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:310 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:310 = 3
			ENDIF
			;그 이외
			IF (CFLAG:310 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:310 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;11,자위
;-------------------------------------------------
	CASE 11
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:311 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:311 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:311 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:311 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:311 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:311 = 3
			ENDIF
			;그 이외
			IF (CFLAG:311 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:311 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;12,날개애무
;-------------------------------------------------
	CASE 12
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;처음
			IF CFLAG:435 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:435 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:435 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:435 = 4
				ENDIF
				;순종Lv3 이상
				IF ABL:0 >= 3 && (CFLAG:435 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:435 = 3
				ENDIF
				;그 이외
				IF (CFLAG:435 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:435 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
;-------------------------------------------------
;13,꼬리애무
;-------------------------------------------------
	CASE 13
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;처음
			IF CFLAG:436 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:436 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:436 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:436 = 4
				ENDIF
				;순종Lv3 이상
				IF ABL:0 >= 3 && (CFLAG:436 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:436 = 3
				ENDIF
				;그 이외
				IF (CFLAG:436 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:436 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
;-------------------------------------------------
;14,클리애무
;-------------------------------------------------
	CASE 14
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:442 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:442 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:442 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:442 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:442 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:442 = 3
			ENDIF
			;그 이외
			IF (CFLAG:442 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:442 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;15,엉덩이애무
;-------------------------------------------------
	CASE 15
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;처음
			IF CFLAG:444 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:444 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:444 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:444 = 4
				ENDIF
				;순종Lv3 이상
				IF ABL:0 >= 3 && (CFLAG:444 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:444 = 3
				ENDIF
				;그 이외
				IF (CFLAG:444 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:444 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
;-------------------------------------------------
;가슴系共通
;6가슴주무르기　7유두자극　8가슴애무
;-------------------------------------------------
	CASE 11111
		$가슴系共通
		;처음
		IF CFLAG:K == 0
			PRINTFORML 
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;그 이외
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF
;=================================================
;커뮤니케이션계 커맨드
;=================================================
;-------------------------------------------------
;20,키스한다
;-------------------------------------------------
	CASE 20
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:312 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:312 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:312 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:312 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:312 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:312 = 3
			ENDIF
			;그 이외
			IF (CFLAG:312 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:312 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;21,아무것도안한다
;-------------------------------------------------
	CASE 21
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:313 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:313 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:313 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:313 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:313 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:313 = 3
			ENDIF
			;그 이외
			IF (CFLAG:313 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:313 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;22,회화　연모, 친애만
;-------------------------------------------------
	CASE 22
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:314 == 0
			PRINTFORML 
			CFLAG:314 = 1
		;2번째 이후
		ELSEIF CFLAG:314 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:314 = 2
		ENDIF
;-------------------------------------------------
;23,프레젠트　연모, 친애만
;-------------------------------------------------
	CASE 23
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:315 == 0
			PRINTFORML 
			CFLAG:315 = 1
		;2번째 이후
		ELSEIF CFLAG:315 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:315 = 2
		ENDIF
;-------------------------------------------------
;24,스킨쉽　연모, 친애만
;-------------------------------------------------
	CASE 24
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:316 == 0
			PRINTFORML 
			CFLAG:316 = 1
		;2번째 이후
		ELSEIF CFLAG:316 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:316 = 2
		ENDIF
;-------------------------------------------------
;25,느긋하게있는다　연모, 친애만
;-------------------------------------------------
	CASE 25
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:317 == 0
			PRINTFORML 
			CFLAG:317 = 1
		;2번째 이후
		ELSEIF CFLAG:317 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:317 = 2
		ENDIF
;-------------------------------------------------
;26,가사　연모, 친애만
;-------------------------------------------------
	CASE 26
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:318 == 0
			PRINTFORML 
			CFLAG:318 = 1
		;2번째 이후
		ELSEIF CFLAG:318 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:318 = 2
		ENDIF
;-------------------------------------------------
;27,노래연습　연모, 친애만
;-------------------------------------------------
	CASE 27
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:319 == 0
			PRINTFORML 
			CFLAG:319 = 1
		;2번째 이후
		ELSEIF CFLAG:319 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:319 = 2
		ENDIF
;-------------------------------------------------
;28,매도
;-------------------------------------------------
	CASE 28
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:320 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:320 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:320 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:320 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:320 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:320 = 3
			ENDIF
			;그 이외
			IF (CFLAG:320 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:320 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;29,비디오감상　함락완료만
;-------------------------------------------------
	CASE 29
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:321 == 0
			;연모, 친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란, 창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종, 예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락완료共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:321 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;음란, 창부、음란도1000이상1/4
			IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
				LOCALS:0 =
			ENDIF
			;연모, 친애
			IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
				LOCALS:0 =
			;복종, 예속
			ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;함락완료共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:321 = 2
		ENDIF
;-------------------------------------------------
;30,파후파후　함락완료만
;-------------------------------------------------
	CASE 30
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:422 == 0
			;연모, 친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란, 창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종, 예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락완료共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:422 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;음란, 창부、음란도1000이상1/4
			IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
				LOCALS:0 =
			ENDIF
			;연모, 친애
			IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
				LOCALS:0 =
			;복종, 예속
			ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;함락완료共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:422 = 2
		ENDIF
;=================================================
;도구사용 커맨드
;=================================================
;-------------------------------------------------
;40,로터
;-------------------------------------------------
	CASE 40
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:322 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:322 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:322 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:322 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:322 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:322 = 3
			ENDIF
			;그 이외
			IF (CFLAG:322 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:322 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;41,전기마사지기
;-------------------------------------------------
	CASE 41
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:323 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:323 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:323 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:323 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:323 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:323 = 3
			ENDIF
			;그 이외
			IF (CFLAG:323 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:323 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;42,클리캡
;-------------------------------------------------
	CASE 42
		CALL TRAIN_MESSAGE
		IF TEQUIP:11
			;처음
			IF CFLAG:324 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:324 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:324 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:324 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:324 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:324 = 3
				ENDIF
				;그 이외
				IF (CFLAG:324 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:324 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;43,오나홀
;-------------------------------------------------
	CASE 43
		CALL TRAIN_MESSAGE
		IF TEQUIP:12
			;처음
			IF CFLAG:325 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:325 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:325 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:325 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:325 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:325 = 3
				ENDIF
				;그 이외
				IF (CFLAG:325 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:325 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;44,바이브
;-------------------------------------------------
	CASE 44
		CALL TRAIN_MESSAGE
		IF TEQUIP:13
			;처음
			IF CFLAG:326 == 0
				;처녀
				IF TALENT:0
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;연모, 친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란, 창부
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종, 예속
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 =
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 =
						ENDIF
					ENDIF
					;그 이외
					IF LOCALS:0 == ""
						PRINTFORML 
						LOCALS:0 =
					ENDIF
				;비처녀
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;연모, 친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란, 창부
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종, 예속
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 =
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 =
						ENDIF
					ENDIF
					;그 이외
					IF LOCALS:0 == ""
						PRINTFORML 
						LOCALS:0 =
					ENDIF
				ENDIF
				CFLAG:326 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:326 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:326 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:326 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:326 = 3
				ENDIF
				;그 이외
				IF (CFLAG:326 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:326 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;45,애널바이브
;-------------------------------------------------
	CASE 45
		CALL TRAIN_MESSAGE
		IF TEQUIP:14
			;처음
			IF CFLAG:327 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:327 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:327 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:327 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:327 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:327 = 3
				ENDIF
				;그 이외
				IF (CFLAG:327 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:327 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;46,애널비즈
;-------------------------------------------------
	CASE 46
		CALL TRAIN_MESSAGE
		IF TEQUIP:15
			;처음
			IF CFLAG:328 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:328 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:328 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:328 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:328 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:328 = 3
				ENDIF
				;그 이외
				IF (CFLAG:328 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:328 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;47,유두캡
;-------------------------------------------------
	CASE 47
		CALL TRAIN_MESSAGE
		IF TEQUIP:16
			;처음
			IF CFLAG:329 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:329 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:329 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:329 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:329 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:329 = 3
				ENDIF
				;그 이외
				IF (CFLAG:329 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:329 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;48,유두로터
;-------------------------------------------------
	CASE 48
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:330 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:330 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:330 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:330 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:330 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:330 = 3
			ENDIF
			;그 이외
			IF (CFLAG:330 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:330 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;49,착유기
;-------------------------------------------------
	CASE 49
		CALL TRAIN_MESSAGE
		IF TEQUIP:17
			;처음
			IF CFLAG:331 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:331 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:331 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:331 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:331 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:331 = 3
				ENDIF
				;그 이외
				IF (CFLAG:331 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:331 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;=================================================
;조교자도구계コマンド
;=================================================
;-------------------------------------------------
;50,클리캡쓴다
;-------------------------------------------------
	CASE 50
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:11
			;처음
			IF CFLAG:453 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:453 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:453 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:453 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:453 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:453 = 3
				ENDIF
				;그 이외
				IF (CFLAG:453 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:453 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:453 == 1
				CFLAG:453 = 0
			K = 453
			L = 11
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;51,오나홀쓴다
;-------------------------------------------------
	CASE 51
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:12
			;처음
			IF CFLAG:454 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:454 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:454 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:454 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:454 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:454 = 3
				ENDIF
				;그 이외
				IF (CFLAG:454 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:454 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:454 == 1
				CFLAG:454 = 0
			K = 454
			L = 12
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;52,바이브쓴다
;-------------------------------------------------
	CASE 52
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:13
			;처음
			IF CFLAG:455 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:455 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:455 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:455 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:455 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:455 = 3
				ENDIF
				;그 이외
				IF (CFLAG:455 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:455 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:455 == 1
				CFLAG:455 = 0
			K = 455
			L = 13
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;53,애널바이브쓴다
;-------------------------------------------------
	CASE 53
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:14
			;처음
			IF CFLAG:456 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:456 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:456 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:456 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:456 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:456 = 3
				ENDIF
				;그 이외
				IF (CFLAG:456 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:456 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:456 == 1
				CFLAG:456 = 0
			K = 456
			L = 14
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;54,애널비즈쓴다
;-------------------------------------------------
	CASE 54
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:15
			;처음
			IF CFLAG:457 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:457 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:457 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:457 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:457 = 3
				ENDIF
				;그 이외
				IF (CFLAG:457 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:457 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:457 == 1
				CFLAG:457 = 0
			K = 457
			L = 15
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;55,유두캡쓴다
;-------------------------------------------------
	CASE 55
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:16
			;처음
			IF CFLAG:458 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:458 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:458 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:458 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:458 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:458 = 3
				ENDIF
				;그 이외
				IF (CFLAG:458 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:458 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:458 == 1
				CFLAG:458 = 0
			K = 458
			L = 16
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;56,착유기쓴다
;-------------------------------------------------
	CASE 56
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:17
			;처음
			IF CFLAG:459 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:459 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:459 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:459 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:459 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:459 = 3
				ENDIF
				;그 이외
				IF (CFLAG:459 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:459 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:459 == 1
				CFLAG:459 = 0
			K = 459
			L = 17
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;조교자도구계コマンド共通
;50클리캡쓴다　51오나홀쓴다　　52바이브쓴다
;53애널바이브쓴다　54애널비즈쓴다　55유두캡쓴다
;56착유기쓴다
;-------------------------------------------------
	CASE 11111
		$조교자도구계コマンド共通
		IF TEQUIP:PLAYER:L
			;처음
			IF CFLAG:K == 0
				PRINTFORML 
				CFLAG:K = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:K = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:K = 3
				ENDIF
				;그 이외
				IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					CFLAG:K = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
		ENDIF
;=================================================
;섹스계 커맨드
;=================================================
;-------------------------------------------------
;60,정상위
;-------------------------------------------------
	CASE 60
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:332 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:332 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:332 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:332 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:332 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:332 = 3
			ENDIF
			;그 이외
			IF (CFLAG:332 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:332 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:332 == 1
				CFLAG:332 = 0
			K = 332
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;61,후배위
;-------------------------------------------------
	CASE 61
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:333 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:333 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:333 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:333 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:333 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:333 = 3
			ENDIF
			;그 이외
			IF (CFLAG:333 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:333 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:333 == 1
				CFLAG:333 = 0
			K = 333
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;62,정상위애널
;-------------------------------------------------
	CASE 62
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:334 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:334 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:334 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:334 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:334 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:334 = 3
			ENDIF
			;그 이외
			IF (CFLAG:334 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:334 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:334 == 1
				CFLAG:334 = 0
			K = 334
			GOTO 애널섹스계共通
		ENDIF
;-------------------------------------------------
;63,후배위애널
;-------------------------------------------------
	CASE 63
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:335 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:335 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:335 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:335 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:335 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:335 = 3
			ENDIF
			;그 이외
			IF (CFLAG:335 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:335 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:335 == 1
				CFLAG:335 = 0
			K = 335
			GOTO 애널섹스계共通
		ENDIF
;-------------------------------------------------
;64,역강간
;-------------------------------------------------
	CASE 64
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:336 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:336 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:336 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:336 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:336 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:336 = 3
			ENDIF
			;그 이외
			IF (CFLAG:336 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:336 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;65,젖성교
;-------------------------------------------------
	CASE 65
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:337 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:337 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:337 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:337 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:337 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:337 = 3
			ENDIF
			;그 이외
			IF (CFLAG:337 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:337 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;66,기승위
;-------------------------------------------------
	CASE 66
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:338 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:338 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:338 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:338 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:338 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:338 = 3
			ENDIF
			;그 이외
			IF (CFLAG:338 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:338 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:338 == 1
				CFLAG:338 = 0
			K = 338
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;67,기승위애널
;-------------------------------------------------
	CASE 67
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:339 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:339 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:339 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:339 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:339 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:339 = 3
			ENDIF
			;그 이외
			IF (CFLAG:339 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:339 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:339 == 1
				CFLAG:339 = 0
			K = 339
			GOTO 애널섹스계共通
		ENDIF
;-------------------------------------------------
;68,대면좌위
;-------------------------------------------------
	CASE 68
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:340 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:340 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:340 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:340 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:340 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:340 = 3
			ENDIF
			;그 이외
			IF (CFLAG:340 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:340 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:340 == 1
				CFLAG:340 = 0
			K = 340
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;69,배면좌위
;-------------------------------------------------
	CASE 69
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:341 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:341 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:341 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:341 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:341 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:341 = 3
			ENDIF
			;그 이외
			IF (CFLAG:341 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:341 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:341 == 1
				CFLAG:341 = 0
			K = 341
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;70,대면좌위애널
;-------------------------------------------------
	CASE 70
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:342 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:342 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:342 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:342 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:342 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:342 = 3
			ENDIF
			;그 이외
			IF (CFLAG:342 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:342 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:342 == 1
				CFLAG:342 = 0
			K = 342
			GOTO 애널섹스계共通
		ENDIF
;-------------------------------------------------
;71,배면좌위애널
;-------------------------------------------------
	CASE 71
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:343 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:343 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:343 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:343 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:343 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:343 = 3
			ENDIF
			;그 이외
			IF (CFLAG:343 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:343 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:343 == 1
				CFLAG:343 = 0
			K = 343
			GOTO 애널섹스계共通
		ENDIF
;-------------------------------------------------
;72,속옷비켜삽입
;-------------------------------------------------
	CASE 72
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:420 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:420 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:420 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:420 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:420 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:420 = 3
			ENDIF
			;그 이외
			IF (CFLAG:420 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:420 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:420 == 1
				CFLAG:420 = 0
			K = 420
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;73,속옷비켜삽입애널
;-------------------------------------------------
	CASE 73
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:421 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:421 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:421 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:421 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:421 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:421 = 3
			ENDIF
			;그 이외
			IF (CFLAG:421 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:421 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:421 == 1
				CFLAG:421 = 0
			K = 421
			GOTO 애널섹스계共通
		ENDIF
;-------------------------------------------------
;74,역강간당한다
;-------------------------------------------------
	CASE 74
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:460 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:460 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:460 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:460 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:460 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:460 = 3
			ENDIF
			;그 이외
			IF (CFLAG:460 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:460 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;섹스계共通
;60,정상위　　61,후배위　　66,기승위
;68,대면좌위　69,배면좌위　72,속옷비켜삽입
;-------------------------------------------------
	CASE 11111
		$섹스系共通
		;처음
		IF CFLAG:K == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
				ENDIF
			ENDIF
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;그 이외
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;애널섹스계共通
;62,정상위애널　　63,후배위애널　　67,기승위애널
;70,대면좌위애널　71,배면좌위애널　72,속옷비켜삽입애널
;-------------------------------------------------
	CASE 11111
		$애널섹스계共通
		;처음
		IF CFLAG:K == 0
			PRINTFORML 
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;그 이외
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF
;=================================================
;봉사계 커맨드
;=================================================
;-------------------------------------------------
;80,수음
;-------------------------------------------------
	CASE 80
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:344 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:344 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:344 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:344 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:344 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:344 = 3
			ENDIF
			;그 이외
			IF (CFLAG:344 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:344 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;81,펠라치오
;-------------------------------------------------
	CASE 81
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:345 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:345 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:345 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:345 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:345 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:345 = 3
			ENDIF
			;그 이외
			IF (CFLAG:345 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:345 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:345 == 1
				CFLAG:345 = 0
			K = 345
			GOTO フェラ共通
		ENDIF
;-------------------------------------------------
;82,파이즈리
;-------------------------------------------------
	CASE 82
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:346 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:346 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:346 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:346 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:346 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:346 = 3
			ENDIF
			;그 이외
			IF (CFLAG:346 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:346 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:346 == 1
				CFLAG:346 = 0
			K = 346
			GOTO 파이즈리共通
		ENDIF
;-------------------------------------------------
;83,스마타
;-------------------------------------------------
	CASE 83
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:347 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:347 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:347 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:347 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:347 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:347 = 3
			ENDIF
			;그 이외
			IF (CFLAG:347 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:347 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;84,거품춤
;-------------------------------------------------
	CASE 84
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:348 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:348 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:348 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:348 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:348 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:348 = 3
			ENDIF
			;그 이외
			IF (CFLAG:348 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:348 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;85,풋잡
;-------------------------------------------------
	CASE 85
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:349 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:349 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:349 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:349 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:349 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:349 = 3
			ENDIF
			;그 이외
			IF (CFLAG:349 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:349 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;86,다리핥기강제
;-------------------------------------------------
	CASE 86
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:350 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:350 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:350 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:350 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:350 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:350 = 3
			ENDIF
			;그 이외
			IF (CFLAG:350 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:350 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;87,세로파이즈리
;-------------------------------------------------
	CASE 87
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:351 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:351 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:351 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:351 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:351 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:351 = 3
			ENDIF
			;그 이외
			IF (CFLAG:351 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:351 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:351 == 1
				CFLAG:351 = 0
			K = 351
			GOTO 파이즈리共通
		ENDIF
;-------------------------------------------------
;88,파이즈리펠라
;-------------------------------------------------
	CASE 88
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:352 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:352 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:352 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:352 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:352 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:352 = 3
			ENDIF
			;그 이외
			IF (CFLAG:352 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:352 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:352 == 1
				CFLAG:352 = 0
			K = 352
			GOTO フェラ共通
		ENDIF
;-------------------------------------------------
;89,모유마시기　함락완료만
;-------------------------------------------------
	CASE 89
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:353 == 0
			;연모, 친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란, 창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종, 예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락완료共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:353 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;음란, 창부、음란도1000이상1/4
			IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
				LOCALS:0 =
			ENDIF
			;연모, 친애
			IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
				LOCALS:0 =
			;복종, 예속
			ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;함락완료共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:353 = 2
		ENDIF
;-------------------------------------------------
;211,귀두공격(受)
;-------------------------------------------------
	CASE 211
		;처음
		IF CFLAG:479 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:479 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:479 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:479 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:479 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:479 = 3
			ENDIF
			;그 이외
			IF (CFLAG:479 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:479 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			K = 353
			GOTO フェラ共通
		ENDIF
;-------------------------------------------------
;フェラ共通
;81,펠라치오　88,파이즈리펠라　211,귀두공격(受)
;-------------------------------------------------
	CASE 11111
		$フェラ共通
		;처음
		IF CFLAG:K == 0
			PRINTFORML 
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;그 이외
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;파이즈리共通
;82,파이즈리　87,세로파이즈리
;-------------------------------------------------
	CASE 11111
		$파이즈리共通
		;처음
		IF CFLAG:K == 0
			PRINTFORML 
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;그 이외
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF
;=================================================
;조교자봉사계 커맨드
;=================================================
;-------------------------------------------------
;90,수음한다
;-------------------------------------------------
	CASE 90
		CALL TRAIN_MESSAGE
		IF TEQUIP:12 == 0
			;처음
			IF CFLAG:423 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:423 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:423 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:423 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:423 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:423 = 3
				ENDIF
				;그 이외
				IF (CFLAG:423 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:423 = 2
				ENDIF
			ENDIF
		ELSE
;-------------------------------------------------
;오나홀훑기한다
;-------------------------------------------------
			;처음
			IF CFLAG:461 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:461 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:461 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:461 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:461 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:461 = 3
				ENDIF
				;그 이외
				IF (CFLAG:461 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:461 = 2
				ENDIF
			ENDIF
		ENDIF
;-------------------------------------------------
;91,파이즈리한다
;-------------------------------------------------
	CASE 91
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:424 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:424 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:424 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:424 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:424 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:424 = 3
			ENDIF
			;그 이외
			IF (CFLAG:424 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:424 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;92,정상위시킨다
;-------------------------------------------------
	CASE 92
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:425 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:425 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:425 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:425 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:425 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:425 = 3
			ENDIF
			;그 이외
			IF (CFLAG:425 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:425 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;93,후배위시킨다
;-------------------------------------------------
	CASE 93
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:426 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:426 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:426 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:426 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:426 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:426 = 3
			ENDIF
			;그 이외
			IF (CFLAG:426 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:426 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;94,기승위한다
;-------------------------------------------------
	CASE 94
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:427 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:427 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:427 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:427 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:427 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:427 = 3
			ENDIF
			;그 이외
			IF (CFLAG:427 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:427 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;95,애널정상위시킨다
;-------------------------------------------------
	CASE 95
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:428 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:428 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:428 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:428 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:428 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:428 = 3
			ENDIF
			;그 이외
			IF (CFLAG:428 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:428 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;96,애널후배위시킨다
;-------------------------------------------------
	CASE 96
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:429 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:429 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:429 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:429 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:429 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:429 = 3
			ENDIF
			;그 이외
			IF (CFLAG:429 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:429 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;97,애널기승위한다
;-------------------------------------------------
	CASE 97
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:430 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:430 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:430 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:430 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:430 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:430 = 3
			ENDIF
			;그 이외
			IF (CFLAG:430 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:430 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;98,모유먹이기　함락완료만
;-------------------------------------------------
	CASE 98
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:443 == 0
			;연모, 친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란, 창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종, 예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락완료共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:443 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;음란, 창부、음란도1000이상1/4
			IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
				LOCALS:0 =
			ENDIF
			;연모, 친애
			IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
				LOCALS:0 =
			;복종, 예속
			ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;함락완료共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:443 = 2
		ENDIF
;-------------------------------------------------
;99,애무시킨다
;-------------------------------------------------
	CASE 99
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:462 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:462 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:462 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:462 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:462 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:462 = 3
			ENDIF
			;그 이외
			IF (CFLAG:462 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:462 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;210,귀두공격
;-------------------------------------------------
	CASE 210
		;처음
		IF CFLAG:480 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:480 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:480 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:480 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:480 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:480 = 3
			ENDIF
			;그 이외
			IF (CFLAG:480 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:480 = 2
			ENDIF
		ENDIF
;=================================================
;SM계 커맨드
;=================================================
;-------------------------------------------------
;100,스팽킹
;-------------------------------------------------
	CASE 100
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:354 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:354 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:354 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:354 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:354 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:354 = 3
			ENDIF
			;그 이외
			IF (CFLAG:354 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:354 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;101,젖스팽킹
;-------------------------------------------------
	CASE 101
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:355 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:355 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:355 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:355 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:355 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:355 = 3
			ENDIF
			;그 이외
			IF (CFLAG:355 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:355 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;102,채찍
;-------------------------------------------------
	CASE 102
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:356 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:356 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:356 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:356 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:356 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:356 = 3
			ENDIF
			;그 이외
			IF (CFLAG:356 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:356 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;103,양초
;-------------------------------------------------
	CASE 103
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:357 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:357 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:357 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:357 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:357 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:357 = 3
			ENDIF
			;그 이외
			IF (CFLAG:357 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:357 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;104,바늘
;-------------------------------------------------
	CASE 104
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:358 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:358 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:358 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:358 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:358 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:358 = 3
			ENDIF
			;그 이외
			IF (CFLAG:358 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:358 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;105,아이마스크
;-------------------------------------------------
	CASE 105
		CALL TRAIN_MESSAGE
		IF TEQUIP:18
			;처음
			IF CFLAG:359 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:359 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:359 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:359 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:359 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:359 = 3
				ENDIF
				;그 이외
				IF (CFLAG:359 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:359 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;106,밧줄
;-------------------------------------------------
	CASE 106
		CALL TRAIN_MESSAGE
		IF TEQUIP:19
			;처음
			IF CFLAG:360 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:360 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:360 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:360 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:360 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:360 = 3
				ENDIF
				;그 이외
				IF (CFLAG:360 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:360 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;107,볼재갈
;-------------------------------------------------
	CASE 107
		CALL TRAIN_MESSAGE
		IF TEQUIP:20
			;처음
			IF CFLAG:361 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:361 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:361 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:361 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:361 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:361 = 3
				ENDIF
				;그 이외
				IF (CFLAG:361 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:361 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;108,삼각목마
;-------------------------------------------------
	CASE 108
		CALL TRAIN_MESSAGE
		IF TEQUIP:41
			;처음
			IF CFLAG:463 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:463 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:463 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:463 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:463 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:463 = 3
				ENDIF
				;그 이외
				IF (CFLAG:463 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:463 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;109,사정멈추기
;-------------------------------------------------
	CASE 109
		CALL TRAIN_MESSAGE
		IF TEQUIP:42
			;처음
			IF CFLAG:464 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:464 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:464 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:464 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:464 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:464 = 3
				ENDIF
				;그 이외
				IF (CFLAG:464 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:464 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;=================================================
;조수에 의한 커맨드, 및 레즈플레이
;=================================================
;-------------------------------------------------
;120,커닐링구스강제
;-------------------------------------------------
	CASE 120
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:362 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:362 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:362 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:362 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:362 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:362 = 3
			ENDIF
			;그 이외
			IF (CFLAG:362 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:362 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;121,조개맞대기
;-------------------------------------------------
	CASE 121
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:363 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:363 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:363 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:363 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:363 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:363 = 3
			ENDIF
			;그 이외
			IF (CFLAG:363 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:363 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;122,조수를범하게한다
;-------------------------------------------------
	CASE 122
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:364 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:364 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:364 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:364 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:364 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:364 = 3
			ENDIF
			;그 이외
			IF (CFLAG:364 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:364 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;조수が気持ちよくて感じているセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;조수がこっちに来てと言うセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;조수に大丈남편と言っているセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;범해지며 강한 척을 하는 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;능숙해졌네 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		
		
		;대사가 없었을시의 보험(1행)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;123,풋잡한다
;-------------------------------------------------
	CASE 123
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:365 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:365 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:365 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:365 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:365 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:365 = 3
			ENDIF
			;그 이외
			IF (CFLAG:365 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:365 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;124,더블펠라
;-------------------------------------------------
	CASE 124
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:366 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:366 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:366 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:366 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:366 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:366 = 3
			ENDIF
			;그 이외
			IF (CFLAG:366 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:366 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;영향에 대한 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;평소보다 느끼고 있구나 라는 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;레즈 같은 대사에 대한 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		;둘의 펠라는 기분 좋지？대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(6)
		
		;대사가 없었을시의 보험(1행)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;125,더블파이즈리
;-------------------------------------------------
	CASE 125
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:367 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:367 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:367 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:367 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:367 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:367 = 3
			ENDIF
			;그 이외
			IF (CFLAG:367 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:367 = 2
			ENDIF
		ENDIF
		
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;영향에 대한 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;평소보다 느끼고 있구나 라는 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;서로 젖꼭지가 스쳐서 기분 좋아 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		;둘의 파이즈리로 기분 좋아져 줘 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(6)
		;파이즈리로 느끼고 있어？대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(7)
		
		;대사가 없었을시의 보험(1행)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;126,가슴맞대기
;-------------------------------------------------
	CASE 126
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:368 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:368 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:368 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:368 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:368 = 3
			ENDIF
			;그 이외
			IF (CFLAG:368 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:368 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;127,자위과시
;-------------------------------------------------
	CASE 127
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:432 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:432 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:432 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:432 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:432 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:432 = 3
			ENDIF
			;그 이외
			IF (CFLAG:432 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:432 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;128,조개젖히기과시
;-------------------------------------------------
	CASE 128
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:465 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:465 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:465 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:465 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:465 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:465 = 3
			ENDIF
			;그 이외
			IF (CFLAG:465 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:465 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;129,자위구음과시
;-------------------------------------------------
	CASE 129
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:466 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:466 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:466 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:466 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:466 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:466 = 3
			ENDIF
			;그 이외
			IF (CFLAG:466 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:466 = 2
			ENDIF
		ENDIF
;=================================================
;하드한조교 커맨드
;=================================================
;-------------------------------------------------
;140,이라마치오
;-------------------------------------------------
	CASE 140
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:369 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:369 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:369 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:369 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:369 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:369 = 3
			ENDIF
			;그 이외
			IF (CFLAG:369 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:369 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;141,피스트퍽　복종, 예속만
;-------------------------------------------------
	CASE 141
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:370 == 0
			;처녀
			IF TALENT:0
				PRINTFORML 
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:370 = 1
		;2번째 이후
		ELSEIF CFLAG:370 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:370 = 2
		ENDIF
;-------------------------------------------------
;142,애널피스트　복종, 예속만
;-------------------------------------------------
	CASE 142
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:371 == 0
			PRINTFORML 
			CFLAG:371 = 1
		;2번째 이후
		ELSEIF CFLAG:371 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:371 = 2
		ENDIF
;-------------------------------------------------
;143,양구멍피스트　복종, 예속만
;-------------------------------------------------
	CASE 143
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:372 == 0
			;처녀
			IF TALENT:0
				PRINTFORML 
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:372 = 1
		;2번째 이후
		ELSEIF CFLAG:372 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:372 = 2
		ENDIF
;-------------------------------------------------
;144,방뇨
;-------------------------------------------------
	CASE 144
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:373 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:373 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:373 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:373 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:373 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:373 = 3
			ENDIF
			;그 이외
			IF (CFLAG:373 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:373 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;145,애널봉사강제
;-------------------------------------------------
	CASE 145
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:374 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:374 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:374 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:374 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:374 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:374 = 3
			ENDIF
			;그 이외
			IF (CFLAG:374 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:374 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;146,관장기＋플러그　해제すると方便
;-------------------------------------------------
	CASE 146
		CALL TRAIN_MESSAGE
		IF TEQUIP:21
			;처음
			IF CFLAG:375 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:375 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:375 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:375 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:375 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:375 = 3
				ENDIF
				;그 이외
				IF (CFLAG:375 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:375 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;147,공기관장　방귀뀌게한다
;-------------------------------------------------
	CASE 147
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:376 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:376 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:376 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:376 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:376 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:376 = 3
			ENDIF
			;그 이외
			IF (CFLAG:376 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:376 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;148,확장벌룬
;-------------------------------------------------
	CASE 148
		CALL TRAIN_MESSAGE
		IF TEQUIP:22
			;처음
			IF CFLAG:377 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:377 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:377 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:377 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:377 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:377 = 3
				ENDIF
				;그 이외
				IF (CFLAG:377 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:377 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;149,애널전극　복종, 예속만
;-------------------------------------------------
	CASE 149
		CALL TRAIN_MESSAGE
		IF TEQUIP:23
			;처음
			IF CFLAG:378 == 0
				PRINTFORML 
				CFLAG:378 = 1
			;2번째 이후
			ELSEIF CFLAG:378 < 2 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:378 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:378 == 1 || FLAG:7 == 2
				PRINTFORML 
			ENDIF
		ENDIF
;-------------------------------------------------
;150,유방전극　복종, 예속만
;-------------------------------------------------
	CASE 150
		CALL TRAIN_MESSAGE
		IF TEQUIP:24
			;처음
			IF CFLAG:379 == 0
				PRINTFORML 
				CFLAG:379 = 1
			;2번째 이후
			ELSEIF CFLAG:379 < 2 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:379 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:379 == 1 || FLAG:7 == 2
				PRINTFORML 
			ENDIF
		ENDIF
;-------------------------------------------------
;151,뱀장어욕탕　복종, 예속만
;-------------------------------------------------
	CASE 151
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:380 == 0
			;처녀
			IF TALENT:0
				PRINTFORML 
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:380 = 1
		;2번째 이후
		ELSEIF CFLAG:380 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:380 = 2
		ENDIF
;-------------------------------------------------
;152,벌레욕탕　복종, 예속만
;-------------------------------------------------
	CASE 152
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:381 == 0
			PRINTFORML 
			CFLAG:381 = 1
		;2번째 이후
		ELSEIF CFLAG:381 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:381 = 2
		ENDIF
;-------------------------------------------------
;154,요정오나홀　소인체형계만
;-------------------------------------------------
	CASE 154
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:467 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:467 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:467 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:467 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:467 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:467 = 3
			ENDIF
			;그 이외
			IF (CFLAG:467 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:467 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;155,구슬킥
;-------------------------------------------------
	CASE 155
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:468 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:468 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:468 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:468 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:468 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:468 = 3
			ENDIF
			;그 이외
			IF (CFLAG:468 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:468 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;156,음뇨
;-------------------------------------------------
	CASE 156
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:469 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:469 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:469 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:469 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:469 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:469 = 3
			ENDIF
			;그 이외
			IF (CFLAG:469 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:469 = 2
			ENDIF
		ENDIF
;=================================================
;촉수계 커맨드
;=================================================
;-------------------------------------------------
;160,촉수소환
;-------------------------------------------------
	CASE 160
		CALL TRAIN_MESSAGE
		IF TEQUIP:25
			;처음
			IF CFLAG:382 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:382 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:382 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:382 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:382 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:382 = 3
				ENDIF
				;그 이외
				IF (CFLAG:382 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:382 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
	CASE 44949
		$SHOKUSHU_PLAY
		SELECTCASE SELECTCOM
;-------------------------------------------------
;0,촉수애무
;-------------------------------------------------
			CASE 0
				;처음
				IF CFLAG:455 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:455 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:455 < 3 || FLAG:7 == 2)
						;음란, 창부、음란도1000이상1/4
						IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
							LOCALS:0 = 
						ENDIF
						;연모, 친애
						IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
							LOCALS:0 = 
						;복종, 예속	
						ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:455 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:455 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:455 = 3
					ENDIF
					;그 이외
					IF (CFLAG:455 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:455 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;5,촉수애널애무
;-------------------------------------------------
			CASE 5
				;처음
				IF CFLAG:456 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:456 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:456 < 3 || FLAG:7 == 2)
						;음란, 창부、음란도1000이상1/4
						IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
							LOCALS:0 = 
						ENDIF
						;연모, 친애
						IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
							LOCALS:0 = 
						;복종, 예속	
						ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:456 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:456 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:456 = 3
					ENDIF
					;그 이외
					IF (CFLAG:456 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:456 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;6,촉수가슴주무르기
;-------------------------------------------------
			CASE 6
				;처음
				IF CFLAG:457 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:457 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:457 < 3 || FLAG:7 == 2)
						;음란, 창부、음란도1000이상1/4
						IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
							LOCALS:0 = 
						ENDIF
						;연모, 친애
						IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
							LOCALS:0 = 
						;복종, 예속	
						ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:457 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:457 = 3
					ENDIF
					;그 이외
					IF (CFLAG:457 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:457 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;7,촉수유두자극（短）
;-------------------------------------------------
			CASE 7
				;처음
				IF CFLAG:458 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:458 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:458 < 3 || FLAG:7 == 2)
						;음란, 창부、음란도1000이상1/4
						IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
							LOCALS:0 = 
						ENDIF
						;연모, 친애
						IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
							LOCALS:0 = 
						;복종, 예속	
						ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:458 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:458 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:458 = 3
					ENDIF
					;그 이외
					IF (CFLAG:458 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:458 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;8,촉수가슴애무
;-------------------------------------------------
			CASE 8
				;처음
				IF CFLAG:459 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:459 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:459 < 3 || FLAG:7 == 2)
						;음란, 창부、음란도1000이상1/4
						IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
							LOCALS:0 = 
						ENDIF
						;연모, 친애
						IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
							LOCALS:0 = 
						;복종, 예속	
						ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:459 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:459 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:459 = 3
					ENDIF
					;그 이외
					IF (CFLAG:459 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:459 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;12,촉수날개애무
;-------------------------------------------------
			CASE 12
				;처음
				IF CFLAG:460 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:460 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:460 < 3 || FLAG:7 == 2)
						;음란, 창부、음란도1000이상1/4
						IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
							LOCALS:0 = 
						ENDIF
						;연모, 친애
						IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
							LOCALS:0 = 
						;복종, 예속	
						ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:460 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:460 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:460 = 3
					ENDIF
					;그 이외
					IF (CFLAG:460 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:460 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;13,촉수꼬리애무
;-------------------------------------------------
			CASE 13
				;처음
				IF CFLAG:461 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:461 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:461 < 3 || FLAG:7 == 2)
						;음란, 창부、음란도1000이상1/4
						IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
							LOCALS:0 = 
						ENDIF
						;연모, 친애
						IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
							LOCALS:0 = 
						;복종, 예속	
						ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:461 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:461 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:461 = 3
					ENDIF
					;그 이외
					IF (CFLAG:461 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:461 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;15,촉수엉덩이애무
;-------------------------------------------------
			CASE 15
				;처음
				IF CFLAG:462 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:462 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:462 < 3 || FLAG:7 == 2)
						;음란, 창부、음란도1000이상1/4
						IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
							LOCALS:0 = 
						ENDIF
						;연모, 친애
						IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
							LOCALS:0 = 
						;복종, 예속	
						ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:462 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:462 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:462 = 3
					ENDIF
					;그 이외
					IF (CFLAG:462 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:462 = 2
					ENDIF
				ENDIF
		ENDSELECT
;~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
;-------------------------------------------------
;161,촉수삽입
;-------------------------------------------------
	CASE 161
		CALL TRAIN_MESSAGE
		IF TEQUIP:13
			;처음
			IF CFLAG:383 == 0
				;처녀
				IF TALENT:0
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;연모, 친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란, 창부
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종, 예속
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 =
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 =
						ENDIF
					ENDIF
					;그 이외
					IF LOCALS:0 == ""
						PRINTFORML 
						LOCALS:0 =
					ENDIF
				;비처녀
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;연모, 친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란, 창부
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종, 예속
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 =
						ENDIF
						;함락완료共通
						IF LOCALS:0 == ""
							LOCALS:0 =
						ENDIF
					ENDIF
					;그 이외
					IF LOCALS:0 == ""
						PRINTFORML 
						LOCALS:0 =
					ENDIF
				ENDIF
				CFLAG:383 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:383 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:383 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:383 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:383 = 3
				ENDIF
				;그 이외
				IF (CFLAG:383 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:383 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;162,애널촉수
;-------------------------------------------------
	CASE 162
		CALL TRAIN_MESSAGE
		IF TEQUIP:14
			;처음
			IF CFLAG:384 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:384 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:384 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:384 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:384 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:384 = 3
				ENDIF
				;그 이외
				IF (CFLAG:384 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:384 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;163,촉수클리자극
;-------------------------------------------------
	CASE 163
		CALL TRAIN_MESSAGE
		IF TEQUIP:11
			;처음
			IF CFLAG:385 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:385 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:385 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:385 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:385 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:385 = 3
				ENDIF
				;그 이외
				IF (CFLAG:385 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:385 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;164,촉수유두자극
;-------------------------------------------------
	CASE 164
		CALL TRAIN_MESSAGE
		IF TEQUIP:16
			;처음
			IF CFLAG:386 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:386 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:386 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:386 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:386 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:386 = 3
				ENDIF
				;그 이외
				IF (CFLAG:386 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:386 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;165,촉수착유
;-------------------------------------------------
	CASE 165
		CALL TRAIN_MESSAGE
		IF TEQUIP:17
			;처음
			IF CFLAG:387 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:387 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:387 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:387 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:387 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:387 = 3
				ENDIF
				;그 이외
				IF (CFLAG:387 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:387 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;166,촉수긴박
;-------------------------------------------------
	CASE 166
		CALL TRAIN_MESSAGE
		IF TEQUIP:19
			;처음
			IF CFLAG:388 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:388 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:388 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:388 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:388 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:388 = 3
				ENDIF
				;그 이외
				IF (CFLAG:388 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:388 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;167,촉수관장　極太の촉수が菊穴をふさぎ、体液を流し込んだ
;-------------------------------------------------
	CASE 167
		CALL TRAIN_MESSAGE
		IF TEQUIP:21
			;처음
			IF CFLAG:389 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:389 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:389 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:389 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:389 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:389 = 3
				ENDIF
				;그 이외
				IF (CFLAG:389 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:389 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;168,촉수구욕
;-------------------------------------------------
	CASE 168
		CALL TRAIN_MESSAGE
		IF TEQUIP:36
			;처음
			IF CFLAG:390 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:390 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:390 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:390 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:390 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:390 = 3
				ENDIF
				;그 이외
				IF (CFLAG:390 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:390 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;169,촉수페니스자극
;-------------------------------------------------
	CASE 169
		CALL TRAIN_MESSAGE
		IF TEQUIP:12
			;처음
			IF CFLAG:391 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:391 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:391 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:391 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:391 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:391 = 3
				ENDIF
				;그 이외
				IF (CFLAG:391 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:391 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;170,촉수삽입시킨다
;-------------------------------------------------
	CASE 170
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:13
			;처음
			IF CFLAG:470 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:470 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:470 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:470 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:470 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:470 = 3
				ENDIF
				;그 이외
				IF (CFLAG:470 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:470 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;171,애널촉수시킨다
;-------------------------------------------------
	CASE 171
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:14
			;처음
			IF CFLAG:471 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:471 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:471 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:471 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:471 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:471 = 3
				ENDIF
				;그 이외
				IF (CFLAG:471 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:471 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;172,촉수클리자극시킨다
;-------------------------------------------------
	CASE 172
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:11
			;처음
			IF CFLAG:472 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:472 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:472 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:472 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:472 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:472 = 3
				ENDIF
				;그 이외
				IF (CFLAG:472 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:472 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;173,촉수유두자극시킨다
;-------------------------------------------------
	CASE 173
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:16
			;처음
			IF CFLAG:473 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:473 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:473 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:473 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:473 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:473 = 3
				ENDIF
				;그 이외
				IF (CFLAG:473 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:473 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;174,촉수착유시킨다
;-------------------------------------------------
	CASE 174
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:17
			;처음
			IF CFLAG:474 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:474 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:474 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:474 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:474 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:474 = 3
				ENDIF
				;그 이외
				IF (CFLAG:474 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:474 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;175,촉수페니스자극시킨다
;-------------------------------------------------
	CASE 175
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:12
			;처음
			IF CFLAG:475 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:475 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:475 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:475 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:475 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:475 = 3
				ENDIF
				;그 이외
				IF (CFLAG:475 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:475 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;=================================================
;특수커맨드
;=================================================
;-------------------------------------------------
;180,로션
;-------------------------------------------------
	CASE 180
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:392 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:392 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:392 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:392 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:392 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:392 = 3
			ENDIF
			;그 이외
			IF (CFLAG:392 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:392 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;181,미약
;-------------------------------------------------
	CASE 181
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:393 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:393 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:393 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:393 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:393 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:393 = 3
			ENDIF
			;그 이외
			IF (CFLAG:393 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:393 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;182,이뇨제
;-------------------------------------------------
	CASE 182
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:394 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:394 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:394 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:394 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:394 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:394 = 3
			ENDIF
			;그 이외
			IF (CFLAG:394 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:394 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;183,비디오카메라
;-------------------------------------------------
	CASE 183
		CALL TRAIN_MESSAGE
		IF TEQUIP:28
			;처음
			IF CFLAG:395 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:395 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:395 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:395 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:395 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:395 = 3
				ENDIF
				;그 이외
				IF (CFLAG:395 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:395 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;184,야외플레이
;-------------------------------------------------
	CASE 184
		CALL TRAIN_MESSAGE
		;TEQUIP:29でどこの場所選んでも同じ구상が出るように
		;변경･場所ごとで変えたければ==にして数字をいじる
		;폐빌딩=1　삼림=2　공원=4　냇가=8
		IF TEQUIP:29
			;처음
			IF CFLAG:396 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:396 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:396 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:396 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:396 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:396 = 3
				ENDIF
				;그 이외
				IF (CFLAG:396 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:396 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;185,조수를범한다
;-------------------------------------------------
	CASE 185
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:397 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:397 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:397 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:397 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:397 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:397 = 3
			ENDIF
			;그 이외
			IF (CFLAG:397 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:397 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;범해져서 무척 기분 좋아 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;우리들, 정말 사이 좋지？대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;부럽지？대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;이쪽이 신경쓰이는 것 같다 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;대사가 없었을시의 보험(1행)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;186,수치플레이
;-------------------------------------------------
	CASE 186
		CALL TRAIN_MESSAGE
		IF TEQUIP:30
			;처음
			IF CFLAG:398 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:398 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:398 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:398 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:398 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:398 = 3
				ENDIF
				;그 이외
				IF (CFLAG:398 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:398 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;187,목욕탕플레이
;-------------------------------------------------
	CASE 187
		CALL TRAIN_MESSAGE
		IF TEQUIP:31
			;처음
			IF CFLAG:399 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:399 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:399 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:399 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:399 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:399 = 3
				ENDIF
				;그 이외
				IF (CFLAG:399 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:399 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;188,샤워
;-------------------------------------------------
	CASE 188
		CALL TRAIN_MESSAGE
		IF TEQUIP:32
			;처음
			IF CFLAG:400 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:400 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:400 < 3 || FLAG:7 == 2)
					;음란, 창부、음란도1000이상1/4
					IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
						LOCALS:0 = 
					ENDIF
					;연모, 친애
					IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:400 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:400 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:400 = 3
				ENDIF
				;그 이외
				IF (CFLAG:400 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:400 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란, 창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;189,새댁플레이　연모, 친애만
;-------------------------------------------------
	CASE 189
		CALL TRAIN_MESSAGE
		IF TEQUIP:33
			;처음
			IF CFLAG:401 == 0
				PRINTFORML 
				CFLAG:401 = 1
			;2번째 이후
			ELSEIF CFLAG:401 < 2 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:401 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:401 == 1 || FLAG:7 == 2
				PRINTFORML 
			ENDIF
		ENDIF
;-------------------------------------------------
;190,사진촬영
;-------------------------------------------------
	CASE 190
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:402 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:402 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:402 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:402 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:402 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:402 = 3
			ENDIF
			;그 이외
			IF (CFLAG:402 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:402 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;191,강정신약
;-------------------------------------------------
	CASE 191
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:403 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:403 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:403 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:403 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:403 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:403 = 3
			ENDIF
			;그 이외
			IF (CFLAG:403 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:403 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;192,콘돔정음（노예）
;-------------------------------------------------
	CASE 192
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:404 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:404 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:404 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:404 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:404 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:404 = 3
			ENDIF
			;그 이외
			IF (CFLAG:404 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:404 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;193,콘돔정음（조수）
;-------------------------------------------------
	CASE 193
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:405 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:405 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:405 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:405 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:405 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:405 = 3
			ENDIF
			;그 이외
			IF (CFLAG:405 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:405 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;정액이 무척 맛있어 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;이건 내꺼야！대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;부럽지？대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;먼저 받아갈께 대사（사이좋음）
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;대사가 없었을시의 보험(1행)
		;SIF RESULT == 0
 
;-------------------------------------------------
;194,정액키스
;-------------------------------------------------
	CASE 194
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:406 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:406 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:406 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:406 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:406 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:406 = 3
			ENDIF
			;그 이외
			IF (CFLAG:406 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:406 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;195,유선개발
;-------------------------------------------------
	CASE 195
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:431 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:431 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:431 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:431 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:431 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:431 = 3
			ENDIF
			;그 이외
			IF (CFLAG:431 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:431 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;196,미약쓴다
;-------------------------------------------------
	CASE 196
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:437 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:437 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:437 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:437 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:437 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:437 = 3
			ENDIF
			;그 이외
			IF (CFLAG:437 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:437 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;197,팬티훑기
;-------------------------------------------------
	CASE 197
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:438 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:438 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:438 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:438 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:438 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:438 = 3
			ENDIF
			;그 이외
			IF (CFLAG:438 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:438 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;200,사람을부른다
;-------------------------------------------------
	CASE 200
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:433 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:433 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:433 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:433 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:433 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:433 = 3
			ENDIF
			;그 이외
			IF (CFLAG:433 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:433 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;201,윤간
;-------------------------------------------------
	CASE 201
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:434 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:434 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:434 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:434 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:434 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:434 = 3
			ENDIF
			;그 이외
			IF (CFLAG:434 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:434 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;202,사람을모으게한다
;-------------------------------------------------
	CASE 202
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:476 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:476 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:476 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:476 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:476 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:476 = 3
			ENDIF
			;그 이외
			IF (CFLAG:476 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:476 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;203,원격조교
;-------------------------------------------------
	CASE 203
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:477 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:477 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:477 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:477 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:477 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:477 = 3
			ENDIF
			;그 이외
			IF (CFLAG:477 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:477 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;204,정액먹기
;-------------------------------------------------
	CASE 204
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:478 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:478 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:478 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:478 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:478 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:478 = 3
			ENDIF
			;그 이외
			IF (CFLAG:478 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:478 = 2
			ENDIF
		ENDIF
;=================================================
;500번대,파생커맨드
;=================================================
;-------------------------------------------------
;500,３Ｐ
;-------------------------------------------------
	CASE 500
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:407 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:407 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:407 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:407 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:407 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:407 = 3
			ENDIF
			;그 이외
			IF (CFLAG:407 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:407 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;안쪽이 무척 기분 좋아 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;계속 가버려라！대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;무척 기분 좋아 보여 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;여기에 넣어줘 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;대사가 없었을시의 보험(1행)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;501,식스나인
;-------------------------------------------------
	CASE 501
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:408 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:408 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:408 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:408 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:408 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:408 = 3
			ENDIF
			;그 이외
			IF (CFLAG:408 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:408 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;502,이와시미즈
;-------------------------------------------------
	CASE 502
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:409 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:409 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:409 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:409 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:409 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:409 = 3
			ENDIF
			;그 이외
			IF (CFLAG:409 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:409 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;503,G스팟자극
;-------------------------------------------------
	CASE 503
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:410 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:410 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:410 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:410 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:410 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:410 = 3
			ENDIF
			;그 이외
			IF (CFLAG:410 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:410 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;504,흐트러진모란
;-------------------------------------------------
	CASE 504
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:411 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:411 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:411 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:411 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:411 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:411 = 3
			ENDIF
			;그 이외
			IF (CFLAG:411 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:411 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;505,기승유방자위
;-------------------------------------------------
	CASE 505
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:412 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;비처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락완료共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;그 이외
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:412 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:412 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:412 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:412 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:412 = 3
			ENDIF
			;그 이외
			IF (CFLAG:412 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:412 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;506,삽입음부자극
;-------------------------------------------------
	CASE 506
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:413 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:413 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:413 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:413 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:413 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:413 = 3
			ENDIF
			;그 이외
			IF (CFLAG:413 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:413 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;507,삽입자궁구자극
;-------------------------------------------------
	CASE 507
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:414 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:414 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:414 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:414 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:414 = 3
			ENDIF
			;그 이외
			IF (CFLAG:414 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:414 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;508,６９파이즈리
;-------------------------------------------------
	CASE 508
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:415 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:415 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:415 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:415 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:415 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:415 = 3
			ENDIF
			;그 이외
			IF (CFLAG:415 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:415 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;509,착유
;-------------------------------------------------
	CASE 509
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:416 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:416 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:416 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:416 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:416 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:416 = 3
			ENDIF
			;그 이외
			IF (CFLAG:416 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:416 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;510,더블스마타
;-------------------------------------------------
	CASE 510
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:417 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:417 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:417 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:417 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:417 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:417 = 3
			ENDIF
			;그 이외
			IF (CFLAG:417 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:417 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;영향에 대한 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;평소보다 느끼고 있구나 라는 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;서로 기분 좋네 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		
		;대사가 없었을시의 보험(1행)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;511,두개펠라
;-------------------------------------------------
	CASE 511
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:418 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:418 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:418 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:418 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:418 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:418 = 3
			ENDIF
			;그 이외
			IF (CFLAG:418 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:418 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;무척 기분 좋아 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;자신도 확실히 기분 좋게 해줘 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;マスターの페니스が当たって気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;二人分の정액飲ませてあげるセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;능숙해졌네 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		
		;대사가 없었을시의 보험(1행)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;512,애널기승유방자위
;-------------------------------------------------
	CASE 512
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:419 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:419 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:419 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:419 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:419 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:419 = 3
			ENDIF
			;그 이외
			IF (CFLAG:419 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:419 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;513,입과바기나를범한다
;-------------------------------------------------
	CASE 513
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:439 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:439 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:439 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:439 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:439 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:439 = 3
			ENDIF
			;그 이외
			IF (CFLAG:439 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:439 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;안쪽이 무척 기분 좋아 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;입がとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;계속 가버려라！대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;무척 기분 좋아 보여 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;대사가 없었을시의 보험(1행)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;514,입과애널을범한다
;-------------------------------------------------
	CASE 514
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:440 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:440 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:440 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:440 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:440 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:440 = 3
			ENDIF
			;그 이외
			IF (CFLAG:440 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:440 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;안쪽이 무척 기분 좋아 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;입がとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;계속 가버려라！대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;무척 기분 좋아 보여 대사
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;대사가 없었을시의 보험(1행)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;515,애널흐트러진모란
;-------------------------------------------------
	CASE 515
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:441 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:441 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:441 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:441 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:441 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:441 = 3
			ENDIF
			;그 이외
			IF (CFLAG:441 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:441 = 2
			ENDIF
		ENDIF
;=================================================
;600番台,비전투원コマンド
;=================================================
;-------------------------------------------------
;600,기초훈련
;-------------------------------------------------
	CASE 600
		CALL TRAIN_MESSAGE
		;처음
		IF CFLAG:481 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:481 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:481 < 3 || FLAG:7 == 2)
				;음란, 창부、음란도1000이상1/4
				IF (TALENT:4 || TALENT:7) || (CFLAG:음란도 > 1000 && RAND:4 == 0)
					LOCALS:0 = 
				ENDIF
				;연모, 친애
				IF (TALENT:3 || TALENT:6) && LOCALS:0 == ""
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8) && LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				;함락완료共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:481 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:481 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:481 = 3
			ENDIF
			;그 이외
			IF (CFLAG:481 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:481 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;601,시험소환
;-------------------------------------------------
	CASE 601
		CALL TRAIN_MESSAGE
		IF TALENT:서머너 && !TALENT:비전투원
			PRINTFORML 
		ENDIF
;-------------------------------------------------
;602,그림자추출
;-------------------------------------------------
	CASE 602
		CALL TRAIN_MESSAGE
		IF TALENT:페르소나구사자 && !TALENT:비전투원
			PRINTFORML 
;			;초기페르소나추가
			;ABL:초기페르소나 =
			
;			;초기페르소나능력변경
			;ABL:페르소나1데이터／스킬1 =
			
			;ABL:페르소나1데이터／습득스킬1 =
			;ABL:페르소나1데이터／습득LV1 =
 
		ENDIF
;-------------------------------------------------
;603,각성원조
;-------------------------------------------------
	CASE 603
		CALL TRAIN_MESSAGE
		IF TALENT:이능자 && !TALENT:비전투원
			PRINTFORML 
;			;능력변경
			;ABL:스킬1 = 
			
			;ABL:습득스킬1
			;ABL:습득LV1
 
		ENDIF
;-------------------------------------------------
;604,바이러스조사
;-------------------------------------------------
	CASE 604
		CALL TRAIN_MESSAGE
		IF TALENT:식노 && !TALENT:비전투원
			PRINTFORML 
		ENDIF
;-------------------------------------------------
;605,영매실험
;-------------------------------------------------
	CASE 605
		CALL TRAIN_MESSAGE
		IF TALENT:데빌시프터 && !TALENT:비전투원
			PRINTFORML 
;			;초기링크악마
			;CFLAG:초기링크악마
			
;			;초기링크악마능력변경
			;ABL:초기변신악마스킬1
			
			;ABL:초기변신악마습득스킬1
			;ABL:초기변신악마습득LV1
			
			;ABL:초기링크악마공격상성
			;ABL:초기링크악마최저공격횟수
			;ABL:초기링크악마최대공격횟수
			;ABL:초기링크악마사거리
			;ABL:초기링크악마공격범위
			;ABL:초기링크악마추가효과
			;ABL:초기링크악마추가효과상성
			;ABL:초기링크악마추가효과명중률
			;ABL:초기링크악마추가효과최대명중률
		ENDIF
 
;-------------------------------------------------
;606,개조수술
;-------------------------------------------------
	CASE 606
		CALL TRAIN_MESSAGE
		IF TALENT:기계의처녀 && !TALENT:비전투원
			PRINTFORML 
		ENDIF
 
;------------------------------------------------
;커맨드구상 여기까지
;------------------------------------------------
	CASEELSE
		SIF FLAG:6 > 0
			CALL TRAIN_MESSAGE
ENDSELECT
 
FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:GYOU == 0
	PRINTW
	
;-------------------------------------------------
;윤간時の구상
;CASE 0 内は必ず通るようになっており
;RETURNで0を返すと、この後통상の윤간行動を出します
;CFLAGは使っていません
;COMF201_윤간内でLOCALを使っているので使いません
;-------------------------------------------------
@KOJO_MESSAGE_RINKAN_K4972, ARG:1 , ARG:2
#DIM GYOU

FOR GYOU,0,51
	LOCALS:GYOU = 
NEXT

SELECTCASE ARG:1
	CASE 0
	;CASE 0はオリジナル윤간구상
	;C:一回あたりの帰宅人数、D：興奮度증가(及び減少)
	;CとDはオリジナル윤간구상内のみ使う
	;C += 、D += を使う
		IF ARG:2 == 1
		;하드ON
		
		ELSE
		;하드OFF
		
		ENDIF
	;하드계
	;가슴を踏まれる（거유・폭유のみ）
	CASE 1				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:221 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:221 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
		ENDIF		
		;그 이외		
		IF (CFLAG:221 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
		ENDIF		
	;머리を踏まれる
	CASE 2
	
	;폭력을 당하다
	CASE 3
	
	;담배빵
	;興奮度が低く、반항적、꿋꿋함、프라이드높음、건방짐、저항を持っている
	CASE 4
	
	;地面に垂れた정액舐めとらせる
	;마조끼３이상
	CASE 5
	
	;몸에 낙서를 당하고 범해진다
	CASE 6
	
	;尻突き上げてバックから犯されつつ、머리を踏んづけられて屈辱で涙눈상태
	CASE 7
	
	;통상系
	;음란系で눈の前に棒並べられて、どれが欲しいか選ばせるとか（음란・창부のみ）
	CASE 8
	
	;거유이상의 キャラだと「その乳は飾りか？遊ばせてるんじゃねえ」とか罵られ파이즈리한다（거유・폭유のみ）
	CASE 9
	
	;挿れられるのだけは회피しようと必死に입と손で群がるチンポにご봉사する（恋幕・친애のみ）
	CASE 10
	
	;;両穴突かれながら無理やり笑顔とピースサインとらされて記念촬영
	CASE 11
	
	;;輸姦後の後始末描写で、本人정액でどろどろにされてるのに周囲の男達の肉棒を舌で掃除させられてる
	CASE 12
	
	;안에 사정당해도 즉시 다른 사람으로 교대
	CASE 13
	
	;一斉사정で정액まみれ
	CASE 14
	
	;;両穴突かれて同時사정
	CASE 15
	
	;기승위+양겨드랑이잡
	CASE 16
	
	;;입+보지
	CASE 17
	
ENDSELECT

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORML %LOCALS:GYOU%
NEXT
SIF LOCALS:0 != ""
	PRINTW
SIF LOCALS:0 != ""
	RETURN 1
RETURN 0
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG 관련（4972을 캐릭터번호로 치환）
;파라미터 변화 등을 트리거로 한 구상　CFLAG 221～260을 사용
;-------------------------------------------------
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_A（4972을 캐릭터번호로 치환）
;조교자의 사정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K4972
#DIM GYOU
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_A
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4972을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K4972
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K4972
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교자의 사정시의 텍스트
;-------------------------------------------------

FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT
;-------------------------------------------------
;사정장소 변경시의 텍스트　유효시 여기서 함수 종료
;-------------------------------------------------


IF CFLAG:106
	CALL PALAM_MESSAGE_A
	;머리카락
	IF CFLAG:106 == 1				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:221 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:221= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:221= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:221= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:221= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:221 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:221= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:221 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:221= 1	
		ENDIF		
	;얼굴
	ELSEIF CFLAG:106 == 2				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:222 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:222= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:222= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:222= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:222= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:222 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:222= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:222 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:222= 1	
		ENDIF		
	;가슴
	ELSEIF CFLAG:106 == 3				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:223 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:223= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:223= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:223= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:223= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:223 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:223= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:223 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:223= 1	
		ENDIF		
	;배
	ELSEIF CFLAG:106 == 4				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:224 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:224= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:224= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:224= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:224= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:224 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:224= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:224 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:224= 1	
		ENDIF		
	;겨드랑이
	ELSEIF CFLAG:106 == 5				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:225 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:225= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:225= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:225= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:225= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:225 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:225= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:225 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:225= 1	
		ENDIF		
	;허벅지
	ELSEIF CFLAG:106 == 6				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:226 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:226= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:226= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:226= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:226= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:226 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:226= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:226 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:226= 1	
		ENDIF		
	;가랑이
	ELSEIF CFLAG:106 == 7				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:227 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:227= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:227= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:227= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:227= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:227 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:227= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:227 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:227= 1	
		ENDIF		
	;엉덩이
	ELSEIF CFLAG:106 == 8				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:228 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:228= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:228= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:228= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:228= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:228 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:228= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:228 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:228= 1	
		ENDIF		
	;콘돔
	ELSEIF CFLAG:106 == 9
		;섹스로 사정하고, 애널섹스 이외를 실행 시
		IF GROUPMATCH(SELECTCOM , 60 , 61 , 66 , 68 , 69 , 72 , 504 , 505 , 506 , 507)					
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:229 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:229= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:229= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:229= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:229= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:229 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:229= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:229 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:229= 1	
			ENDIF		
		ELSE					
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:230= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:230= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:230= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:230= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:230= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:230 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:230= 1	
			ENDIF		
		ENDIF
	ENDIF
ENDIF


;-------------------------------------------------
;손, 입 이외로 사정
;대응 커맨드는 키스, 스마타, 풋잡, 애널강제봉사, 더블스마타, 조수를범하게한다
;-------------------------------------------------
IF TFLAG:9 > 0
	CALL PALAM_MESSAGE_A
	;大量사정				
	IF TFLAG:9 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:230= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:230= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:230= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:230= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:230= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:230 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:230= 1	
		ENDIF		
	ENDIF
	;통상				
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:230= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:230= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:230= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:230= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:230= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:230 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:230= 1	
		ENDIF		
	ENDIF
ENDIF

;-------------------------------------------------
;조수를범한다로 사정
;-------------------------------------------------
IF TFLAG:7 > 0
	CALL PALAM_MESSAGE_A			
	;大量사정					
	IF TFLAG:7 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:232 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:232= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:232= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:232= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:232= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:232= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:232 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:232= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:232 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:232= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:232= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:232= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:232= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:232= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:232 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:232= 1	
		ENDIF		
	ENDIF	
ENDIF

;-------------------------------------------------
;입으로 사정
;-------------------------------------------------
IF TFLAG:0 > 0
	CALL PALAM_MESSAGE_A
	;이라마치오로 사정
	IF SELECTCOM == 140			
		;大量사정						
		IF TFLAG:0 > 1
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF LOCALS:0 == ""
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
	;그 이외의 입사정
	ELSE
		;大量사정						
		IF TFLAG:0 > 1
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF LOCALS:0 == ""
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
	ENDIF
ENDIF

;-------------------------------------------------
;손으로 사정
;-------------------------------------------------
IF TFLAG:1 > 0
	CALL PALAM_MESSAGE_A	
	;大量사정					
	IF TFLAG:1 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:234 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:234= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:234= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:234= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:234= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:234= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:234 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:234= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:234 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:234= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:234= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:234= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:234= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:234= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:234 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:234= 1	
		ENDIF		
	ENDIF	
ENDIF

;-------------------------------------------------
;가슴으로 사정
;-------------------------------------------------
IF TFLAG:34 > 0
	CALL PALAM_MESSAGE_A		
	;大量사정					
	IF TFLAG:34 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:235 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:235= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:235= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:235= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:235= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:235= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:235 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:235= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:235 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:235= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:235= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:235= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:235= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:235= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:235 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:235= 1	
		ENDIF		
	ENDIF	
ENDIF
;-------------------------------------------------
;오나홀에 사정
;-------------------------------------------------
IF TFLAG:16 == 1
	CALL PALAM_MESSAGE_A		
	;大量사정					
	IF TFLAG:16 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:236 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:236= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:236= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:236= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:236= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:236= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:236 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:236= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:236 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:236= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:236= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:236= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:236= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:236= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:236 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:236= 1	
		ENDIF		
	ENDIF	
ENDIF

;-------------------------------------------------
;섹스로 사정
;-------------------------------------------------
IF TFLAG:2 > 0
	CALL PALAM_MESSAGE_A
	;애널섹스계
	IF SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || (SELECTCOM == 505 && TFLAG:40 == 2)
		;大量사정						
		IF TFLAG:2 > 1
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:237 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:237= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:237= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:237= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:237= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:237= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:237 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:237= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF LOCALS:0 == ""
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:237 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:237= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:237= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:237= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:237= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:237= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:237 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:237= 1	
			ENDIF		
		ENDIF
	;안쪽 자극 섹스계
	ELSEIF SELECTCOM == 506 || SELECTCOM == 507
		;大量사정						
		IF TFLAG:2 > 1
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:238 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:238= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:238= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:238= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:238= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:238= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:238 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:238= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF LOCALS:0 == ""
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:238 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:238= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:238= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:238= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:238= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:238= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:238 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:238= 1	
			ENDIF		
		ENDIF
	;그 외 섹스계
	ELSE
		;大量사정						
		IF TFLAG:2 > 1
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:239 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:239= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:239= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:239= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:239= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:239= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:239 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:239= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF LOCALS:0 == ""
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:239 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:239= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:239= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:239= 3
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:239= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:239= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:239 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:239= 1	
			ENDIF		
		ENDIF	
	ENDIF
ENDIF

;-------------------------------------------------
;역강간으로 사정당했다
;-------------------------------------------------
IF SELECTCOM == 64 && TFLAG:10 > 0
	CALL PALAM_MESSAGE_A		
	;大量사정					
	IF TFLAG:10 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:240= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:240= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:240= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:240= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:240= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:240= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:240= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:240= 3
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:240= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:240= 1	
		ENDIF		
	ENDIF	
ENDIF

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:GYOU == 0
	PRINTW

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_E（4972을 캐릭터번호로 치환）
;촉수의 사정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_E_K4972
#DIM GYOU
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_E
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4972을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K4972
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K4972
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;촉수의 사정시의 텍스트
;-------------------------------------------------

FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT
;-------------------------------------------------
;촉수가 사정
;-------------------------------------------------
IF TFLAG:15 >0
	CALL PALAM_MESSAGE_E			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:241 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:241= 4
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:241= 3
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:241= 3
		ENDIF	
		;함락완료共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:241= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:241 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:241= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:241 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		PRINTFORML 
		CFLAG:241= 1	
	ENDIF		
ENDIF

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:GYOU == 0
	PRINTW
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_B 관련（4972을 캐릭터번호로 치환）
;노예의 절정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_B_K4972
#DIM GYOU
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_B
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4972을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K4972
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K4972
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;노예의 절정시의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;처음 C절정
;-------------------------------------------------
IF NOWEX:0 > 0 && CFLAG:242 == 0
	PRINTFORMW 
	CFLAG:242 = 1
ENDIF

;-------------------------------------------------
;처음 V절정
;-------------------------------------------------
IF NOWEX:1 > 0 && CFLAG:243 == 0
	PRINTFORMW 
	CFLAG:243 = 1
ENDIF

;-------------------------------------------------
;처음 A절정
;-------------------------------------------------
IF NOWEX:2 > 0 && CFLAG:244 == 0
	PRINTFORMW 
	CFLAG:244 = 1
ENDIF

;-------------------------------------------------
;처음 B절정
;-------------------------------------------------
IF NOWEX:3 > 0 && CFLAG:245 == 0
	PRINTFORMW 
	CFLAG:245 = 1
ENDIF

;-------------------------------------------------
;절정
;-------------------------------------------------

FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT

;강절정
IF TFLAG:29 > 1
	CALL PALAM_MESSAGE_B			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:246 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:246= 4
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:246= 3
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:246= 3
		ENDIF	
		;함락완료共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:246= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:246= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:246 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:246= 1	
	ENDIF		
;절정
ELSEIF TFLAG:29 > 0
	CALL PALAM_MESSAGE_B			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:246 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:246= 4
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:246= 3
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:246= 3
		ENDIF	
		;함락완료共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:246= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:246= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:246 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		PRINTFORML 
		CFLAG:246= 1	
	ENDIF		
ENDIF

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;사정
;-------------------------------------------------
FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT
;사정하고 있다
IF TFLAG:10 > 0				
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:247 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:247= 4
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:247= 3
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:247= 3
		ENDIF	
		;함락완료共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:247= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:247 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:247= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:247 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		PRINTFORML 
		CFLAG:247= 1	
	ENDIF		
ENDIF

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;분유
;-------------------------------------------------
FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT
;분유하고 있다
IF TFLAG:11 > 0			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:248 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:248= 4
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:248= 3
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:248= 3
		ENDIF	
		;함락완료共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:248= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:248 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:248= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:248 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		PRINTFORML 
		CFLAG:248= 1	
	ENDIF		
ENDIF

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;방뇨
;-------------------------------------------------
FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT
;방뇨하고 있다
IF TFLAG:29 > 0 && (TEQUIP:27 || TALENT:57)			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:249 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:249= 4
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:249= 3
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:249= 3
		ENDIF	
		;함락완료共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:249= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:249 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:249= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:249 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		PRINTFORML 
		CFLAG:249= 1	
	ENDIF		
ENDIF

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_C 관련（4972을 캐릭터번호로 치환）
;조교 후의 변화를 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_C_K4972
#DIM GYOU
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_C
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4972을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K4972
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K4972
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교 후의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;처녀상실(처녀만)
;-------------------------------------------------

FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT
IF TFLAG:3 > 0
	CALL PALAM_MESSAGE_C
	;연모나 친애이면서 주인의 섹스에 의한 처녀상실
	IF TFLAG:20
		PRINTFORMW 
	;그 이외의 때에 처녀상실
	ELSE			
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
		ENDIF	
		;굴복각인Lv3
		IF MARK:2 == 3 && LOCALS:0 == ""
			LOCALS:0 = 
		ENDIF
		;공통	
		IF LOCALS:0 == ""	
			PRINTFORML 
		ENDIF	
	ENDIF
ENDIF

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:GYOU == 0
	PRINTW

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_D 관련（4972을 캐릭터번호로 치환）
;파라미터 변화를 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_D_K4972
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_D
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4972을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K4972
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K4972
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;파라미터 변화시의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;처음 윤활이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:4 + CUP:4
IF P > PALAMLV:4 && CFLAG:250 == 0
	CALL PALAM_MESSAGE_D
	;로션을 사용한 경우
	IF SELECTCOM == 180
		PRINTFORMW 
	;그 이외
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:250 = 1
ENDIF

;-------------------------------------------------
;처음 욕정이 LV2를 넘었다
;-------------------------------------------------
P = PALAM:6 + CUP:6
IF P > PALAMLV:3 && CFLAG:251 == 0
	CALL PALAM_MESSAGE_D
	;미약을 사용한 경우
	IF SELECTCOM == 181
		PRINTFORMW 
	;그 이외
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:251 = 1
ENDIF

;-------------------------------------------------
;처음 굴복이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:7 + CUP:7
IF P > PALAMLV:3 && CFLAG:252 == 0
	CALL PALAM_MESSAGE_D
	PRINTFORMW 
	CFLAG:252 = 1
ENDIF

;-------------------------------------------------
;처음 복종이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:5 + CUP:5
IF P > PALAMLV:3 && CFLAG:253 == 0
	CALL PALAM_MESSAGE_D
	;함락완료
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		PRINTFORMW 
	;그 이외
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:253 = 1
ENDIF

;-------------------------------------------------
;처음 공포가 LV3을 넘었다
;-------------------------------------------------
P = PALAM:11 + CUP:11
IF P > PALAMLV:3 && CFLAG:254 == 0
	CALL PALAM_MESSAGE_D
	;함락완료
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		PRINTFORMW 
	;그 이외
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:254 = 1
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_MARKCNG 관련（4972을 캐릭터번호로 치환）
;각인 변동을 트리거로 한 구상
;각인 변동 후에 구상을 발동합니다
;-------------------------------------------------
@KOJO_MESSAGE_MARKCNG_K4972
#DIM GYOU
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL MARK_MESSAGE
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4972을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_MARKCNG_PY1_K4972
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_MARKCNG_PY1_K4972
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;고통각인Lv3
;-------------------------------------------------

FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT

IF TFLAG:22 == 3
	CALL MARK_MESSAGE_A	
	;연모, 친애	
	IF (TALENT:3 || TALENT:6)	
		LOCALS:0 = 
	;음란, 창부	
	ELSEIF (TALENT:4 || TALENT:7)	
		LOCALS:0 = 
	;복종, 예속	
	ELSEIF (TALENT:5 || TALENT:8)	
		LOCALS:0 = 
	ENDIF	
	;공통
	IF LOCALS:0 == ""		
		PRINTFORML 
	ENDIF		
ENDIF

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;쾌락각인Lv3
;-------------------------------------------------
FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT

IF TFLAG:23 == 3
	CALL MARK_MESSAGE_P
	;연모, 친애	
	IF (TALENT:3 || TALENT:6)	
		LOCALS:0 = 
	;복종, 예속	
	ELSEIF (TALENT:5 || TALENT:8)	
		LOCALS:0 = 
	ENDIF	
	;공통
	IF LOCALS:0 == ""		
		PRINTFORML 
	ENDIF		
ENDIF

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;굴복각인Lv3
;-------------------------------------------------
IF TFLAG:24 == 13 || TFLAG:24 == 23 || TFLAG:24 == 33
	CALL MARK_MESSAGE_S
	;쾌락에 의한 굴복
	IF TFLAG:24 == 13
		PRINTFORMW 
	;공포와 고통에 의한 굴복
	ELSEIF TFLAG:24 == 23
		PRINTFORMW 
	;굴복과 복종에 의한 굴복
	ELSEIF TFLAG:24 == 33
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;반발각인Lv3
;-------------------------------------------------
FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT

IF TFLAG:21 == 3
	CALL MARK_MESSAGE_R
	;함락완료
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
		ENDIF	
		;함락완료共通	
		IF LOCALS:0 == ""
			LOCALS:0 = 
		ENDIF	
	ENDIF
	;그 이외
	IF LOCALS:0 == ""
		PRINTFORML 
	ENDIF
ENDIF

FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;이벤트구상 모음（4972을 캐릭터번호로 치환한다） CFLAG 261～299를 사용
;-------------------------------------------------
@KOJO_MESSAGE_EVENT_K4972

;--------------------------------------------------
;특정 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4972을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_EVENT_PY1_K4972
;	RETURN 0
;ENDIF

#DIM GYOU

FOR GYOU,0,51
	LOCALS:GYOU = 
	LOCAL:GYOU = 0
NEXT

SELECTCASE TFLAG:13
		;-------------------------------------------------
		;아침펠라
		;-------------------------------------------------
	CASE 1
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:261 < 1 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:261 = 1
			ENDIF

		;-------------------------------------------------
		;아침파이즈리
		;-------------------------------------------------
	CASE 2
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:262 < 1 || FLAG:7 == 2)
				;모유체질
				IF TALENT:149						
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락완료共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
				ENDIF
				;모유체질じゃない
				IF LOCALS:0 == ""
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락완료共通	
					IF LOCALS:0 == ""	
						PRINTFORML 
					ENDIF	
				ENDIF
				CFLAG:262 = 1
			ENDIF

		;-------------------------------------------------
		;아침기승위
		;-------------------------------------------------
	CASE 3
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침기승위A
		;-------------------------------------------------
	CASE 24
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침키스 연모, 친애 전제
		;-------------------------------------------------
	CASE 25
			CALL EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침식사
		;-------------------------------------------------
	CASE 26
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;노래 부르면서
				IF (TALENT:25 || TALENT:28) && ABL:14 >= 2						
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락완료共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
				ENDIF
				;노래 부르지 않음
				IF LOCALS:0 == ""
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락완료共通	
					IF LOCALS:0 == ""	
						PRINTFORML 
					ENDIF	
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;일어나자 곁에서 자고 있다 연모, 친애 전제
		;-------------------------------------------------
	CASE 27
			CALL EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침풋잡
		;-------------------------------------------------
	CASE 28
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;아침에 갓 짠 밀크
		;-------------------------------------------------
	CASE 29
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;잠자다 실례
		;-------------------------------------------------
	CASE 30
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 레즈플레이
		;-------------------------------------------------
	CASE 4
			CALL EVENT_MESSAGE
			;처음
			IF CFLAG:264 == 0
				PRINTFORML 
				CFLAG:264 = 1
			;그 이외
			ELSEIF CFLAG:264 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:264 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 섹스
		;-------------------------------------------------
	CASE 5
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:265 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:265 = 1
			ENDIF

		;-------------------------------------------------
		;강정신약의 반동
		;-------------------------------------------------
	CASE 6
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:266 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:266 = 2
			ENDIF
			IF (CFLAG:266 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:266 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 착유
		;-------------------------------------------------
	CASE 7
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:267 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:267 = 2
			ENDIF
			IF (CFLAG:267 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:267 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 자위
		;-------------------------------------------------
	CASE 8
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:268 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:268 = 2
			ENDIF
			IF (CFLAG:268 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:268 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인C 상대V）
		;-------------------------------------------------
	CASE 9
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인C 상대A）
		;-------------------------------------------------
	CASE 31
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인V 상대C）
		;-------------------------------------------------
	CASE 32
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인A 상대C）
		;-------------------------------------------------
	CASE 33
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（양쪽C（스마타）・양쪽C（조개맞대기））
		;-------------------------------------------------
	CASE 34
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;밤 자위
		;-------------------------------------------------
	CASE 35
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;같이잔다 연모, 친애
		;-------------------------------------------------
	CASE 36
			CALL EVENT_MESSAGE
			IF CFLAG:269 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;황색사탕 사용시
		;-------------------------------------------------
	CASE 10
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:270 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:270 = 2
			ENDIF
			IF (CFLAG:270 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:270 = 1
			ENDIF

		;-------------------------------------------------
		;임신 발각시
		;-------------------------------------------------
	CASE 11
			SIF CFLAG:271 >= 1 && FLAG:7 != 2
				RETURN 0
			CALL EVENT_MESSAGE
			;임신대상을 TARGET에 격납하고, 부친은 노예는 V, 조수는 W로 한다
			;붕괴되어 버린 경우
			IF TALENT:9 == 1
				PRINTFORML 
			;부친이 주인이고 모친이 연모나 친애 보유
			ELSEIF (TALENT:3 || TALENT:6) && CFLAG:102 == 1
				PRINTFORML 
			;부친이 노예나 조수고 모친이 음란이나 창부 보유
			ELSEIF (TALENT:4 || TALENT:7) && (CFLAG:102 == 2 || CFLAG:102 == 3)
				;부친(조수）과의 상성이 125보다 큰 경우
				IF CFLAG:102 == 2 && GET_RELATION(TARGET, W) > 125
					PRINTFORML 
			 	;부친(노예）과의 상성이 125보다 큰 경우
				ELSEIF CFLAG:102 == 3 && GET_RELATION(TARGET, V) > 125
					PRINTFORML 
				ELSE
					PRINTFORML 
				ENDIF
			;복종이나 예속 보유
			ELSEIF (TALENT:5 || TALENT:8)
				PRINTFORML 
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:271 = 1

		;-------------------------------------------------
		;출산시
		;-------------------------------------------------
	CASE 12
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:272 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:272 = 2
			ENDIF
			IF (CFLAG:272 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:272 = 1
			ENDIF

		;-------------------------------------------------
		;육아실
		;-------------------------------------------------
	CASE 13
			CALL EVENT_MESSAGE
			;출산전
			IF TALENT:153
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 2 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락완료共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
					CFLAG:273 = 2
				ENDIF
				IF (CFLAG:273 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML 
					CFLAG:273 = 1
				ENDIF
			;출산후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 4 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락완료共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
					CFLAG:273 = 4
				ENDIF
				IF (CFLAG:273 < 3 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML 
					CFLAG:273 = 3
				ENDIF
			ENDIF

		;-------------------------------------------------
		;자식 독립시
		;-------------------------------------------------
	CASE 14
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:274 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:274 = 2
			ENDIF
			IF (CFLAG:274 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:274 = 1
			ENDIF
		;-------------------------------------------------
		;정식포장마차시
		;-------------------------------------------------
	CASE 15
			CALL EVENT_MESSAGE
			;처음
			IF CFLAG:275 == 0
				PRINTFORML 
				CFLAG:275 = 1
			;그 이외
			ELSEIF CFLAG:275 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:275 = 1
			ENDIF

		;-------------------------------------------------
		;창관에서 아르바이트・설정시
		;-------------------------------------------------
	CASE 16
			CALL EVENT_MESSAGE
			;처음
			IF CFLAG:276 == 0
				PRINTFORML 
				CFLAG:276 = 1
			;그 이외
			ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:276 = 1
			ENDIF
		;-------------------------------------------------
		;창관에서 아르바이트・실행시
		;-------------------------------------------------
	CASE 63
			CALL EVENT_MESSAGE
			;처음
			IF CFLAG:606 == 0
				PRINTFORML 
				CFLAG:606 = 1
			;그 이외
			ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:606 = 1
			ENDIF


		;-------------------------------------------------
		;렌탈시
		;-------------------------------------------------
	CASE 17
			CALL EVENT_MESSAGE
			;처음
			IF CFLAG:277 == 0
				PRINTFORML 
				CFLAG:277 = 1
			;그 이외
			ELSEIF CFLAG:277 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:277 = 1
			ENDIF

		;-------------------------------------------------
		;매각시
		;-------------------------------------------------
	CASE 18
			CALL EVENT_MESSAGE
			;반발각인Lv3
			IF MARK:3 == 3
				LOCALS:0 = 
			ENDIF
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && MARK:3 < 3 && LOCALS:0 == ""
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
			ENDIF
			;그 이외
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF

		;-------------------------------------------------
		;비디오 매각시
		;-------------------------------------------------
	CASE 19
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:278 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:278 = 2
			ENDIF
			IF (CFLAG:278 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:278 = 1
			ENDIF

		;-------------------------------------------------
		;콘서트시
		;-------------------------------------------------
	CASE 20
			CALL EVENT_MESSAGE
			;처음
			IF CFLAG:279 == 0
				PRINTFORML 
				CFLAG:279 = 1
			;그 이외
			ELSEIF CFLAG:279 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:279 = 1
			ENDIF

		;-------------------------------------------------
		;난교라이브시
		;-------------------------------------------------
	CASE 21
			CALL EVENT_MESSAGE
			;처음
			IF CFLAG:280 == 0
				PRINTFORML 
				CFLAG:280 = 1
			;그 이외
			ELSEIF CFLAG:280 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:280 = 1
			ENDIF

		;-------------------------------------------------
		;공중변소 플레이시
		;-------------------------------------------------
	CASE 22
			CALL EVENT_MESSAGE
			;처음
			IF CFLAG:281 == 0
				PRINTFORML 
				CFLAG:281 = 1
			;그 이외
			ELSEIF CFLAG:281 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:281 = 1
			ENDIF

		;-------------------------------------------------
		;비디오 보유시
		;-------------------------------------------------
	CASE 23
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:282 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:282 = 2
			ENDIF
			IF (CFLAG:282 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:282 = 1
			ENDIF

		;-------------------------------------------------
		;기억소거약 사용시
		;-------------------------------------------------
	CASE 30
			CALL EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:283 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락완료共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:283 = 2
			ENDIF
			IF (CFLAG:283 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:283 = 1
			ENDIF

		;-------------------------------------------------
		;;[아내][남편]취득시 친애 전제
		;-------------------------------------------------
	CASE 40
			CALL EVENT_MESSAGE
			IF CFLAG:283 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:283 = 1
			ENDIF
		;-------------------------------------------------
		;;[음마]취득시 창부 전제
		;-------------------------------------------------
	CASE 41
			CALL EVENT_MESSAGE
			IF CFLAG:284 < 1 || FLAG:7 == 2
				PRINTFORMW %CALLNAME%の体を引き寄せると、その耳に金の耳飾を着けてやった。
				PRINTFORMW 存在を逆に変質させる呪を해방し、おまえは음마だと囁く。
				PRINTFORMW 「음마…？」
				PRINTFORMW 姿見を示す。そこには煌めく呪いの耳飾に彩られた、淫欲に溺れた창부がいる。
				PRINTFORMW 「%CSTR:일인칭%は음마なの…？」
				PRINTFORMW おまえのような음란な인간がいるか。
				PRINTFORMW 섹스漬けの日々を送り、精と쾌락を貪るために磨き上げられた、인간も악마も욕정させる淫らな存在。
				PRINTFORMW 눈にした誰もが犯したいと望む蠱惑の乙女。
				PRINTFORMW 「そうだったんだ…。%CSTR:일인칭%は음마だったのね…」
				PRINTFORMW 「くすくす…。嬉しい…。これから永遠に、당신を유혹して、당신に犯されるために生きるの」
				PRINTFORMW 「…さよなら、弱い%CSTR:일인칭%。そしてこんにちは、음마の%CSTR:일인칭%…」
				PRINTFORMW 　
				PRINTFORMW 　
				PRINTFORMW ―――%CALLNAME%は%CALLNAME:MASTER%の[음마]になりました―――
				PRINTFORML 
				CFLAG:284 = 1
			ENDIF
		;-------------------------------------------------
		;;[전속노예]취득시 예속 전제
		;-------------------------------------------------
	CASE 42
			CALL EVENT_MESSAGE
			IF CFLAG:285 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:285 = 1
			ENDIF
		;-------------------------------------------------
		;;오픈/클로즈・오프・가슴
		;-------------------------------------------------
	CASE 44
			CALL EVENT_MESSAGE
			IF TEQUIP:가슴앞열림
				;가슴을 드러낸다
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락완료共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
				ENDIF
				;그 외
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:285 = 1
			ELSE
				;가슴을 집어넣는다
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락완료共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
				ENDIF
				;그 외
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:287 = 1
			ENDIF
		;-------------------------------------------------
		;;[애ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 45
		CALL EVENT_MESSAGE
		;함락완료
		IF (CFLAG:288 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF	
				ENDIF
				;함락완료共通	
				IF LOCALS:0 == ""	
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						PRINTFORML 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						PRINTFORML 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						PRINTFORML 
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:애ＮＴＲ진행도 == 0
					PRINTFORML 
				;2번째（리치）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 1
					PRINTFORML 
				;3번째（함락）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 2
					PRINTFORML 
				ENDIF
			ENDIF
			CFLAG:288 = 1
		ENDIF
		;-------------------------------------------------
		;;[음ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 46
		CALL EVENT_MESSAGE
		;함락완료
		IF (CFLAG:289 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				ENDIF
				;함락완료共通	
				IF LOCALS:0 == ""	
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						PRINTFORML 
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						PRINTFORML 
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						PRINTFORML 
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:음ＮＴＲ진행도 == 0
					PRINTFORML 
				;2번째（리치）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 1
					PRINTFORML 
				;3번째（함락）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 2
					PRINTFORML 
				ENDIF
			ENDIF
			CFLAG:289 = 1
		ENDIF
		;-------------------------------------------------
		;;[예ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 47
		CALL EVENT_MESSAGE
		;함락완료
		IF (CFLAG:290 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				ENDIF
				;함락완료共通	
				IF LOCALS:0 == ""	
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						PRINTFORML 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						PRINTFORML 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						PRINTFORML 
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:예ＮＴＲ진행도 == 0
					PRINTFORML 
				;2번째（리치）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 1
					PRINTFORML 
				;3번째（함락）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 2
					PRINTFORML 
				ENDIF
			ENDIF
			CFLAG:290 = 1
		ENDIF
		;-------------------------------------------------
		;;애(愛)계 재함락시
		;;주인에게 연모계 함락되었거나, 한번 애(愛)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 48
		CALL EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
		
		IF (CFLAG:291 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				PRINTFORML 
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:291 = 1
		ENDIF
		;-------------------------------------------------
		;;음(淫)계 재함락시
		;;주인에게 음란계 함락되었거나, 한번 음(淫)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 49
		CALL EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
					
		IF (CFLAG:292 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				PRINTFORML 
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:292 = 1
		ENDIF
		;-------------------------------------------------
		;;예(隷)계 재함락시
		;;주인에게 복종계 함락되었거나, 한번 예(隷)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 50
		CALL EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
					
		IF (CFLAG:293 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				PRINTFORML 
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:293 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・난교파티
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 51
		CALL EVENT_MESSAGE
		IF (CFLAG:294 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				LOCALS:0 =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				LOCALS:0 = 
			ENDIF	
			;음란、함락済み共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:294 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・공원
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 52
		CALL EVENT_MESSAGE
		IF (CFLAG:295 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				LOCALS:0 =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				LOCALS:0 = 
			ENDIF	
			;음란、함락済み共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:295 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・변기
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 53
		CALL EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				LOCALS:0 =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				LOCALS:0 = 
			ENDIF	
			;음란、함락済み共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・밤의 거리
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 54
		CALL EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				LOCALS:0 =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				LOCALS:0 = 
			ENDIF	
			;음란、함락済み共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ전 밀회
		;;ＮＴＲ상태가 되기 전에 밀회할 때
		;-------------------------------------------------
	CASE 55
		CALL EVENT_MESSAGE
		IF (CFLAG:297 < 1 || FLAG:7 == 2)				
			IF CFLAG:함락캐릭터 != 0
			;	;주인 이외에 함락되어 있는 경우
				LOCALS:0 = 
			ELSEIF (TALENT:3 || TALENT:6)	
			;	;연모 루트
				LOCALS:0 = 
			ELSEIF (TALENT:4 || TALENT:7)	
			;	;음란 루트
				LOCALS:0 = 
			ELSEIF (TALENT:5 || TALENT:8)	
			;	;복종 루트
				LOCALS:0 = 
			ENDIF	
			;함락완료共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:297 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ후 첫 밀회
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)가 된 직후 하루 종료 후, 한번만 행한다
		;다른 캐릭터에 함락될 때마다 일어난다. 같은 캐릭터에 다시 함락되는 경우는 일어나지 않는다
		;-------------------------------------------------
	CASE 56
		CALL EVENT_MESSAGE
		IF (CFLAG:298 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;공통	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:298 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・데이트
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;-------------------------------------------------
	CASE 57
		CALL EVENT_MESSAGE
		IF (CFLAG:299 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;공통	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:299 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・본방
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;여성이라면 삽입당하는 쪽, 남성이라면 삽입하는 쪽으로 고정
		;-------------------------------------------------
	CASE 58
		CALL EVENT_MESSAGE
		IF (CFLAG:601 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;공통	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:601 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・자위서로과시
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;애ＮＴＲ상태인 경우, 노출증 3이나 자위중독 1 중 하나를 충족시키는 것이 조건이 됩니다.
		;-------------------------------------------------
	CASE 59
		CALL EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;공통	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・애널
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;CFLAG:애널횟수가 1 이상이면 삽입당하고, CFLAG:삽입횟수가 1 이상면 삽입하고 있습니다.
		;양 플래그 모두 서있는 경우, 지문에서는 삽입당하는 쪽이 우선됩니다.
		;-------------------------------------------------
	CASE 60
		CALL EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;공통	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외플레이
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;애ＮＴＲ의 경우, 노출증이 4 이상이 아니라면 발생하지 않습니다
		;-------------------------------------------------
	CASE 61
		CALL EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;공통	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외조교
		;;예ＮＴＲ상태(TALENT:ＮＴＲ == 3, 6)에서 CFLAG:현ＮＴＲ진행도가 6이상인 경우 하루 종료시에 발생.
		;-------------------------------------------------
	CASE 62
		CALL EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			;예ＮＴＲ
			PRINTFORML 
			CFLAG:605 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마와 섹스한다
		;당하는 쪽
		;PLAYER에 실행자의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 79
		CALL EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마에게 봉사시킨다
		;봉사하는 쪽
		;PLAYER에 실행자의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 80
		CALL EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：그 장소에서 자위
		;봉사하는 쪽
		;-------------------------------------------------
	CASE 81
		CALL EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：그늘에서 자위
		;봉사하는 쪽
		;-------------------------------------------------
	CASE 82
		CALL EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:605 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마와 섹스한다
		;하는 쪽
		;PLAYER에 상대의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 83
		CALL EVENT_MESSAGE
		IF (CFLAG:606 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:606 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마에게 봉사시킨다
		;봉사받는 쪽
		;PLAYER에 봉사하는 쪽의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 84
		CALL EVENT_MESSAGE
		IF (CFLAG:607 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:607 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：윤간이벤트
		;봉사하는 쪽
		;PLAYER에 실행자 = 윤간하는 악마의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 85
		CALL EVENT_MESSAGE
		IF (CFLAG:608 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:608 = 1
		ENDIF
		

		;-------------------------------------------------
		;교회에서 기도
		;-------------------------------------------------
	CASE 65
			CALL EVENT_MESSAGE
			;교회の종류によって分ける
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:609 == 0
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:609 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:609 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:610 == 0
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:610 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:610 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:611 == 0
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:611 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:611 = 1
					ENDIF
			ENDSELECT
		;-------------------------------------------------
		;교회에서 입봉사
		;-------------------------------------------------
	CASE 66
			CALL EVENT_MESSAGE
			;교회の종류によって分ける
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:612 == 0
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:612 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:612 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:613 == 0
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:613 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:613 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:614 == 0
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:614 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:614 = 1
					ENDIF
			ENDSELECT
		;-------------------------------------------------
		;교회에서 성봉사
		;-------------------------------------------------
	CASE 67
			CALL EVENT_MESSAGE
			;교회の종류によって分ける
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:615 == 0
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:615 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:615 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:616 == 0
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:616 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:616 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:617 == 0
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:617 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;뉴트럴
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:617 = 1
					ENDIF
			ENDSELECT
	CASEELSE
		SIF FLAG:6 > 0
			CALL EVENT_MESSAGE
ENDSELECT


FOR GYOU,0,51
	SIF LOCALS:GYOU == ""
		BREAK
	PRINTFORM  %LOCALS:GYOU%
	IF LOCAL:GYOU
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:GYOU == 0
	PRINTW
;-------------------------------------------------
;투기장 패배시 구상 모음（4972을 캐릭터번호로 치환한다） 기본적으로 보면 게임오버기 때문에 플래그를 사용하지 않도록
;-------------------------------------------------
@KOJO_MESSAGE_LOSE_COLOSSEUM_K4972
SELECTCASE TFLAG:투기장패배
		;-------------------------------------------------
		;오니＆덩굴
		;-------------------------------------------------
	CASE 1
			CALL MESSAGE_LOSE_COLOSSEUM
			IF 1 == 2
			;연모, 친애
			;IF TALENT:3 || TALENT:6
			;	PRINTFORMW 
			;음란, 창부
			;ELSEIF TALENT:4 || TALENT:7
			;	PRINTFORMW 
			;복종, 예속
			;ELSEIF TALENT:5 || TALENT:8
			;	PRINTFORMW 
			ELSE
				PRINTFORMW 
			ENDIF
ENDSELECT


;-------------------------------------------------
;@KOJO_MESSAGE_ENDING 관련(4972, X2를 캐릭터 번호로 치환한다)
;-------------------------------------------------
;-------------------------------------------------
;엔딩구상의 유무를 판별하는 함수
;기술한 엔딩의 종류에 따라 주석처리를 해제
;-------------------------------------------------
@ENDING_CHECK_K4972
SELECTCASE TSTR:END종별
	;CASE "개별END"
	;	SIF TALENT:아내 || TALENT:남편
	;		RETURN 1
	;	SIF TALENT:음마
	;		RETURN 1
	;	SIF TALENT:노리개
	;		RETURN 1
	;CASE "조교사경영END"
	;	RETURN 1
	;CASE "희대의조교사END"
	;	RETURN 1
	;CASE "식당경영END"
	;	RETURN 1
	;CASE "창관경영END"
	;	RETURN 1
	;CASE "아이돌END"
	;	RETURN 1
ENDSELECT
RETURN 0

;-------------------------------------------------
;엔딩 본문
;-------------------------------------------------
@KOJO_MESSAGE_ENDING_K4972
;--------------------------------------------------
SELECTCASE TSTR:END종별
	;CASE "개별END"
		;연모 1체 엔딩
		;IF TALENT:아내 || TALENT:남편 && CFLAG:21 == 1
			;DRAWLINE
			;PRINTFORMW …………
			;PRINTFORMW ………
			;PRINTFORMW ……
			;PRINTFORMW 

			;PRINTFORMW 
			;PRINTFORMW 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;음란 1체 엔딩
		;ELSEIF TALENT:음마 && CFLAG:21 == 1
			;DRAWLINE
			;PRINTFORMW 昼も夜もわからない空間で宴は続いていた
			;PRINTFORMW その空間では何人もの%CALLNAME%が、何人もの%CALLNAME:MASTER%や악마たちに犯されていた
			;PRINTFORMW 夢なのか現実なのかも定かではないが、%CALLNAME%を犯せるのであれば何も問題はない
			;PRINTFORMW 疲労も空腹も無く、蠱惑的な%CALLNAME%の声と仕草に욕정し、ただ쾌락を貪る
			;PRINTFORMW 何をしても良いのだ。%CALLNAME%は음마なのだから。それを望んでいるのだから
			;PRINTFORMW 終わりも飽きも無い쾌感。何時から続いているのかも定かでは無い
			;PRINTFORMW この状況から抜け出す理由など何処にも無い
			;PRINTFORMW ならばこの爛れ堕落しきった%CALLNAME%と闇の세계を愉しもう
			;PRINTFORMW 세계が終わり、次の세계が始まるまでは
			;PRINTFORMW 
			;PRINTFORMW 　　　　　　　　―― Extra Ending　（마이ルド음마エンド）
		;복종 1체 엔딩
		;ELSEIF TALENT:노리개 && CFLAG:21 == 1
			;DRAWLINE
			;PRINTFORMW …………
			;PRINTFORMW ………
			;PRINTFORMW ……
			;PRINTFORMW 

			;PRINTFORMW 
			;PRINTFORMW 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;ENDIF
	;CASE "조교사경영END"
	;CASE "희대의조교사END"
	;CASE "식당경영END"
	;CASE "창관경영END"
	;CASE "아이돌END"
ENDSELECT
;개별END의 경우 여기서 RETURN 1하면, 그 후 게임은 계속되지 않고 다음 주차로 넘어갈지 게임종료할지 선택하게 됩니다
RETURN 1


@ENEMY_BATTLE_EVENT_K4972, ARG, ARG:1, ARG:2
IF TFLAG:전투이벤트 == 1
;적에서의 데빌ＣＯ－ＯＰ 발동시의 대사
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 2
;적에서의 데밀ＣＯ－ＯＰ 참가시의 대사
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

@BATTLE_EVENT_K4972, ARG, ARG:1, ARG:2
IF TFLAG:전투이벤트 == 1
	;아군 파티에서의 데빌ＣＯ－ＯＰ 발동시의 대사
	;イ마이チ美しくないが、用量削減のため囲いを分割（上）。
	;（下）は、『イ마이チ美しくないが』で検索권장
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	IF RAND:8 == 0
		PRINTFORML ┃% CSTR:이인칭 + "、か弱い私を守ってください",72,LEFT%┃
	ELSEIF RAND:7 == 0
		PRINTFORML ┃%"戦局を変えるの！",72,LEFT%┃
	ELSEIF RAND:6 == 0
		PRINTFORML ┃%"どうやら効いてるみたいです",72,LEFT%┃
	ELSEIF RAND:5 == 0
		PRINTFORML ┃%"ウィーク포인트を共有します",72,LEFT%┃
	ELSEIF RAND:4 == 0
		PRINTFORML ┃%"みんな行って。援護するから",72,LEFT%┃
	ELSEIF RAND:3 == 0
		PRINTFORML ┃%"私以外の全員、とつげきっ！",72,LEFT%┃
	ELSEIF RAND:2 == 0
		PRINTFORML ┃%"ふふふ。追い打ちをお願いします",72,LEFT%┃
	ELSE
		PRINTFORML ┃%"さあ、いきましょう" + CSTR:이인칭,72,LEFT%┃
	ENDIF
	;イ마이チ美しくないが、用量削減のため囲いを分割（下）。
	;（上）は、『イ마이チ美しくないが』で検索권장
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 2
;아군 파티에서의 데빌ＣＯ－ＯＰ 참가시의 대사
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	IF RAND:6 == 0
		PRINTFORML ┃%"マスターの邪魔をするのは許さない。消えてね",72,LEFT%┃
	ELSEIF RAND:5 == 0
		PRINTFORML ┃%"早く駆除しちゃいましょうね～",72,LEFT%┃
	ELSEIF RAND:4 == 0
		PRINTFORML ┃%"くすくす。お깨달음悟ー♪",72,LEFT%┃
	ELSEIF RAND:3 == 0
		PRINTFORML ┃%"張り切って行きましょう",72,LEFT%┃
	ELSEIF RAND:2 == 0
		PRINTFORML ┃%"見ててくださいね、" + CSTR:이인칭,72,LEFT%┃
	ELSE
		PRINTFORML ┃%"援護しますね",72,LEFT%┃
	ENDIF
	;イ마이チ美しくないが、用量削減のため囲いを分割（下）。
	;（上）は、『イ마이チ美しくないが』で検索권장
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 3
;아군パーティでの被소환時の台詞	
;			PRINTFORML ┏＠%CALLNAME:ARG,20,LEFT%━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF


@BATTLE_MESSAGE_K4972,ARG,ARG:1 = 0

;선택한 커맨드로 분기시키고 싶은 경우, IF문의 조건에 명령어 No를 기입할 것
;IF CFLAG:ARG:입력행동 == 
;	PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;	PRINTFORML ┃%"",72,LEFT%┃
;	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

;殴り
IF CFLAG:ARG:입력행동 == 0
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	IF RAND:3 == 0
		PRINTFORML ┃%"데모니카、近接モード",72,LEFT%┃
	ELSEIF RAND:2 == 0
		PRINTFORML ┃%"えい、やあ",72,LEFT%┃
	ELSE
		PRINTFORML ┃%"非힘な私でも데모니카と장비次第です",72,LEFT%┃
	ENDIF
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

;총撃
IF CFLAG:ARG:입력행동 == 2101
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	IF RAND:4 == 0
		PRINTFORML ┃%"こっちこないで！",72,LEFT%┃
	ELSEIF RAND:3 == 0
		PRINTFORML ┃%"邪魔です。鬱陶しいです",72,LEFT%┃
	ELSEIF RAND:2 == 0
		PRINTFORML ┃%"さすがに머리を撃ち抜けば死ぬでしょう",72,LEFT%┃
	ELSE
		PRINTFORML ┃%"데모니카のアシスト通りに撃つ簡가운なお仕事です",72,LEFT%┃
	ENDIF
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

;마석
IF CFLAG:ARG:입력행동 == 3002
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	IF RAND:2 == 0
		PRINTFORML ┃%"このくらいなら마석で十分ね",72,LEFT%┃
	ELSE
		PRINTFORML ┃%"ええと、마석のストックは…っと",72,LEFT%┃
	ENDIF
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
;보옥
IF CFLAG:ARG:입력행동 == 3004
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	PRINTFORML ┃%"凄いでしょう。傷がみるみる消えるの",72,LEFT%┃
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
;화염병
IF CFLAG:ARG:입력행동 == 3101
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	PRINTFORML ┃%"あぎーっ！",72,LEFT%┃
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
;도반옥
IF CFLAG:ARG:입력행동 == 3008
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	PRINTFORML ┃%"ほら、起きて。そしてたたかうの",72,LEFT%┃
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
;SUMMON
IF CFLAG:ARG:입력행동 == 2303
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	IF RAND:4 == 0
		PRINTFORML ┃%"私だってファントムの서머너だもの",72,LEFT%┃
	ELSEIF RAND:3 == 0
		PRINTFORML ┃%"さあ、行きなさい。わが主と私のために",72,LEFT%┃
	ELSEIF RAND:2 == 0
		PRINTFORML ┃%"ふふ。간호사コール、入ってます？",72,LEFT%┃
	ELSE
		PRINTFORML ┃%"えっと、この악마で良かったっけ？",72,LEFT%┃
	ENDIF
	;イ마이チ美しくないが、用量削減のため囲いを分割（下）。
	;（上）は、『イ마이チ美しくないが』で検索권장
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

;Hi-DAS
IF CFLAG:ARG:입력행동 == 2306
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	IF RAND:2 == 0
		PRINTFORML ┃%"解析開始。전투힘200… 400…",72,LEFT%┃
	ELSE
		PRINTFORML ┃%"ええと、약점は… ふうん。そこね？",72,LEFT%┃
	ENDIF
	;イ마이チ美しくないが、用量削減のため囲いを分割（下）。
	;（上）は、『イ마이チ美しくないが』で検索권장
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

;崖っぷち粉末
IF CFLAG:ARG:입력행동 == 3016
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	PRINTFORML ┃%"これを使う日がくるなんて…。あとでご褒美せがまないと…",72,LEFT%┃
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

;디스포이즌など
IF RANGE(CFLAG:ARG:입력행동,3017, 3024)
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	PRINTFORML ┃%"備えあればうれいなしよ",72,LEFT%┃
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

;공격아이템など
IF RANGE(CFLAG:ARG:입력행동,3102, 3119)
	PRINTFORML ┏┓＠%CALLNAME:ARG,20,LEFT%　┏━━━━━━━━━━━━━━━━━━━━━━┓
	IF RAND:2 == 0
		PRINTFORML ┃%"인간はね。道具をつかうのよ",72,LEFT%┃
	ELSE
		PRINTFORML ┃%"出し惜しみは無しよ。깨달음悟してね？",72,LEFT%┃
	ENDIF
	;イ마이チ美しくないが、用量削減のため囲いを分割（下）。
	;（上）は、『イ마이チ美しくないが』で検索권장
	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

;ELSEIF ARG:1 > 0
	;충성도가 상승하는 행동을 취했을 경우에 표시하는 구상

;	CALLFORM SKILL_NAME_{CFLAG:ARG:입력행동},ARG
;	;사용한 스킬의 이름을 LOCALS에 대입
;	LOCALS = %RESULTS%

;	SELECTCASE ARG:1
;		;물리데미지를 주는 행동으로, 영맹 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"영맹")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;마법데미지를 주는 행동으로, 교활 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"교활")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;고위력의 행동으로, 잔학 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"잔학")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;상대의 약점을 찌르는 공격 or 행동불능의 상태이상의 적에 대한 공격으로, 현명 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"현명")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;파마 상성의 행동으로, 천사 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"천사")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;주살 상성의 행동으로, 타천사 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"타천사")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;회복・상태회복 행동으로는 우애 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"우애")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;상태변화로는 교만 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"교만")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;특수로는 냉정 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"냉정")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;우둔 보유자는 주인과의 Lv차이 만큼 충성도가 상승
;		CASE GETNUM(TALENT,"우둔")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;허심 보유자는 최저 ２만큼 충성도가 상승
;		CASE GETNUM(TALENT,"허심")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;	ENDSELECT
;ELSE
;	;시스템에 의지하지 않고 충성도를 높이고 싶거나 다른 목표가 있으면 여기에 기재
;ENDIF

