;-------------------------------------------------
;人修羅くん口上
;幸薄めな少年っぽい人修羅くんを目指す。
;연모、淫乱中心に埋める
;연모→この人の側にいれば安心できる
;음란→戦いで俺の力を活かせるしなんか気持ちいいしいい感じ
;친애→そうか、俺はこの人と一緒に居ていいんだ、一人じゃないんだ
;창부→いっぱい戦ったからいっぱい気持ちよくしてほしい、褒めて褒めて!
;未陥落でも手を出されても気にしないし悲観しない、なんなら好きにしていいという感覚。
;マスター(＠あなた)は女性>男性で想定しています。
;合わない又はこんな口上要らないという方はゴミ箱へ。
;誤字やエラーなどは板へご一報を頂けますとありがたいです。加筆修正バチコイです。
;作業履歴　490メモリー
;20.11.17～　SMコマンドの一部を加筆。
;20.11.19～　恋慕親愛限定コマンド(コミュニケーション系コマンドの一部)を加筆、調節。
;20.11.28～　淫乱陥落ルート開通。
;21.09.21～　絶頂、射精分岐を増強。
;-------------------------------------------------

;-------------------------------------------------
;eraMegaten전용 구상 템플릿
;X1과 X2를 캐릭터 번호로 치환하고, Y1을 조수의 캐릭터 번호로 치환해주세요
;단, 캐릭터번호가 한자리인 캐릭터의 경우, X1, Y1은 1, X2는 01과 같이 바꿔주세요
;그리고 커맨드 내의 적당한 조건 아래의 PRINTFORMW의 뒤에 반각 공백을 두고 대사를 추가해주세요
;구상을 추가하지 않는 커맨드는 무기입하셔도 상관없습니다만, CFLAG:XXX의 앞에『;』를 붙여서 주석처리를 하시면 더욱 좋습니다
;엑스트라 구문 등, 주석처리가 되어 있는 구문을 사용할 때는,『;』를 제거해 주석처리를 해제할 필요가 있습니다
;마지막으로 read me도 훑어보시는걸 추천드립니다
;-------------------------------------------------

;-------------------------------------------------
;범용 구상의 작성에 사용하는 경우는, X1→캐릭터번호가 아니라,
;K4199→PUB회화타입번호　로 변경해서 사용해주세요
;-------------------------------------------------
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	Module		:EVENT_K4199_ASSI.ERB
;	Facility	:회화타입：助手時口上
;
;	Modification Data:
;	Edit	Date			Author					Reason
;	001		20XX/XX/XX		Jガン					新規作成
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;ライセンスについて
;	ㆍ基本項目(許可する項目を"○"に、許可しない項目を"×"に変更してください。)
;	+----+----+-------------------------------+
;	|番号|許可|ライセンス内容                 |
;	+----+----+-------------------------------+
;	|   1| ○ | 処理上のバグ修正              |
;	+----+----+-------------------------------+
;	|   2| ○ | 口上の誤字ㆍ脱字の修正        |
;	+----+----+-------------------------------+
;	|   3| ○ | 口上まとめへの収録            |
;	+----+----+-------------------------------+
;	|   4| ○ | 口上の新規追加                |
;	+----+----+-------------------------------+
;	|   5| ○ | 既存口上の改変                |
;	+----+----+-------------------------------+
;	|   6| ○ | 改変した口上の自由な再配布    |
;	+----+----+-------------------------------+
;	|   7| ○ | 口上内容の転載                |
;	+----+----+-------------------------------+

;--------------------------------------------------
;구상셀렉트 기능：구상설명
;--------------------------------------------------
;이 함수의 주석처리를 해제하면, 구상의 선택시에 선택지로 후보에 오릅니다
;이미 구상이 있는 캐릭터나 회화타입의 다른 버전의 구상을 작성할 때 이용해주세요
;이 때, 이 함수를 포함한 이 파일 내의 함수명 전체에 _임의의1-49(범용구상이면 1-9)의 숫자를 추가해주세요
;예： EVENTTRAIN_K4199 → EVENTTRAIN_K4199_12
;--------------------------------------------------
@셀렉트구상설명_K4199,ARG = 0
SIF ARG == 1
	RETURN 0
;;구상명
PRINTFORML 人修羅くん口上
;;구상설명문
PRINTFORML    趣味に走った口上、(初期は)無口な幸薄くんのイメージです。

;--------------------------------------------------
;EVENTTRAIN 관련（X1을 캐릭터번호로 치환）
;조교 개시시의 대화 CFLAG 201～219를 사용
;--------------------------------------------------
@EVENTTRAIN_K4199
CALL 구상변수초기화
;--------------------------------------------------
;엑스트라모드 구문　CFLAG:220을 사용
;네 를 선택시 첫조교시 구상설정용 소질로 변경 가능합니다
;아래에 처녀화할 경우의 구문을 샘플로 올려두겠습니다
;--------------------------------------------------
IF CFLAG:220 == 0
	PRINTL この口上は素質変更が前提の口上です
	PRINTW この口上に合うように素質を変更しますか？
	PRINTL 
	PRINTL [0] 想定素質ㆍ服装に変更する
	PRINTL [1] 素質を変更しない
	;PRINTL [2] 汎用口上を適用する
	$INPUT_LOOP
	INPUT
	IF RESULT == 0
		PRINTFORMW ―素質を変更して調教を開始します―
		TALENT:불결무시 = 1
		TALENT:얌전함 = 1
		TALENT:습득빠름 = 1
		
		CSTR:이인칭 = 彼方
		CSTR:일인칭 = 俺
	ELSEIF RESULT == 1
		PRINTFORMW ―通常素質で調教を開始します―
	;ELSEIF RESULT == 2
		;サラマンダー口上さんからもってきました
		;;これ動かないの？　わからん
		;PRINTFORMW ―汎用口上で調教を開始します―
		;CFLAG:구상셀렉트 = -1
		;TRYJUMPFORM EVENTTRAIN_PUB{ABL:회화타입}
	ELSE
		GOTO INPUT_LOOP
	ENDIF
	CFLAG:220 = 1
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTTRAIN_PY1_K4199
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL EVENTTRAIN_PY1_K4199
;		RETURN 0
;	ENDIF
;ENDIF
;--------------------------------------------------
;간이 조수구상 발동시에 첫조교시 구상을 스킵（Y1을 조수의 캐릭터번호로 한다）
;여러 명이 있을 경우는, SIF NO:ASSI == Y1 || NO:ASSI == Z1으로 추가해 나간다
;--------------------------------------------------
;IF CFLAG:201 == 0 && ASSI > 0
;	SIF NO:ASSI == Y1
;		CFLAG:201 =1
;ENDIF
;-------------------------------------------------
;조교 개시시（1회만） CFLAG201
;-------------------------------------------------
DRAWLINE
;첫조교시
IF CFLAG:201 == 0 && TALENT:연모 == 0
	KSTR:(K++) = アマラ深界。あの場所に所縁がありそうな悪魔を調教することにしよう。
	KSTR:(K++) = そう思い至った%CALLNAME:MASTER%の頭に金髪の少年が話していた内容が過ぎる。
	WSTR:(K++) = 普通の悪魔では扱えないマガタマを宿す、『半魔人』の悪魔。
	WSTR:(K++) = あの部屋に呼び出されるということが何なのかは%NAME:TARGET%でも判るだろう。
	KSTR:(K++) = 「……%CSTR:이인칭%、よく変な人だって言われてないか？」
	WSTR:(K++) = そんなことを言いながら%CALLNAME:TARGET%は大人しくしていた。
	KSTR:(K++) = 変わらない表情も何所か覚悟を決めた風に強張っている。
	KSTR:(K++) = 暗闇の中を滴る水滴のように身体中の模様を光らせ背を丸める姿態。
	WSTR:(K++) = 気高く孤独な獣は%CALLNAME:MASTER%を観る。
	WSTR:(K++) = %CALLNAME:MASTER%は何者にも立ち入る隙を与えぬよう鍵をかけた。
	CFLAG:201 = 1
	GOTO 表示
;굴복각인Lv1
ELSEIF CFLAG:201 < 2 && MARK:2 == 1
	KSTR:(K++) = 「……今日は何するんだ？」
	WSTR:(K++) = 暇潰しもなく飼い殺されるも同然な待遇だというのに。
	WSTR:(K++) = %CALLNAME:TARGET%の態度は思ったより平気そうだ。
	KSTR:(K++) = %CALLNAME:MASTER%はこんなに調教に慣れるのが早い悪魔が居たのかと衝撃を受けた。
	WSTR:(K++) = しかしこれでは屈服ではなく懐かれているといった方が適している。
	KSTR:(K++) = じっと視線を逸らさない%CALLNAME:TARGET%をベッドへ押し倒した。
	CFLAG:201 = 2
	GOTO 表示
;굴복각인Lv2
ELSEIF CFLAG:201 < 3 && MARK:2 == 2
	KSTR:(K++) = %CALLNAME:MASTER%は部屋の前に立ち鍵を開けた。
	WSTR:(K++) = 何の変哲もない扉の奥には%CALLNAME:TARGET%がいる。
	KSTR:(K++) = 「……」
	WSTR:(K++) = %CALLNAME:MASTER%の姿を認めるや否や口を噤み視線を逸らす。
	KSTR:(K++) = 突然変化の兆しが現れた%CALLNAME:TARGET%の態度。
	WSTR:(K++) = それでも　%CALLNAME:MASTER%は調教の時間だと告げる。
	WSTR:(K++) = この魔人はそれに激しく抵抗する訳でも諦めて受け入れる訳でもないだろう。
	KSTR:(K++) = そこはかとなく苦い顔をしたままの%CALLNAME:TARGET%をベッドへ押し倒した。
	CFLAG:201 = 3
	GOTO 表示
;굴복각인Lv3
ELSEIF CFLAG:201 < 4 && MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	KSTR:(K++) = %CALLNAME:MASTER%はいつも通り部屋の前に立った。
	KSTR:(K++) = 胸騒ぎを覚え%CALLNAME:TARGET%の調教を早めたのだがその必要はなかったのかもしれない。
	WSTR:(K++) = 商品を仕上げるためには念には念を入れたほうがいい。
	KSTR:(K++) = %CALLNAME:MASTER%はは鍵を開け扉を引いて部屋の中へ足を踏み入れる。
	WSTR:(K++) = ここには余計な家具ひとつ置いておらずすっきりしている。
	WSTR:(K++) = その上密室なのでどこにも隠れ様がないはずだ。
	KSTR:(K++) = けれど%CALLNAME:TARGET%の姿は見当たらない。
	WSTR:(K++) = 仲魔たちからの逃げ出した悪魔の報告もなかった。
	KSTR:(K++) = となると。
	KSTR:(K++) = ㆍㆍㆍㆍㆍ
	WSTR:(K++) = ㆍㆍㆍㆍㆍ
	KSTR:(K++) = ベッドの上の布団が怪しい。%CALLNAME:MASTER%は慎重に近づく。
	WSTR:(K++) = わざと足音を立ててじりじりと目標へ迫っていく。
	KSTR:(K++) = 「……わ、ぷっ！」
	WSTR:(K++) = %CALLNAME:TARGET%が緊張感から布団を飛び出す瞬間を狙いすかさず捕獲した。
	KSTR:(K++) = やはり居た。貴重な報酬をみすみす取り逃すなんてあってはならない。
	WSTR:(K++) = まだ腕の中から逃げようと抵抗している%CALLNAME:TARGET%を引っ掴みベッドへ押し倒した。
	CFLAG:201 = 4
	GOTO 表示
;함락
ELSEIF CFLAG:201 < 5 && (TALENT:3 || TALENT:4 || TALENT:5)
	;연모
	IF TALENT:3
		IF CFLAG:미조교함락 == 1
			;조교하지 않고 연모를 단 경우
			WSTR:(K++) = 
		ELSE
			WSTR:(K++) = 
		ENDIF
	;음란
	ELSEIF TALENT:4
		WSTR:(K++) = 
	;복종
	ELSEIF TALENT:5
		WSTR:(K++) = 
	ENDIF
	;공통
	IF !구상유무확인(0)
		KSTR:(K++) = 
	ENDIF
	CFLAG:201 = 5
	GOTO 表示
ENDIF

;-------------------------------------------------
;간이 조수구상（Y1을 조수의 캐릭터번호로 치환, X1을 캐릭터번호로 변경한다) CFLAG203
;말할 캐릭터를 늘릴 경우, 구문을 복사＆붙여넣기 해, CFLAG의 수를 204, 205로 늘린다
;-------------------------------------------------
;조수의 유무를 체크（없을 경우는 2번째 이후로 건너뛴다）
IF ASSI < 0 
	CALL EVENTTRAIN_SECOND_K4199
;조수
;ELSEIF NO:ASSI == Y1
	;처음
;	IF CFLAG:203 == 0
		;함락완료＋함락시 첫 구상이 발생완료
;		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && CFLAG:201 >= 5
;			WSTR:(K++) = 
		;그 이외
;		ELSE
;			KSTR:(K++) = 
;		ENDIF
;		CFLAG:203 = 1
	;2번째 이후
;	ELSEIF CFLAG:203 < 2 || FLAG:7 ==2
		;함락완료
;		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
;			WSTR:(K++) = 
		;그 이외
;		ELSE
;			KSTR:(K++) = 
;		ENDIF
;		CFLAG:203 = 2
;	ENDIF
;구상이 있는 조수가 없을 경우, 통상의 2번째 이후의 구상으로 건너뛴다
ELSE
	CALL EVENTTRAIN_SECOND_K4199
ENDIF

$表示

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM EVENTTRAIN_ZZZ
	CATCH
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;조교 개시시（2번째 이후, X1을 캐릭터번호로 변경한다）
;-------------------------------------------------
@EVENTTRAIN_SECOND_K4199
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:202 < 4 || FLAG:7 == 2)
	KSTR:(K++) = 「……」
	KSTR:(K++) = %CALLNAME:TARGET%は闘志と嫌悪を宿した眼でこちらを見ている。
	CFLAG:202 = 4
;굴복각인Lv0
ELSEIF MARK:2 == 0 && (CFLAG:202 < 1 || FLAG:7 == 2)
	KSTR:(K++) = 「%CSTR:일인칭%は」
	KSTR:(K++) = 「……こんな事されても、なんとも」
	KSTR:(K++) = %CALLNAME:TARGET%の経験人数は知らないが今までの事と一緒にされては困る。
	KSTR:(K++) = %CALLNAME:MASTER%はさっそく調教を始めることにした。
	CFLAG:202 = 1
;굴복각인Lv1
ELSEIF MARK:2 == 1 && (CFLAG:202 < 2 || FLAG:7 == 2)
	KSTR:(K++) = 「……やっぱり。そろそろかと思った」
	CFLAG:202 = 2
;굴복각인Lv2
ELSEIF MARK:2 == 2 && (CFLAG:202 < 3 || FLAG:7 == 2)
	KSTR:(K++) = 「……%CSTR:이인칭%にされること、さ」
	KSTR:(K++) = 「どうやったって……%CSTR:일인칭%のなかに刻まれてくから、こわいんだ」
	CFLAG:202 = 3
;굴복각인Lv3＋함락소질없음
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0) && (CFLAG:202 < 4 || FLAG:7 == 2)
	KSTR:(K++) = 「……」
	KSTR:(K++) = %CALLNAME:TARGET%はむっつりと仏頂面をしている。だが攻撃の意志はなさそうだ。
	CFLAG:202 = 4
;함락완료
ELSEIF CFLAG:202 < 5 || FLAG:7 == 2
	;연모, 친애
	IF TALENT:3 || TALENT:6
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:4 == 0
			WSTR:(K++) = 「あ、%CSTR:이인칭%」
			WSTR:(K++) = 「……わざわざ来てくれたの？」
		ELSEIF RAND:3 == 0
			WSTR:(K++) = 「…%CSTR:이인칭%って…」
			WSTR:(K++) = 「ううん、いや、なんでもない」
		ELSEIF RAND:2 == 0
			WSTR:(K++) = 「いらっしゃい、%CSTR:이인칭%」
			WSTR:(K++) = 「何をしてたのかって?　うーん…」
			WSTR:(K++) = 「ここの掃除とか、それぐらいかな」
		ELSE
			WSTR:(K++) = 「せっかくだし…ちょっとゆっくりしていくのもいいんじゃないか？」
		ENDIF
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			WSTR:(K++) =
		ELSEIF RAND:2 == 0
			WSTR:(K++) =
		ELSE
			WSTR:(K++) =
		ENDIF
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			WSTR:(K++) =
		ELSEIF RAND:2 == 0
			WSTR:(K++) =
		ELSE
			WSTR:(K++) =
		ENDIF
	ENDIF
	;그 외, 공통
	IF !구상유무확인(0)
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			KSTR:(K++) = 
		ELSEIF RAND:2 == 0
			KSTR:(K++) = 
		ELSE
			KSTR:(K++) = 
		ENDIF
	ENDIF
	CFLAG:202 = 5
ENDIF


;-------------------------------------------------
;EVENTEND 관련（X1을 캐릭터번호로 치환）
;조교 종료시의 대사
;-------------------------------------------------
@EVENTEND_K4199
CALL 구상변수초기화
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTEND_PY1_K4199
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	SIF NO:ASSI == Y1
;	CALL EVENTEND_PY1_K4199
;	RETURN 0
;ENDIF
;--------------------------------------------------
;조교 종료시의 대사
;--------------------------------------------------
DRAWLINE
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:217 < 1 || FLAG:7 == 2)
	KSTR:(K++) = 「何にだってやめ時があるもの……」
	KSTR:(K++) = 「そうだろ？」
	KSTR:(K++) = %CALLNAME:TARGET%は今すぐにでもその首をへし折れるぞというサインをしている。
	KSTR:(K++) = それはCOMPで統制されていなければの話だ。
	CFLAG:217 = 1
;굴복각인Lv1 이하
ELSEIF MARK:2 <= 1 && (CFLAG:219 < 1 || FLAG:7 == 2)
	KSTR:(K++) = 「……終わり？」
	KSTR:(K++) = 「……なんでもない」
	CFLAG:219 = 1
;굴복각인Lv2
ELSEIF MARK:2 == 2
	KSTR:(K++) = 「……それじゃあまた」
	CFLAG:219 = 2
;굴복각인Lv3＋함락소질없음
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	KSTR:(K++) = 「……」
	KSTR:(K++) = %CALLNAME:TARGET%はそわそわと居心地が悪そうにしている。
	KSTR:(K++) = 色々な後処理と傷がついていないかを確認する%CALLNAME:MASTER%の事を気にしているようだ。
	CFLAG:219 = 3
;함락완료＋체력 500미만
ELSEIF BASE:0 < 500 && (CFLAG:218 < 1 || FLAG:7 == 2)
	;연모, 친애
	IF TALENT:3 || TALENT:6
		IF RAND:3 == 0
			WSTR:(K++) = 「……」
			WSTR:(K++) = %CALLNAME:TARGET%は体力の限界を迎えたらしくベッドの上で伸びている。
			WSTR:(K++) = 横になって今にも眠りに落ちそうな顔に張り付いた髪の一房を剥がしてやる。
			WSTR:(K++) = %CALLNAME:MASTER%と%CALLNAME:TARGET%の間には穏やかな時間が流れていた。
		ELSEIF RAND:2 == 0
			WSTR:(K++) = 「……%CSTR:이인칭%？」
			WSTR:(K++) = %CALLNAME:MASTER%は気まぐれに%CALLNAME:TARGET%にキスをする。
			WSTR:(K++) = %CALLNAME:TARGET%には%CSTR:이인칭%の皮膚の下に潜む何かが見えた気がした。
		ELSE
			WSTR:(K++) = 微睡み始めた%CALLNAME:TARGET%を尻目に%CALLNAME:MASTER%は身なりを整える。
			WSTR:(K++) = 「……次は、さ…%CSTR:일인칭%も、一緒に…」
			WSTR:(K++) = %CALLNAME:MASTER%の答を聴いた%CALLNAME:TARGET%は目を閉じた。
		ENDIF
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		WSTR:(K++) =
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		WSTR:(K++) =
	ENDIF
	;공통
	IF !구상유무확인(0)
		KSTR:(K++) = 
	ENDIF
	CFLAG:218 = 1
;함락완료
ELSEIF CFLAG:219 < 4 || FLAG:7 == 2
	;연모, 친애
	IF TALENT:3 || TALENT:6
		IF RAND:3 == 0
			WSTR:(K++) = 「%CSTR:일인칭%の、匂い……」
			WSTR:(K++) = 調教で付いた汚れを落としていると%CALLNAME:TARGET%が引っ付いてきた。
			WSTR:(K++) = %CALLNAME:TARGET%は素っ裸の%CALLNAME:MASTER%の匂いを嗅いで数分たりとも離れようとしなかった。
		ELSEIF RAND:2 == 0
			WSTR:(K++) = 「それじゃ、またね」
			WSTR:(K++) = %CALLNAME:TARGET%はすっかりここでの生活に慣れてきたようだ。
		ELSE
			WSTR:(K++) = 「……もう、終わり?」
			WSTR:(K++) = %CALLNAME:TARGET%はまだまだやる気満々だ。
		ENDIF
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		KSTR:(K++) = 
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		KSTR:(K++) = 
	ENDIF
	;공통
	IF !구상유무확인(0)
		WSTR:(K++) =
	ENDIF
	CFLAG:219 = 4
ENDIF

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM EVENTEND_ZZZ
	CATCH
	ENDCATCH
	
ENDIF

;--------------------------------------------------
;@KOJO_MESSAGE_COM 관련（X1을 캐릭터번호로 치환）
;커맨드 실행시에 출력됩니다
;--------------------------------------------------
@KOJO_MESSAGE_COM_K4199
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL TRAIN_MESSAGE
	RETURN 0
ENDIF
;상정외의 구상이라면 스킵(이거 지우면 버그)
IF SELECTCOM < 0 || SELECTCOM > 699
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_COM_PY1_K4199
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_COM_PY1_K4199
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;함수초기화
;-------------------------------------------------
CALL 구상변수초기화
 
CALLFORM COMTYPE_{SELECTCOM}
;-------------------------------------------------
;커맨드 실행시의 대사 CFLAG 300～500
;-------------------------------------------------
IF 0 == 1
;=================================================
;애무계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("애무계")
SIF TEQUIP:25 > 0 && ( SELECTCOM == 0 || SELECTCOM == 5 || SELECTCOM == 6 || SELECTCOM == 7 || SELECTCOM == 8 || SELECTCOM == 12 || SELECTCOM == 13 || SELECTCOM == 15 )
	GOTO SHOKUSHU_PLAY
SELECTCASE SELECTCOM
;-------------------------------------------------
;0,애무
;-------------------------------------------------
	CASE 0
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:300 == 0
			KSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%の肌を這う文様を弄るように撫でる。
			KSTR:(K++) = 「……」
			KSTR:(K++) = ピクリともしない%CALLNAME:TARGET%は無表情のまま身体を預けていた。
			CFLAG:300 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:300 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 「……なんか優しい触り方、だな」
					KSTR:(K++) = 「それなら%CSTR:일인칭%も、したい」
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:300 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:300 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「ぅ、……」
				KSTR:(K++) = ほんの僅かに%CALLNAME:TARGET%から漏れた声に内心驚愕しながら際どい箇所を狙う。
				KSTR:(K++) = %CALLNAME:MASTER%が頸筋にキスを落とせば肩が震えるのも開発が進んだ証拠だろう。
				CFLAG:300 = 3
			ENDIF
			;그 이외
			IF (CFLAG:300 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:300 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;1,커닐링구스
;-------------------------------------------------
	CASE 1
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:301 == 0
			KSTR:(K++) =
			CFLAG:301 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:301 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:301 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:301 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:301 = 3
			ENDIF
			;그 이외
			IF (CFLAG:301 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:301 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;3,손가락삽입
;-------------------------------------------------
	CASE 3
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:303 == 0
			KSTR:(K++) =
			CFLAG:303 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:303 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:303 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:303 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:303 = 3
			ENDIF
			;그 이외
			IF (CFLAG:303 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:303 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;4,애널핥기
;-------------------------------------------------
	CASE 4
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:304 == 0
			KSTR:(K++) = 「……！？」
			KSTR:(K++) = %CALLNAME:TARGET%はあらぬ場所を舐られ声もなく跳び上がった。
			CFLAG:304 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:304 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 「いくらなんでも、そん、な…とこッ…」
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:304 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:304 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:304 = 3
			ENDIF
			;그 이외
			IF (CFLAG:304 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:304 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;5,애널애무
;-------------------------------------------------
	CASE 5
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:305 == 0
			KSTR:(K++) =
			CFLAG:305 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:305 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:305 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:305 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:305 = 3
			ENDIF
			;그 이외
			IF (CFLAG:305 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:305 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;6,가슴주무르기
;-------------------------------------------------
	CASE 6
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:306 == 0
			KSTR:(K++) = 「……%CSTR:일인칭%の胸のどこに揉む要素があるんだ？」
			KSTR:(K++) = げんなりしている%CALLNAME:TARGET%の薄い胸板をもみほぐすように刺激する。
			KSTR:(K++) = 　安心しろ、育ててやるからな。
			KSTR:(K++) = これはきっと%CALLNAME:MASTER%の技量が試されているのだろう。
			CFLAG:306 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:306 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:306 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:306 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「……」
				KSTR:(K++) = %CALLNAME:TARGET%はかなり嫌そうにしている。
				CFLAG:306 = 3
			ENDIF
			;그 이외
			IF (CFLAG:306 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:306 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;7,유두자극
;-------------------------------------------------
	CASE 7
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:307 == 0
			KSTR:(K++) = 「……あまりこすられると痛いからやめてほしい」
			KSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%の主張の薄い乳首を延々と摘まんで擦る。
			KSTR:(K++) = だんだんと乳首が赤く熟れてくると持主から痛みを訴えられた。
			CFLAG:307 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:307 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 「……」
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:307 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:307 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「……ひ、ぃ！」
				KSTR:(K++) = 幾分か勃ちやすくなった%CALLNAME:TARGET%の乳首をれろりと優しく舐る。
				KSTR:(K++) = そのまま舐め続けると解りやすく噛み締めた口から喘ぎ声が漏れた。
				CFLAG:307 = 3
			ENDIF
			;그 이외
			IF (CFLAG:307 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「ぅ、……」
				CFLAG:307 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;8,가슴애무
;-------------------------------------------------
	CASE 8
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:308 == 0
			KSTR:(K++) = %CALLNAME:TARGET%は胸周りばかり弄られ面倒くさそうにしている。
			CFLAG:308 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:308 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:308 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:308 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:308 = 3
			ENDIF
			;그 이외
			IF (CFLAG:308 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:308 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;9,조개젖히기
;-------------------------------------------------
	CASE 9
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:309 == 0
			KSTR:(K++) =
			CFLAG:309 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:309 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:309 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:309 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:309 = 3
			ENDIF
			;그 이외
			IF (CFLAG:309 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:309 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;10,가슴자위
;-------------------------------------------------
	CASE 10
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:310 == 0
			KSTR:(K++) =
			CFLAG:310 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:310 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:310 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:310 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:310 = 3
			ENDIF
			;그 이외
			IF (CFLAG:310 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:310 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;11,자위
;-------------------------------------------------
	CASE 11
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:311 == 0
			KSTR:(K++) = 「……」
			KSTR:(K++) = %CALLNAME:TARGET%はおずおずと自身を弄りながら%CALLNAME:MASTER%の視線を気にしていた。
			CFLAG:311 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:311 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 「%CSTR:일인칭%が一人でシてるの見てて、楽しいの…？」
					KSTR:(K++) = %CALLNAME:MASTER%が肯定すると%CALLNAME:TARGET%は恥ずかしそうに俯いた。
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:311 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:311 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:311 = 3
			ENDIF
			;그 이외
			IF (CFLAG:311 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:311 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;12,날개애무
;-------------------------------------------------
	CASE 12
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:435 == 0
			KSTR:(K++) =
			CFLAG:435 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:435 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:435 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:435 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:435 = 3
			ENDIF
			;그 이외
			IF (CFLAG:435 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:435 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;13,꼬리애무
;-------------------------------------------------
	CASE 13
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:436 == 0
			KSTR:(K++) =
			CFLAG:436 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:436 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:436 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:436 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:436 = 3
			ENDIF
			;그 이외
			IF (CFLAG:436 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:436 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;14,클리애무
;-------------------------------------------------
	CASE 14
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:442 == 0
			KSTR:(K++) =
			CFLAG:442 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:442 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:442 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:442 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:442 = 3
			ENDIF
			;그 이외
			IF (CFLAG:442 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:442 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;15,엉덩이애무
;-------------------------------------------------
	CASE 15
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:444 == 0
			KSTR:(K++) =
			CFLAG:444 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:444 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:444 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:444 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:444 = 3
			ENDIF
			;그 이외
			IF (CFLAG:444 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:444 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;99,애무시킨다
;-------------------------------------------------
	CASE 99
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:462 == 0
			KSTR:(K++) = 「わざわざ%CSTR:일인칭%にさせなくても……」
			KSTR:(K++) = %CALLNAME:TARGET%は命令された通りぎこちなく愛撫してくる。
			CFLAG:462 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:462 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					IF RAND:3 == 0
						WSTR:(K++) = 「%CSTR:이인칭%は、どこが好き…？」
						WSTR:(K++) = 「ぁ……ここ、ひくひく…してる」
					ELSEIF RAND:2 == 0
						WSTR:(K++) = 「ふふ、おっぱい。やわらかくて、ふにゃふにゃだ…」
						WSTR:(K++) = 「……っ、な、なんでもない！」
					ELSE
						WSTR:(K++) = 「%CSTR:일인칭%も%CSTR:이인칭%を気持ちよく出来てたら、いいな…」
					ENDIF
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:462 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:462 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:462 = 3
			ENDIF
			;그 이외
			IF (CFLAG:462 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:462 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;애무계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;커뮤니케이션계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("커뮤계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;20,키스한다
;-------------------------------------------------
	CASE 20
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:312 == 0
			KSTR:(K++) = 「……ああ。これ、キスだったのか」
			KSTR:(K++) = キスをされた実感がないらしい%CALLNAME:TARGET%は微妙な反応を示した。
			CFLAG:312 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:312 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					IF TEQUIP:주도권 == 1
						IF RAND:3 == 0
							WSTR:(K++) = 「ん、んん……っ」
							WSTR:(K++) = 「れぅ、ちゅ、はっ、ふ……」
						ELSEIF RAND:2 == 0
							WSTR:(K++) = 「んぁ…はぅ、ン……ンーっ」
							WSTR:(K++) = 「これっきもち、よくて…あたま、どろどろに…なる…%UNICODE(0x2661)%」
						ELSE
							WSTR:(K++) = 「ンぅ、ぁ…%UNICODE(0x2661)%」
							WSTR:(K++) = 「ぁむ、っ…%CSTR:이인칭%……」
						ENDIF
					ELSE
						WSTR:(K++) = %CALLNAME:TARGET%は%CSTR:이인칭%からの口づけを甘受するように薄く瞼を開いていた。
					ENDIF
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:312 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:312 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:312 = 3
			ENDIF
			;그 이외
			IF (CFLAG:312 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:312 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;21,아무것도안한다
;-------------------------------------------------
	CASE 21
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:313 == 0
			KSTR:(K++) = 
			CFLAG:313 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:313 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:313 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:313 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:313 = 3
			ENDIF
			;그 이외
			IF (CFLAG:313 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:313 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;22,회화　연모, 친애만
;会話内容は妄想、独自考察多めです。
;-------------------------------------------------
	CASE 22
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:314 == 0
			KSTR:(K++) = 「え…？」
			KSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%へ好きなものについて尋ねる。
			KSTR:(K++) = %CALLNAME:TARGET%よりも%CALLNAME:MASTER%が喋ってしまい会話らしい会話にはならなかった。
			CFLAG:314 = 1
		;2번째 이후
		ELSEIF CFLAG:314 < 2 || FLAG:7 == 2
			IF RAND:4 == 0
				KSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%へ目立つ文様のことを尋ねる。
				IF RAND:2 == 0
					WSTR:(K++) = 弱ると青から赤くなるのはなぜか？
					KSTR:(K++) = 「……理由は%CSTR:일인칭%にもあんまりわからない、かな」
					WSTR:(K++) = 「でも、弱った%CSTR:일인칭%に、体の中に在るマガタマが…ちょっかいをかけようとしてるのは感じる」
					WSTR:(K++) = %CALLNAME:TARGET%は苦い経験があるのかそう話しながら顔を顰めていた。
				ELSE
					WSTR:(K++) = 暗い場所では青い縁が光るがあれは制御できるのか？
					KSTR:(K++) = 「え…%CSTR:일인칭%のこれって、光ってたの?」
					WSTR:(K++) = 「本当に?　……ここまで来るとびっくり人間みたいだよな」
					WSTR:(K++) = %CALLNAME:TARGET%は%CALLNAME:MASTER%と話しながら不思議そうに腕に走る文様を触っていた。
				ENDIF
			ELSEIF RAND:3 == 0
				KSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%へ傷薬の用途をそれとなく尋ねる。
				IF RAND:2 == 0
					WSTR:(K++) = 傷薬は飲んで使うものか？
					KSTR:(K++) = 「……そうじゃないの?」
					WSTR:(K++) = 「あれって、飲み薬としての傷薬じゃなかったっけ…」
					WSTR:(K++) = %CALLNAME:TARGET%は%CALLNAME:MASTER%と話しつつあやふやな記憶を手繰り寄せ悩み始めた。
				ELSE
					WSTR:(K++) = 傷薬はかけて使うものか？
					KSTR:(K++) = 「あの傷薬ってめちゃくちゃ沁みるから嫌なんだよな…」
					WSTR:(K++) = 「…%CSTR:이인칭%は平気なの?」
					WSTR:(K++) = 純粋な%CALLNAME:TARGET%は%CALLNAME:MASTER%と話しながら楽しそうにしていた。
				ENDIF
			ELSEIF RAND:2 == 0
				KSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%へ表情について話題を振る。
				IF RAND:2 == 0
					WSTR:(K++) = 笑った顔が見てみたい。
					KSTR:(K++) = 「……今、笑って見せろってことだろ?」
					WSTR:(K++) = %CALLNAME:TARGET%は複雑そうにしながら%CALLNAME:MASTER%を見ている。
					WSTR:(K++) = 「……」
					WSTR:(K++) = %CALLNAME:MASTER%の要望通り%CALLNAME:TARGET%はふわりと笑顔を浮かべた。
					WSTR:(K++) = しばらくして%CALLNAME:TARGET%は少し恥ずかしそうに俯いた。
				ELSE
					WSTR:(K++) = 照れたりしないのか？
					;アラ？照れ…ない？
					KSTR:(K++) = 「そうだ、なぁ……別に。人並みだと思う」
					WSTR:(K++) = %CALLNAME:TARGET%はなんでそんな事を聞くのかと言わんばかりの顔をしている。
					WSTR:(K++) = %CALLNAME:MASTER%の頭には%CALLNAME:TARGET%は人並みの定義を間違えているのではという意識が残った。
				ENDIF
			ELSE
				KSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%へ仲魔について尋ねる。
				IF RAND:2 == 0
					WSTR:(K++) = 仲魔にするなら強さが一番か？
					KSTR:(K++) = 「まぁ、それも判断基準ではあるよ」
					WSTR:(K++) = 「……でも仲魔なんだし、なんか好きだとか、気に入ったっていう理由があってもいいと思う」
					WSTR:(K++) = %CALLNAME:TARGET%は何かを思い描いているのか遠い目をしながらそう語った。
				ELSE
					WSTR:(K++) = 仲魔にするならば可愛さが一番か？
					KSTR:(K++) = 「うん。%CSTR:일인칭%は……そっちかな」
					WSTR:(K++) = 「単純に強い仲魔は頼もしい。けど、可愛さがあるとか好きだっていう気持ちがあったら強くもなれるんじゃないかな」
					WSTR:(K++) = そう言えば%CALLNAME:TARGET%は戦闘中に現れた毛むくじゃらの悪魔に目を輝かせていた。
				ENDIF
			ENDIF
			CFLAG:314 = 2
		ENDIF
;-------------------------------------------------
;23,프레젠트　연모, 친애만
;-------------------------------------------------
	CASE 23
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:315 == 0
			KSTR:(K++) = 「……貰っていいのか？」
			KSTR:(K++) = 「…ありがとう」
			CFLAG:315 = 1
		;2번째 이후
		ELSEIF CFLAG:315 < 2 || FLAG:7 == 2
			WSTR:(K++) = 「%CSTR:일인칭%にプレゼント…?」
			WSTR:(K++) = 「%CSTR:이인칭%にはいつも、世話になりっぱなしなのに…」
			WSTR:(K++) = そう言いながらも%CALLNAME:TARGET%は目を細めて嬉しそうにしていた。
			CFLAG:315 = 2
		ENDIF
;-------------------------------------------------
;24,스킨쉽　연모, 친애만
;-------------------------------------------------
	CASE 24
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:316 == 0
			KSTR:(K++) = 「……」
			KSTR:(K++) = %CALLNAME:TARGET%はぎこちなく%CALLNAME:MASTER%のされるがままになっていた。
			CFLAG:316 = 1
		;2번째 이후
		ELSEIF CFLAG:316 < 2 || FLAG:7 == 2
			IF RAND:3 == 0
				WSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%の髪を手櫛で梳いてやった。
				WSTR:(K++) = やけに%CALLNAME:TARGET%は静かだと思えばうとうとと眠落ちしかけていた。
			ELSEIF RAND:2 == 0
				WSTR:(K++) = 珍しいことに%CALLNAME:TARGET%が%CALLNAME:MASTER%へ引っ付いてきた。
				WSTR:(K++) = 「……%CSTR:이인칭%の近くは、なんか…落ち着くんだ」
			ELSE
				WSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%の歯を観察していた。
				WSTR:(K++) = 「……んぁ…っ、ぅ゛ー!」
			ENDIF
			CFLAG:316 = 2
		ENDIF
;-------------------------------------------------
;25,느긋하게있는다　연모, 친애만
;-------------------------------------------------
	CASE 25
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:317 == 0
			KSTR:(K++) = 「……こんなことしてて、いいのか?」
			KSTR:(K++) = %CALLNAME:TARGET%は落ち着かない様子で背を預けてくる%CALLNAME:MASTER%を支えていた。
			CFLAG:317 = 1
		;2번째 이후
		ELSEIF CFLAG:317 < 2 || FLAG:7 == 2
			IF RAND:3 == 0
				WSTR:(K++) = %CALLNAME:MASTER%は休憩も兼ねて%CALLNAME:TARGET%の横へ座る。
				WSTR:(K++) = 「…%CSTR:이인칭%はさ、なんで%CSTR:일인칭%のこと売らないの?」
				WSTR:(K++) = %CALLNAME:TARGET%は自身が売られる未来を確定しているような言い方で尋ねてくる。
				WSTR:(K++) = %CALLNAME:MASTER%はまだ手元に置いておきたい存在で居てくれればいいと答えた。
			ELSEIF RAND:2 == 0
				WSTR:(K++) = %CALLNAME:TARGET%は部屋の端の方へ行き頻りに辺りをキョロキョロと見回す。
				WSTR:(K++) = %CALLNAME:MASTER%は何をしているんだと呆れた目でその様子を見ていた。
			ELSE
				WSTR:(K++) = %CALLNAME:TARGET%は窓越しに外を見ていた。
				NSTR:(K++) = 「…今日も外は、
				IF TIME == 0
					WSTR:(K++) = 明るいし雲も少なそうだ」
				ELSEIF TIME == 1
					WSTR:(K++) = いい天気だったなぁ」
				ELSE
					WSTR:(K++) = かなり暗くなってるね」
				ENDIF
			ENDIF
			CFLAG:317 = 2
		ENDIF
;-------------------------------------------------
;26,가사　연모, 친애만
;だいたい次の周回するまでに50人以上の仲魔がいるから消耗品や食品の買い物とかどうしてるのかと思ってしまった…。
;自主的に料理、洗濯、お掃除、買い出ししに行ってくれる人修羅くんはここにいます。
;-------------------------------------------------
	CASE 26
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:318 == 0
			KSTR:(K++) = 「……%CSTR:일인칭%に家事能力があるように見えた?」
			KSTR:(K++) = %CALLNAME:TARGET%は%CALLNAME:MASTER%から命令された内容をこなしむすっとしながら報告を行った。
			CFLAG:318 = 1
		;2번째 이후
		ELSEIF CFLAG:318 < 2 || FLAG:7 == 2
			SELECTCASE RAND:7
			CASE 0
				WSTR:(K++) = 「よし。掃除は…終わった」
				WSTR:(K++) = %CALLNAME:TARGET%は立ち上がり伸びをしながら達成感に満ちた顔をしている。
				WSTR:(K++) = 掃除道具を片付けに物置へ歩いていく%CALLNAME:TARGET%の足取りは%CALLNAME:MASTER%から見ても軽かった。
			CASE 1
				WSTR:(K++) = 「%CSTR:이인칭%ー、これって洗っておいてもいいの?」
				WSTR:(K++) = 部屋で寛いでいた%CALLNAME:MASTER%へ%CALLNAME:TARGET%から声がかかる。
				WSTR:(K++) = %CALLNAME:MASTER%の任せるという一言で全てを洗濯機で回してくれた。
			CASE 2
				WSTR:(K++) = 「ご飯…にはちょっと早いから軽く食べれるものでも作るよ」
				;家事マスターといっても過言ではない(主夫…？)
				IF EXP:가사경험 >= 100
					KSTR:(K++) = %CALLNAME:TARGET%に任せれば冷蔵庫の中身が少なくとも美味しい料理を作ってくれる。
					WSTR:(K++) = %CALLNAME:MASTER%は謎の信頼感で台所へ向かう%CALLNAME:TARGET%を見送った。
					WSTR:(K++) = 「はい、出来たよ」
					WSTR:(K++) = %CALLNAME:MASTER%は十数分後に%CALLNAME:TARGET%が持ってきてくれた料理に舌鼓を打った。
				;家事一通りできるし失敗もほぼない
				ELSEIF EXP:가사경험 >= 50
					KSTR:(K++) = %CALLNAME:TARGET%に任せておけば食べられて美味しい料理を作ってもらえるだろう。
					WSTR:(K++) = %CALLNAME:MASTER%は密かな期待を込めてメニューのリクエストを飛ばし台所へ向かう%CALLNAME:TARGET%を見送った。
					WSTR:(K++) = 「お待たせ。出来たよ」
					WSTR:(K++) = %CALLNAME:MASTER%はふわりと漂う料理のいい香りに待ちきれず台所へ向かい様子を窺う。
					WSTR:(K++) = あっさりと%CALLNAME:TARGET%に見つかるもお目当ての料理を差し出され%CALLNAME:MASTER%は料理に真っ先に手をつけた。
				;家事にちょっと慣れてきてさらに持ち前の器用さでこなし始める
				ELSEIF EXP:가사경험 >= 20
					KSTR:(K++) = %CALLNAME:TARGET%に任せるとなると不安は残る。
					WSTR:(K++) = %CALLNAME:MASTER%は台所へ向かう%CALLNAME:TARGET%の後ろをこっそりと着いていった。
					WSTR:(K++) = 「うーん…うん、まぁ、不味くはないけど」
					WSTR:(K++) = 思ったよりも大丈夫そうだ。当の本人である%CALLNAME:TARGET%は味の決め手に悩んでいた。
				;家事させたらまずいぜ。論外
				ELSE
					WSTR:(K++) = %CALLNAME:TARGET%に任せるわけにはいかない。
					WSTR:(K++) = %CALLNAME:MASTER%は慌てて%CALLNAME:TARGET%を言いくるめ料理してもらうのをなんとか先延ばしにした。
				ENDIF
			CASE 3
				WSTR:(K++) = 「%CSTR:일인칭%、ちょっと…買い物してくる」
				WSTR:(K++) = %CALLNAME:TARGET%は何やらメモを見ながら外出の準備をしている。
				WSTR:(K++) = メモには%CALLNAME:MASTER%の頼んだ品物の取りこぼしもなく綺麗な文字が並んでいてとても見やすい。
				WSTR:(K++) = 　　　
				WSTR:(K++) = しばらくして%CALLNAME:TARGET%が買い物から帰ってきた。特売品があったと自慢気に話していた。
			CASE 4
				WSTR:(K++) = 「%CSTR:이인칭%の寝室って結構汚れやすいけど…」
				WSTR:(K++) = 「うん、まぁ……あれだけ来られればね」
				WSTR:(K++) = ベッドのシーツ交換をしている%CALLNAME:TARGET%は気まずそうにフォローを入れた。
			CASE 5
				WSTR:(K++) = 「～♪」
				WSTR:(K++) = %CALLNAME:TARGET%は小さな鼻歌混じりに乾いた洗濯物を畳んでいた。
			CASEELSE
				WSTR:(K++) = 「はい、…お終いっと」
				WSTR:(K++) = %CALLNAME:TARGET%は手慣れた様子で頼まれていた家事を終わらせた。
			ENDSELECT
			CFLAG:318 = 2
		ENDIF
;-------------------------------------------------
;27,노래연습　연모, 친애만
;-------------------------------------------------
	CASE 27
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:319 == 0
			KSTR:(K++) = 「歌…?」
			KSTR:(K++) = %CALLNAME:TARGET%はしぶしぶ歌おうとして息を吸ってからぴたりと止めてしまった。
			KSTR:(K++) = %CALLNAME:TARGET%は今の流行の歌がわからないとしょんぼりしている。
			CFLAG:319 = 1
		;2번째 이후
		ELSEIF CFLAG:319 < 2 || FLAG:7 == 2
			KSTR:(K++) = 
			CFLAG:319 = 2
		ENDIF
;-------------------------------------------------
;28,매도
;-------------------------------------------------
	CASE 28
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:320 == 0
			KSTR:(K++) = 「……そう?」
			KSTR:(K++) = 
			CFLAG:320 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:320 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:320 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:320 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:320 = 3
			ENDIF
			;그 이외
			IF (CFLAG:320 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:320 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;29,비디오감상　함락완료만
;-------------------------------------------------
	CASE 29
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:321 == 0
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:321 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:321 = 2
		ENDIF
;-------------------------------------------------
;30,파후파후　함락완료만
;-------------------------------------------------
	CASE 30
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:422 == 0
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) = 「…改まってやる事?　それって」
				KSTR:(K++) = %CALLNAME:TARGET%は%CALLNAME:MASTER%と触れ合いながら穏やかな表情をしている。
				KSTR:(K++) = 揶揄いを含んだ%CALLNAME:MASTER%のキスのおねだりは聞き流されてしまった。
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) = 「え?」
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:422 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				IF RAND:3 == 0
					WSTR:(K++) = 
				ELSEIF RAND:2 == 0
					WSTR:(K++) = 
				ELSE
					WSTR:(K++) = 
				ENDIF
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				IF RAND:3 == 0
					WSTR:(K++) = 
				ELSEIF RAND:2 == 0
					WSTR:(K++) = 
				ELSE
					WSTR:(K++) = 
				ENDIF
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:422 = 2
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;커뮤니케이션계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;도구사용 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("도구계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;40,로터
;-------------------------------------------------
	CASE 40
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:322 == 0
			KSTR:(K++) = 「……」
			KSTR:(K++) = %CALLNAME:MASTER%が持つ物から嫌な気配を感じたのだろう。
			KSTR:(K++) = 咄嗟に抵抗せずに身構えたのが運の尽きだ。
			KSTR:(K++) = 「ッ、……!」
			CFLAG:322 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:322 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 「ぁっ、んんぅ…!」
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:322 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:322 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「……～～ッ!」
				KSTR:(K++) = %CALLNAME:TARGET%はたまらなさそうにゆらゆらと腰を揺らしている。
				CFLAG:322 = 3
			ENDIF
			;그 이외
			IF (CFLAG:322 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「……ん、」
				KSTR:(K++) = %CALLNAME:TARGET%のモノへ丁寧に刺激を与えていく。
				CFLAG:322 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;41,전기마사지기
;-------------------------------------------------
	CASE 41
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:323 == 0
			KSTR:(K++) = 「……それは?」
			KSTR:(K++) = 「ッ…?!　ァ、ぁ…!」
			KSTR:(K++) = %CALLNAME:TARGET%はそれを股間へ当ててどうなるのかと不思議そうな顔をする。
			KSTR:(K++) = %CALLNAME:MASTER%の指がスイッチを入れた途端に体を激しく震わせ焦り出した。
			CFLAG:323 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:323 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:323 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:323 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:323 = 3
			ENDIF
			;그 이외
			IF (CFLAG:323 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:323 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;42,클리캡
;-------------------------------------------------
	CASE 42
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:11
			;처음
			IF CFLAG:324 == 0
				KSTR:(K++) =
				CFLAG:324 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:324 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:324 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:324 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:324 = 3
				ENDIF
				;그 이외
				IF (CFLAG:324 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:324 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;43,오나홀
;-------------------------------------------------
	CASE 43
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:12
			;처음
			IF CFLAG:325 == 0
				KSTR:(K++) =
				CFLAG:325 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:325 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:325 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:325 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:325 = 3
				ENDIF
				;그 이외
				IF (CFLAG:325 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:325 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;44,바이브
;-------------------------------------------------
	CASE 44
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:13
			;처음
			IF CFLAG:326 == 0
				;처녀
				IF TALENT:0
					IF 함락( ) > 0
						;연모, 친애
						IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
							KSTR:(K++) = 
						;음란, 창부
						ELSEIF 함락( ) > 0 && RESULTS == "음란"
							KSTR:(K++) = 
						;복종, 예속
						ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
							KSTR:(K++) =
						ENDIF
						;함락완료 공통
						IF !구상유무확인(0)
							KSTR:(K++) =
						ENDIF
					ENDIF
					;그 이외
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				;비처녀
				ELSE
					IF 함락( ) > 0
						;연모, 친애
						IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
							KSTR:(K++) = 
						;음란, 창부
						ELSEIF 함락( ) > 0 && RESULTS == "음란"
							KSTR:(K++) = 
						;복종, 예속
						ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
							KSTR:(K++) =
						ENDIF
						;함락완료 공통
						IF !구상유무확인(0)
							KSTR:(K++) =
						ENDIF
					ENDIF
					;그 이외
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				CFLAG:326 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:326 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:326 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:326 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:326 = 3
				ENDIF
				;그 이외
				IF (CFLAG:326 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:326 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;45,애널바이브
;-------------------------------------------------
	CASE 45
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:14
			;처음
			IF CFLAG:327 == 0
				KSTR:(K++) =
				CFLAG:327 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:327 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:327 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:327 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:327 = 3
				ENDIF
				;그 이외
				IF (CFLAG:327 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:327 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;46,애널비즈
;-------------------------------------------------
	CASE 46
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:15
			;처음
			IF CFLAG:328 == 0
				KSTR:(K++) =
				CFLAG:328 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:328 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:328 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:328 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:328 = 3
				ENDIF
				;그 이외
				IF (CFLAG:328 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:328 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;47,유두캡
;-------------------------------------------------
	CASE 47
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:16
			;처음
			IF CFLAG:329 == 0
				KSTR:(K++) =
				CFLAG:329 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:329 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:329 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:329 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:329 = 3
				ENDIF
				;그 이외
				IF (CFLAG:329 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:329 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;48,유두로터
;-------------------------------------------------
	CASE 48
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:330 == 0
			KSTR:(K++) =
			CFLAG:330 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:330 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:330 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:330 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:330 = 3
			ENDIF
			;그 이외
			IF (CFLAG:330 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:330 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;49,착유기
;-------------------------------------------------
	CASE 49
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:17
			;처음
			IF CFLAG:331 == 0
				KSTR:(K++) =
				CFLAG:331 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:331 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:331 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:331 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:331 = 3
				ENDIF
				;그 이외
				IF (CFLAG:331 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:331 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;109,사정멈추기
;-------------------------------------------------
	CASE 109
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:42
			;처음
			IF CFLAG:464 == 0
				KSTR:(K++) = 「……」
				KSTR:(K++) = %CALLNAME:TARGET%は射精を無理やり止められても意に介した様子はない。
				CFLAG:464 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:464 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						IF RAND:3 == 0
							WSTR:(K++) = 
						ELSEIF RAND:2 == 0
							WSTR:(K++) = 
						ELSE
							WSTR:(K++) = 
						ENDIF
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:464 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:464 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:464 = 3
				ENDIF
				;그 이외
				IF (CFLAG:464 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:464 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;도구사용 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;조교자도구계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("대조교자도구계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;50,클리캡쓴다
;-------------------------------------------------
	CASE 50
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:11
			;처음
			IF CFLAG:453 == 0
				KSTR:(K++) =
				CFLAG:453 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:453 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:453 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:453 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:453 = 3
				ENDIF
				;그 이외
				IF (CFLAG:453 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:453 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;51,오나홀쓴다
;-------------------------------------------------
	CASE 51
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:12
			;처음
			IF CFLAG:454 == 0
				KSTR:(K++) =
				CFLAG:454 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:454 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:454 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:454 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:454 = 3
				ENDIF
				;그 이외
				IF (CFLAG:454 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:454 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;52,바이브쓴다
;-------------------------------------------------
	CASE 52
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:13
			;처음
			IF CFLAG:455 == 0
				KSTR:(K++) =
				CFLAG:455 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:455 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:455 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:455 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:455 = 3
				ENDIF
				;그 이외
				IF (CFLAG:455 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:455 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;53,애널바이브쓴다
;-------------------------------------------------
	CASE 53
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:14
			;처음
			IF CFLAG:456 == 0
				KSTR:(K++) =
				CFLAG:456 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:456 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:456 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:456 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:456 = 3
				ENDIF
				;그 이외
				IF (CFLAG:456 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:456 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;54,애널비즈쓴다
;-------------------------------------------------
	CASE 54
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:15
			;처음
			IF CFLAG:457 == 0
				KSTR:(K++) =
				CFLAG:457 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:457 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:457 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:457 = 3
				ENDIF
				;그 이외
				IF (CFLAG:457 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:457 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;55,유두캡쓴다
;-------------------------------------------------
	CASE 55
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:16
			;처음
			IF CFLAG:458 == 0
				KSTR:(K++) =
				CFLAG:458 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:458 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:458 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:458 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:458 = 3
				ENDIF
				;그 이외
				IF (CFLAG:458 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:458 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;56,착유기쓴다
;-------------------------------------------------
	CASE 56
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:17
			;처음
			IF CFLAG:459 == 0
				KSTR:(K++) =
				CFLAG:459 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:459 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:459 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:459 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:459 = 3
				ENDIF
				;그 이외
				IF (CFLAG:459 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:459 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;196,미약쓴다
;-------------------------------------------------
	CASE 196
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:437 == 0
			KSTR:(K++) =
			CFLAG:437 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:437 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:437 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:437 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:437 = 3
			ENDIF
			;그 이외
			IF (CFLAG:437 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:437 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;조교자도구계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;섹스계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("Ｖ성교계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;60,정상위
;-------------------------------------------------
	CASE 60
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:332 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:332 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:332 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:332 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:332 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:332 = 3
			ENDIF
			;그 이외
			IF (CFLAG:332 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:332 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;61,후배위
;-------------------------------------------------
	CASE 61
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:333 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:333 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:333 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:333 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:333 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:333 = 3
			ENDIF
			;그 이외
			IF (CFLAG:333 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:333 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;66,기승위
;-------------------------------------------------
	CASE 66
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:338 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:338 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:338 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:338 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:338 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:338 = 3
			ENDIF
			;그 이외
			IF (CFLAG:338 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:338 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;68,대면좌위
;-------------------------------------------------
	CASE 68
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:340 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:340 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:340 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:340 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:340 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:340 = 3
			ENDIF
			;그 이외
			IF (CFLAG:340 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:340 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;69,배면좌위
;-------------------------------------------------
	CASE 69
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:341 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:341 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:341 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:341 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:341 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:341 = 3
			ENDIF
			;그 이외
			IF (CFLAG:341 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:341 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;72,속옷비켜삽입
;-------------------------------------------------
	CASE 72
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:420 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:420 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:420 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:420 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:420 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:420 = 3
			ENDIF
			;그 이외
			IF (CFLAG:420 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:420 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;74,역강간당한다
;-------------------------------------------------
	CASE 74
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:460 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:460 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:460 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:460 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:460 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:460 = 3
			ENDIF
			;그 이외
			IF (CFLAG:460 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:460 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;섹스계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;애널섹스계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("Ａ성교계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;62,정상위애널
;-------------------------------------------------
	CASE 62
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:334 == 0
			KSTR:(K++) =
			CFLAG:334 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:334 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:334 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:334 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:334 = 3
			ENDIF
			;그 이외
			IF (CFLAG:334 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:334 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;63,후배위애널
;-------------------------------------------------
	CASE 63
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:335 == 0
			KSTR:(K++) =
			CFLAG:335 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:335 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:335 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:335 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:335 = 3
			ENDIF
			;그 이외
			IF (CFLAG:335 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:335 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;67,기승위애널
;-------------------------------------------------
	CASE 67
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:339 == 0
			KSTR:(K++) =
			CFLAG:339 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:339 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:339 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:339 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:339 = 3
			ENDIF
			;그 이외
			IF (CFLAG:339 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:339 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;70,대면좌위애널
;-------------------------------------------------
	CASE 70
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:342 == 0
			KSTR:(K++) =
			CFLAG:342 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:342 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:342 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:342 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:342 = 3
			ENDIF
			;그 이외
			IF (CFLAG:342 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:342 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;71,배면좌위애널
;-------------------------------------------------
	CASE 71
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:343 == 0
			KSTR:(K++) =
			CFLAG:343 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:343 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:343 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:343 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:343 = 3
			ENDIF
			;그 이외
			IF (CFLAG:343 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:343 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;73,속옷비켜삽입애널
;-------------------------------------------------
	CASE 73
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:421 == 0
			KSTR:(K++) =
			CFLAG:421 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:421 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:421 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:421 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:421 = 3
			ENDIF
			;그 이외
			IF (CFLAG:421 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:421 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;봉사계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("봉사계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;80,수음
;-------------------------------------------------
	CASE 80
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:344 == 0
			KSTR:(K++) =
			CFLAG:344 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:344 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:344 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:344 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:344 = 3
			ENDIF
			;그 이외
			IF (CFLAG:344 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:344 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;81,펠라치오
;-------------------------------------------------
	CASE 81
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:345 == 0
			KSTR:(K++) =
			CFLAG:345 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:345 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:345 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:345 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:345 = 3
			ENDIF
			;그 이외
			IF (CFLAG:345 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:345 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;82,파이즈리
;-------------------------------------------------
	CASE 82
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:346 == 0
			KSTR:(K++) =
			CFLAG:346 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:346 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:346 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:346 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:346 = 3
			ENDIF
			;그 이외
			IF (CFLAG:346 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:346 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;83,스마타
;-------------------------------------------------
	CASE 83
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:347 == 0
			KSTR:(K++) =
			CFLAG:347 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:347 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:347 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:347 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:347 = 3
			ENDIF
			;그 이외
			IF (CFLAG:347 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:347 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;84,거품춤
;-------------------------------------------------
	CASE 84
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:348 == 0
			KSTR:(K++) =
			CFLAG:348 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:348 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:348 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:348 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:348 = 3
			ENDIF
			;그 이외
			IF (CFLAG:348 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:348 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;85,풋잡
;-------------------------------------------------
	CASE 85
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:349 == 0
			KSTR:(K++) =
			CFLAG:349 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:349 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:349 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:349 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:349 = 3
			ENDIF
			;그 이외
			IF (CFLAG:349 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:349 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;86,다리핥기강제
;-------------------------------------------------
	CASE 86
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:350 == 0
			KSTR:(K++) =
			CFLAG:350 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:350 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:350 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:350 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:350 = 3
			ENDIF
			;그 이외
			IF (CFLAG:350 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:350 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;87,세로파이즈리
;-------------------------------------------------
	CASE 87
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:351 == 0
			KSTR:(K++) =
			CFLAG:351 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:351 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:351 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:351 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:351 = 3
			ENDIF
			;그 이외
			IF (CFLAG:351 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:351 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;88,파이즈리펠라
;-------------------------------------------------
	CASE 88
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:352 == 0
			KSTR:(K++) =
			CFLAG:352 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:352 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:352 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:352 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:352 = 3
			ENDIF
			;그 이외
			IF (CFLAG:352 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:352 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;89,모유마시기　함락완료만
;-------------------------------------------------
	CASE 89
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:353 == 0
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:353 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:353 = 2
		ENDIF
;-------------------------------------------------
;197,팬티훑기
;-------------------------------------------------
	CASE 197
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:438 == 0
			KSTR:(K++) =
			CFLAG:438 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:438 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:438 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:438 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:438 = 3
			ENDIF
			;그 이외
			IF (CFLAG:438 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:438 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;211,귀두공격(受)
;-------------------------------------------------
	CASE 211
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:479 == 0
			KSTR:(K++) =
			CFLAG:479 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:479 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:479 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:479 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:479 = 3
			ENDIF
			;그 이외
			IF (CFLAG:479 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:479 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;봉사계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;조교자봉사계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("조교자봉사계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;2,펠라한다
;-------------------------------------------------
	CASE 2
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:302 == 0
			KSTR:(K++) = 「……舐めても平気なのか？」
			KSTR:(K++) = そんな所を。と言いたそうにしながらも%CALLNAME:TARGET%は愛撫を受けている。
			CFLAG:302 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:302 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					IF TEQUIP:주도권 == 1
						WSTR:(K++) = 「ぁ、っ……そこ、いっぱい舐めて…?」
						WSTR:(K++) = 「……ン、ふぁ、%CSTR:이인칭%っ…%UNICODE(0x2661)%」
					ELSE
						WSTR:(K++) = 「……%CSTR:일인칭%、%CSTR:이인칭%のそのカッコ見てるとさ」
						WSTR:(K++) = 「なんか、目茶苦茶ドキドキ…する」
					ENDIF
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:302 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:302 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「……ッ、く、あ……」
				KSTR:(K++) = %CALLNAME:TARGET%は必死に与えられる快感からくる声を噛み殺している。
				CFLAG:302 = 3
			ENDIF
			;그 이외
			IF (CFLAG:302 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「……」
				KSTR:(K++) = %CALLNAME:TARGET%はあまり反応を示さないが大人しく%CALLNAME:MASTER%の口の中に咥えられている。
				CFLAG:302 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;90,수음한다
;-------------------------------------------------
	CASE 90
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:12 == 0
			;처음
			IF CFLAG:423 == 0
				KSTR:(K++) = 「……変なことするんだな」
				KSTR:(K++) = %CALLNAME:MASTER%は緩やかなリズムで擦り上げながら%CALLNAME:TARGET%の表情を観察した。
				CFLAG:423 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:423 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 「ぅあ…ダメぇ、それ…は……っ」
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:423 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:423 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 「ふ、ンぐッ……」
					KSTR:(K++) = %CALLNAME:TARGET%は今は顔を見るなとでも言うように手で隠す。
					KSTR:(K++) = 抵抗を無視して扱き上げればにちゅにちと粘着的な音が耳を犯していく。
					KSTR:(K++) = %CALLNAME:MASTER%はかなり質量を増してきたなと涎を溢れさせる尿道口を浅く突いた。
					CFLAG:423 = 3
				ENDIF
				;그 이외
				IF (CFLAG:423 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 「……それ、触ってて楽しいか？」
					CFLAG:423 = 2
				ENDIF
			ENDIF
		ELSE
;-------------------------------------------------
;오나홀훑기한다
;-------------------------------------------------
			;처음
			IF CFLAG:461 == 0
				KSTR:(K++) = 「……それは何だ？」
				KSTR:(K++) = 「本当に知らないのか、と言われても…」
				KSTR:(K++) = 怪訝そうな%CALLNAME:TARGET%の目の前でオナホールに指を突き込む。
				KSTR:(K++) = %CALLNAME:MASTER%はこれはこういう物だと告げながらその先端をオナホールへ飲み込ませた。
				CFLAG:461 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:461 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 「ンん、んっーーーー…%UNICODE(0x2661)%」
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:461 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:461 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 「……」
					KSTR:(K++) = %CALLNAME:TARGET%は俯きながらぶるぶる震えている。
					CFLAG:461 = 3
				ENDIF
				;그 이외
				IF (CFLAG:461 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:461 = 2
				ENDIF
			ENDIF
		ENDIF
;-------------------------------------------------
;91,파이즈리한다
;-------------------------------------------------
	CASE 91
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:424 == 0
			KSTR:(K++) = 「……」
			KSTR:(K++) = 「理由がよく判らないけど、それなら好きにしてくれ」
			CFLAG:424 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:424 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 「……それ、戦ってる最中に思い出しそうになるっ、から、も、やめて…」
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:424 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:424 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「……」
				KSTR:(K++) = %CALLNAME:TARGET%はふぅふぅと荒い呼吸を繰り返し瞼を閉じて耐えている。
				CFLAG:424 = 3
			ENDIF
			;그 이외
			IF (CFLAG:424 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:424 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;92,정상위시킨다
;-------------------------------------------------
	CASE 92
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:425 == 0
			KSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%の前で横になり命令を下す。
			KSTR:(K++) = 「……」
			;あなたが処女
			IF TALENT:MASTER:0
				KSTR:(K++) = 「それは%CSTR:이인칭%だって嫌じゃないの？」
				KSTR:(K++) = 妙に冷静な%CALLNAME:TARGET%にそう尋ねられる。
				KSTR:(K++) = %CALLNAME:MASTER%はそんな事気にしないと言い切った。
				WSTR:(K++) = 「……そう。それじゃあ、%CSTR:이인칭%に従うしかないか」
				WSTR:(K++) = %CALLNAME:TARGET%は憂いに包まれたように呟きながら%CALLNAME:MASTER%の足を割り開く。
				KSTR:(K++) = 宛がった泥濘に自身が触れた瞬間%CALLNAME:TARGET%の顔色が悪くなった。
				WSTR:(K++) = 「……っ、」
				KSTR:(K++) = %CALLNAME:TARGET%は強い恐怖を感じているらしい。
				KSTR:(K++) = %CALLNAME:MASTER%の身体から視線を逸らし力任せに挿れようとする。
				KSTR:(K++) = %CALLNAME:MASTER%は秘肉を拓かれ削がれるような痛みに身を硬くしぐっと耐える。
				WSTR:(K++) = 「ぃ、っ……ぅ、ンぅｯ、は、ぁ…」
				WSTR:(K++) = 次第に%CALLNAME:TARGET%は女の身体を掻き抱いて本能的に揺さぶり続け始めた。
			ELSE
				KSTR:(K++) = 「%CSTR:이인칭%は、それでいいかもしれない」
				KSTR:(K++) = 「でも%CSTR:일인칭%は……」
				KSTR:(K++) = %CALLNAME:TARGET%にしては珍しく渋っている。
				KSTR:(K++) = %CALLNAME:MASTER%にもう一度強く命令され%CALLNAME:TARGET%は機械的に抱いた。
			ENDIF
			CFLAG:425 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:425 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					IF TEQUIP:주도권 == 1
						IF RAND:3 == 0
							WSTR:(K++) = 「はぁ、ふ、っく…」
							WSTR:(K++) = %CALLNAME:TARGET%はうっとりとした表情で夢中で腰を振っている。
							WSTR:(K++) = %CALLNAME:MASTER%も身体ごと抱きすくめられ一切の遠慮なしに熱を交わしていた。
						ELSEIF RAND:2 == 0
							WSTR:(K++) = 「…あ、っぁ……!」
							WSTR:(K++) = 「%CSTR:이인칭%っ、ごめん、もぉ…むりっ」
						ELSE
							WSTR:(K++) = 「ん、ぁ……っ」
							WSTR:(K++) = 「%CSTR:일인칭%の、すごい締め付け、られて…!」
						ENDIF
					ELSE
						WSTR:(K++) = 「……%CSTR:일인칭%ので、良くなって、くれてる？」
						WSTR:(K++) = %CALLNAME:TARGET%の言葉に%CALLNAME:MASTER%は頷いた。
					ENDIF
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:425 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:425 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:425 = 3
			ENDIF
			;그 이외
			IF (CFLAG:425 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:425 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;93,후배위시킨다
;-------------------------------------------------
	CASE 93
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:426 == 0
			KSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%の前で尻を向け命令を下す。
			KSTR:(K++) = 「…そう。従えばいいんだよね？」
			KSTR:(K++) = どことなくぎこちない%CALLNAME:TARGET%に身を任せた。
			CFLAG:426 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:426 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					IF TEQUIP:주도권 == 1
						IF RAND:2 == 0
							WSTR:(K++) = 「これ、さ……」
							WSTR:(K++) = 「%CSTR:일인칭%のも%CSTR:이인칭%のもどろどろになってるのが、よく見えるんだ」
						ELSE
							WSTR:(K++) = 「ぁ、っ…だめ、深くはいって、ぅう…%UNICODE(0x2661)%」
							WSTR:(K++) = 「あ゛っ、あ、だめ、うぁ……%UNICODE(0x2661)%」
						ENDIF
					ELSE
						WSTR:(K++) = 「ふぁっ、く、ぅ……」
						WSTR:(K++) = 「……っひ、んんぅ%UNICODE(0x2661)%」
					ENDIF
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:426 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:426 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:426 = 3
			ENDIF
			;그 이외
			IF (CFLAG:426 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:426 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;94,기승위한다
;-------------------------------------------------
	CASE 94
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:427 == 0
			KSTR:(K++) =
			CFLAG:427 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:427 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					IF TEQUIP:주도권 == 1
						WSTR:(K++) = 
					ELSE
						WSTR:(K++) = 
					ENDIF
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:427 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:427 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:427 = 3
			ENDIF
			;그 이외
			IF (CFLAG:427 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:427 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;95,애널정상위시킨다
;-------------------------------------------------
	CASE 95
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:428 == 0
			KSTR:(K++) =
			CFLAG:428 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:428 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:428 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:428 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:428 = 3
			ENDIF
			;그 이외
			IF (CFLAG:428 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:428 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;96,애널후배위시킨다
;-------------------------------------------------
	CASE 96
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:429 == 0
			KSTR:(K++) =
			CFLAG:429 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:429 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:429 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:429 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:429 = 3
			ENDIF
			;그 이외
			IF (CFLAG:429 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:429 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;97,애널기승위한다
;-------------------------------------------------
	CASE 97
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:430 == 0
			KSTR:(K++) =
			CFLAG:430 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:430 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:430 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:430 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:430 = 3
			ENDIF
			;그 이외
			IF (CFLAG:430 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:430 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;98,모유먹이기　함락완료만
;-------------------------------------------------
	CASE 98
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:443 == 0
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				IF TEQUIP:주도권 == 1
					WSTR:(K++) = 「すごいな、%CSTR:이인칭%のあまい味…」
				ELSE
					WSTR:(K++) = 「……%CSTR:일인칭%に飲ませて興奮してる？」
					WSTR:(K++) = 「……だって、すごくえっちな顔だ」
				ENDIF
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:443 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				IF TEQUIP:주도권 == 1
					IF RAND:2 == 0
						WSTR:(K++) = 「っふふ、……ごめん」
						WSTR:(K++) = 「あんまり%CSTR:이인칭%から美味しそうなにおいがしたから」
					ELSE
						WSTR:(K++) = 「ん、ぅん……%UNICODE(0x2661)%」
						WSTR:(K++) = 「……は、ふ…えっろい味してる」
					ENDIF
				ELSE
					WSTR:(K++) = 「……さすがに恥ずかしいから、あまり見ないでよ」
				ENDIF
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:443 = 2
		ENDIF
;-------------------------------------------------
;210,귀두공격
;-------------------------------------------------
	CASE 210
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:480 == 0
			KSTR:(K++) =
			CFLAG:480 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:480 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:480 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:480 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:480 = 3
			ENDIF
			;그 이외
			IF (CFLAG:480 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:480 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;조교자봉사계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;SM계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("ＳＭ계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;100,스팽킹
;-------------------------------------------------
	CASE 100
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:354 == 0
			KSTR:(K++) = 「……」
			KSTR:(K++) = %CALLNAME:TARGET%は何度ぶたれても悲鳴を一つも上げず無関心そうに涼しい顔をしていた。
			CFLAG:354 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:354 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 「……んぃ゛ッ！」
					KSTR:(K++) = べちんッ。
					IF RAND:3 == 0
						WSTR:(K++) = 一際強く振りかぶった%CALLNAME:MASTER%の腕は%CALLNAME:TARGET%の尻目掛けて叩き付けられる。
						WSTR:(K++) = %CALLNAME:TARGET%から短く上がった悲鳴に%CALLNAME:MASTER%は笑みを絶やさず腕を構えた。
					ELSEIF RAND:2 == 0
						WSTR:(K++) = 連続して強く叩き続けた%CALLNAME:TARGET%の真っ赤になった尻へ仕上げの一発を入れる。
						WSTR:(K++) = %CALLNAME:TARGET%はびくびくと震え身体をモゾ付かせている。
						WSTR:(K++) = 「……はぁ、ぅ…ぁ…」
						WSTR:(K++) = %CALLNAME:MASTER%がその箇所を労るように撫でさすれば%CALLNAME:TARGET%はくったりと脱力した。
					ELSE
						WSTR:(K++) = %CALLNAME:TARGET%は目を伏せ与えられる痛みを堪えていた。
					ENDIF
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = %CALLNAME:TARGET%は目を伏せ与えられる痛みを堪えていた。
				ENDIF
				CFLAG:354 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:354 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:354 = 3
			ENDIF
			;그 이외
			IF (CFLAG:354 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:354 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;101,젖스팽킹
;-------------------------------------------------
	CASE 101
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:355 == 0
			KSTR:(K++) =
			CFLAG:355 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:355 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:355 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:355 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:355 = 3
			ENDIF
			;그 이외
			IF (CFLAG:355 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:355 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;102,채찍
;-------------------------------------------------
	CASE 102
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:356 == 0
			KSTR:(K++) =
			CFLAG:356 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:356 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:356 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:356 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:356 = 3
			ENDIF
			;그 이외
			IF (CFLAG:356 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:356 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;103,양초
;-------------------------------------------------
	CASE 103
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:357 == 0
			KSTR:(K++) = 「……ッ、ひ、ぃ…」
			KSTR:(K++) = %CALLNAME:TARGET%はロウを垂らされた箇所を襲う灼けるような痛みに涙を零した。
			CFLAG:357 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:357 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 「ぁ゛…ッ゛…ん、ぃっ…」
					IF RAND:3 == 0
						WSTR:(K++) = %CALLNAME:MASTER%の手に納まっている蝋燭の炎が揺れる。
						WSTR:(K++) = %CALLNAME:TARGET%は身体のあちこちに落とされたロウを見てきゅっと唇を噛む。
						WSTR:(K++) = 「…これじゃあ血塗れになったみたいで、なんか……」
						WSTR:(K++) = %CALLNAME:MASTER%は期待に応えるように蝋燭を回しながらゆっくりと傾けた。
					ELSEIF RAND:2 == 0
						WSTR:(K++) = 「%CSTR:일인칭%…こうされるのに慣れてきてて、嫌だ…」
						WSTR:(K++) = %CALLNAME:TARGET%は下手に動けば火傷で済まないことを理解できたのだという。
						WSTR:(K++) = %CALLNAME:TARGET%の場合は痛みに慣れてきたともいうかもしれない。
						WSTR:(K++) = 蝋燭を掲げる%CALLNAME:MASTER%へ注がれる%CALLNAME:TARGET%の視線はどこか恍惚としていた。
					ELSE
						WSTR:(K++) = 「……」
						WSTR:(K++) = %CALLNAME:MASTER%は強張った表情で見上げてくる%CALLNAME:TARGET%へロウを垂らした。
					ENDIF
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:357 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:357 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = %CALLNAME:MASTER%は強張った表情で見上げてくる%CALLNAME:TARGET%へロウを垂らした。
				CFLAG:357 = 3
			ENDIF
			;그 이외
			IF (CFLAG:357 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:357 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;104,바늘
;-------------------------------------------------
	CASE 104
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:358 == 0
			KSTR:(K++) =
			CFLAG:358 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:358 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:358 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:358 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:358 = 3
			ENDIF
			;그 이외
			IF (CFLAG:358 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:358 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;105,아이마스크
;-------------------------------------------------
	CASE 105
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:18
			;처음
			IF CFLAG:359 == 0
				KSTR:(K++) = 「……」
				KSTR:(K++) = %CALLNAME:MASTER%がアイマスクを取り出すと%CALLNAME:TARGET%は嫌そうにしながらも大人しくしていた。
				CFLAG:359 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:359 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 「%CSTR:이인칭%……？」
						KSTR:(K++) = %CALLNAME:MASTER%は不安そうな%CALLNAME:TARGET%の眼をアイマスクで塞いだ。
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:359 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:359 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:359 = 3
				ENDIF
				;그 이외
				IF (CFLAG:359 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:359 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) = 「……」
				KSTR:(K++) = %CALLNAME:TARGET%はアイマスクを外す%CALLNAME:MASTER%へ無意識のうちに擦り寄った。
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;106,밧줄
;-------------------------------------------------
	CASE 106
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:19
			;처음
			IF CFLAG:360 == 0
				KSTR:(K++) =
				CFLAG:360 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:360 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:360 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:360 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:360 = 3
				ENDIF
				;그 이외
				IF (CFLAG:360 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:360 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;107,볼재갈
;-------------------------------------------------
	CASE 107
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:20
			;처음
			IF CFLAG:361 == 0
				KSTR:(K++) =
				CFLAG:361 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:361 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:361 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:361 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:361 = 3
				ENDIF
				;그 이외
				IF (CFLAG:361 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:361 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;SM계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;조수에 의한 커맨드, 및 레즈플레이
;=================================================
ELSEIF RESULT == COMTYPE("조수ㆍ레즈계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;120,커닐링구스강제
;-------------------------------------------------
	CASE 120
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:362 == 0
			KSTR:(K++) = 「むぐっ……！」
			KSTR:(K++) = %CALLNAME:TARGET%はいきなり押し当てられた秘肉へ不慣れながらも奉仕していた。
			CFLAG:362 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:362 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					IF TEQUIP:주도권 == 1
						WSTR:(K++) = 「……ぅむ、っちゅ…」
						WSTR:(K++) = %CALLNAME:TARGET%は秘肉を舐め回しながら興奮した様子で膣内へ舌を差し込み愛撫していた。
					ELSE
						WSTR:(K++) = 「んぅ、っ……」
						WSTR:(K++) = 「知ってる。……ここ、好きだよな」
					ENDIF 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:362 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:362 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:362 = 3
			ENDIF
			;그 이외
			IF (CFLAG:362 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:362 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;121,조개맞대기
;-------------------------------------------------
	CASE 121
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:363 == 0
			KSTR:(K++) =
			CFLAG:363 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:363 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:363 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:363 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:363 = 3
			ENDIF
			;그 이외
			IF (CFLAG:363 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:363 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;122,조수를범하게한다
;-------------------------------------------------
	CASE 122
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:364 == 0
			KSTR:(K++) =
			CFLAG:364 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:364 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:364 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:364 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:364 = 3
			ENDIF
			;그 이외
			IF (CFLAG:364 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:364 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;조수가 기분 좋게 느끼고 있다 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;조수가 이리 오라고 말하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;조수가 괜찮다고 말하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;범해지며 강한 척을 하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;능숙해졌네 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
 
;-------------------------------------------------
;123,풋잡한다
;-------------------------------------------------
	CASE 123
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:365 == 0
			KSTR:(K++) = 「……」
			KSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%の自身を詰るように足で扱き上げた。
			CFLAG:365 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:365 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 「ふ、ぁあ……%CSTR:이인칭%っ…」
					IF RAND:3 == 0
						WSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%の亀頭を重点的に足先で擽る。
						WSTR:(K++) = 自ら腰を揺らして更なる快感を得ようとする%CALLNAME:TARGET%を言葉で制しておく。
						WSTR:(K++) = 「ごめ、ッごめんなさ……ぁぐっ！」
						WSTR:(K++) = %CALLNAME:MASTER%は謝罪を聞き流し容赦なく%CALLNAME:TARGET%の自身を足裏で踏み付けた。
					ELSEIF RAND:2 == 0
						WSTR:(K++) = %CALLNAME:TARGET%は足で弱点をつつかれ恐怖を感じ嫌々するように首を左右に振る。
						WSTR:(K++) = 嫌がりながらもしっかりと硬度を保っているそこを両足の土踏まずで包んでやる。
						WSTR:(K++) = 「くぁ、んっ、や、やめて…こわい…」
						WSTR:(K++) = %CALLNAME:MASTER%が両足の動きを止めれば%CALLNAME:TARGET%はほんの少し物足りなさそうにしていた。
					ELSE
						WSTR:(K++) = 「……%CSTR:일인칭%って、ヘンタイなのか」
						WSTR:(K++) = %CALLNAME:TARGET%は%CALLNAME:MASTER%に足コキされながらもそこを見つめている。
						WSTR:(K++) = アブノーマルな行為に%CALLNAME:TARGET%自身は戸惑っているが興奮を隠し切れないようだ。
					ENDIF
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:365 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:365 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「ぅ、ぅう……！」
				KSTR:(K++) = %CALLNAME:TARGET%は屈辱からか顔を背け呻いていた。
				CFLAG:365 = 3
			ENDIF
			;그 이외
			IF (CFLAG:365 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:365 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;124,더블펠라
;-------------------------------------------------
	CASE 124
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:366 == 0
			KSTR:(K++) =
			CFLAG:366 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:366 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:366 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:366 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:366 = 3
			ENDIF
			;그 이외
			IF (CFLAG:366 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:366 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;영향에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;평소보다 느끼고 있구나 라는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;레즈 같은 대사에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
		;둘의 펠라는 기분 좋지？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_6
 
;-------------------------------------------------
;125,더블파이즈리
;-------------------------------------------------
	CASE 125
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:367 == 0
			KSTR:(K++) =
			CFLAG:367 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:367 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:367 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:367 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:367 = 3
			ENDIF
			;그 이외
			IF (CFLAG:367 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:367 = 2
			ENDIF
		ENDIF
		
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;영향에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;평소보다 느끼고 있구나 라는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;서로 젖꼭지가 스쳐서 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
		;둘의 파이즈리로 기분 좋아져 줘 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_6
		;파이즈리로 느끼고 있어？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_7
 
;-------------------------------------------------
;126,가슴맞대기
;-------------------------------------------------
	CASE 126
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:368 == 0
			KSTR:(K++) =
			CFLAG:368 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:368 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:368 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:368 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:368 = 3
			ENDIF
			;그 이외
			IF (CFLAG:368 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:368 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;127,자위과시
;-------------------------------------------------
	CASE 127
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:432 == 0
			KSTR:(K++) =
			CFLAG:432 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:432 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:432 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:432 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:432 = 3
			ENDIF
			;그 이외
			IF (CFLAG:432 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:432 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;128,조개젖히기과시
;-------------------------------------------------
	CASE 128
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:465 == 0
			KSTR:(K++) = 「……」
			KSTR:(K++) = %CALLNAME:TARGET%はくぱ、と開く%CALLNAME:MASTER%の秘肉の様を何も言わずに見ている。
			CFLAG:465 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:465 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 「ぅ、わざわざそんな風に見せつけないで…」
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 「へぇ……」
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:465 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:465 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:465 = 3
			ENDIF
			;그 이외
			IF (CFLAG:465 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:465 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;129,자위구음과시
;-------------------------------------------------
	CASE 129
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:466 == 0
			KSTR:(K++) =
			CFLAG:466 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:466 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:466 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:466 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:466 = 3
			ENDIF
			;그 이외
			IF (CFLAG:466 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:466 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;조수에 의한 공통, 및 레즈플레이
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;하드한조교 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("하드계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;64,역강간
;-------------------------------------------------
	CASE 64
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:336 == 0
			KSTR:(K++) =
			CFLAG:336 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:336 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:336 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:336 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:336 = 3
			ENDIF
			;그 이외
			IF (CFLAG:336 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:336 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;65,젖성교
;-------------------------------------------------
	CASE 65
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:337 == 0
			KSTR:(K++) =
			CFLAG:337 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:337 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:337 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:337 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:337 = 3
			ENDIF
			;그 이외
			IF (CFLAG:337 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:337 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;108,삼각목마
;-------------------------------------------------
	CASE 108
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:41
			;처음
			IF CFLAG:463 == 0
				KSTR:(K++) =
				CFLAG:463 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:463 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:463 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:463 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:463 = 3
				ENDIF
				;그 이외
				IF (CFLAG:463 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:463 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;140,이라마치오
;-------------------------------------------------
	CASE 140
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:369 == 0
			KSTR:(K++) =
			CFLAG:369 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:369 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:369 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:369 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:369 = 3
			ENDIF
			;그 이외
			IF (CFLAG:369 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:369 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;141,피스트퍽　복종, 예속만
;-------------------------------------------------
	CASE 141
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:370 == 0
			;처녀
			IF TALENT:0
				KSTR:(K++) =
			ELSE
				KSTR:(K++) =
			ENDIF
			CFLAG:370 = 1
		;2번째 이후
		ELSEIF CFLAG:370 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:370 = 2
		ENDIF
;-------------------------------------------------
;142,애널피스트　복종, 예속만
;-------------------------------------------------
	CASE 142
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:371 == 0
			KSTR:(K++) =
			CFLAG:371 = 1
		;2번째 이후
		ELSEIF CFLAG:371 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:371 = 2
		ENDIF
;-------------------------------------------------
;143,양구멍피스트　복종, 예속만
;-------------------------------------------------
	CASE 143
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:372 == 0
			;처녀
			IF TALENT:0
				KSTR:(K++) =
			ELSE
				KSTR:(K++) =
			ENDIF
			CFLAG:372 = 1
		;2번째 이후
		ELSEIF CFLAG:372 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:372 = 2
		ENDIF
;-------------------------------------------------
;144,방뇨
;-------------------------------------------------
	CASE 144
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:373 == 0
			KSTR:(K++) =
			CFLAG:373 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:373 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:373 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:373 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:373 = 3
			ENDIF
			;그 이외
			IF (CFLAG:373 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:373 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;145,애널봉사강제
;-------------------------------------------------
	CASE 145
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:374 == 0
			KSTR:(K++) =
			CFLAG:374 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:374 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:374 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:374 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:374 = 3
			ENDIF
			;그 이외
			IF (CFLAG:374 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:374 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;146,관장기＋플러그　해제할 때 쪽
;-------------------------------------------------
	CASE 146
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:21
			;처음
			IF CFLAG:375 == 0
				KSTR:(K++) =
				CFLAG:375 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:375 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:375 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:375 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:375 = 3
				ENDIF
				;그 이외
				IF (CFLAG:375 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:375 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;147,공기관장　방귀뀌게한다
;-------------------------------------------------
	CASE 147
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:376 == 0
			KSTR:(K++) =
			CFLAG:376 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:376 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:376 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:376 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:376 = 3
			ENDIF
			;그 이외
			IF (CFLAG:376 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:376 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;148,확장벌룬
;-------------------------------------------------
	CASE 148
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:22
			;처음
			IF CFLAG:377 == 0
				KSTR:(K++) =
				CFLAG:377 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:377 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:377 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:377 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:377 = 3
				ENDIF
				;그 이외
				IF (CFLAG:377 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:377 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;149,애널전극　복종, 예속만
;-------------------------------------------------
	CASE 149
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:23
			;처음
			IF CFLAG:378 == 0
				KSTR:(K++) =
				CFLAG:378 = 1
			;2번째 이후
			ELSEIF CFLAG:378 < 2 || FLAG:7 == 2
				KSTR:(K++) =
				CFLAG:378 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:378 == 1 || FLAG:7 == 2
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;150,유방전극　복종, 예속만
;-------------------------------------------------
	CASE 150
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:24
			;처음
			IF CFLAG:379 == 0
				KSTR:(K++) =
				CFLAG:379 = 1
			;2번째 이후
			ELSEIF CFLAG:379 < 2 || FLAG:7 == 2
				KSTR:(K++) =
				CFLAG:379 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:379 == 1 || FLAG:7 == 2
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;151,뱀장어욕탕　복종, 예속만
;-------------------------------------------------
	CASE 151
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:380 == 0
			;처녀
			IF TALENT:0
				KSTR:(K++) =
			ELSE
				KSTR:(K++) =
			ENDIF
			CFLAG:380 = 1
		;2번째 이후
		ELSEIF CFLAG:380 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:380 = 2
		ENDIF
;-------------------------------------------------
;152,벌레욕탕　복종, 예속만
;-------------------------------------------------
	CASE 152
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:381 == 0
			KSTR:(K++) =
			CFLAG:381 = 1
		;2번째 이후
		ELSEIF CFLAG:381 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:381 = 2
		ENDIF
;-------------------------------------------------
;154,요정오나홀　소인체형계만
;-------------------------------------------------
	CASE 154
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:467 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:467 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:467 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:467 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:467 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:467 = 3
			ENDIF
			;그 이외
			IF (CFLAG:467 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:467 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;155,구슬킥
;-------------------------------------------------
	CASE 155
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:468 == 0
			KSTR:(K++) =
			CFLAG:468 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:468 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:468 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:468 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:468 = 3
			ENDIF
			;그 이외
			IF (CFLAG:468 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:468 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;156,음뇨
;-------------------------------------------------
	CASE 156
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:469 == 0
			KSTR:(K++) =
			CFLAG:469 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:469 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:469 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:469 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:469 = 3
			ENDIF
			;그 이외
			IF (CFLAG:469 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:469 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;하드한조교 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;촉수계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("촉수계")
$SHOKUSHU_PLAY
SELECTCASE SELECTCOM
;-------------------------------------------------
;0,촉수애무
;-------------------------------------------------
	CASE 0
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:455 == 0
			KSTR:(K++) =
			CFLAG:455 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:455 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:455 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:455 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:455 = 3
			ENDIF
			;그 이외
			IF (CFLAG:455 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:455 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;5,촉수애널애무
;-------------------------------------------------
	CASE 5
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:456 == 0
			KSTR:(K++) =
			CFLAG:456 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:456 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:456 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:456 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:456 = 3
			ENDIF
			;그 이외
			IF (CFLAG:456 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:456 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;6,촉수가슴주무르기
;-------------------------------------------------
	CASE 6
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:457 == 0
			KSTR:(K++) =
			CFLAG:457 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:457 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:457 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:457 = 3
			ENDIF
			;그 이외
			IF (CFLAG:457 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:457 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;7,촉수유두자극(短)
;-------------------------------------------------
	CASE 7
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:458 == 0
			KSTR:(K++) =
			CFLAG:458 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:458 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:458 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:458 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:458 = 3
			ENDIF
			;그 이외
			IF (CFLAG:458 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:458 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;8,촉수가슴애무
;-------------------------------------------------
	CASE 8
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:459 == 0
			KSTR:(K++) =
			CFLAG:459 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:459 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:459 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:459 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:459 = 3
			ENDIF
			;그 이외
			IF (CFLAG:459 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:459 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;12,촉수날개애무
;-------------------------------------------------
	CASE 12
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:460 == 0
			KSTR:(K++) =
			CFLAG:460 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:460 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:460 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:460 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:460 = 3
			ENDIF
			;그 이외
			IF (CFLAG:460 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:460 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;13,촉수꼬리애무
;-------------------------------------------------
	CASE 13
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:461 == 0
			KSTR:(K++) =
			CFLAG:461 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:461 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:461 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:461 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:461 = 3
			ENDIF
			;그 이외
			IF (CFLAG:461 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:461 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;15,촉수엉덩이애무
;-------------------------------------------------
	CASE 15
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:462 == 0
			KSTR:(K++) =
			CFLAG:462 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:462 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:462 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:462 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:462 = 3
			ENDIF
			;그 이외
			IF (CFLAG:462 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:462 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;160,촉수소환
;-------------------------------------------------
	CASE 160
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:25
			;처음
			IF CFLAG:382 == 0
				KSTR:(K++) =
				CFLAG:382 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:382 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:382 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:382 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:382 = 3
				ENDIF
				;그 이외
				IF (CFLAG:382 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:382 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;161,촉수삽입
;-------------------------------------------------
	CASE 161
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:13
			;처음
			IF CFLAG:383 == 0
				;처녀
				IF TALENT:0
					IF 함락( ) > 0
						;연모, 친애
						IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
							KSTR:(K++) = 
						;음란, 창부
						ELSEIF 함락( ) > 0 && RESULTS == "음란"
							KSTR:(K++) = 
						;복종, 예속
						ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
							KSTR:(K++) =
						ENDIF
						;함락완료 공통
						IF !구상유무확인(0)
							KSTR:(K++) =
						ENDIF
					ENDIF
					;그 이외
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				;비처녀
				ELSE
					IF 함락( ) > 0
						;연모, 친애
						IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
							KSTR:(K++) = 
						;음란, 창부
						ELSEIF 함락( ) > 0 && RESULTS == "음란"
							KSTR:(K++) = 
						;복종, 예속
						ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
							KSTR:(K++) =
						ENDIF
						;함락완료 공통
						IF !구상유무확인(0)
							KSTR:(K++) =
						ENDIF
					ENDIF
					;그 이외
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				CFLAG:383 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:383 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:383 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:383 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:383 = 3
				ENDIF
				;그 이외
				IF (CFLAG:383 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:383 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;162,애널촉수
;-------------------------------------------------
	CASE 162
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:14
			;처음
			IF CFLAG:384 == 0
				KSTR:(K++) =
				CFLAG:384 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:384 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:384 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:384 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:384 = 3
				ENDIF
				;그 이외
				IF (CFLAG:384 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:384 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;163,촉수클리자극
;-------------------------------------------------
	CASE 163
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:11
			;처음
			IF CFLAG:385 == 0
				KSTR:(K++) =
				CFLAG:385 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:385 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:385 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:385 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:385 = 3
				ENDIF
				;그 이외
				IF (CFLAG:385 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:385 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;164,촉수유두자극
;-------------------------------------------------
	CASE 164
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:16
			;처음
			IF CFLAG:386 == 0
				KSTR:(K++) =
				CFLAG:386 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:386 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:386 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:386 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:386 = 3
				ENDIF
				;그 이외
				IF (CFLAG:386 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:386 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;165,촉수착유
;-------------------------------------------------
	CASE 165
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:17
			;처음
			IF CFLAG:387 == 0
				KSTR:(K++) =
				CFLAG:387 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:387 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:387 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:387 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:387 = 3
				ENDIF
				;그 이외
				IF (CFLAG:387 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:387 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;166,촉수긴박
;-------------------------------------------------
	CASE 166
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:19
			;처음
			IF CFLAG:388 == 0
				KSTR:(K++) =
				CFLAG:388 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:388 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:388 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:388 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:388 = 3
				ENDIF
				;그 이외
				IF (CFLAG:388 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:388 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;167,촉수관장　극태 촉수가 항문을 막고, 체액을 부어넣는다
;-------------------------------------------------
	CASE 167
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:21
			;처음
			IF CFLAG:389 == 0
				KSTR:(K++) =
				CFLAG:389 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:389 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:389 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:389 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:389 = 3
				ENDIF
				;그 이외
				IF (CFLAG:389 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:389 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;168,촉수구욕
;-------------------------------------------------
	CASE 168
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:36
			;처음
			IF CFLAG:390 == 0
				KSTR:(K++) =
				CFLAG:390 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:390 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:390 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:390 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:390 = 3
				ENDIF
				;그 이외
				IF (CFLAG:390 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:390 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;169,촉수페니스자극
;-------------------------------------------------
	CASE 169
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:12
			;처음
			IF CFLAG:391 == 0
				KSTR:(K++) =
				CFLAG:391 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:391 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:391 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:391 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:391 = 3
				ENDIF
				;그 이외
				IF (CFLAG:391 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:391 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;170,촉수삽입시킨다
;-------------------------------------------------
	CASE 170
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:13
			;처음
			IF CFLAG:470 == 0
				KSTR:(K++) =
				CFLAG:470 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:470 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:470 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:470 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:470 = 3
				ENDIF
				;그 이외
				IF (CFLAG:470 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:470 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;171,애널촉수시킨다
;-------------------------------------------------
	CASE 171
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:14
			;처음
			IF CFLAG:471 == 0
				KSTR:(K++) =
				CFLAG:471 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:471 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:471 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:471 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:471 = 3
				ENDIF
				;그 이외
				IF (CFLAG:471 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:471 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;172,촉수클리자극시킨다
;-------------------------------------------------
	CASE 172
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:11
			;처음
			IF CFLAG:472 == 0
				KSTR:(K++) =
				CFLAG:472 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:472 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:472 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:472 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:472 = 3
				ENDIF
				;그 이외
				IF (CFLAG:472 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:472 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;173,촉수유두자극시킨다
;-------------------------------------------------
	CASE 173
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:16
			;처음
			IF CFLAG:473 == 0
				KSTR:(K++) =
				CFLAG:473 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:473 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:473 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:473 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:473 = 3
				ENDIF
				;그 이외
				IF (CFLAG:473 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:473 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;174,촉수착유시킨다
;-------------------------------------------------
	CASE 174
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:17
			;처음
			IF CFLAG:474 == 0
				KSTR:(K++) =
				CFLAG:474 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:474 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:474 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:474 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:474 = 3
				ENDIF
				;그 이외
				IF (CFLAG:474 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:474 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;175,촉수페니스자극시킨다
;-------------------------------------------------
	CASE 175
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:12
			;처음
			IF CFLAG:475 == 0
				KSTR:(K++) =
				CFLAG:475 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:475 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:475 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:475 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:475 = 3
				ENDIF
				;그 이외
				IF (CFLAG:475 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:475 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;촉수계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;특수커맨드
;=================================================
ELSEIF RESULT == COMTYPE("특수커맨드계") && SELECTCOM < 600
SELECTCASE SELECTCOM
;-------------------------------------------------
;180,로션
;-------------------------------------------------
	CASE 180
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:392 == 0
			KSTR:(K++) =
			CFLAG:392 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:392 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:392 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:392 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:392 = 3
			ENDIF
			;그 이외
			IF (CFLAG:392 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:392 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;181,미약
;-------------------------------------------------
	CASE 181
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:393 == 0
			KSTR:(K++) =
			CFLAG:393 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:393 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:393 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:393 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:393 = 3
			ENDIF
			;그 이외
			IF (CFLAG:393 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:393 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;182,이뇨제
;-------------------------------------------------
	CASE 182
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:394 == 0
			KSTR:(K++) =
			CFLAG:394 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:394 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:394 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:394 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:394 = 3
			ENDIF
			;그 이외
			IF (CFLAG:394 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:394 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;183,비디오카메라
;-------------------------------------------------
	CASE 183
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:28
			;처음
			IF CFLAG:395 == 0
				KSTR:(K++) =
				CFLAG:395 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:395 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:395 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:395 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:395 = 3
				ENDIF
				;그 이외
				IF (CFLAG:395 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:395 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;184,야외플레이
;-------------------------------------------------
	CASE 184
		K_CALL:K = TRAIN_MESSAGE
		;TEQUIP:29로 어느 장소를 선택해도 같은 구상이 나오도록
		;변경･장소마다 바꾸고 싶으면 == 로 하고 숫자를 만지기
		;폐빌딩=1　삼림=2　공원=4　냇가=8
		IF TEQUIP:29
			;처음
			IF CFLAG:396 == 0
				KSTR:(K++) =
				CFLAG:396 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:396 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:396 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:396 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:396 = 3
				ENDIF
				;그 이외
				IF (CFLAG:396 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:396 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;185,조수를범한다
;-------------------------------------------------
	CASE 185
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:397 == 0
			KSTR:(K++) =
			CFLAG:397 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:397 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:397 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:397 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:397 = 3
			ENDIF
			;그 이외
			IF (CFLAG:397 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:397 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;범해져서 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;우리들, 정말 사이 좋지？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;부럽지？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;이쪽이 신경쓰이는 것 같다 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
 
;-------------------------------------------------
;186,수치플레이
;-------------------------------------------------
	CASE 186
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:30
			;처음
			IF CFLAG:398 == 0
				KSTR:(K++) =
				CFLAG:398 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:398 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:398 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:398 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:398 = 3
				ENDIF
				;그 이외
				IF (CFLAG:398 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:398 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;187,목욕탕플레이
;-------------------------------------------------
	CASE 187
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:31
			;처음
			IF CFLAG:399 == 0
				KSTR:(K++) =
				CFLAG:399 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:399 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:399 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:399 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:399 = 3
				ENDIF
				;그 이외
				IF (CFLAG:399 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:399 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;188,샤워
;-------------------------------------------------
	CASE 188
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:32
			;처음
			IF CFLAG:400 == 0
				KSTR:(K++) =
				CFLAG:400 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:400 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:400 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:400 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:400 = 3
				ENDIF
				;그 이외
				IF (CFLAG:400 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:400 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;189,새댁플레이　연모, 친애만
;-------------------------------------------------
	CASE 189
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:33
			;처음
			IF CFLAG:401 == 0
				KSTR:(K++) =
				CFLAG:401 = 1
			;2번째 이후
			ELSEIF CFLAG:401 < 2 || FLAG:7 == 2
				KSTR:(K++) =
				CFLAG:401 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:401 == 1 || FLAG:7 == 2
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;190,사진촬영
;-------------------------------------------------
	CASE 190
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:402 == 0
			KSTR:(K++) =
			CFLAG:402 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:402 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:402 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:402 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:402 = 3
			ENDIF
			;그 이외
			IF (CFLAG:402 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:402 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;191,강정신약
;-------------------------------------------------
	CASE 191
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:403 == 0
			KSTR:(K++) =
			CFLAG:403 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:403 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:403 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:403 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:403 = 3
			ENDIF
			;그 이외
			IF (CFLAG:403 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:403 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;192,콘돔정음（노예）
;-------------------------------------------------
	CASE 192
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:404 == 0
			KSTR:(K++) =
			CFLAG:404 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:404 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:404 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:404 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:404 = 3
			ENDIF
			;그 이외
			IF (CFLAG:404 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:404 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;193,콘돔정음（조수）
;-------------------------------------------------
	CASE 193
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:405 == 0
			KSTR:(K++) =
			CFLAG:405 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:405 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:405 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:405 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:405 = 3
			ENDIF
			;그 이외
			IF (CFLAG:405 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:405 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;정액이 무척 맛있어 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;이건 내꺼야！대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;부럽지？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;먼저 받아갈께 대사（사이좋음）
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
 
;-------------------------------------------------
;194,정액키스
;-------------------------------------------------
	CASE 194
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:406 == 0
			KSTR:(K++) = 「ン、……ぷぁ、」
			KSTR:(K++) = %CALLNAME:TARGET%は己の口に流し込まれた苦さになんとも微妙な顔をした。
			CFLAG:406 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:406 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:406 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:406 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 「……ぅぇ」
				KSTR:(K++) = %CALLNAME:TARGET%は顔を顰めてしきりに口をもごもごさせている。
				KSTR:(K++) = 唾液で薄まっているとはいえなかなかに苦くえぐい味なことだろう。
				CFLAG:406 = 3
			ENDIF
			;그 이외
			IF (CFLAG:406 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:406 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;195,유선개발
;-------------------------------------------------
	CASE 195
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:431 == 0
			KSTR:(K++) =
			CFLAG:431 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:431 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:431 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:431 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:431 = 3
			ENDIF
			;그 이외
			IF (CFLAG:431 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:431 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;200,사람을부른다
;-------------------------------------------------
	CASE 200
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:433 == 0
			KSTR:(K++) =
			CFLAG:433 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:433 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:433 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:433 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:433 = 3
			ENDIF
			;그 이외
			IF (CFLAG:433 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:433 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;201,윤간
;-------------------------------------------------
	CASE 201
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:434 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:434 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:434 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:434 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:434 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:434 = 3
			ENDIF
			;그 이외
			IF (CFLAG:434 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:434 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;202,사람을모으게한다
;-------------------------------------------------
	CASE 202
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:476 == 0
			KSTR:(K++) =
			CFLAG:476 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:476 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:476 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:476 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:476 = 3
			ENDIF
			;그 이외
			IF (CFLAG:476 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:476 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;203,원격조교
;-------------------------------------------------
	CASE 203
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:477 == 0
			KSTR:(K++) =
			CFLAG:477 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:477 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:477 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:477 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:477 = 3
			ENDIF
			;그 이외
			IF (CFLAG:477 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:477 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;204,정액먹기
;-------------------------------------------------
	CASE 204
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:478 == 0
			KSTR:(K++) =
			CFLAG:478 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:478 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:478 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:478 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:478 = 3
			ENDIF
			;그 이외
			IF (CFLAG:478 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:478 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;특수 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;500번대,파생커맨드
;=================================================
ELSEIF RESULT == COMTYPE("派生系") && SELECTCOM < 600
SELECTCASE SELECTCOM
;-------------------------------------------------
;500,３Ｐ
;-------------------------------------------------
	CASE 500
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:407 == 0
			KSTR:(K++) =
			CFLAG:407 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:407 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:407 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:407 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:407 = 3
			ENDIF
			;그 이외
			IF (CFLAG:407 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:407 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;안쪽이 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;계속 가버려라！대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;무척 기분 좋아 보여 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;여기에 넣어줘 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
 
;-------------------------------------------------
;501,식스나인
;-------------------------------------------------
	CASE 501
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:408 == 0
			KSTR:(K++) =
			CFLAG:408 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:408 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:408 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:408 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:408 = 3
			ENDIF
			;그 이외
			IF (CFLAG:408 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:408 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;502,이와시미즈
;-------------------------------------------------
	CASE 502
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:409 == 0
			KSTR:(K++) =
			CFLAG:409 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:409 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:409 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:409 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:409 = 3
			ENDIF
			;그 이외
			IF (CFLAG:409 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:409 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;503,G스팟자극
;-------------------------------------------------
	CASE 503
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:410 == 0
			KSTR:(K++) =
			CFLAG:410 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:410 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:410 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:410 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:410 = 3
			ENDIF
			;그 이외
			IF (CFLAG:410 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:410 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;504,흐트러진모란
;-------------------------------------------------
	CASE 504
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:411 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:411 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:411 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:411 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:411 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:411 = 3
			ENDIF
			;그 이외
			IF (CFLAG:411 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:411 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;505,기승유방자위
;-------------------------------------------------
	CASE 505
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:412 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:412 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:412 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:412 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:412 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:412 = 3
			ENDIF
			;그 이외
			IF (CFLAG:412 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:412 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;506,삽입음부자극
;-------------------------------------------------
	CASE 506
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:413 == 0
			KSTR:(K++) =
			CFLAG:413 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:413 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:413 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:413 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:413 = 3
			ENDIF
			;그 이외
			IF (CFLAG:413 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:413 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;507,삽입자궁구자극
;-------------------------------------------------
	CASE 507
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:414 == 0
			KSTR:(K++) =
			CFLAG:414 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:414 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:414 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:414 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:414 = 3
			ENDIF
			;그 이외
			IF (CFLAG:414 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:414 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;508,６９파이즈리
;-------------------------------------------------
	CASE 508
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:415 == 0
			KSTR:(K++) =
			CFLAG:415 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:415 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:415 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:415 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:415 = 3
			ENDIF
			;그 이외
			IF (CFLAG:415 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:415 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;509,착유
;-------------------------------------------------
	CASE 509
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:416 == 0
			KSTR:(K++) =
			CFLAG:416 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:416 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:416 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:416 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:416 = 3
			ENDIF
			;그 이외
			IF (CFLAG:416 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:416 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;510,더블스마타
;-------------------------------------------------
	CASE 510
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:417 == 0
			KSTR:(K++) =
			CFLAG:417 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:417 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:417 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:417 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:417 = 3
			ENDIF
			;그 이외
			IF (CFLAG:417 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:417 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;영향에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;평소보다 느끼고 있구나 라는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;서로 기분 좋네 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
 
;-------------------------------------------------
;511,두개펠라
;-------------------------------------------------
	CASE 511
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:418 == 0
			KSTR:(K++) =
			CFLAG:418 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:418 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:418 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:418 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:418 = 3
			ENDIF
			;그 이외
			IF (CFLAG:418 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:418 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;자신도 확실히 기분 좋게 해줘 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;마스터의 페니스가 닿아서 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;2인분의 정액 마시게 해줄께 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;능숙해졌네 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
 
;-------------------------------------------------
;512,애널기승유방자위
;-------------------------------------------------
	CASE 512
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:419 == 0
			KSTR:(K++) =
			CFLAG:419 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:419 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:419 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:419 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:419 = 3
			ENDIF
			;그 이외
			IF (CFLAG:419 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:419 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;513,입과바기나를범한다
;-------------------------------------------------
	CASE 513
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:439 == 0
			KSTR:(K++) =
			CFLAG:439 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:439 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:439 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:439 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:439 = 3
			ENDIF
			;그 이외
			IF (CFLAG:439 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:439 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;안쪽이 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;입이 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;계속 가버려라！대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;무척 기분 좋아 보여 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
 
;-------------------------------------------------
;514,입과애널을범한다
;-------------------------------------------------
	CASE 514
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:440 == 0
			KSTR:(K++) =
			CFLAG:440 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:440 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:440 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:440 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:440 = 3
			ENDIF
			;그 이외
			IF (CFLAG:440 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:440 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;안쪽이 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;입이 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;계속 가버려라！대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;무척 기분 좋아 보여 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
 
;-------------------------------------------------
;515,애널흐트러진모란
;-------------------------------------------------
	CASE 515
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:441 == 0
			KSTR:(K++) =
			CFLAG:441 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:441 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:441 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:441 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:441 = 3
			ENDIF
			;그 이외
			IF (CFLAG:441 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:441 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;500번대,파생 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;600번대,비전투원 커맨드
;=================================================
ELSEIF SELECTCOM >= 600
SELECTCASE SELECTCOM
;-------------------------------------------------
;600,기초훈련
;-------------------------------------------------
	CASE 600
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:481 == 0
			KSTR:(K++) =
			CFLAG:481 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:481 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:481 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:481 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:481 = 3
			ENDIF
			;그 이외
			IF (CFLAG:481 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:481 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;601,시험소환
;-------------------------------------------------
	CASE 601
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:서머너 && !TALENT:비전투원
			KSTR:(K++) =
		ENDIF
;-------------------------------------------------
;602,그림자추출
;-------------------------------------------------
	CASE 602
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:페르소나구사자 && !TALENT:비전투원
			KSTR:(K++) =
;			;초기페르소나추가
			;ABL:초기페르소나 =
			
;			;초기페르소나능력변경
			;ABL:페르소나1데이터／스킬1 =
			
			;ABL:페르소나1데이터／습득스킬1 =
			;ABL:페르소나1데이터／습득LV1 =
 
		ENDIF
;-------------------------------------------------
;603,각성원조
;-------------------------------------------------
	CASE 603
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:이능자 && !TALENT:비전투원
			KSTR:(K++) =
;			;능력변경
			;ABL:스킬1 = 
			
			;ABL:습득스킬1
			;ABL:습득LV1
 
		ENDIF
;-------------------------------------------------
;604,바이러스조사
;-------------------------------------------------
	CASE 604
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:식노 && !TALENT:비전투원
			KSTR:(K++) =
		ENDIF
;-------------------------------------------------
;605,영매실험
;-------------------------------------------------
	CASE 605
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:데빌시프터 && !TALENT:비전투원
			KSTR:(K++) =
;			;초기링크악마
			;CFLAG:초기링크악마
			
;			;초기링크악마능력변경
			;ABL:초기변신악마스킬1
			
			;ABL:초기변신악마습득스킬1
			;ABL:초기변신악마습득LV1
			
			;ABL:초기링크악마공격상성
			;ABL:초기링크악마최저공격횟수
			;ABL:초기링크악마최대공격횟수
			;ABL:초기링크악마사거리
			;ABL:초기링크악마공격범위
			;ABL:초기링크악마추가효과
			;ABL:초기링크악마추가효과상성
			;ABL:초기링크악마추가효과명중률
			;ABL:초기링크악마추가효과최대명중률
		ENDIF
;-------------------------------------------------
;606,개조수술
;-------------------------------------------------
	CASE 606
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:기계의처녀 && !TALENT:비전투원
			KSTR:(K++) =
		ENDIF
	CASEELSE
ENDSELECT
ENDIF
;-------------------------------------------------
;그 외, 어느 계통의 조교인지 몰랐던 녀셕
;-------------------------------------------------
IF !구상유무확인(0)
SELECTCASE SELECTCOM
 
ENDSELECT
ENDIF
;------------------------------------------------
;커맨드구상 여기까지
;------------------------------------------------
 
CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_COM_ZZZ
	CATCH
		CALL TRAIN_MESSAGE
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;윤간시의 구상
;CASE 0 안은 반드시 건너뛰게 되어있으며
;RETURN에서 0을 반환하면, 이후 통상적인 윤간행동을 출력합니다
;CFLAG는 사용하지 않습니다
;COMF201_윤간 내에서 LOCAL을 사용하고 있으므로 사용하지 않습니다
;-------------------------------------------------
@KOJO_MESSAGE_RINKAN_K4199, ARG:1 , ARG:2
CALL 구상변수초기화

SELECTCASE ARG:1
	CASE 0
	;CASE 0은 오리지널 윤간 구상
	;C:1회당 귀가 인수 D：흥분도 증가(및 감소)
	;C와 D는 오리지널 윤간 구상 내에서만 사용
	;C += 、D += 를 사용
		IF ARG:2 == 1
		;하드ON
		
		ELSE
		;하드OFF
		
		ENDIF
	;하드계
	;가슴을 밟힌다（거유ㆍ폭유만）
	CASE 1
		IF (함락( ) > 0) && FLAG:7 == 2
			;연모, 친애
			IF 함락( ) > 0 && RESULTS == "연모"
				WSTR:(K++) = 
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				WSTR:(K++) = 
			;복종, 예속	
			ELSEIF 함락( ) > 0 && RESULTS == "복종"
				WSTR:(K++) = 
			ENDIF
			;함락완료 공통
			IF KSTR:0 == ""
				WSTR:(K++) = 
			ENDIF
		ENDIF
		;굴복각인Lv3
		IF MARK:2 == 3 && FLAG:7 == 2 && KSTR:0 == ""
			WSTR:(K++) = 
		ENDIF
		;그 이외
		IF FLAG:7 == 2 && KSTR:0 == ""
			WSTR:(K++) =
		ENDIF
	;머리를 밟힌다
	CASE 2
	
	;폭력을 당하다
	CASE 3
	
	;담배빵
	;흥분도가 낮고, 반항적, 꿋꿋함, 프라이드높음, 건방짐, 저항을 가지고 있다
	CASE 4
	
	;지면에 늘어진 정액 핥아먹인다
	;마조끼３이상
	CASE 5
	
	;몸에 낙서를 당하고 범해진다
	CASE 6
	
	;엉덩이를 치켜올려 뒤로부터 범해지면서, 머리를 짓밟혀 굴욕으로 글썽이는 상태
	CASE 7
	
	;통상계
	;음란계로 눈앞에 장대가 진열되어, 어떤 것을 원하는지 선택하게 하거나（음란ㆍ창부만）
	CASE 8
	
	;거유 이상의 캐릭터라면 「그 젖은 장식이냐？ 쉬고 있지 말라고」등등 매도당하며 파이즈리한다（거유ㆍ폭유만）
	CASE 9
	
	;삽입만은 피하려고 필사적으로 입과 손으로 몰려드는 자지에 봉사한다（연모ㆍ친애만）
	CASE 10
	
	;;양구멍을 뚫리면서 억지로 미소와 피스싸인을 취하게 시켜져 기념촬영
	CASE 11
	
	;;윤간후의 뒷처리 묘사에서, 본인 정액 투성이가 되었는데도 주위 남자들의 육봉을 혀로 청소시키고 있다
	CASE 12
	
	;안에 사정당해도 즉시 다른 사람으로 교대
	CASE 13
	
	;일제사정으로 정액투성이
	CASE 14
	
	;;양구멍 찔리며 동시 사정
	CASE 15
	
	;기승위+양겨드랑이잡
	CASE 16
	
	;;입+보지
	CASE 17
	
ENDSELECT

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_RINKAN_ZZZ
	CATCH
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG 관련（X1을 캐릭터번호로 치환）
;파라미터 변화 등을 트리거로 한 구상　CFLAG 221～260을 사용
;-------------------------------------------------
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_A（X1을 캐릭터번호로 치환）
;조교자의 사정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K4199
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_A
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K4199
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K4199
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교자의 사정시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
LOCAL = 0;사정량
;-------------------------------------------------
;사정장소 변경시의 텍스트　유효시 여기서 함수 종료
;-------------------------------------------------


IF CFLAG:106
	LOCAL = 1
	K_CALL:K = PALAM_MESSAGE_A
	IF 1 == 0
	;머리카락
	ELSEIF CFLAG:106 == 1				
		IF (함락( ) > 0) && (CFLAG:221 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:221= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:221= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:221= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:221= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:221 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:221= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:221 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:221= 1	
		ENDIF		
	;얼굴
	ELSEIF CFLAG:106 == 2				
		IF (함락( ) > 0) && (CFLAG:222 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:222= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:222= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:222= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:222= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:222 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:222= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:222 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:222= 1	
		ENDIF		
	;가슴
	ELSEIF CFLAG:106 == 3				
		IF (함락( ) > 0) && (CFLAG:223 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:223= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:223= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:223= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:223= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:223 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:223= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:223 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:223= 1	
		ENDIF		
	;배
	ELSEIF CFLAG:106 == 4				
		IF (함락( ) > 0) && (CFLAG:224 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:224= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:224= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:224= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:224= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:224 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:224= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:224 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:224= 1	
		ENDIF		
	;겨드랑이
	ELSEIF CFLAG:106 == 5				
		IF (함락( ) > 0) && (CFLAG:225 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:225= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:225= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:225= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:225= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:225 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:225= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:225 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:225= 1	
		ENDIF		
	;허벅지
	ELSEIF CFLAG:106 == 6				
		IF (함락( ) > 0) && (CFLAG:226 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:226= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:226= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:226= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:226= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:226 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:226= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:226 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:226= 1	
		ENDIF		
	;가랑이
	ELSEIF CFLAG:106 == 7				
		IF (함락( ) > 0) && (CFLAG:227 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:227= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:227= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:227= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:227= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:227 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:227= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:227 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:227= 1	
		ENDIF		
	;엉덩이
	ELSEIF CFLAG:106 == 8				
		IF (함락( ) > 0) && (CFLAG:228 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:228= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:228= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:228= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:228= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:228 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:228= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:228 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:228= 1	
		ENDIF		
	;콘돔
	ELSEIF CFLAG:106 == 9
		LOCAL = 0
		;섹스로 사정하고, 애널섹스 이외를 실행 시
		IF GROUPMATCH(SELECTCOM , 60 , 61 , 66 , 68 , 69 , 72 , 504 , 505 , 506 , 507)					
			IF (함락( ) > 0) && (CFLAG:229 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:229= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:229= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:229= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:229= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:229 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:229= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:229 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:229= 1	
			ENDIF		
		ELSE					
			IF (함락( ) > 0) && (CFLAG:230 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:230= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:230= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:230= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:230= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:230= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:230 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:230= 1	
			ENDIF		
		ENDIF
	ENDIF
ENDIF


;-------------------------------------------------
;손, 입 이외로 사정
;대응 커맨드는 키스, 스마타, 풋잡, 애널강제봉사, 더블스마타, 조수를범하게한다
;-------------------------------------------------
IF TFLAG:9 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;대량사정				
	IF TFLAG:9 > 1
		IF (함락( ) > 0) && (CFLAG:230 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:230= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:230= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:230= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:230= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:230= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:230 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:230= 1	
		ENDIF		
	ENDIF
	;통상				
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:230 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:230= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:230= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:230= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:230= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:230= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:230 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:230= 1	
		ENDIF		
	ENDIF
	LOCAL = TFLAG:9
ENDIF

;-------------------------------------------------
;조수를범한다로 사정
;-------------------------------------------------
IF TFLAG:7 > 0
	K_CALL:K = PALAM_MESSAGE_A			
	;대량사정					
	IF TFLAG:7 > 1
		IF (함락( ) > 0) && (CFLAG:232 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:232= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:232= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:232= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:232= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:232= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:232 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:232= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:232 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:232= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:232= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:232= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:232= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:232= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:232 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:232= 1	
		ENDIF		
	ENDIF	
	LOCAL = TFLAG:7
ENDIF

;-------------------------------------------------
;입으로 사정
;-------------------------------------------------
IF TFLAG:0 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;이라마치오로 사정
	IF SELECTCOM == 140			
		;대량사정						
		IF TFLAG:0 > 1
			IF (함락( ) > 0) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF !구상유무확인(0)
			IF (함락( ) > 0) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
	;그 이외의 입사정
	ELSE
		;대량사정						
		IF TFLAG:0 > 1
			IF (함락( ) > 0) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF !구상유무확인(0)
			IF (함락( ) > 0) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
	ENDIF
	LOCAL = TFLAG:0
ENDIF

;-------------------------------------------------
;손으로 사정
;-------------------------------------------------
IF TFLAG:1 > 0
	K_CALL:K = PALAM_MESSAGE_A	
	;대량사정					
	IF TFLAG:1 > 1
		IF (함락( ) > 0) && (CFLAG:234 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:234= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:234= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:234= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:234= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:234= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:234 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:234= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:234 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:234= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:234= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:234= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:234= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:234= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:234 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:234= 1	
		ENDIF		
	ENDIF	
	LOCAL = TFLAG:1
ENDIF

;-------------------------------------------------
;가슴으로 사정
;-------------------------------------------------
IF TFLAG:34 > 0
	K_CALL:K = PALAM_MESSAGE_A		
	;대량사정					
	IF TFLAG:34 > 1
		IF (함락( ) > 0) && (CFLAG:235 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:235= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:235= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:235= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:235= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:235= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:235 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:235= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:235 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:235= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:235= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:235= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:235= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:235= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:235 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:235= 1	
		ENDIF		
	ENDIF	
	LOCAL = TFLAG:34
ENDIF
W:K = 1
;-------------------------------------------------
;오나홀에 사정
;-------------------------------------------------
IF TFLAG:16 == 1
	K_CALL:K = PALAM_MESSAGE_A		
	;대량사정					
	IF TFLAG:16 > 1
		IF (함락( ) > 0) && (CFLAG:236 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:236= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:236= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:236= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:236= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:236= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:236 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:236= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:236 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:236= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:236= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:236= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:236= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:236= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:236 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:236= 1	
		ENDIF		
	ENDIF	
	LOCAL = 0
ENDIF

;-------------------------------------------------
;섹스로 사정
;-------------------------------------------------
IF TFLAG:2 > 0
	LOCAL = TFLAG:2
	K_CALL:K = PALAM_MESSAGE_A
	;애널섹스계
	IF SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || SELECTCOM == 512
		;대량사정						
		IF TFLAG:2 > 1
			IF (함락( ) > 0) && (CFLAG:237 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:237= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:237= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:237= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:237= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:237= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:237 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:237= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF !구상유무확인(0)
			IF (함락( ) > 0) && (CFLAG:237 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:237= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:237= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:237= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:237= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:237= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:237 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:237= 1	
			ENDIF		
		ENDIF
	;V계 섹스
	ELSE
		;안쪽 자극 섹스계
		IF SELECTCOM == 506 || SELECTCOM == 507
			;대량사정						
			IF TFLAG:2 > 1
				IF (함락( ) > 0) && (CFLAG:238 < 3 || FLAG:7 == 2)		
					;연모, 친애	
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
						WSTR:(K++) = 
						CFLAG:238= 4
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"	
						WSTR:(K++) = 
						CFLAG:238= 3
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
						WSTR:(K++) = 
						CFLAG:238= 3
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
						CFLAG:238= 3
					ENDIF	
				ENDIF		
				;굴복각인Lv3		
				IF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:238= 2	
				ENDIF		
				;그 이외		
				IF (CFLAG:238 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) =
					CFLAG:238= 1	
				ENDIF		
			ENDIF	
			;통상						
			IF !구상유무확인(0)
				IF (함락( ) > 0) && (CFLAG:238 < 3 || FLAG:7 == 2)		
					;연모, 친애	
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
						WSTR:(K++) = 
						CFLAG:238= 4
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"	
						WSTR:(K++) = 
						CFLAG:238= 3
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
						WSTR:(K++) = 
						CFLAG:238= 3
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
						CFLAG:238= 3
					ENDIF	
				ENDIF		
				;굴복각인Lv3		
				IF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:238= 2	
				ENDIF		
				;그 이외		
				IF (CFLAG:238 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:238= 1	
				ENDIF		
			ENDIF
		
		ENDIF
		;그 외 섹스계
		IF !구상유무확인(0)
			;대량사정						
			IF TFLAG:2 > 1
				IF (함락( ) > 0) && (CFLAG:239 < 3 || FLAG:7 == 2)		
					;연모, 친애	
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
						WSTR:(K++) = 
						CFLAG:239= 4
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"	
						WSTR:(K++) = 
						CFLAG:239= 3
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
						WSTR:(K++) = 
						CFLAG:239= 3
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
						CFLAG:239= 3
					ENDIF	
				ENDIF		
				;굴복각인Lv3		
				IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:239= 2	
				ENDIF		
				;그 이외		
				IF (CFLAG:239 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:239= 1	
				ENDIF		
			ENDIF	
			;통상						
			IF !구상유무확인(0)
				IF (함락( ) > 0) && (CFLAG:239 < 3 || FLAG:7 == 2)		
					;연모, 친애	
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
						WSTR:(K++) = 
						CFLAG:239= 4
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"	
						WSTR:(K++) = 
						CFLAG:239= 3
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
						WSTR:(K++) = 
						CFLAG:239= 3
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
						CFLAG:239= 3
					ENDIF	
				ENDIF		
				;굴복각인Lv3		
				IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:239= 2	
				ENDIF		
				;그 이외		
				IF (CFLAG:239 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) =
					CFLAG:239= 1	
				ENDIF		
			ENDIF	
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;역강간으로 사정시켰다
;-------------------------------------------------
IF SELECTCOM == 64 && TFLAG:10 > 0
	LOCAL = TFLAG:10
	K_CALL:K = PALAM_MESSAGE_A		
	;대량사정					
	IF TFLAG:10 > 1
		IF (함락( ) > 0) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:240= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:240= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:240= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:240= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:240= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:240= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:240= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:240= 1	
		ENDIF		
	ENDIF	
ENDIF
;-------------------------------------------------
;사정 공통
;-------------------------------------------------
	IF !구상유무확인(0) && LOCAL > 0
		IF (함락( ) > 0) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:240= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:240= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:240= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:240= 1	
		ENDIF		
	ENDIF	

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_PALAMCNG_A_ZZZ
	CATCH
		CALL PALAM_MESSAGE_A
	ENDCATCH
	
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_E（X1을 캐릭터번호로 치환）
;촉수의 사정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_E_K4199
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_E
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K4199
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K4199
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;촉수의 사정시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
;-------------------------------------------------
;촉수가 사정
;-------------------------------------------------
IF TFLAG:15 >0
	K_CALL:K = PALAM_MESSAGE_E			
	IF (함락( ) > 0) && (CFLAG:241 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
			CFLAG:241= 4
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
			CFLAG:241= 3
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
			CFLAG:241= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(0)	
			WSTR:(K++) = 
			CFLAG:241= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:241 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
		WSTR:(K++) = 	
		CFLAG:241= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:241 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
		WSTR:(K++) =
		CFLAG:241= 1	
	ENDIF		
ENDIF

CALL 구상변수표시
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_COM_ZZZ
	CATCH
		CALL PALAM_MESSAGE_E
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_B 관련（X1을 캐릭터번호로 치환）
;노예의 절정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_B_K4199
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_B
	RETURN 0
ENDIF
;상정외의 구상이라면 스킵(이거 지우면 버그)
IF SELECTCOM < 0 || SELECTCOM > 700
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K4199
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K4199
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;노예의 절정시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
;-------------------------------------------------
;처음 C절정
;-------------------------------------------------
IF NOWEX:0 > 0 && CFLAG:242 == 0
	WSTR:(K++) = 「……」
	WSTR:(K++) = %CALLNAME:TARGET%から声は出なかったものの絶頂の余韻でひくひくと下半身を震わせていた。
	CFLAG:242 = 1
ENDIF

;-------------------------------------------------
;처음 V절정
;-------------------------------------------------
IF NOWEX:1 > 0 && CFLAG:243 == 0
	WSTR:(K++) =
	CFLAG:243 = 1
ENDIF

;-------------------------------------------------
;처음 A절정
;-------------------------------------------------
IF NOWEX:2 > 0 && CFLAG:244 == 0
	WSTR:(K++) = 「……」
	WSTR:(K++) = %CALLNAME:TARGET%は自分がアナルで絶頂したことが信じられないという顔をしている。
	CFLAG:244 = 1
ENDIF

;-------------------------------------------------
;처음 B절정
;-------------------------------------------------
IF NOWEX:3 > 0 && CFLAG:245 == 0
	WSTR:(K++) = 「……」
	WSTR:(K++) = 絶頂したばかりの%CALLNAME:TARGET%の乳首をくいくいと引っ張ると%CALLNAME:TARGET%はやめろと首を振った。
	CFLAG:245 = 1
ENDIF

;-------------------------------------------------
;절정
;-------------------------------------------------
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

IF TFLAG:29 > 0
	K_CALL:K = PALAM_MESSAGE_B			
	;강절정
	IF TFLAG:29 > 1
		IF (함락( ) > 0) && (CFLAG:246 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				IF RAND:3 == 0
					WSTR:(K++) = 「は……ぁ、……あぅ%UNICODE(0x2661)%」
				ELSEIF RAND:2 == 0
					WSTR:(K++) = 「ひっ、ぃ、ぅあ、ぁあ……!」
				ELSE
					WSTR:(K++) = %CALLNAME:TARGET%は目を剥いて声も出せずに絶頂した。
				ENDIF
				CFLAG:246= 3
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:246= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(LOCAL)	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			WSTR:(K++) = %CALLNAME:TARGET%は目を剥き声も出せずに絶頂した。
			CFLAG:246= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:246 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			WSTR:(K++) = 	
			CFLAG:246= 1	
		ENDIF		
	ENDIF
	;절정
	IF !구상유무확인(LOCAL)
		IF (함락( ) > 0) && (CFLAG:246 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				IF RAND:3 == 0
					WSTR:(K++) = 「ん……はは、%CSTR:이인칭%にされると…気持ちいい」
				ELSEIF RAND:2 == 0
					WSTR:(K++) = 「……ぁ……好き、だ」
				ELSE
					WSTR:(K++) = 絶頂した%CALLNAME:TARGET%はたまらなさそうに息を吐いた。
				ENDIF
				CFLAG:246= 3
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:246= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(LOCAL)	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			WSTR:(K++) = 「……っ、ぐ」
			CFLAG:246= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:246 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			WSTR:(K++) = %CALLNAME:TARGET%は声を出さないようにと慌てて小さく息を詰めた。
			CFLAG:246= 1	
		ENDIF		
	ENDIF
ENDIF
;-------------------------------------------------
;사정
;-------------------------------------------------
;사정하고 있다
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

IF TFLAG:10 > 0				
	IF (함락( ) > 0) && (CFLAG:247 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			;COM「フェラする」
			IF SELECTCOM == 2
				IF RAND:3 == 0
					WSTR:(K++) = 「ひぁ、ご、めっ…も、でちゃぅ…!」
				ELSEIF RAND:2 == 0
					WSTR:(K++) = 「ン、あ、ぁく、ぅ…ぁあ…ッ」
				ELSE
					WSTR:(K++) = 「あッぁ、……ぁ%UNICODE(0x2661)%」
				ENDIF
				WSTR:(K++) = %CALLNAME%はがくりと腰を反らし、%CALLNAME:MASTER%の咥内へ白濁を放った。
			;COM「胸揉みㆍ乳首攻めㆍ胸愛撫」
			ELSEIF SELECTCOM == 6 || SELECTCOM == 7 || SELECTCOM == 8
				IF RAND:3 == 0
					WSTR:(K++) = 「ひぃ、ぁあ、や…ひっぱら、ないで…ッ」
					WSTR:(K++) = 「ぅう～～%UNICODE(0x2661)%」
				ELSEIF RAND:2 == 0
					WSTR:(K++) = 「ひ、ん…ゃ、ソコっ…だけで…」
					WSTR:(K++) = 「んぐ、ぅう゛…」
				ELSE
					WSTR:(K++) = 「ふ、ン…ぁ、ふ、ぁぅ!?」
					WSTR:(K++) = 「ぁ……」
				ENDIF
				WSTR:(K++) = %CALLNAME%はぶるぶると激しく震え、乳首への刺激だけで射精した。
			;COM「正常位させる、後背位させる、騎乗位する」
			ELSEIF SELECTCOM == 92 || SELECTCOM == 93 || SELECTCOM == 94
				IF RAND:4 == 0
					IF TEQUIP:주도권 == 1
						WSTR:(K++) = 「ふ、はぁ…でる、っ」
						WSTR:(K++) = 「%CSTR:이인칭%、%CSTR:이인칭%ッ…すき、好きだよ…ッ」
					ELSE
						WSTR:(K++) = 「この、まま…ナカに、出していいの…?」
						WSTR:(K++) = 「わかっ、た…%UNICODE(0x2661)%」
						WSTR:(K++) = 「%CSTR:이인칭%、だいすき…ッ」
					ENDIF
				ELSEIF RAND:3 == 0
					IF TEQUIP:주도권 == 1
						WSTR:(K++) = 「あ…%CSTR:이인칭%も、イきそうなの…?」
						WSTR:(K++) = 「%CSTR:일인칭%も、でちゃう…っ」
					ELSE
						WSTR:(K++) = 「もっと、もっと、%CSTR:일인칭%ので犯したい…%UNICODE(0x2661)%」
					ENDIF
				ELSEIF RAND:2 == 0
					WSTR:(K++) = 「よかった、%CSTR:일인칭%のでお腹いっぱいだね%UNICODE(0x2661)%」
				ELSE
					WSTR:(K++) = 「んぅ、ん、んん…ッ%UNICODE(0x2661)%」
				ENDIF
				WSTR:(K++) = %CALLNAME%は幸せそうに愛おしい%CALLNAME:MASTER%の膣内へ吐精した。
			;그 이외(恋慕ㆍ親愛汎用)
			ELSE
				IF RAND:3 == 0
					WSTR:(K++) = 「ぁ、あ……～～っ%UNICODE(0x2661)%」
				ELSEIF RAND:2 == 0
					WSTR:(K++) = %CALLNAME:TARGET%は泣いているかのような声を上げびくびくと震えた。
				ELSE
					WSTR:(K++) = 「……ふ、ゃ…ぅう…あ、ぁ……」
				ENDIF
			ENDIF
			CFLAG:247= 4
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
			CFLAG:247= 3
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
			CFLAG:247= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)	
			WSTR:(K++) = 「ぁ、…～～っ%UNICODE(0x2661)%」
			CFLAG:247= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:247 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) = 	
		CFLAG:247= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:247 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) =
		CFLAG:247= 1	
	ENDIF		
ENDIF

;-------------------------------------------------
;분유
;-------------------------------------------------
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

;분유하고 있다
IF TFLAG:11 > 0			
	IF (함락( ) > 0) && (CFLAG:248 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
			CFLAG:248= 4
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
			CFLAG:248= 3
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
			CFLAG:248= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)	
			WSTR:(K++) = 
			CFLAG:248= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:248 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) = 	
		CFLAG:248= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:248 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) =
		CFLAG:248= 1	
	ENDIF		
ENDIF

;-------------------------------------------------
;방뇨
;-------------------------------------------------
;방뇨하고 있다
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

IF TFLAG:29 > 0 && (TEQUIP:27 || TALENT:57)			
	IF (함락( ) > 0) && (CFLAG:249 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			IF RAND:3 == 0
				WSTR:(K++) = 「……ぅ、ぇ…？」
				WSTR:(K++) = お漏らししてしまったことに気づいた%CALLNAME:TARGET%はさっと顔を赤くした。
			ELSEIF RAND:2 == 0
				WSTR:(K++) = 「んぎ、…ッ…や、もれ、ちゃ、っんぁ…！」
				WSTR:(K++) = %CALLNAME:TARGET%は限界まで尿意を我慢していたものの耐え切れずに放尿してしまった。
			ELSE
				WSTR:(K++) = 「……ッ」
				WSTR:(K++) = %CALLNAME:TARGET%は打ちひしがれるような羞恥で半泣きになっている。
			ENDIF
			CFLAG:249= 4
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
			CFLAG:249= 3
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
			CFLAG:249= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)	
			WSTR:(K++) = 「……ッ」
			WSTR:(K++) = %CALLNAME:TARGET%は打ちひしがれるような羞恥で半泣きになっている。
			CFLAG:249= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:249 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) = 「……っ、」
		WSTR:(K++) = %CALLNAME:TARGET%はお漏らしして泣いてしまった。
		CFLAG:249= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:249 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) =
		CFLAG:249= 1	
	ENDIF		
ENDIF

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_PALAMCNG_B_ZZZ
	CATCH
		CALL PALAM_MESSAGE_B
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_C 관련（X1을 캐릭터번호로 치환）
;조교 후의 변화를 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_C_K4199
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_C
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K4199
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K4199
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교 후의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;처녀상실(처녀만)
;-------------------------------------------------
CALL 구상변수초기화

IF TFLAG:3 > 0
	K_CALL:K = PALAM_MESSAGE_C
	;연모나 친애이면서 주인의 섹스에 의한 처녀상실
	IF TFLAG:20
		WSTR:(K++) =
	;그 이외의 때에 처녀상실
	ELSE			
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
		ENDIF	
		;굴복각인Lv3
		IF MARK:2 == 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
ENDIF
;-------------------------------------------------
;아무것도안한다 후
;-------------------------------------------------
IF SELECTCOM == 21
	K_CALL:K = PALAM_MESSAGE_C
	;欲情LV5以上
	IF !구상유무확인(0) && PALAM:5 >= PALAMLV:5
		;함락완료
		IF 함락()
			IF RAND:3 == 0
				WSTR:(K++) = 「……ん～、ん%UNICODE(0x2661)%」
				WSTR:(K++) = %CALLNAME:TARGET%はじゃれつく猫のように纏わりついてきた。
			ELSEIF RAND:2 == 0
				WSTR:(K++) = 極度に興奮している%CALLNAME:TARGET%は%CALLNAME:MASTER%のことをうっとりと見つめている。
			ELSE
				WSTR:(K++) = 「ぅ、ん……？」
				WSTR:(K++) = %CALLNAME:TARGET%はとろんと欲情しきった表情で所在なさげに手を彷徨わせている。
			ENDIF
		ENDIF
		;그 이외
		IF !구상유무확인(0)
			WSTR:(K++) =
		ENDIF
	ENDIF	
	;欲情LV3以上 + 媚薬
	IF !구상유무확인(0) && PALAM:5 >= PALAMLV:3 && TEQUIP:26
		;함락완료
		IF 함락()
			WSTR:(K++) =
		ENDIF
		;그 이외
		IF !구상유무확인(0)
			WSTR:(K++) =
		ENDIF
	ENDIF	
	;欲情LV3以上
	IF !구상유무확인(0) && PALAM:5 >= PALAMLV:3
		;함락완료
		IF 함락()
			WSTR:(K++) =
		ENDIF
		;그 이외
		IF !구상유무확인(0)
			WSTR:(K++) =
		ENDIF
	ENDIF	
ENDIF

;-------------------------------------------------
;청소펠라
;-------------------------------------------------
IF TFLAG:8 > 0
	;ABL:정액중독　ABL:ASSI:정액중독
	K_CALL:K = PALAM_MESSAGE_C
	;더블펠라 후 조수와 정액키스　조건:조교대상이나 조수가 정액중독 보유
	IF TFLAG:8 >= 2
		;함락완료
		IF 함락()
			WSTR:(K++) =
		ENDIF
		;순종Lv3
		IF ABL:0 >= 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
	;더블펠라로 조수와 청소펠라
	IF TFLAG:8 >= 2 && !구상유무확인(0)
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
		ENDIF	
		;순종Lv3
		IF ABL:0 >= 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
	;두개펠라 후 청소펠라
	IF TFLAG:0 && TFLAG:6 && !구상유무확인(0)
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
		ENDIF	
		;순종Lv3
		IF ABL:0 >= 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
	;펠라 후 청소펠라
	IF !구상유무확인(0)
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
		ENDIF	
		;순종Lv3
		IF ABL:0 >= 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
ENDIF

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_COM_ZZZ
	CATCH
		CALL PALAM_MESSAGE_C
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_D 관련（X1을 캐릭터번호로 치환）
;파라미터 변화를 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_D_K4199
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_D
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K4199
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K4199
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;파라미터 변화시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
;-------------------------------------------------
;처음 윤활이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:4 + CUP:4
IF P > PALAMLV:4 && CFLAG:250 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;로션을 사용한 경우
	IF SELECTCOM == 180
		WSTR:(K++) =
	;그 이외
	ELSE
		WSTR:(K++) =
	ENDIF
	CFLAG:250 = 1
ENDIF

;-------------------------------------------------
;처음 욕정이 LV2를 넘었다
;-------------------------------------------------
P = PALAM:6 + CUP:6
IF P > PALAMLV:3 && CFLAG:251 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;미약을 사용한 경우
	IF SELECTCOM == 181
		WSTR:(K++) =
	;그 이외
	ELSE
		WSTR:(K++) =
	ENDIF
	CFLAG:251 = 1
ENDIF

;-------------------------------------------------
;처음 굴복이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:7 + CUP:7
IF P > PALAMLV:3 && CFLAG:252 == 0
	K_CALL:K = PALAM_MESSAGE_D
	WSTR:(K++) =
	CFLAG:252 = 1
ENDIF

;-------------------------------------------------
;처음 복종이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:5 + CUP:5
IF P > PALAMLV:3 && CFLAG:253 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;함락완료
	IF 함락( ) > 0
		WSTR:(K++) =
	;그 이외
	ELSE
		WSTR:(K++) =
	ENDIF
	CFLAG:253 = 1
ENDIF

;-------------------------------------------------
;처음 공포가 LV3을 넘었다
;-------------------------------------------------
P = PALAM:11 + CUP:11
IF P > PALAMLV:3 && CFLAG:254 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;함락완료
	IF 함락( ) > 0
		WSTR:(K++) =
	;그 이외
	ELSE
		WSTR:(K++) =
	ENDIF
	CFLAG:254 = 1
ENDIF

CALL 구상변수표시
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_PALAMCNG_D_ZZZ
	CATCH
		CALL PALAM_MESSAGE_D
	ENDCATCH
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_MARKCNG 관련（X1을 캐릭터번호로 치환）
;각인 변동을 트리거로 한 구상
;각인 변동 후에 구상을 발동합니다
;-------------------------------------------------
@KOJO_MESSAGE_MARKCNG_K4199
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL MARK_MESSAGE
	RETURN 0
ENDIF

CALL 구상변수초기화
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_MARKCNG_PY1_K4199
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_MARKCNG_PY1_K4199
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;고통각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:22 == 3
	K_CALL:K = MARK_MESSAGE_A	
	;연모, 친애	
	IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
		WSTR:(K++) = 「……」
		WSTR:(K++) = %CALLNAME:TARGET%は絶望した顔で己の身体を抱き締めている。
	;음란, 창부	
	ELSEIF 함락( ) > 0 && RESULTS == "음란"	
		WSTR:(K++) = 
	;복종, 예속	
	ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
		WSTR:(K++) = 
	ENDIF	
	;공통
	IF !구상유무확인(LOCAL)
		WSTR:(K++) = 
	ENDIF		
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_A
ENDIF
;-------------------------------------------------
;쾌락각인Lv3
;-------------------------------------------------

LOCAL = K
IF TFLAG:23 == 3
	K_CALL:K = MARK_MESSAGE_P
	;연모, 친애	
	IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
		WSTR:(K++) = 「は、ぁ……%UNICODE(0x2661)%」
		WSTR:(K++) = %CALLNAME:MASTER%には一瞬だけ%CALLNAME:TARGET%の瞳に何かが映ったように見えた。
	;복종, 예속	
	ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
		WSTR:(K++) = 
	ENDIF	
	;공통
	IF !구상유무확인(LOCAL)
		WSTR:(K++) = 
	ENDIF		
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_P
ENDIF

;-------------------------------------------------
;굴복각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:24 == 13 || TFLAG:24 == 23 || TFLAG:24 == 33
	K_CALL:K = MARK_MESSAGE_S
	;쾌락에 의한 굴복
	IF TFLAG:24 == 13
		WSTR:(K++) = (逆らう気なんて元からなかった…けど、これは、もう……)
	;공포와 고통에 의한 굴복
	ELSEIF TFLAG:24 == 23
		WSTR:(K++) = (いたい。こわい。くるしい。こんなのは……もう嫌だ)
	;굴복과 복종에 의한 굴복
	ELSEIF TFLAG:24 == 33
		WSTR:(K++) = (たぶん%CSTR:일인칭%…もう%CSTR:이인칭%に逆えなくなったな…)
	ENDIF
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_S
ENDIF

;-------------------------------------------------
;반발각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:21 == 3
	K_CALL:K = MARK_MESSAGE_R
	;함락완료
	IF 함락( ) > 0
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)
			WSTR:(K++) = 
		ENDIF	
	ENDIF
	;그 이외
	IF !구상유무확인(LOCAL)
		WSTR:(K++) = 
	ENDIF
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_R
ENDIF

CALL 구상변수표시
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_MARKCNG_ZZZ
	CATCH
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;이벤트구상 모음（X1을 캐릭터번호로 치환한다） CFLAG 261～299를 사용
;-------------------------------------------------
@KOJO_MESSAGE_EVENT_K4199

;--------------------------------------------------
;특정 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_EVENT_PY1_K4199
;	RETURN 0
;ENDIF


CALL 구상변수초기화

SELECTCASE TFLAG:13
;　　1:아침펠라
;　　2:아침파이즈리
;　　3:아침기승위
;　　4:조교 후 레즈플레이
;　　5:조교 후 섹스
;　　6:강정신약의 반동
;　　7:조교 후 착유
;　　8:조교 후 자위
;　　9:조교 후 요바이
;　　10:황색사탕 사용시
;　　11:임신 발각시
;　　12:출산시
;　　13:육아실에서의 이벤트구상
;　　14:자식 독립시
;　　15:정식포장마차 실행시
;　　16:창관에서의 아르바이트시
;　　17:렌탈 실행시
;　　18:매각시
;　　19:비디오 매각시
;　　20:콘서트 실행시
;　　21:난교라이브 실행시
;　　22:공중변소 플레이 실행시
;　　23:비디오 보유시
;　　24:아침기승위A
;　　25:아침키스
;　　26:아침식사
;　　27:일어나자 곁에서 자고 있다
;　　28:아침풋잡
;　　29:아침에 갓 짠 밀크
;　　30:잠자다 실례
;　　31:요바이 C/A
;　　32:요바이 V/C
;　　33:요바이 A/C
;　　34:요바이 C/C
;　　35:밤 자위
;　　36:같이잔다
;　　37:起きたら隣で寝てる
;　　38:朝の足コキ
;　　39:朝の搾りたてミルク
;　　40:아내ㆍ남편 취득
;　　41:음마 취득
;　　42:노리개 취득
;　　43:スト劇場
;　　44:갈아입기ㆍ가슴을 드러낸다
;　　45:애(愛)계 ＮＴＲ이벤트
;　　46:음(淫)계 ＮＴＲ이벤트
;　　47:예(隷)계 ＮＴＲ이벤트
;　　48:애(愛)계 재함락 이벤트
;　　49:음(淫)계 재함락 이벤트
;　　50:예(隷)계 재함락 이벤트
;　　51:ＮＴＲ요바이ㆍ난교파티
;　　52:ＮＴＲ요바이ㆍ공원
;　　53:ＮＴＲ요바이ㆍ변기
;　　54:ＮＴＲ요바이ㆍ밤의 거리
;　　55:ＮＴＲ요바이ㆍＮＴＲ전 밀회
;　　56:ＮＴＲ요바이ㆍＮＴＲ후 첫 밀회
;　　57:ＮＴＲ요바이ㆍ데이트
;　　58:ＮＴＲ요바이ㆍ본방
;　　59:ＮＴＲ요바이ㆍ자위서로과시
;　　60:ＮＴＲ요바이ㆍ애널섹스
;　　61:ＮＴＲ요바이ㆍ야외플레이
;　　62:ＮＴＲ요바이ㆍ야외조교
;　　63:창관에서 실제로 일했을 때
;　　65:교회에서 기도
;　　66:교회에서 입봉사
;　　67:교회에서 성봉사
;　　70:陰唇ㆍ胸同時露出(調教対象)
;　　71:陰唇ㆍ胸同時露出(主人)
;　　72:陰唇ㆍ胸同時露出(助手)
;　　73:陰唇出(調教対象)
;　　74:음순노출(主人)
;　　75:음순노출(助手)
;　　76:가슴露出(調教対象)
;　　77:가슴露出(主人)
;　　78:가슴露出(助手)
;　　79:던전내 성욕처리：동료마와 섹스(당하는 측)
;　　80:던전내 성욕처리：동료마에게 봉사시킨다(봉사측)
;　　81:던전내 성욕처리：그 장소에서 자위
;　　82:던전내 성욕처리：그늘에서 자위
;　　83:던전내 성욕처리：동료마와 섹스(하는 측)
;　　84:던전내 성욕처리：동료마에게 봉사시킨다(봉사시키는 측)
;　　85:던전내 성욕처리：윤간당한다
;　　90:도장 개시
;　　91:도장 종료
;　　92:맹우
;　　93:탄광
		;-------------------------------------------------
		;아침펠라
		;-------------------------------------------------
	CASE 1
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:261 < 1 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					IF RAND:2 == 0
						WSTR:(K++) = 「……ん、む」
						WSTR:(K++) = 「これも%CSTR:이인칭%のお世話の一環だから、許して」
					ELSE
						WSTR:(K++) = 「…んちゅ、は、ぷっ……あ、起きた」
						WSTR:(K++) = 「…おはよう」
					ENDIF
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 「は、ぷっ……起きた？」
					WSTR:(K++) = 「おはよう」
				ENDIF	
				CFLAG:261 = 1
			ENDIF

		;-------------------------------------------------
		;아침파이즈리
		;-------------------------------------------------
	CASE 2
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:262 < 1 || FLAG:7 == 2)
				;母乳体質
				IF TALENT:149						
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;모유체질이 아니다
				IF !구상유무확인(0)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) =
					ENDIF	
				ENDIF
				CFLAG:262 = 1
			ENDIF

		;-------------------------------------------------
		;아침기승위
		;-------------------------------------------------
	CASE 3
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침기승위A
		;-------------------------------------------------
	CASE 24
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침키스 연모, 친애 전제
		;-------------------------------------------------
	CASE 25
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				WSTR:(K++) = 「……！」
				WSTR:(K++) = 「いつもこの時間には起きてるんじゃないかと思って。おはよう」
				WSTR:(K++) = %CALLNAME:TARGET%の笑顔がまぶしい。
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침식사
		;-------------------------------------------------
	CASE 26
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;노래 부르면서
				IF (TALENT:25 || TALENT:28) && ABL:14 >= 2						
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 「～♪　……%CSTR:이인칭%、おはよう」
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;노래 부르지 않음
				IF !구상유무확인(0)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 「朝ご飯なら出来てる。あ、また…食材買ってくるね、何がいい？」
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) =
					ENDIF	
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;일어나자 곁에서 자고 있다 연모, 친애 전제
		;-------------------------------------------------
	CASE 27
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				WSTR:(K++) = 「……ぐぅ」
				WSTR:(K++) = %CALLNAME:TARGET%はいくら呼びかけても全く起きない。
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침풋잡
		;-------------------------------------------------
	CASE 28
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 「……」
					WSTR:(K++) = %CALLNAME:TARGET%はなんとなく楽しそうだ。
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;아침에 갓 짠 밀크
		;-------------------------------------------------
	CASE 29
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;잠자다 실례
		;-------------------------------------------------
	CASE 30
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 레즈플레이
		;-------------------------------------------------
	CASE 4
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:264 == 0
				WSTR:(K++) =
				CFLAG:264 = 1
			;그 이외
			ELSEIF CFLAG:264 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:264 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 섹스
		;-------------------------------------------------
	CASE 5
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:265 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 「もう少しだけ、%CSTR:이인칭%をぐちゃぐちゃにしたいんだ……」
					WSTR:(K++) = 「だからごめん、付き合って%UNICODE(0x2661)%」
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:265 = 1
			ENDIF

		;-------------------------------------------------
		;강정신약의 반동
		;-------------------------------------------------
	CASE 6
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:266 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:266 = 2
			ENDIF
			IF (CFLAG:266 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:266 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 착유
		;-------------------------------------------------
	CASE 7
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:267 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:267 = 2
			ENDIF
			IF (CFLAG:267 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:267 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 자위
		;-------------------------------------------------
	CASE 8
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:268 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:268 = 2
			ENDIF
			IF (CFLAG:268 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:268 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인C 상대V）
		;-------------------------------------------------
	CASE 9
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인C 상대A）
		;-------------------------------------------------
	CASE 31
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인V 상대C）
		;-------------------------------------------------
	CASE 32
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 「今日は%CSTR:일인칭%がいっぱいするから……」
					WSTR:(K++) = 「%CSTR:이인칭%はたくさん、気持ちよくなって」
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 「……%CSTR:일인칭%に任せてくれていいから」
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인A 상대C）
		;-------------------------------------------------
	CASE 33
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 「……別に、いいよね？」
					WSTR:(K++) = %CALLNAME:TARGET%は満足そうだ。
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（양쪽C（스마타）ㆍ양쪽C（조개맞대기））
		;-------------------------------------------------
	CASE 34
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;밤 자위
		;-------------------------------------------------
	CASE 35
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 「…ッ%UNICODE(0x2661)%　……%UNICODE(0x2661)%%UNICODE(0x2661)%」
					WSTR:(K++) = %CALLNAME:TARGET%は声を潜めて自慰に没頭していた。
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;같이잔다 연모, 친애
		;-------------------------------------------------
	CASE 36
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:269 < 1 || FLAG:7 == 2
				WSTR:(K++) = 「……おやすみ」
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;황색사탕 사용시
		;-------------------------------------------------
	CASE 10
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:270 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:270 = 2
			ENDIF
			IF (CFLAG:270 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:270 = 1
			ENDIF

		;-------------------------------------------------
		;임신 발각시
		;-------------------------------------------------
	CASE 11
			SIF CFLAG:271 >= 1 && FLAG:7 != 2
				GOTO 表示
			K_CALL:K = EVENT_MESSAGE
			;임신대상을 TARGET에 격납하고, 부친은 노예는 V, 조수는 W로 한다
			;붕괴되어 버린 경우
			IF TALENT:9 == 1
				WSTR:(K++) =
			;부친이 주인이고 모친이 연모나 친애 보유
			ELSEIF (TALENT:3 || TALENT:6) && CFLAG:102 == 1
				WSTR:(K++) =
			;부친이 노예나 조수고 모친이 음란이나 창부 보유
			ELSEIF (TALENT:4 || TALENT:7) && (CFLAG:102 == 2 || CFLAG:102 == 3)
				;부친(조수）과의 상성이 125보다 큰 경우
				IF CFLAG:102 == 2 && GET_RELATION(TARGET, W) > 125
					WSTR:(K++) =
			 	;부친(노예）과의 상성이 125보다 큰 경우
				ELSEIF CFLAG:102 == 3 && GET_RELATION(TARGET, V) > 125
					WSTR:(K++) =
				ELSE
					WSTR:(K++) =
				ENDIF
			;복종이나 예속 보유
			ELSEIF (TALENT:5 || TALENT:8)
				WSTR:(K++) =
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:271 = 1

		;-------------------------------------------------
		;출산시
		;-------------------------------------------------
	CASE 12
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:272 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:272 = 2
			ENDIF
			IF (CFLAG:272 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:272 = 1
			ENDIF

		;-------------------------------------------------
		;육아실
		;-------------------------------------------------
	CASE 13
			K_CALL:K = EVENT_MESSAGE
			;출산전
			IF TALENT:153
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 2 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
					CFLAG:273 = 2
				ENDIF
				IF (CFLAG:273 < 1 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) =
					CFLAG:273 = 1
				ENDIF
			;출산후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 4 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
					CFLAG:273 = 4
				ENDIF
				IF (CFLAG:273 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) =
					CFLAG:273 = 3
				ENDIF
			ENDIF

		;-------------------------------------------------
		;자식 독립시
		;-------------------------------------------------
	CASE 14
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:274 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:274 = 2
			ENDIF
			IF (CFLAG:274 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:274 = 1
			ENDIF
		;-------------------------------------------------
		;정식포장마차시
		;-------------------------------------------------
	CASE 15
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:275 == 0
				WSTR:(K++) =
				CFLAG:275 = 1
			;그 이외
			ELSEIF CFLAG:275 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:275 = 1
			ENDIF

		;-------------------------------------------------
		;창관에서 아르바이트ㆍ설정시
		;-------------------------------------------------
	CASE 16
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:276 == 0
				WSTR:(K++) =
				CFLAG:276 = 1
			;그 이외
			ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:276 = 1
			ENDIF
		;-------------------------------------------------
		;창관에서 아르바이트ㆍ실행시
		;-------------------------------------------------
	CASE 63
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:606 == 0
				WSTR:(K++) =
				CFLAG:606 = 1
			;그 이외
			ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:606 = 1
			ENDIF


		;-------------------------------------------------
		;렌탈시
		;-------------------------------------------------
	CASE 17
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:277 == 0
				WSTR:(K++) =
				CFLAG:277 = 1
			;그 이외
			ELSEIF CFLAG:277 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:277 = 1
			ENDIF

		;-------------------------------------------------
		;매각시
		;-------------------------------------------------
	CASE 18
			K_CALL:K = EVENT_MESSAGE
			;반발각인Lv3
			IF MARK:3 == 3
				WSTR:(K++) = 
			ENDIF
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && MARK:3 < 3 && !구상유무확인(0)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				WSTR:(K++) =
			ENDIF

		;-------------------------------------------------
		;비디오 매각시
		;-------------------------------------------------
	CASE 19
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:278 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:278 = 2
			ENDIF
			IF (CFLAG:278 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:278 = 1
			ENDIF

		;-------------------------------------------------
		;콘서트시
		;-------------------------------------------------
	CASE 20
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:279 == 0
				WSTR:(K++) =
				CFLAG:279 = 1
			;그 이외
			ELSEIF CFLAG:279 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:279 = 1
			ENDIF

		;-------------------------------------------------
		;난교라이브시
		;-------------------------------------------------
	CASE 21
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:280 == 0
				WSTR:(K++) =
				CFLAG:280 = 1
			;그 이외
			ELSEIF CFLAG:280 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:280 = 1
			ENDIF

		;-------------------------------------------------
		;공중변소 플레이시
		;-------------------------------------------------
	CASE 22
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:281 == 0
				WSTR:(K++) =
				CFLAG:281 = 1
			;그 이외
			ELSEIF CFLAG:281 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:281 = 1
			ENDIF

		;-------------------------------------------------
		;비디오 보유시
		;-------------------------------------------------
	CASE 23
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:282 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					IF RAND:3 == 0
						WSTR:(K++) = 
					ELSEIF RAND:2 == 0
						WSTR:(K++) = 
					ELSE
						WSTR:(K++) = 
					ENDIF
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:282 = 2
			ENDIF
			IF (CFLAG:282 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:282 = 1
			ENDIF

		;-------------------------------------------------
		;기억소거약 사용시
		;-------------------------------------------------
	CASE 30
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:283 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:283 = 2
			ENDIF
			IF (CFLAG:283 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:283 = 1
			ENDIF

		;-------------------------------------------------
		;;[아내][남편]취득시 친애 전제
		;-------------------------------------------------
	CASE 40
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:283 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:283 = 1
			ENDIF
		;-------------------------------------------------
		;;[음마]취득시 창부 전제
		;-------------------------------------------------
	CASE 41
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:284 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:284 = 1
			ENDIF
		;-------------------------------------------------
		;;[전속노예]취득시 예속 전제
		;-------------------------------------------------
	CASE 42
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:285 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:285 = 1
			ENDIF
		;-------------------------------------------------
		;;오픈/클로즈ㆍ오프ㆍ가슴
		;-------------------------------------------------
	CASE 44
			K_CALL:K = EVENT_MESSAGE
			IF TEQUIP:가슴앞열림
				;가슴을 드러낸다
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;그 외
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:285 = 1
			ELSE
				;가슴을 집어넣는다
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;그 외
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:287 = 1
			ENDIF
		;-------------------------------------------------
		;;[애ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 45
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:288 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;함락완료 공통	
				IF !구상유무확인(0)	
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) =
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) =
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) =
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:애ＮＴＲ진행도 == 0
					WSTR:(K++) =
				;2번째（리치）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 1
					WSTR:(K++) =
				;3번째（함락）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 2
					WSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:288 = 1
		ENDIF
		;-------------------------------------------------
		;;[음ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 46
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:289 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				ENDIF
				;함락완료 공통	
				IF !구상유무확인(0)	
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						WSTR:(K++) =
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						WSTR:(K++) =
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						WSTR:(K++) =
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:음ＮＴＲ진행도 == 0
					WSTR:(K++) =
				;2번째（리치）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 1
					WSTR:(K++) =
				;3번째（함락）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 2
					WSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:289 = 1
		ENDIF
		;-------------------------------------------------
		;;[예ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 47
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:290 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				ENDIF
				;함락완료 공통	
				IF !구상유무확인(0)	
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) =
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) =
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) =
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:예ＮＴＲ진행도 == 0
					WSTR:(K++) =
				;2번째（리치）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 1
					WSTR:(K++) =
				;3번째（함락）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 2
					WSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:290 = 1
		ENDIF
		;-------------------------------------------------
		;;애(愛)계 재함락시
		;;주인에게 연모계 함락되었거나, 한번 애(愛)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 48
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
		
		IF (CFLAG:291 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				WSTR:(K++) =
			;그 외 캐릭터ㆍＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:291 = 1
		ENDIF
		;-------------------------------------------------
		;;음(淫)계 재함락시
		;;주인에게 음란계 함락되었거나, 한번 음(淫)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 49
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
					
		IF (CFLAG:292 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				WSTR:(K++) =
			;그 외 캐릭터ㆍＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:292 = 1
		ENDIF
		;-------------------------------------------------
		;;예(隷)계 재함락시
		;;주인에게 복종계 함락되었거나, 한번 예(隷)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 50
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
					
		IF (CFLAG:293 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				WSTR:(K++) =
			;그 외 캐릭터ㆍＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:293 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍ난교파티
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 51
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:294 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:294 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍ공원
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 52
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:295 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:295 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍ변기
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 53
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍ밤의 거리
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 54
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍＮＴＲ전 밀회
		;;ＮＴＲ상태가 되기 전에 밀회할 때
		;-------------------------------------------------
	CASE 55
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:297 < 1 || FLAG:7 == 2)				
			IF CFLAG:함락캐릭터 != 0
			;	;주인 이외에 함락되어 있는 경우
				WSTR:(K++) = 
			ELSEIF (TALENT:3 || TALENT:6)	
			;	;연모 루트
				WSTR:(K++) = 
			ELSEIF (TALENT:4 || TALENT:7)	
			;	;음란 루트
				WSTR:(K++) = 
			ELSEIF (TALENT:5 || TALENT:8)	
			;	;복종 루트
				WSTR:(K++) = 
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:297 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍＮＴＲ후 첫 밀회
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)가 된 직후 하루 종료 후, 한번만 행한다
		;다른 캐릭터에 함락될 때마다 일어난다. 같은 캐릭터에 다시 함락되는 경우는 일어나지 않는다
		;-------------------------------------------------
	CASE 56
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:298 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:298 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍ데이트
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;-------------------------------------------------
	CASE 57
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:299 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:299 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍ본방
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;여성이라면 삽입당하는 쪽, 남성이라면 삽입하는 쪽으로 고정
		;-------------------------------------------------
	CASE 58
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:601 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:601 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍ자위서로과시
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;애ＮＴＲ상태인 경우, 노출증 3이나 자위중독 1 중 하나를 충족시키는 것이 조건이 됩니다.
		;-------------------------------------------------
	CASE 59
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍ애널
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;CFLAG:애널횟수가 1 이상이면 삽입당하고, CFLAG:삽입횟수가 1 이상면 삽입하고 있습니다.
		;양 플래그 모두 서있는 경우, 지문에서는 삽입당하는 쪽이 우선됩니다.
		;-------------------------------------------------
	CASE 60
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍ야외플레이
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;애ＮＴＲ의 경우, 노출증이 4 이상이 아니라면 발생하지 않습니다
		;-------------------------------------------------
	CASE 61
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이ㆍ야외조교
		;;예ＮＴＲ상태(TALENT:ＮＴＲ == 3, 6)에서 CFLAG:현ＮＴＲ진행도가 6이상인 경우 하루 종료시에 발생.
		;-------------------------------------------------
	CASE 62
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			;예ＮＴＲ
			WSTR:(K++) =
			CFLAG:605 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마와 섹스한다
		;당하는 쪽
		;PLAYER에 실행자의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 79
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마에게 봉사시킨다
		;봉사하는 쪽
		;PLAYER에 실행자의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 80
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：그 장소에서 자위
		;봉사하는 쪽
		;-------------------------------------------------
	CASE 81
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：그늘에서 자위
		;봉사하는 쪽
		;-------------------------------------------------
	CASE 82
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:605 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마와 섹스한다
		;하는 쪽
		;PLAYER에 상대의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 83
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:606 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:606 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마에게 봉사시킨다
		;봉사받는 쪽
		;PLAYER에 봉사하는 쪽의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 84
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:607 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:607 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：윤간이벤트
		;봉사하는 쪽
		;PLAYER에 실행자 = 윤간하는 악마의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 85
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:608 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:608 = 1
		ENDIF
		

		;-------------------------------------------------
		;교회에서 기도
		;-------------------------------------------------
	CASE 65
			K_CALL:K = EVENT_MESSAGE
			;교회의 종류에 따라 나눈다
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:609 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:609 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:609 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:610 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:610 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:610 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:611 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:611 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:611 = 1
					ENDIF
			ENDSELECT
		;-------------------------------------------------
		;교회에서 입봉사
		;-------------------------------------------------
	CASE 66
			K_CALL:K = EVENT_MESSAGE
			;교회의 종류에 따라 나눈다
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:612 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:612 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:612 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:613 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:613 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:613 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:614 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:614 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:614 = 1
					ENDIF
			ENDSELECT
		;-------------------------------------------------
		;교회에서 성봉사
		;-------------------------------------------------
	CASE 67
			K_CALL:K = EVENT_MESSAGE
			;교회의 종류에 따라 나눈다
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:615 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:615 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:615 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:616 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:616 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:616 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:617 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:617 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:617 = 1
					ENDIF
			ENDSELECT
	CASEELSE
ENDSELECT

$表示
CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_EVENT_ZZZ
	CATCH
		CALL EVENT_MESSAGE
	ENDCATCH
ENDIF
;-------------------------------------------------
;투기장 패배시 구상 모음（X1을 캐릭터번호로 치환한다） 기본적으로 보면 게임오버기 때문에 플래그를 사용하지 않도록
;-------------------------------------------------
@KOJO_MESSAGE_LOSE_COLOSSEUM_K4199
CALL 구상변수초기화
SELECTCASE TFLAG:투기장패배
		;-------------------------------------------------
		;오니＆덩굴
		;-------------------------------------------------
	CASE 1
			K_CALL:K = MESSAGE_LOSE_COLOSSEUM
			;연모, 친애
			IF TALENT:3 || TALENT:6
				WSTR:(K++) = 
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
				WSTR:(K++) = 
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
				WSTR:(K++) = 
			ENDIF
			;그 외
			IF !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
ENDSELECT
CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_LOSE_COLOSSEUM_ZZZ
	CATCH
		CALL MESSAGE_LOSE_COLOSSEUM
	ENDCATCH
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_ENDING 관련(X1, X2를 캐릭터 번호로 치환한다)
;-------------------------------------------------
;-------------------------------------------------
;엔딩구상의 유무를 판별하는 함수
;기술한 엔딩의 종류에 따라 주석처리를 해제
;-------------------------------------------------
@ENDING_CHECK_K4199
SELECTCASE TSTR:END종별
	;CASE "개별END"
	;	SIF TALENT:아내 || TALENT:남편
	;		RETURN 1
	;	SIF TALENT:음마
	;		RETURN 1
	;	SIF TALENT:노리개
	;		RETURN 1
	;CASE "조교사경영END"
	;	RETURN 1
	;CASE "희대의조교사END"
	;	RETURN 1
	;CASE "식당경영END"
	;	RETURN 1
	;CASE "창관경영END"
	;	RETURN 1
	;CASE "아이돌END"
	;	RETURN 1
ENDSELECT
RETURN 0

;-------------------------------------------------
;엔딩 본문
;-------------------------------------------------
@KOJO_MESSAGE_ENDING_K4199
;--------------------------------------------------
CALL 구상변수초기화
SELECTCASE TSTR:END종별
	;CASE "개별END"
		;연모 1체 엔딩
		;IF TALENT:아내 || TALENT:남편 && CFLAG:21 == 1
			;DRAWLINE
			;WSTR:(K++) = …………
			;WSTR:(K++) = ………
			;WSTR:(K++) = ……
			;WSTR:(K++) = 

			;WSTR:(K++) = 
			;WSTR:(K++) = 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;음란 1체 엔딩
		;ELSEIF TALENT:음마 && CFLAG:21 == 1
			;DRAWLINE
			;WSTR:(K++) = …………
			;WSTR:(K++) = ………
			;WSTR:(K++) = ……
			;WSTR:(K++) = 

			;WSTR:(K++) = 
			;WSTR:(K++) = 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;복종 1체 엔딩
		;ELSEIF TALENT:노리개 && CFLAG:21 == 1
			;DRAWLINE
			;WSTR:(K++) = …………
			;WSTR:(K++) = ………
			;WSTR:(K++) = ……
			;WSTR:(K++) = 

			;WSTR:(K++) = 
			;WSTR:(K++) = 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;ENDIF
	;CASE "조교사경영END"
	;CASE "희대의조교사END"
	;CASE "식당경영END"
	;CASE "창관경영END"
	;CASE "아이돌END"
ENDSELECT
CALL 구상변수표시
;개별END의 경우 여기서 RETURN 1하면, 그 후 게임은 계속되지 않고 다음 주차로 넘어갈지 게임종료할지 선택하게 됩니다
RETURN 1


@ENEMY_BATTLE_EVENT_K4199, ARG, ARG:1, ARG:2
IF TFLAG:전투이벤트 == 1
;적에서의 데빌ＣＯ－ＯＰ 발동시의 대사
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 2
;적에서의 데밀ＣＯ－ＯＰ 참가시의 대사
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

@BATTLE_EVENT_K4199, ARG, ARG:1, ARG:2
;■대사의 행 수를 늘리고 싶을 때는
;			LOCALS:10 = 1행째
;			LOCALS:11 = 2행째
;			LOCALS:12 = 3행째
;			LOCALS:13 = 4행째
;			LOCALS:14 = 5행째
;로 늘려나간다
SIF ARG < 0 && NO:ARG != 4199
	RETURN 0
VARSET LOCALS

SELECTCASE TFLAG:전투이벤트
	;COOP발동 ARG는 발동자
	CASE 1
		LOCALS:10 = ここからが本番…ってね
	;COOP참가
	CASE 2
		;주인이 COOP발동
		IF FLAG:행동순1 != -1 && FLAG:행동순1 == MASTER
			LOCALS:10 = ……やっぱり、%CSTR:이인칭%は凄い
		;그 이외
		ELSE
			LOCALS:10 = 大丈夫、%CSTR:일인칭%も続く
		ENDIF
	;피소환시
	CASE 3
			LOCALS:10 = ……準備は出来てるよ
	;공격　ARG가 공격하는 동료마, ARG:1이 적, ARG:2가 데미지량
	CASE 11
		CALLFORM SKILL_TYPE_{CFLAG:ARG:입력행동}
		LOCAL = MAXBASE:(ARG:1):GET_TYPE(RESULT)
		;상대가 죽었다
		IF BASE:(ARG:1):ＨＰ <= 0
			LOCALS:10 = ……さ、次だ
		;약점을 찔렀다. 즉 COOP
		ELSEIF LOCAL > 100 && LOCAL < 999
			IF CFLAG:(ARG:1):방어플래그
				LOCALS:10 = ……そうくるか
			ELSE
				;이쪽은 COOP발동으로 돌리기 위해서 CUT
				RETURN 0
			ENDIF
		;대 데미지
		ELSEIF 데미지비율(ARG:1, ARG:2) > 50
			LOCALS:10 = うん、効いてそうだ
		ELSEIF 데미지비율(ARG:1, ARG:2) > 25
			LOCALS:10 = ……なるほどね
		ELSEIF 데미지비율(ARG:1, ARG:2) > 0
			LOCALS:10 = ……それはそうなるか
		;무효되었다
		ELSEIF 데미지비율(ARG:1, ARG:2) <= 0
			LOCALS:10 = ……残念
		ENDIF
	;피데미지
	;ARG가 공격을 받는 동료마, ARG:1이 적, ARG:2가 데미지량
	CASE 12
		;죽었다
		IF BASE:ARG:ＨＰ <= 0
			LOCALS:10 = %CSTR:일인칭%は……まだ、……
		;대 데미지
		ELSEIF 데미지비율(ARG, ARG:2) > 50 || 현HP비율(ARG) < 25
			LOCALS:10 = ……これは困ったな
		;무효
		ELSEIF ARG:2 == 0
			LOCALS:10 = ……いいや、それじゃあ効かない
		;흡수
		ELSEIF ARG:2 < 0
			LOCALS:10 = ……ありがとう
		ENDIF
	;반사당했다
	CASE 15
			LOCALS:10 = ……ふぅん、こうくるか
	;반사했다
	CASE 16
			LOCALS:10 = 大丈夫、ちゃんと準備はしてるから
	;회피당했다
	CASE 17
			LOCALS:10 = ……うーん
	;회피했다
	CASE 18
			LOCALS:10 = ……なんとか躱せるね
	;반격스킬 발동했다
	CASE 20
			LOCALS:10 = ……ハァッ！！
	;배틀에서 승리했다
	CASE 90
			LOCALS:10 = ……お疲れ様
	;레벨 업했다
	CASE 91
			LOCALS:10 = ああ、……強くなったかな？
	;도주에 성공했다
	CASE 92
			LOCALS:10 = ……ここまで来れば大丈夫そうだ
	;도주에 실패했다
	CASE 93
			LOCALS:10 = ……駄目だったみたい
	;전투 개시시
	CASE 94
		IF FLAG:선제공격플래그
			LOCALS:10 = 隙をつけばすぐ終わりそうだね
		ELSEIF 현HP비율(ARG) < 25
			;빈사
			LOCALS:10 = はやく……消えて
		ELSE
			LOCALS:10 = 邪魔……
		ENDIF
	;백어택을 당했다
	CASE 95
		IF 현HP비율(ARG) < 25
			;빈사
			LOCALS:10 = くっ、死んで…たまるか…！
		ELSE
			LOCALS:10 = ……退かさないと
		ENDIF
	;적에게 선제공격을 당했다
	CASE 96
		IF 현HP비율(ARG) < 25
			;빈사
			LOCALS:10 = ……！
		ELSE
			LOCALS:10 = ……痛い
		ENDIF
ENDSELECT
SIF !(LOCALS:10 == "")
	CALL MESSAGE_B2(CALLNAME:ARG,0, LOCALS:10, LOCALS:11, LOCALS:12, LOCALS:13, LOCALS:14)


@BATTLE_MESSAGE_K4199,ARG,ARG:1 = 0
;■대사의 행 수를 늘리고 싶을 때는
;			LOCALS:10 = 1행째
;			LOCALS:11 = 2행째
;			LOCALS:12 = 3행째
;			LOCALS:13 = 4행째
;			LOCALS:14 = 5행째
;로 늘려나간다
VARSET LOCALS
;사용한 스킬의 이름을 LOCALS에 대입
CALLFORM SKILL_NAME_{CFLAG:ARG:입력행동},ARG
LOCALS = %RESULTS%
;FLAG:ARG:타겟

;붕괴된 경우에는 구상 표시하지 않도록 할 수 있다
;SIF TALENT:ARG:9
;	RETURN 0

CALLFORM SKILL_EFECT_{CFLAG:ARG:입력행동}
;주석처리하기 쉽도록
IF 1 == 0
;-------------------------------------------------
;상태이상시
;-------------------------------------------------
;매료시
ELSEIF CFLAG:ARG:상태이상 == GET_STATE_NUM("CHARM")
	;행동불능（헤롱헤롱 거리고 있다）
	IF CFLAG:ARG:입력행동 == 2314
		LOCALS:10 = んは、ぁ……
	ELSE
		LOCALS:10 = なにこ、れ……きもち、わるい…
	ENDIF
;혼란시
ELSEIF CFLAG:ARG:상태이상 == GET_STATE_NUM("PANIC")
	;금전뿌리기
	IF CFLAG:ARG:입력행동 == 2315
		LOCALS:10 = からだが、かってに……
	;행동불능（멍하니 멈춰 있다）
	ELSEIF CFLAG:ARG:입력행동 == 2314
		LOCALS:10 = ……
	ELSE
		LOCALS:10 = ぅぅ……
	ENDIF
;-------------------------------------------------
;특정스킬
;-------------------------------------------------
;이쪽은 통상공격
ELSEIF CFLAG:ARG:입력행동 == 0
	LOCALS:10 = ……ふっ！
;ELSEIF CFLAG:ARG:입력행동 == (스킬번호)
;	LOCALS:10 = 
;섹시댄스
ELSEIF CFLAG:ARG:입력행동 == 1212
	LOCALS:10 = 
;캔디보이스
ELSEIF CFLAG:ARG:입력행동 == 1213
	LOCALS:10 = 
;파이널누드
ELSEIF CFLAG:ARG:입력행동 == 1214
	LOCALS:10 = 
;-------------------------------------------------
;총공격（특수탄환 포함）
;-------------------------------------------------
ELSEIF RANGE(CFLAG:ARG:입력행동,2101, 2200)
	;GUN
	IF CFLAG:ARG:입력행동 == 2101
		LOCALS:10 = 
	;그 외（특수탄환）
	ELSE
		LOCALS:10 = 
	ENDIF
;-------------------------------------------------
;기본행동
;-------------------------------------------------
ELSEIF RANGE(CFLAG:ARG:입력행동,2301, 2400)
	;ＣＨＡＮＧＥ
	IF CFLAG:ARG:입력행동 == 2301
		LOCALS:10 = ……交代？
	ELSE
		LOCALS:10 = ……了解
	ENDIF
;-------------------------------------------------
;아이템
;-------------------------------------------------
ELSEIF RANGE(CFLAG:ARG:입력행동,3001, 3500)
	LOCALS:10 = これ……使うね
;-------------------------------------------------
;링케이지
;-------------------------------------------------
ELSEIF RANGE(CFLAG:ARG:입력행동,4000, 5000)
	;메시아라이저
	IF CFLAG:ARG:입력행동 == 4160
		LOCALS:10 = 
	;L히트라이저
	ELSEIF CFLAG:ARG:입력행동 == 4200
		LOCALS:10 = 
	;사망유희
	ELSEIF CFLAG:ARG:입력행동 == 4800
		LOCALS:10 = 
	ELSE
		LOCALS:10 = 
	ENDIF
;-------------------------------------------------
;범용스킬
;-------------------------------------------------
;검격
ELSEIF RANGE(CFLAG:ARG:입력행동,1, 99)
	LOCALS:10 = ……ハァｯ！
;비구
ELSEIF RANGE(CFLAG:ARG:입력행동,101, 199)
	LOCALS:10 = ……もしかしてこの方がいいかな
;타격
ELSEIF RANGE(CFLAG:ARG:입력행동,201, 299)
	LOCALS:10 = フッ！！
;전술
ELSEIF RANGE(CFLAG:ARG:입력행동,301, 400)
	;물어뜯기계
	IF RANGE(CFLAG:ARG:입력행동,321, 326)
		LOCALS:10 = %CSTR:일인칭%サマ、マルカジリ……？
	;그 외
	ELSE
		LOCALS:10 = こうすれば……
	ENDIF
;회복
ELSEIF RANGE(CFLAG:ARG:입력행동,401, 499)
	;전체회복
	IF CFLAG:ARG:타겟 > 6
		LOCALS:10 = ……皆、怪我は気にしてね
	ELSE
		LOCALS:10 = ……ちゃんと治そう
	ENDIF
;지원
ELSEIF RANGE(CFLAG:ARG:입력행동,501, 599)
	;강화계
	IF RANGE(CFLAG:ARG:입력행동,501, 505) || CFLAG:ARG:입력행동 == 558
		LOCALS:10 = ……まだまだ強くなってくれ
	;약체계
	ELSEIF RANGE(CFLAG:ARG:입력행동,506, 509) || CFLAG:ARG:입력행동 == 541 || CFLAG:ARG:입력행동 == 556
		LOCALS:10 = 弱く、柔く……
	;도발
	ELSEIF CFLAG:ARG:입력행동 == 515
		LOCALS:10 = ……君たちの力はそんなもの？
	;그 외
	ELSE
		LOCALS:10 = ……いくよ
	ENDIF
;방어
ELSEIF RANGE(CFLAG:ARG:입력행동,601, 699)
	LOCALS:10 = 
;화염
ELSEIF RANGE(CFLAG:ARG:입력행동,701, 799)
	LOCALS:10 = ……しっかり燃えて
;빙결
ELSEIF RANGE(CFLAG:ARG:입력행동,801, 899)
	LOCALS:10 = ……凍えるぐらい冷えると思うよ
;전격
ELSEIF RANGE(CFLAG:ARG:입력행동,901, 999)
	LOCALS:10 = ……きっと身体中が痺れるよね
;충격
ELSEIF RANGE(CFLAG:ARG:입력행동,1001, 1099)
	LOCALS:10 = ……立っていられる？
;신경
ELSEIF RANGE(CFLAG:ARG:입력행동,1101, 1199)
	LOCALS:10 = ……大丈夫だよ、効くから
;정신
ELSEIF RANGE(CFLAG:ARG:입력행동,1201, 1299)
	LOCALS:10 = ……もう、何も考えなくていいよ
;파마
ELSEIF RANGE(CFLAG:ARG:입력행동,1301, 1399)
	LOCALS:10 = さぁ、……呼んでるよ
;주살
ELSEIF RANGE(CFLAG:ARG:입력행동,1401, 1499)
	LOCALS:10 = ……引き摺り落される気分、味わえばいい
;지변
ELSEIF RANGE(CFLAG:ARG:입력행동,1501, 1599)
	LOCALS:10 = ……自然の力は怖いからね
;수격
ELSEIF RANGE(CFLAG:ARG:입력행동,1601, 1699)
	LOCALS:10 = ……ばっしゃーん
;질풍
ELSEIF RANGE(CFLAG:ARG:입력행동,1701, 1799)
	LOCALS:10 = ……葉っぱみたいに、消えて
;중력
ELSEIF RANGE(CFLAG:ARG:입력행동,1801, 1899)
	LOCALS:10 = ……動けないでしょ？
;핵열
ELSEIF RANGE(CFLAG:ARG:입력행동,1901, 1999)
	LOCALS:10 = ……えい
;만능
ELSEIF RANGE(CFLAG:ARG:입력행동,2001, 2099)
	;자폭계
	IF RANGE(CFLAG:ARG:입력행동,2005, 2007)
		LOCALS:10 = ……あとは、頼む
	ELSE
		LOCALS:10 = ……逆らえない力もあるんだよ
	ENDIF
;마법소녀계
ELSEIF RANGE(CFLAG:ARG:입력행동,2989, 2999)
	LOCALS:10 = 
;-------------------------------------------------
;그 외
;-------------------------------------------------
ELSE
	LOCALS:10 = ……はやく終わらないかな
ENDIF
;-------------------------------------------------
;진ㆍ그 외
;-------------------------------------------------
IF LOCALS:10 == ""
	;반드시 무언가 말한다
	LOCALS:10 = ……
ENDIF

SIF !(LOCALS:10 == "")
	CALL MESSAGE_B2(CALLNAME:ARG,1, LOCALS:10, LOCALS:11, LOCALS:12, LOCALS:13, LOCALS:14)
