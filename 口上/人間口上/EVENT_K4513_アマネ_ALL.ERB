;-------------------------------------------------
;九頭竜　天音口上（キャラクターの名前を入れる、下にコンセプトなど一言あれば入れる）
;	◆ライセンスについて
;	+----+----+-------------------------------+
;	|番号|許可|ライセンス内容                 |
;	+----+----+-------------------------------+
;	|   1| ○ | 処理上のバグ修正              |
;	+----+----+-------------------------------+
;	|   2| ○ | 口上の誤字・脱字の修正        |
;	+----+----+-------------------------------+
;	|   3| ○ | 口上まとめへの収録            |
;	+----+----+-------------------------------+
;	|   4| ○ | 口上の新規追加                |
;	+----+----+-------------------------------+
;	|   5| ○ | 既存口上の改変                |
;	+----+----+-------------------------------+
;	|   6| ○ | 改変した口上の自由な再配布    |
;	+----+----+-------------------------------+
;	|   7| ○ | 口上内容の転載・流用          |
;	+----+----+-------------------------------+
;
;	Modification Data:
;
;	Edit	Date		Author			Reason
;	001	20111130	P1系作成者		調教開始時（一回のみ） を埋める
;	002	20111130	P1系作成者		調教開始時（2回目）
;	003	2012020 	P1系作成者		テンプレ対応、調教終了
;									次は助手口上とか考えておこう‥‥とりあえず、枠だけ先に作っておくとして。
;   004 20200619    口上まとめ		口上まとめの都合で、ver3.3化および口上セレクト対応
;-------------------------------------------------

;-------------------------------------------------
;eraMegaten전용 구상 템플릿
;4513と4513를 캐릭터 번호로 치환하고, Y1을 조수의 캐릭터 번호로 치환해주세요
;단, 캐릭터번호가 한자리인 캐릭터의 경우, 4513,Y1は1、4513は01のように変えてください
;그리고 커맨드 내의 적당한 조건 아래의 PRINTFORMW의 뒤에 반각 공백을 두고 대사를 추가해주세요
;구상을 추가하지 않는 커맨드는 무기입하셔도 상관없습니다만, CFLAG:XXX의 앞에『;』를 붙여서 주석처리를 하시면 더욱 좋습니다
;엑스트라 구문 등, 주석처리가 되어 있는 구문을 사용할 때는,『;』를 제거해 주석처리를 해제할 필요가 있습니다
;마지막으로 read me도 훑어보시는걸 추천드립니다
;-------------------------------------------------

;-------------------------------------------------
;범용 구상의 작성에 사용하는 경우는, 4513→캐릭터번호가 아니라,
;K4513_1→PUB회화타입번호　로 변경해서 사용해주세요
;-------------------------------------------------
;--------------------------------------------------
;구상셀렉트 기능：구상설명
;--------------------------------------------------
;이 함수의 주석처리를 해제하면, 구상의 선택시에 선택지로 후보에 오릅니다
;이미 구상이 있는 캐릭터나 회화타입의 다른 버전의 구상을 작성할 때 이용해주세요
;이 때, 이 함수를 포함한 이 파일 내의 함수명 전체에 _임의의1-49(범용구상이면 1-9)의 숫자를 추가해주세요
;예： EVENTTRAIN_KX1 → EVENTTRAIN_KX1_12
;--------------------------------------------------
@셀렉트구상설명_K4513_1,ARG = 0
SIF ARG == 1
	RETURN 0
;구상명
PRINTFORML 쿠즈류 아마네 구상（P1계 작성자판）
;구상설명문
PRINTFORML 조교 개시、종료시의 구상만

;--------------------------------------------------
;EVENTTRAIN 관련（X1을 캐릭터번호로 치환）
;조교 개시시의 대화 CFLAG 201～219를 사용
;--------------------------------------------------
@EVENTTRAIN_K4513_1
CALL 구상변수초기화
;--------------------------------------------------
;엑스트라모드 구문　CFLAG:220을 사용
;네 를 선택시 첫조교시 구상설정용 소질로 변경 가능합니다
;아래에 처녀화할 경우의 구문을 샘플로 올려두겠습니다
;--------------------------------------------------
;IF CFLAG:220 == 0
;	PRINTL 処女モードに移行しますか？
;	PRINTL [0] はい
;	PRINTL [1] いいえ
;	$INPUT_LOOP
;	INPUT
;	IF RESULT == 0
;		PRINTFORMW ―処女モードに素質、能力を変更します―
;		TALENT:TARGET:0 = 1
;		ABL:TARGET:4 = 0
;		EXP:TARGET:0 = 0
;	ELSEIF RESULT == 1
;		PRINTFORMW ―通常モードで調教を開始します―
;	ELSE
;		GOTO INPUT_LOOP
;	ENDIF
;	CFLAG:220 = 1
;ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTTRAIN_PY1_K4513_1
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL EVENTTRAIN_PY1_K4513_1
;		RETURN 0
;	ENDIF
;ENDIF
;--------------------------------------------------
;간이 조수구상 발동시에 첫조교시 구상을 스킵（Y1을 조수의 캐릭터번호로 한다）
;여러 명이 있을 경우는, SIF NO:ASSI == Y1 || NO:ASSI == Z1으로 추가해 나간다
;--------------------------------------------------
;IF CFLAG:201 == 0 && ASSI > 0
;	SIF NO:ASSI == Y1
;		CFLAG:201 =1
;ENDIF
;-------------------------------------------------
;조교 개시시（1회만） CFLAG201
;-------------------------------------------------
DRAWLINE
;첫조교시
IF CFLAG:201 == 0 && TALENT:연모 == 0
	IF FLAG:백합의꽃루트
		KSTR:(K++) = %조사처리(NAME,"는")% 호출된 방에서、%CALLNAME:MASTER% 보다 먼저와서 기다리고 있었다.
		KSTR:(K++) = ‥‥이 방이 무엇 때문에 존재하는지, 모르는 것은 아닐 것이다.
		KSTR:(K++) = 콘크리트로 둘러싼 것 뿐인 방에、비치된 가구는 침대 하나뿐.
		KSTR:(K++) = 그것도 바닥에 고정되어 있어、그리고는 옆방에 샤워가 있는 것 뿐.
		KSTR:(K++) = %조사처리(CALLNAME,"는")% 마중하듯이, 걸터앉아있던 침대에서 일어섰다.
		KSTR:(K++) = 「‥‥%CALLNAME:MASTER%、내가 당신에게 구해진 것은 확실합니다」
		KSTR:(K++) = 「그 대로、이자・벨을 배제하지 못했다면、머지않아 나는 망가져、그의 지배하에 들어갔을테니까」
		KSTR:(K++) = 「‥‥그렇지만、은혜가 있다고해서、전부를 받아들일수는 없습니다」
		KSTR:(K++) = %조사처리(CALLNAME,"는")% 똑바로 %CALLNAME:MASTER%의 눈을 바라보며, 강한 어조로 말을 이었다.
		KSTR:(K++) = 「다시 생각해주실 수 없겠습니까？　이런 일은 그만두고‥‥」
		KSTR:(K++) = 그녀에게는 미안하지만、그렇게는 할 수 없다.
		KSTR:(K++) = 그녀가 메시아교의 무녀인 것 처럼、%CALLNAME:MASTER%에게도 위치가 있다.
		KSTR:(K++) = 무엇보다、어딘지 신비로운 외모와는 어울리지 않을만큼 강하게 자기 주장을 하고 있는 거유는, 언밸런스한 색기를 갖고 있다.
		KSTR:(K++) = 「‥‥후회할 뿐이라고 생각해요」
		WSTR:(K++) = %조사처리(CALLNAME,"가")% 단념한듯이 한숨을 내쉰 것과、%마스터가%、그녀를 침대에 넘어뜨린 것은 거의 동시였다.
	ELSEIF DB:4:7 == 3
;アマネ裏切り
		KSTR:(K++) = %조사처리(CALLNAME,"는")% 호출된 방에서、%CALLNAME:MASTER% 보다 먼저와서 기다리고 있었다.
		KSTR:(K++) = ‥‥이 방이 무엇 때문에 존재하는지, 모르는 것은 아닐 것이다.
		KSTR:(K++) = 콘크리트로 둘러싼 것 뿐인 방에、비치된 가구는 침대 하나뿐.
		KSTR:(K++) = 그것도 바닥에 고정되어 있어、그리고는 옆방에 샤워가 있는 것 뿐.
		KSTR:(K++) = %조사처리(CALLNAME,"는")% 마중하듯이, 걸터앉아있던 침대에서 일어섰다.
		KSTR:(K++) = 「‥‥%CALLNAME:MASTER%、기다리고 있었습니다」
		KSTR:(K++) = 「당신이、어떤 분인지、지금까지 어떤 일을 해왔는지는 이미 알고 있습니다」
		KSTR:(K++) = 그걸 어떻게 생각하고 있는것인지、%조사처리(CALLNAME,"는")% %CALLNAME:MASTER%의 눈을 똑바로 바라보며 시선을 마주한다.
		KSTR:(K++) = 「‥‥그렇지만、그건 아무래도 좋은 일입니다. 나는, 신보다도 당신을 선택했습니다. 당신이 가는 길이 나의 길입니다」
		KSTR:(K++) = 신도 그간의 가르침도 전부 버렸을터인 %CALLNAME%에게는, 그런데도 아직 무언가가 더 남아있는 것이 있는 것 같다.
		KSTR:(K++) = 그녀가 %CALLNAME:MASTER%에게 무엇을 기대하고 있는지는 모른다.
		KSTR:(K++) = 하지만, %마스터가% 해야할 일은 변함없다.
		KSTR:(K++) = %CALLNAME%의 어깨에 손을 얹는다.
		WSTR:(K++) = %조사처리(CALLNAME,"는")% 조금 몸을 긴장시켰지만, 저항하려고는 하지 않았다.
	ELSE
		KSTR:(K++) = 제대로 말하는 것을 듣지 않는 %조사처리(CALLNAME,"를")%, 조교실까지 데려오는 것만으로도 큰일이었다.
		KSTR:(K++) = 방에 들어간 순간、%조사처리(CALLNAME,"는")% 혐오감으로 눈을 가늘게떴다.
		KSTR:(K++) = ‥‥이 방이 무엇때문에 존재하는지, 눈치챈 탓일거다.
		KSTR:(K++) = 콘크리트로 둘러싼 것 뿐인 방에、비치된 가구는 침대 하나뿐.
		KSTR:(K++) = 그것도 바닥에 고정되어 있어、그리고는 옆방에 샤워가 있는 것 뿐.
		KSTR:(K++) = 난폭하게 침대 위에 내던져진 %조사처리(CALLNAME,"는")%、그런데도 %마스터를% 노려보는 시선을 보낸다.
		KSTR:(K++) = 「‥‥설령 이 몸을 더럽힌다고해도, 당신의 운명은 정해져있습니다. 지금부터라도 다른 길을‥‥」
		KSTR:(K++) = 이런 상황에서 설교라니 대단하긴하지만,
		WSTR:(K++) = %조사처리(CALLNAME:MASTER,"는")% 입다물고 %CALLNAME%의 어깨에 손을 올렸다.
	ENDIF
	CFLAG:201 = 1
	GOTO 表示
;굴복각인Lv1
ELSEIF CFLAG:201 < 2 && MARK:2 == 1
	IF FLAG:백합의꽃루트
		KSTR:(K++) = 조교실에 모습을 드러낸 %마스터를% 보고、%조사처리(CALLNAME,"는")% 분노를 담은 눈으로 노려보았다.
		KSTR:(K++) = 「‥‥당신은 대체 무슨 생각을 하고있는거죠？　레미엘님도 나도 당신에게 기대한 것은 이런 것이 아니었는데도」
		KSTR:(K++) = 「당신은 청렴결백이라고는 말할 수 없어도, 그런데도 되도록 올바른 길을 걸으려고 했었습니다」
		KSTR:(K++) = 「그렇기 때문에 모르겠습니다. 당신은 대체‥‥」
		KSTR:(K++) = ‥‥아마도、%조사처리(CALLNAME,"과")% 천사가 그 정도로 일관된 생활을 보내지는 않는다는, 고작 그런 정도의 이야기.
		KSTR:(K++) = ‥‥그리고 아마도, 그것이 평범한 인간의 본연의 자세일 것이다.
		WSTR:(K++) = 말로는 이 순수한 소녀에게 그것을 전할 수 없다면, 행위로 전하는 것도 좋겠지.
	ELSEIF DB:4:7 == 3
;アマネ裏切り
		KSTR:(K++) = 조교실에 모습을 드러낸 %마스터를% 보고、%조사처리(CALLNAME,"는")% 깊게 머리를 숙였다.
		KSTR:(K++) = 「‥‥기다리고 있었습니다、%CALLNAME:MASTER%」
		KSTR:(K++) = 「오늘은、저에게 무엇을 소망하십니까？」
		KSTR:(K++) = ‥‥놀라울 정도로 순종적이지만、%CALLNAME%의 태도는 암컷 노예의 그것이라기보다는, 종교자의 그것이다.
		KSTR:(K++) = 그렇다고는해도, 스스로의 몸에 품은 천사조차 등진 그녀는, 도대체 무엇을 믿으려 하고 있는 것일까？
		WSTR:(K++) = 적어도、육욕만으로 부추겨져 움직이고 있는 것이 아닌 것만은 확실하지만.
	ELSE
		KSTR:(K++) = 조교실에 모습을 드러낸 %CALLNAME:MASTER%에게、%조사처리(CALLNAME,"는")% 혐오의 시선을 보내며, 무시하듯이 눈을 피했다.
		KSTR:(K++) = 뭐, 그렇게 나온다면 그걸로 됐다.
		KSTR:(K++) = %CALLNAME%의 풍만한 몸을 억지로 침대에 강하게 누른다.
		KSTR:(K++) = %조사처리(CALLNAME,"는")% 말없이 %마스터를% 노려본다.
		KSTR:(K++) = 「‥‥신은 당신의 죄를 용서하시지 않을겁니다」
		WSTR:(K++) = 그럴지도 모른다. 용서받고 싶은 것도 아니지만.
	ENDIF
	CFLAG:201 = 2
	GOTO 表示
;굴복각인Lv2
ELSEIF CFLAG:201 < 3 && MARK:2 == 2
	IF FLAG:백합의꽃루트
		KSTR:(K++) = 조교실에 모습을 드러낸 %마스터를% 보고、%조사처리(CALLNAME,"는")% 시선을 피했다.
		KSTR:(K++) = 민감하게 개발된 몸은 한때의 강하게 노려보던 시선을、%CALLNAME%에게 허락하지 않겠지.
		KSTR:(K++) = 어딘지 마음 약한듯이, 호소하듯이 %조사처리(CALLNAME,"는")% 입을 연다.
		KSTR:(K++) = 「‥‥무슨 말을 해도 소용없다는 것 정도는 알고 있습니다」
		KSTR:(K++) = 「저 혼자만의 일이라면 체념할수도 있습니다‥‥하지만、당신은 이런 일을 하면서、레미엘님의 기대에 응할 수 있습니까！？」
		KSTR:(K++) = ‥‥%조사처리(CALLNAME,"가")%나 레미엘이 %CALLNAME:MASTER%(이)야말로 구세주로서 충분하다고 기대하고 있다는 것은 알고 있다.
;アライメントで分岐
		IF ABL:MASTER:속성LC == 1
;LAW
			KSTR:(K++) = 특별히、그것을 없었던 일로 할 생각은 없다.
		ELSE
			KSTR:(K++) = 하지만、그런 일을 억지로 떠맡겨도 곤란하다.
		ENDIF
		WSTR:(K++) = %CALLNAME:MASTER%에게도、자신의 방식이라는 것이 있으니까.
	ELSEIF DB:4:7 == 3
;アマネ裏切り
		KSTR:(K++) = 조교실에 모습을 드러낸 %마스터를% 보고、%조사처리(CALLNAME,"는")% 깊게 머리를 숙였다.
		KSTR:(K++) = 「‥‥기다리고 있었습니다、%CALLNAME:MASTER%」
		KSTR:(K++) = 표명상으로는 평소의 %CALLNAME%의 태도와 전혀 다르지 않다‥‥하지만、이 희미하게 들리는 난폭한 숨소리는‥‥
		KSTR:(K++) = %CALLNAME:MASTER%의 묻는듯한 시선을 느꼈는지、%조사처리(CALLNAME,"는")% 단번에 얼굴을 홍조시켰다.
		KSTR:(K++) = 「그‥‥%CALLNAME:MASTER%에게、여기로 불려졌다는 것은, 평소의 그‥‥」
		KSTR:(K++) = 그렇다고 하자、%조사처리(CALLNAME,"는")% 고개를 숙였다.
		KSTR:(K++) = 「‥‥싫다고는 하지 않습니다. 그저‥‥」
		WSTR:(K++) = %조사처리(CALLNAME,"는")% 수치심에 얼굴을 붉히고、머뭇머뭇 허리를 움직였다.
	ELSE
	ENDIF
	CFLAG:201 = 3
	GOTO 表示
;굴복각인Lv3
ELSEIF CFLAG:201 < 4 && MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	IF FLAG:백합의꽃루트
		KSTR:(K++) = 「나는 착각하고 있었던걸지도 모르겠네요」
		KSTR:(K++) = %CALLNAME:MASTER%의 얼굴을 보고 %조사처리(CALLNAME,"는")% 한숨을 내쉬고 입을 열었다.
		KSTR:(K++) = 「구세주의 자질이 있다는 것과, 거기에 어울리는 인격을 갖는다는 것은 별개의 일」
		KSTR:(K++) = 「‥‥레미엘은 당신을 이끌기 위해서야말로, 나를 여기에 남긴것일까요‥‥」
		WSTR:(K++) = %조사처리(CALLNAME,"는")%、속삭이는듯한 목소리로, 확인하듯이 중얼거리고, 살며시 %CALLNAME:MASTER%에게 달라붙었다.
	ELSEIF DB:4:7 == 3
;アマネ裏切り
		KSTR:(K++) = 「조금、이야기를 할 수 있을까요？」
		KSTR:(K++) = %조사처리(CALLNAME,"는")% 침대에 앉은채로, 그렇게 입을 열었다.
		KSTR:(K++) = 「‥‥저의 아버지의‥‥그리고 저의 소망에 관한 이야기입니다」
		KSTR:(K++) = ‥‥그녀의 아버지는、머지않아 신의 시련이 인간에게 부과되는 것을 예측하고, 그것을 두려워하고 있었다고한다.
		KSTR:(K++) = 그녀도 또한、그것을 회피하기위해 메시아교에 의지하고, 인간들 전부가 올바른 일을 해 시련을 피할 수 있다고 생각한 것이라고.
		KSTR:(K++) = 「하지만‥‥레미엘조차 굽힌 인간이 나타났다면‥‥인간이 그 정도의 일을 할 수 있는 것이라면‥‥」
		KSTR:(K++) = 「다른 길도 있을지도 모른다、그렇게 생각했습니다. 시련을 내리는 자가 없는 세계‥‥」
		WSTR:(K++) = 「당신에게、그런 희망을 맡겨도 좋을까요？　마왕의 길을 걷는 당신에게‥‥」
	ELSE
	ENDIF
	CFLAG:201 = 4
	GOTO 表示
;함락
ELSEIF CFLAG:201 < 5 && (TALENT:3 || TALENT:4 || TALENT:5)
	;연모
	IF TALENT:3
		IF CFLAG:미조교함락 == 1
			;조교하지 않고 연모를 단 경우
		ELSE
			KSTR:(K++) = 「%CALLNAME:MASTER% 잠깐만、시간을 내줄 수 있겠습니까？」
			KSTR:(K++) = 평소처럼、조교에 들어가려고 한 %CALLNAME:MASTER%에게、%조사처리(CALLNAME,"가")% 말을 걸었다.
			KSTR:(K++) = 평상시와는 뭔가 다른 분위기를 %CALLNAME%의 표정에서 느껴, 손을 멈춘다.
			KSTR:(K++) = 「‥‥감사합니다」
			KSTR:(K++) = %조사처리(CALLNAME,"는")% 드물게도 잠깐 주저하고나서、얼굴을 숙인채로 입을 열었다.
			KSTR:(K++) = 「‥‥설마, 제가 이런식으로 누군가를 생각하게 될거라고는、생각해 본 적도 없었습니다만‥‥」
			KSTR:(K++) = 「저는、당신을‥‥그、좋아하게 되어버린 것 같습니다‥‥」
			KSTR:(K++) = 말이 끝날 무렵에는 알아듣기 힘들 정도로 작은 목소리로 %조사처리(CALLNAME,"는")% 고백의 말을 입에 올렸다.
			KSTR:(K++) = 고개를 숙인 것은 빨갛게 달아오른 얼굴을 보이고 싶지 않아서인건가.
			WSTR:(K++) = 「대답을 듣고 싶다던가、당신도 같은 마음이었으면 한다던가 하는 생각은 없습니다‥‥그러니까、곁에 있게 해주세요‥‥」
		ENDIF
	;음란
	ELSEIF TALENT:4
		KSTR:(K++) = %마스터가% 방에 들어가자、%조사처리(CALLNAME,"는")% 얼굴을 붉히며, 거칠게 숨을 내쉬었다.
		KSTR:(K++) = 방 안에는 생생한 냄새가 감돌고 있다.
		KSTR:(K++) = 고개를 숙인 %CALLNAME%에게、%조사처리(CALLNAME:MASTER,"는")% 심술궂게 뭘 하고 있었는지 물어보았다.
		KSTR:(K++) = 「‥‥알고 계시죠？」
		KSTR:(K++) = 아름답게 뻗은 허벅지에 애액이 흘러내리며 흔적을 남긴다.
		KSTR:(K++) = 눈을 감은채로 뜨려하지 않는 %CALLNAME%에게、질문을 던진다.
		KSTR:(K++) = 「‥‥스스로、위로하고‥‥있었습니다」
		KSTR:(K++) = 가느다란 목소리로 띄엄띄엄 그렇게 말하는 %CALLNAME%.
		KSTR:(K++) = 「‥‥어째서 이런、나、이런 몸이 되어버려‥‥」
		KSTR:(K++) = %조사처리(CALLNAME,"는")% 눈물을 머금고 후회하는듯이 중얼거린다.
		WSTR:(K++) = 하지만、%CALLNAME:MASTER%에게 팔을 잡혀 침대에 밀어넘어뜨려지면、그 몸은 기대하는듯이 전율했다.
	;복종
	ELSEIF TALENT:5
	ENDIF
	CFLAG:201 = 5
	GOTO 表示
ENDIF

;-------------------------------------------------
;간이 조수구상（Y1을 조수의 캐릭터번호로 치환, 4513을 캐릭터번호로 변경한다) CFLAG203
;말할 캐릭터를 늘릴 경우, 구문을 복사＆붙여넣기 해, CFLAG의 수를 204, 205로 늘린다
;-------------------------------------------------
;조수의 유무를 체크（없을 경우는 2번째 이후로 건너뛴다）
IF ASSI < 0 
	CALL EVENTTRAIN_SECOND_K4513_1
;조수 天使　レミエル
;ELSEIF NO:ASSI == 307
	;처음
;	IF CFLAG:203 == 0
		;함락완료＋함락시 첫 구상이 발생완료
;		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && CFLAG:201 >= 5
;			IF FLAG:백합의꽃루트
;				PRINTFORMW 
;			ELSEIF DB:4:7 == 3
;				PRINTFORMW 
;			ELSE
;				PRINTFORMW 
;			ENDIF
		;그 이외
;		ELSE
;			IF FLAG:백합의꽃루트
;				PRINTFORMW 
;			ELSEIF DB:4:7 == 3
;				PRINTFORMW 
;			ELSE
;				PRINTFORMW 
;			ENDIF
;		ENDIF
;		CFLAG:203 = 1
	;2번째 이후
;	ELSEIF CFLAG:203 < 2 || FLAG:7 ==2
		;함락완료
;		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
;			PRINTFORMW 
		;그 이외
;		ELSE
;			PRINTFORMW 
;		ENDIF
;		CFLAG:203 = 2
;	ENDIF
;구상이 있는 조수가 없을 경우, 통상의 2번째 이후의 구상으로 건너뛴다
ELSE
	CALL EVENTTRAIN_SECOND_K4513_1
ENDIF

;-------------------------------------------------
;조교 개시시（2번째 이후, 4513을 캐릭터번호로 변경한다）
;-------------------------------------------------
$表示
CALL 구상변수표시
@EVENTTRAIN_SECOND_K4513_1
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:202 < 4 || FLAG:7 == 2)
	KSTR:(K++) = 「‥‥앞으로 당신과는 이야기하지 않겠어요」
	KSTR:(K++) = %조사처리(CALLNAME,"는")% %CALLNAME:MASTER%에게 물건을 보는 듯한 시선을 향한다.
	KSTR:(K++) = 「나는、당신에게 상응하는 벌이 내려오기를 그저 기다릴 뿐」
	WSTR:(K++) = ‥‥앞으로의 일은 마음대로 해라、라고 말하고 있는 거겠지.
	CFLAG:202 = 4
;굴복각인Lv0
ELSEIF MARK:2 == 0 && (CFLAG:202 < 1 || FLAG:7 == 2)
	IF FLAG:백합의꽃루트
		KSTR:(K++) = 「‥‥아직、다시 생각할 마음은 생기지 않았습니까‥‥？」
		KSTR:(K++) = %조사처리(CALLNAME:MASTER,"가")%(가) 고개를 젖자、한숨을 쉬며 머리를 흔들었다。
		KSTR:(K++) = 「‥‥지금 당신의 모습을 보신다면、레미엘은 뭐라고 말씀하실까요？」
		WSTR:(K++) = 야유를 하는%CALLNAME%의 태도에는、아직도 여유가 엿보인다。
	ELSEIF DB:4:7 == 3
;アマネ裏切り
		KSTR:(K++) = 「당신은、평소부터 이런 일을？」
		KSTR:(K++) = %조사처리(CALLNAME,"는")% 확인하는듯 그렇게 말하지만、그다지 비난의 기색은 보이지 않는다。
		KSTR:(K++) = 「‥‥이 신체는 이미 보류 하고 잇습니다만、아무래도 지금까지 이러한 일엔 인연이 없었었기 떄문에‥‥」
		WSTR:(K++) = 필요한 일은 가르쳐달라고、아마도 그렇게 말하는 의미인 것 같다。
	ELSE
	ENDIF
	CFLAG:202 = 1
;굴복각인Lv1
ELSEIF MARK:2 == 1 && (CFLAG:202 < 2 || FLAG:7 == 2)
	IF FLAG:백합의꽃루트
		KSTR:(K++) = 「‥‥레미엘의 눈이 흐려졌구나라고는 생각하고 싶지 않습니다만」
		KSTR:(K++) = %조사처리(CALLNAME,"는")%、짐짓 크게 한숨을 내쉬며 그렇게 말한다。
		KSTR:(K++) = 「이런 사람이라고 알고 있었다면‥‥아니、」
		WSTR:(K++) = （레미엘에게、알지 못하는 건 없어‥‥분명、내가 여기에 있는 것에도 의미는 있을터‥‥）
	ELSEIF DB:4:7 == 3
;アマネ裏切り
		KSTR:(K++) = 「행차하실 생각이셨으면、그렇게 말하셨으면 받아들이고 맞이하러 갔을텐데」
		KSTR:(K++) = %조사처리(CALLNAME,"는")%、갑자기 방문한 %CALLNAME%에게도 。
		WSTR:(K++) = 「‥‥그래서、오늘의 용무도 언제나 처럼의 일로？」
	ELSE
	ENDIF
	CFLAG:202 = 2
;굴복각인Lv2
ELSEIF MARK:2 == 2 && (CFLAG:202 < 3 || FLAG:7 == 2)
	IF FLAG:백합의꽃루트
		KSTR:(K++) = %CALLNAME:MASTER%의 모습을 본 %조사처리(CALLNAME,"는")%、눈을 감고 가볍게 기도를 바쳤다。
		KSTR:(K++) = （‥‥주여、지켜주세요。　나를 침범하는 이 벗어나야 할 정역으로부터、나의 영혼을‥‥）
		KSTR:(K++) = 잠시 뒤、%조사처리(CALLNAME,"는")%눈을 뜨고、도전하듯이 이쪽으로 눈을 향했다。
		WSTR:(K++) = ‥‥그래도、개발된 그녀의 신체는、%CALLNAME:MASTER%의 손을 막을 수 없을테지만‥‥
	ELSEIF DB:4:7 == 3
;アマネ裏切り
		KSTR:(K++) = %CALLNAME:MASTER%의 모습을 본%조사처리(CALLNAME,"는")%、조금 얼굴을 붉히며 머리를 숙인다。
		KSTR:(K++) = 「기、기다리고 있었습니다。조속히、평소와 같은 모습으로？」
		WSTR:(K++) = 평소와 같은 모습으로、이라는 말로부터 무엇을 연상했는지、%CALLNAME%의 붉은 얼굴은 무엇인가를 기대하고 있는 것처럼도 보였다。
	ELSE
	ENDIF
	CFLAG:202 = 3
;굴복각인Lv3＋함락소질없음
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0) && (CFLAG:202 < 4 || FLAG:7 == 2)
;この辺からはルート分岐は無くても良いかな
	KSTR:(K++) = 「‥‥기다리고 있었습니다、%CALLNAME:MASTER%」
	KSTR:(K++) = %조사처리(CALLNAME,"는")% 침대에서 일어나 %조사처리(CALLNAME:MASTER,"를")% 마중나간다。
	WSTR:(K++) = 「그래서、노늘도 언제나와 같은 용건입니까？　‥‥이따금은、다른 용건으로 오시면 좋을텐데」
	CFLAG:202 = 4
;함락완료
ELSEIF CFLAG:202 < 5 || FLAG:7 == 2
	;연모, 친애
	IF TALENT:3 || TALENT:6
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		K:1 = RAND:3
		IF K:1 == 0
			WSTR:(K++) = %CALLNAME:MASTER%의 얼굴을 본 순간、%조사처리(CALLNAME,"는")% 기쁜듯이 미소지으며 마중나왔다。
			WSTR:(K++) = 「%CALLNAME:MASTER%、기다리고 있었습니다。　아무쪼록、이쪽으로‥‥」
			WSTR:(K++) = %CALLNAME%에게 침대에 걸터앉게 하고、그 근처에 앉는다。
			KSTR:(K++) = 바로 옆에서 가만히 응시하고 있자、%조사처리(CALLNAME,"는")% 얼굴을 붉히며、결코 이쪽을 보려고 하지 않았다‥‥
		ELSEIF K:1 == 1
			WSTR:(K++) = %CALLNAME:MASTER%의 얼굴을 본 순간、%조사처리(CALLNAME,"는")% 기쁜듯이 미소지으며 마중나왔다。
			WSTR:(K++) = 「%CALLNAME:MASTER%、기다리고 있었습니다。　아무쪼록、이쪽으로‥‥」
			WSTR:(K++) = %CALLNAME%에게 침대에 걸터앉게 하고、그 근처에 앉는다。
			KSTR:(K++) = 바로 옆에서 가만히 응시하고 있자、%조사처리(CALLNAME,"는")% 얼굴을 붉히며、결코 이쪽을 보려고 하지 않았다‥‥
		ELSE
			WSTR:(K++) = %CALLNAME:MASTER%의 얼굴을 본 순간、%조사처리(CALLNAME,"는")% 기쁜듯이 미소지으며 마중나왔다。
			WSTR:(K++) = 「%CALLNAME:MASTER%、기다리고 있었습니다。　아무쪼록、이쪽으로‥‥」
			WSTR:(K++) = %CALLNAME%에게 침대에 걸터앉게 하고、그 근처에 앉는다。
			KSTR:(K++) = 바로 옆에서 가만히 응시하고 있자、%조사처리(CALLNAME,"는")% 얼굴을 붉히며、결코 이쪽을 보려고 하지 않았다‥‥
		ENDIF
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		K:1 = RAND:3
		IF K:1 == 0
			WSTR:(K++) = %CALLNAME:MASTER%의 얼굴을 본 순간、%조사처리(CALLNAME,"는")% 음탕한 미소를 띄우며 마중나왔다。
			WSTR:(K++) = 보고있으면、침대는 방금전까지 사용되고 있던 모양인지 흐트러져 있었고、생각 탓인지 방으로부터는 많이 익숙해진 냄새가 나는 것 같다。
			WSTR:(K++) = 밑져야 본전이라는 생각으로 묻자、%조사처리(CALLNAME,"는")% 시원스럽게 대답했다。
			WSTR:(K++) = 「‥‥네、오늘 올 거라고 듣고 있었습니다만、기다릴 수 없어서‥‥」
		ELSEIF K:1 == 1
			WSTR:(K++) = %CALLNAME:MASTER%의 얼굴을 본 순간、%조사처리(CALLNAME,"는")% 음탕한 미소를 띄우며 마중나왔다。
			WSTR:(K++) = 보고있으면、침대는 방금전까지 사용되고 있던 모양인지 흐트러져 있었고、생각 탓인지 방으로부터는 많이 익숙해진 냄새가 나는 것 같다。
			WSTR:(K++) = 밑져야 본전이라는 생각으로 묻자、%조사처리(CALLNAME,"는")% 시원스럽게 대답했다。
			WSTR:(K++) = 「‥‥네、오늘 올 거라고 듣고 있었습니다만、기다릴 수 없어서‥‥」
		ELSE
			WSTR:(K++) = %CALLNAME:MASTER%의 얼굴을 본 순간、%조사처리(CALLNAME,"는")% 음탕한 미소를 띄우며 마중나왔다。
			WSTR:(K++) = 보고있으면、침대는 방금전까지 사용되고 있던 모양인지 흐트러져 있었고、생각 탓인지 방으로부터는 많이 익숙해진 냄새가 나는 것 같다。
			WSTR:(K++) = 밑져야 본전이라는 생각으로 묻자、%조사처리(CALLNAME,"는")% 시원스럽게 대답했다。
			WSTR:(K++) = 「‥‥네、오늘 올 거라고 듣고 있었습니다만、기다릴 수 없어서‥‥」
		ENDIF
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		K:1 = RAND:3
		IF K:1 == 0
		ELSEIF K:1 == 1
		ELSE
		ENDIF
	ENDIF
	CFLAG:202 = 5
ENDIF
RETURN 0

;-------------------------------------------------
;EVENTEND 관련（4513을 캐릭터번호로 치환）
;조교 종료시의 대사
;-------------------------------------------------
@EVENTEND_K4513_1
CALL 구상변수초기화
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTEND_PY1_K4513_1
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	SIF NO:ASSI == Y1
;	CALL EVENTEND_PY1_K4513_1
;	RETURN 0
;ENDIF
;--------------------------------------------------
;조교 종료시의 대사
;--------------------------------------------------
DRAWLINE
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:217 < 1 || FLAG:7 == 2)
	KSTR:(K++) = 「‥‥용무가 끝났다면、빨리 나가시는게？」
	KSTR:(K++) = 일이 끝난 순간、%조사처리(CALLNAME,"는")% %조사처리(CALLNAME:MASTER,"를")% 추방시킨다。
	WSTR:(K++) = 그 소리도 표정도 얼음처럼 차가웠다。
	CFLAG:217 = 1
;굴복각인Lv1 이하
ELSEIF MARK:2 <= 1 && (CFLAG:219 < 1 || FLAG:7 == 2)
	KSTR:(K++) = 「‥‥만족 해 주셨습니까？」
	IF FLAG:백합의꽃루트
		KSTR:(K++) = %조사처리(NAME,"는")% 옷을 정리하며、얼굴을 숙여 그렇게 중얼거리며、손을 모아 기도하기 시작했다。
		WSTR:(K++) = 그 기도는、자기 자신을 위한것이 아닌 %CALLNAME:MASTER%의 죄에 대한 용서를 요구하는 것이었다‥‥
	ELSEIF DB:4:7 == 3
;アマネ裏切り
		KSTR:(K++) = %조사처리(NAME,"는")%옷을 정돈하며、%CALLNAME:MASTER%의 얼굴을 똑바로 보았다。
		WSTR:(K++) = 「‥‥당신을 만족 시키는 것은 할 수 없었던 것 같네요、죄송합니다‥‥」
	ELSE
		KSTR:(K++) = 「마음이 오가지 않는 교제 같은것에、의미가 없다고 눈치채시지 못하셨습니까？　지금부터라도 고친다면‥‥」
		WSTR:(K++) = %조사처리(CALLNAME:MASTER,"는")% 입을 다물고 방으로부터 떠났다。
	ENDIF
	CFLAG:219 = 1
;굴복각인Lv2
ELSEIF MARK:2 == 2
	IF FLAG:백합의꽃루트
		KSTR:(K++) = %조사처리(NAME,"는")% 흥분에 숨을 뱉으며、옷을 정돈하다가 불결한 자신의 모습을 눈치챈다。
		WSTR:(K++) = 눈을 감곤、샤워룸으로 향하며、눌러 참은 오열이 희미하게 들렸다。
	ELSEIF DB:4:7 == 3
;アマネ裏切り
		KSTR:(K++) = %조사처리(NAME,"는")% 홍조가 든 얼굴로 침대에 앉아、숨을 고르며 %조사처리(CALLNAME:MASTER,"를")% 올려보았다。
		WSTR:(K++) = 「‥‥어떠신가요？　다소 어느정도는、성장하고 있는 걸까요‥‥」
	ELSE
	ENDIF
	CFLAG:219 = 2
;굴복각인Lv3＋함락소질없음
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	IF FLAG:백합의꽃루트 || DB:4:7 == 3
;百合の花ルートかアマネ裏切りルート
		KSTR:(K++) = 종료를 전해들은 %조사처리(CALLNAME,"는")%、탈진한 것처럼 침대에 기대 앉았다。
		KSTR:(K++) = 「‥‥끝、인가요‥‥」
		IF BASE:0 < 500
			KSTR:(K++) = 「끝나면、천천히 이야기를 하고 싶다고 생각하고 있었습니다만‥‥오늘은 어려울 것 같습니다」
			WSTR:(K++) = 「지금이 아니라도 좋으니까、교제해 주실 수 있습니까？」
		ELSE
			WSTR:(K++) = 「그렇다면、지금부터 조금 이야기를 하지 않겠습니까？」
		ENDIF
	ELSE
	ENDIF
	CFLAG:219 = 3
;함락완료＋체력 500미만
ELSEIF BASE:0 < 500 && (CFLAG:218 < 1 || FLAG:7 == 2)
	;연모, 친애
	IF TALENT:3 || TALENT:6
		KSTR:(K++) = 「수고 하셨습니다‥‥다음 오실것을 기다리며‥‥」
		KSTR:(K++) = 완전히 지쳐 버린 모습으로、그런데도 방의 출구까지 %조사처리(CALLNAME:MASTER,"를")% 전송하려고 하는 %CALLNAME%에게、기습으로 키스를 한다。
		KSTR:(K++) = 「에‥‥응‥‥」
		WSTR:(K++) = 대사를 도중에 삼킨 %조사처리(CALLNAME,"는")%、얼굴을 붉히며 자신의 입술을 확인하는 것처럼 손가락으로 만졌다。
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		KSTR:(K++) = 「벌써‥‥끝인건가요‥‥？　조금만 더‥‥」
		KSTR:(K++) = 떠나려고 하는 %CALLNAME:MASTER%에 뒤에서부터 %CALLNAME%의 소리가 들렸다。
		KSTR:(K++) = 그러나、아마네에게 오늘은 한계일 것이다。
		WSTR:(K++) = %조사처리(CALLNAME:MASTER,"는")%、%조사처리(CALLNAME,"를")% 말리곤 방에서 떠났다。
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
	ENDIF
	CFLAG:218 = 1
;함락완료
ELSEIF CFLAG:219 < 4 || FLAG:7 == 2
	;연모, 친애
	IF TALENT:3 || TALENT:6
		KSTR:(K++) = 「수고 하셨습니다、%CALLNAME:MASTER%님」
		KSTR:(K++) = 「저기‥‥또 오셔‥‥주실 수 있으신가요？」
		KSTR:(K++) = %조사처리(CALLNAME,"는")%、불안해하며、확인하는 것처럼 그렇게 중얼거렸다。
		WSTR:(K++) = 그볍게 그녀의 입술을 빼앗아、대답의 대신으로 했다。
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		KSTR:(K++) = 종료를 고한 %CALLNAME:MASTER%에게、%조사처리(CALLNAME,"는")% 의외인 것 같은 얼굴을 보였다
		KSTR:(K++) = 「엣‥‥벌써、인가요‥‥？」
		KSTR:(K++) = 「그렇、습니까‥‥알겠습니다。다음 방문을 기다리고 있겠습니다‥‥」
		WSTR:(K++) = 방을 떠난 %CALLNAME:MASTER%의 뒤로부터는、자위를 하는 %조사처리(CALLNAME,"가")% 억제한 허덕임이 들려왔다‥‥
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
	ENDIF
	CFLAG:219 = 4
ENDIF
GOTO 表示

;--------------------------------------------------
;@KOJO_MESSAGE_COM 관련（4513을 캐릭터번호로 치환）
;커맨드 실행시에 출력됩니다
;--------------------------------------------------
$表示
CALL 구상변수표시
@KOJO_MESSAGE_COM_K4513_1
CALL 구상변수초기화
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL TRAIN_MESSAGE
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_COM_PY1_K4513_1
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_COM_PY1_K4513_1
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;커맨드 실행시의 대사 CFLAG 300～500
;-------------------------------------------------
;=================================================
;애무계 커맨드
;=================================================
;-------------------------------------------------
;0,애무
;-------------------------------------------------
SELECTCASE SELECTCOM
	CASE 0
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:300 == 0
			CFLAG:300 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:300 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:300 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:300 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:300 = 3
			;그 이외
			ENDIF
			IF (CFLAG:300 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:300 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;1,커닐링구스
;-------------------------------------------------
	CASE 1
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:301 == 0
			CFLAG:301 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:301 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:301 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:301 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:301 = 3
			;그 이외
			ENDIF
			IF (CFLAG:301 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:301 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;2,펠라한다
;-------------------------------------------------
	CASE 2
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:302 == 0
			CFLAG:302 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:302 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:302 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:302 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:302 = 3
			;그 이외
			ENDIF
			IF (CFLAG:302 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:302 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;3,손가락삽입
;-------------------------------------------------
	CASE 3
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:303 == 0
			CFLAG:303 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:303 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:303 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:303 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:303 = 3
			;그 이외
			ENDIF
			IF (CFLAG:303 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:303 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;4,애널핥기
;-------------------------------------------------
	CASE 4
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:304 == 0
			CFLAG:304 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:304 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:304 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:304 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:304 = 3
			;그 이외
			ENDIF
			IF (CFLAG:304 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:304 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;5,애널애무
;-------------------------------------------------
	CASE 5
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:305 == 0
			CFLAG:305 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:305 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:305 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:305 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:305 = 3
			;그 이외
			ENDIF
			IF (CFLAG:305 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:305 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;6,가슴주무르기
;-------------------------------------------------
	CASE 6
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:306 == 0
			CFLAG:306 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:306 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:306 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:306 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:306 = 3
			;그 이외
			ENDIF
			IF (CFLAG:306 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:306 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;7,유두자극
;-------------------------------------------------
	CASE 7
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:307 == 0
			CFLAG:307 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:307 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:307 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:307 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:307 = 3
			;그 이외
			ENDIF
			IF (CFLAG:307 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:307 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;8,가슴애무
;-------------------------------------------------
	CASE 8
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:308 == 0
			CFLAG:308 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:308 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:308 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:308 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:308 = 3
			;그 이외
			ENDIF
			IF (CFLAG:308 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:308 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;9,조개젖히기
;-------------------------------------------------
	CASE 9
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:309 == 0
			CFLAG:309 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:309 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:309 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:309 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:309 = 3
			;그 이외
			ENDIF
			IF (CFLAG:309 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:309 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;10,가슴자위
;-------------------------------------------------
	CASE 10
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:310 == 0
			CFLAG:310 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:310 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:310 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:310 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:310 = 3
			;그 이외
			ENDIF
			IF (CFLAG:310 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:310 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;11,자위
;-------------------------------------------------
	CASE 11
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:311 == 0
			CFLAG:311 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:311 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:311 = 4
			;순종Lv3 이상
			ENDIF
			IF ABL:0 >= 3 && (CFLAG:311 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:311 = 3
			;그 이외
			ENDIF
			IF (CFLAG:311 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:311 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;12,날개애무
;-------------------------------------------------
	CASE 12
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:435 == 0
			CFLAG:435 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:435 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:435 = 4
			;순종Lv3 이상
			ENDIF
			IF ABL:0 >= 3 && (CFLAG:435 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:435 = 3
			;그 이외
			ENDIF
			IF (CFLAG:435 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:435 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;13,꼬리애무
;-------------------------------------------------
	CASE 13
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:436 == 0
			CFLAG:436 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:436 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:436 = 4
			;순종Lv3 이상
			ENDIF
			IF ABL:0 >= 3 && (CFLAG:436 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:436 = 3
			;그 이외
			ENDIF
			IF (CFLAG:436 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:436 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;14,클리애무
;-------------------------------------------------
CASE 14
		;처음
		IF CFLAG:442 == 0
			K_CALL:K = TRAIN_MESSAGE
			CFLAG:442 = 1
		;2번째 이후
		;함락완료
		ENDIF
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:442 < 4 || FLAG:7 == 2) && !구상유무확인(0)
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			CFLAG:442 = 4
			ENDIF
		;순종Lv3 이상
		ENDIF
		IF ABL:0 >= 3 && (CFLAG:442 < 3 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:442 = 3
		;그 이외
		ENDIF
		IF (CFLAG:442 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:442 = 2
		ENDIF

;=================================================
;커뮤니케이션계 커맨드
;=================================================
;-------------------------------------------------
;20,키스한다
;-------------------------------------------------
	CASE 20
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:312 == 0
			CFLAG:312 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:312 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:312 = 4
			;순종Lv3 이상
			ENDIF
			IF ABL:0 >= 3 && (CFLAG:312 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:312 = 3
			;그 이외
			ENDIF
			IF (CFLAG:312 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:312 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;21,아무것도안한다
;-------------------------------------------------
	CASE 21
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:313 == 0
			CFLAG:313 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:313 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:313 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:313 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:313 = 3
			;그 이외
			ENDIF
			IF (CFLAG:313 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:313 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;22,회화　연모, 친애만
;-------------------------------------------------
	CASE 22
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:314 == 0
			CFLAG:314 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:314 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:314 = 2
		ENDIF
;-------------------------------------------------
;23,프레젠트　연모, 친애만
;-------------------------------------------------
	CASE 23
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:315 == 0
			CFLAG:315 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:315 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:315 = 2
		ENDIF

;-------------------------------------------------
;24,스킨쉽　연모, 친애만
;-------------------------------------------------
	CASE 24
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:316 == 0
			CFLAG:316 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:316 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:316 = 2
		ENDIF

;-------------------------------------------------
;25,느긋하게있는다　연모, 친애만
;-------------------------------------------------
	CASE 25
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:317 == 0
			CFLAG:317 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:317 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:317 = 2
		ENDIF

;-------------------------------------------------
;26,가사　연모, 친애만
;-------------------------------------------------
	CASE 26
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:318 == 0
			CFLAG:318 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:318 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:318 = 2
		ENDIF
;-------------------------------------------------
;27,노래연습　연모, 친애만
;-------------------------------------------------
	CASE 27
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:319 == 0
			CFLAG:319 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:319 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:319 = 2
		ENDIF

;-------------------------------------------------
;28,매도
;-------------------------------------------------
	CASE 28
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:320 == 0
			CFLAG:320 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:320 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:320 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:320 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:320 = 3
			;그 이외
			ENDIF
			IF (CFLAG:320 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:320 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;29,비디오감상　함락완료만
;-------------------------------------------------
	CASE 29
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:321 == 0
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
		CFLAG:321 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:321 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
		CFLAG:321 = 2
		ENDIF
;-------------------------------------------------
;30,파후파후　함락완료만
;-------------------------------------------------
	CASE 30
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:422 == 0
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
			CFLAG:422 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:422 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
			CFLAG:422 = 2
		ENDIF

;=================================================
;도구사용 커맨드
;=================================================
;40,로터
;-------------------------------------------------
	CASE 40
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:322 == 0
			CFLAG:322 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:322 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:322 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:322 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:322 = 3
			;그 이외
			ENDIF
			IF (CFLAG:322 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:322 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;41,전기마사지기
;-------------------------------------------------
	CASE 41
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:323 == 0
			CFLAG:323 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:323 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:323 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:323 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:323 = 3
			;그 이외
			ENDIF
			IF (CFLAG:323 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:323 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;42,클리캡
;-------------------------------------------------
	CASE 42
		IF TEQUIP:11
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:324 == 0
				CFLAG:324 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:324 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:324 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:324 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:324 = 3
				;그 이외
				ENDIF
				IF (CFLAG:324 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:324 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:324 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;43,오나홀
;-------------------------------------------------
	CASE 43
		IF TEQUIP:12
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:325 == 0
				CFLAG:325 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:325 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:325 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:325 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:325 = 3
				;그 이외
				ENDIF
				IF (CFLAG:325 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:325 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:325 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;44,바이브
;-------------------------------------------------
	CASE 44
	IF TEQUIP:13
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:326 == 0
			;처녀
			IF TALENT:0
			;그 이외
			ELSE
			ENDIF
			CFLAG:326 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:326 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:326 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:326 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:326 = 3
			;그 이외
			ENDIF
			IF (CFLAG:326 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:326 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:326 == 1 || FLAG:7 == 2
			K_CALL:K = TRAIN_MESSAGE
		ENDIF
	ENDIF

;-------------------------------------------------
;45,애널바이브
;-------------------------------------------------
	CASE 45
	IF TEQUIP:14
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:327 == 0
			CFLAG:327 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:327 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:327 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:327 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:327 = 3
			;그 이외
			ENDIF
			IF (CFLAG:327 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:327 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:327 == 1 || FLAG:7 == 2
			K_CALL:K = TRAIN_MESSAGE
		ENDIF
	ENDIF

;-------------------------------------------------
;46,애널비즈
;-------------------------------------------------
	CASE 46
	IF TEQUIP:15
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:328 == 0
			CFLAG:328 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:328 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:328 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:328 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:328 = 3
			;그 이외
			ENDIF
			IF (CFLAG:328 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:328 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:328 == 1 || FLAG:7 == 2
			K_CALL:K = TRAIN_MESSAGE
		ENDIF
	ENDIF

;-------------------------------------------------
;47,유두캡
;-------------------------------------------------
	CASE 47
		IF TEQUIP:16
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:329 == 0
				CFLAG:329 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:329 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:329 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:329 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:329 = 3
				;그 이외
				ENDIF
				IF (CFLAG:329 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:329 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:329 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;48,유두로터
;-------------------------------------------------
	CASE 48
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:330 == 0
			CFLAG:330 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:330 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:330 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:330 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:330 = 3
			;그 이외
			ENDIF
			IF (CFLAG:330 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:330 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;49,착유기
;-------------------------------------------------
	CASE 49
		IF TEQUIP:17
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:331 == 0
				CFLAG:331 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:331 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:331 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:331 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:331 = 3
				;그 이외
				ENDIF
				IF (CFLAG:331 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:331 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:331 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF
;=================================================
;섹스계 커맨드
;=================================================
;-------------------------------------------------
;60,정상위
;-------------------------------------------------
	CASE 60
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:332 == 0
			;처녀
			IF TALENT:0
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			;그 이외
			ELSE
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			ENDIF
			CFLAG:332 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:332 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:332 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:332 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:332 = 3
			;그 이외
			ENDIF
			IF (CFLAG:332 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:332 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;61,후배위
;-------------------------------------------------
	CASE 61
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:333 == 0
			;처녀
			IF TALENT:0
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			;그 이외
			ELSE
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			ENDIF
			CFLAG:333 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:333 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:333 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:333 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:333 = 3
			;그 이외
			ENDIF
			IF (CFLAG:333 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:333 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;62,정상위애널
;-------------------------------------------------
	CASE 62
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:334 == 0
			CFLAG:334 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:334 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:334 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:334 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:334 = 3
			;그 이외
			ENDIF
			IF (CFLAG:334 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:334 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;63,후배위애널
;-------------------------------------------------
	CASE 63
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:335 == 0
			CFLAG:335 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:335 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:335 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:335 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:335 = 3
			;그 이외
			ENDIF
			IF (CFLAG:335 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:335 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;64,역강간
;-------------------------------------------------
	CASE 64
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:336 == 0
			CFLAG:336 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:336 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:336 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:336 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:336 = 3
			;그 이외
			ENDIF
			IF (CFLAG:336 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:336 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;65,젖성교
;-------------------------------------------------
	CASE 65
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:337 == 0
			CFLAG:337 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:337 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:337 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:337 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:337 = 3
			;그 이외
			ENDIF
			IF (CFLAG:337 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:337 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;66,기승위
;-------------------------------------------------
	CASE 66
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:338 == 0
			;처녀
			IF TALENT:0
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			;그 이외
			ELSE
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			ENDIF
			CFLAG:338 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:338 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:338 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:338 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:338 = 3
			;그 이외
			ENDIF
			IF (CFLAG:338 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:338 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;67,기승위애널
;-------------------------------------------------
	CASE 67
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:339 == 0
			CFLAG:339 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:339 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:339 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:339 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:339 = 3
			;그 이외
			ENDIF
			IF (CFLAG:339 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:339 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;68,대면좌위
;-------------------------------------------------
	CASE 68
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:340 == 0
			;처녀
			IF TALENT:0
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			;그 이외
			ELSE
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			ENDIF
			CFLAG:340 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:340 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:340 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:340 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:340 = 3
			;그 이외
			ENDIF
			IF (CFLAG:340 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:340 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;69,배면좌위
;-------------------------------------------------
	CASE 69
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:341 == 0
			;처녀
			IF TALENT:0
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			;그 이외
			ELSE
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			ENDIF
			CFLAG:341 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:341 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:341 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:341 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:341 = 3
			;그 이외
			ENDIF
			IF (CFLAG:341 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:341 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;70,대면좌위애널
;-------------------------------------------------
	CASE 70
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:342 == 0
			CFLAG:342 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:342 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:342 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:342 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:342 = 3
			;그 이외
			ENDIF
			IF (CFLAG:342 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:342 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;71,배면좌위애널
;-------------------------------------------------
	CASE 71
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:343 == 0
			CFLAG:343 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:343 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:343 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:343 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:343 = 3
			;그 이외
			ENDIF
			IF (CFLAG:343 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:343 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;72,속옷비켜삽입
;-------------------------------------------------
	CASE 72
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:420 == 0
			;처녀
			IF TALENT:0
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			;그 이외
			ELSE
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				IF !구상유무확인(0)
				ENDIF
			ENDIF
			CFLAG:420 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:420 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:420 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:420 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:420 = 3
			;그 이외
			ENDIF
			IF (CFLAG:420 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:420 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;73,속옷비켜삽입애널
;-------------------------------------------------
	CASE 73
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:421 == 0
			CFLAG:421 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:421 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:421 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:421 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:421 = 3
			;그 이외
			ENDIF
			IF (CFLAG:421 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:421 = 2
			ENDIF
		ENDIF

;=================================================
;봉사계 커맨드
;=================================================
;-------------------------------------------------
;80,수음
;-------------------------------------------------
	CASE 80
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:344 == 0
			CFLAG:344 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:344 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:344 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:344 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:344 = 3
			;그 이외
			ENDIF
			IF (CFLAG:344 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:344 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;81,펠라치오
;-------------------------------------------------
	CASE 81
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:345 == 0
			CFLAG:345 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:345 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:345 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:345 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:345 = 3
			;그 이외
			ENDIF
			IF (CFLAG:345 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:345 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;82,파이즈리
;-------------------------------------------------
	CASE 82
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:346 == 0
			CFLAG:346 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:346 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:346 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:346 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:346 = 3
			;그 이외
			ENDIF
			IF (CFLAG:346 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:346 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;83,스마타
;-------------------------------------------------
	CASE 83
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:347 == 0
			CFLAG:347 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:347 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:347 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:347 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:347 = 3
			;그 이외
			ENDIF
			IF (CFLAG:347 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:347 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;84,거품춤
;-------------------------------------------------
	CASE 84
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:348 == 0
			CFLAG:348 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:348 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:348 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:348 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:348 = 3
			;그 이외
			ENDIF
			IF (CFLAG:348 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:348 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;85,풋잡
;-------------------------------------------------
	CASE 85
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:349 == 0
			CFLAG:349 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:349 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:349 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:349 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:349 = 3
			;그 이외
			ENDIF
			IF (CFLAG:349 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:349 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;86,다리핥기강제
;-------------------------------------------------
	CASE 86
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:350 == 0
			CFLAG:350 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:350 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:350 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:350 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:350 = 3
			;그 이외
			ENDIF
			IF (CFLAG:350 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:350 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;87,세로파이즈리
;-------------------------------------------------
	CASE 87
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:351 == 0
			CFLAG:351 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:351 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:351 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:351 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:351 = 3
			;그 이외
			ENDIF
			IF (CFLAG:351 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:351 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;88,파이즈리펠라
;-------------------------------------------------
	CASE 88
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:352 == 0
			CFLAG:352 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:352 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:352 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:352 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:352 = 3
			;그 이외
			ENDIF
			IF (CFLAG:352 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:352 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;89,모유마시기　함락완료만
;-------------------------------------------------
	CASE 89
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:353 == 0
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
		CFLAG:353 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:353 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
		CFLAG:353 = 2
		ENDIF

;=================================================
;조교자봉사계 커맨드
;=================================================
;수음한다
;-------------------------------------------------
	CASE 90
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:423 == 0
			CFLAG:423 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:423 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:423 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:423 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:423 = 3
			;그 이외
			ENDIF
			IF (CFLAG:423 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:423 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;파이즈리한다
;-------------------------------------------------
	CASE 91
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:424 == 0
			CFLAG:424 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:424 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:424 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:424 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:424 = 3
			;그 이외
			ENDIF
			IF (CFLAG:424 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:424 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;정상위시킨다
;-------------------------------------------------
	CASE 92
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:425 == 0
			CFLAG:425 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:425 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:425 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:425 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:425 = 3
			;그 이외
			ENDIF
			IF (CFLAG:425 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:425 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;후배위시킨다
;-------------------------------------------------
	CASE 93
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:426 == 0
			CFLAG:426 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:426 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:426 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:426 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:426 = 3
			;그 이외
			ENDIF
			IF (CFLAG:426 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:426 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;기승위한다
;-------------------------------------------------
	CASE 94
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:427 == 0
			CFLAG:427 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:427 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:427 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:427 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:427 = 3
			;그 이외
			ENDIF
			IF (CFLAG:427 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:427 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;애널정상위시킨다
;-------------------------------------------------
	CASE 95
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:428 == 0
			CFLAG:428 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:428 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:428 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:428 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:428 = 3
			;그 이외
			ENDIF
			IF (CFLAG:428 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:428 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;애널후배위시킨다
;-------------------------------------------------
	CASE 96
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:429 == 0
			CFLAG:429 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:429 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:429 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:429 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:429 = 3
			;그 이외
			ENDIF
			IF (CFLAG:429 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:429 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;애널기승위한다
;-------------------------------------------------
	CASE 97
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:430 == 0
			CFLAG:430 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:430 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:430 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:430 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:430 = 3
			;그 이외
			ENDIF
			IF (CFLAG:430 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:430 = 2
			ENDIF
		ENDIF
;=================================================
;SM계 커맨드
;=================================================
;-------------------------------------------------
;100,스팽킹
;-------------------------------------------------
	CASE 100
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:354 == 0
			CFLAG:354 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:354 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:354 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:354 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:354 = 3
			;그 이외
			ENDIF
			IF (CFLAG:354 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:354 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;101,젖스팽킹
;-------------------------------------------------
	CASE 101
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:355 == 0
			CFLAG:355 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:355 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:355 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:355 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:355 = 3
			;그 이외
			ENDIF
			IF (CFLAG:355 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:355 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;102,채찍
;-------------------------------------------------
	CASE 102
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:356 == 0
			CFLAG:356 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:356 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:356 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:356 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:356 = 3
			;그 이외
			ENDIF
			IF (CFLAG:356 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:356 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;103,양초
;-------------------------------------------------
	CASE 103
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:357 == 0
			CFLAG:357 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:357 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:357 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:357 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:357 = 3
			;그 이외
			ENDIF
			IF (CFLAG:357 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:357 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;104,바늘
;-------------------------------------------------
	CASE 104
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:358 == 0
			CFLAG:358 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:358 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:358 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:358 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:358 = 3
			;그 이외
			ENDIF
			IF (CFLAG:358 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:358 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;105,아이마스크
;-------------------------------------------------
	CASE 105
		IF TEQUIP:18
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:359 == 0
				CFLAG:359 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:359 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:359 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:359 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:359 = 3
				;그 이외
				ENDIF
				IF (CFLAG:359 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:359 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:359 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;106,밧줄
;-------------------------------------------------
	CASE 106
		IF TEQUIP:19
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:360 == 0
				CFLAG:360 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:360 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:360 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:360 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:360 = 3
				;그 이외
				ENDIF
				IF (CFLAG:360 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:360 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:360 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;107,볼재갈
;-------------------------------------------------
	CASE 107
		IF TEQUIP:20
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:361 == 0
				CFLAG:361 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:361 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:361 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:361 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:361 = 3
				;그 이외
				ENDIF
				IF (CFLAG:361 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:361 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:361 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;=================================================
;조수에 의한 커맨드, 및 레즈플레이
;=================================================
;-------------------------------------------------
;120,커닐링구스강제
;-------------------------------------------------
	CASE 120
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:362 == 0
			CFLAG:362 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:362 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:362 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:362 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:362 = 3
			;그 이외
			ENDIF
			IF (CFLAG:362 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:362 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;121,조개맞대기
;-------------------------------------------------
	CASE 121
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:363 == 0
			CFLAG:363 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:363 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:363 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:363 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:363 = 3
			;그 이외
			ENDIF
			IF (CFLAG:363 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:363 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;122,조수를범하게한다
;-------------------------------------------------
	CASE 122
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:364 == 0
			CFLAG:364 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:364 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:364 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:364 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:364 = 3
			;그 이외
			ENDIF
			IF (CFLAG:364 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:364 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;123,풋잡한다
;-------------------------------------------------
	CASE 123
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:365 == 0
			CFLAG:365 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:365 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:365 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:365 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:365 = 3
			;그 이외
			ENDIF
			IF (CFLAG:365 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:365 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;124,더블펠라
;-------------------------------------------------
	CASE 124
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:366 == 0
			CFLAG:366 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:366 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:366 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:366 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:366 = 3
			;그 이외
			ENDIF
			IF (CFLAG:366 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:366 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;125,더블파이즈리
;-------------------------------------------------
	CASE 125
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:367 == 0
			CFLAG:367 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:367 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:367 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:367 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:367 = 3
			;그 이외
			ENDIF
			IF (CFLAG:367 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:367 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;126,가슴맞대기
;-------------------------------------------------
	CASE 126
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:368 == 0
			CFLAG:368 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:368 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:368 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:368 = 3
			;그 이외
			ENDIF
			IF (CFLAG:368 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:368 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;127,자위과시
;-------------------------------------------------
	CASE 127
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:432 == 0
			CFLAG:432 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:432 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:368 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:432 = 3
			;그 이외
			ENDIF
			IF (CFLAG:432 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:432 = 2
			ENDIF
		ENDIF

;=================================================
;하드한조교 커맨드
;=================================================
;-------------------------------------------------
;140,이라마치오
;-------------------------------------------------
	CASE 140
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:369 == 0
			CFLAG:369 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:369 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:369 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:369 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:369 = 3
			;그 이외
			ENDIF
			IF (CFLAG:369 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:369 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;141,피스트퍽　복종, 예속만
;-------------------------------------------------
	CASE 141
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:370 == 0
			CFLAG:370 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:370 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:370 = 2
		ENDIF

;-------------------------------------------------
;142,애널피스트　복종, 예속만
;-------------------------------------------------
	CASE 142
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:371 == 0
			CFLAG:371 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:371 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:371 = 2
		ENDIF

;-------------------------------------------------
;143,양구멍피스트　복종, 예속만
;-------------------------------------------------
	CASE 143
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:372 == 0
			CFLAG:372 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:372 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:372 = 2
		ENDIF

;-------------------------------------------------
;144,방뇨
;-------------------------------------------------
	CASE 144
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:373 == 0
			CFLAG:373 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:373 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:373 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:373 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:373 = 3
			;그 이외
			ENDIF
			IF (CFLAG:373 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:373 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;145,애널봉사강제
;-------------------------------------------------
	CASE 145
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:374 == 0
			CFLAG:374 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:374 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:374 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:374 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:374 = 3
			;그 이외
			ENDIF
			IF (CFLAG:374 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:374 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;146,관장기＋플러그
;-------------------------------------------------
	CASE 146
		IF TEQUIP:21
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:375 == 0
				CFLAG:375 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:375 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:375 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:375 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:375 = 3
				;그 이외
				ENDIF
				IF (CFLAG:375 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:375 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:375 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;147,공기관장
;-------------------------------------------------
	CASE 147
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:376 == 0
			CFLAG:376 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:376 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:376 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:376 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:376 = 3
			;그 이외
			ENDIF
			IF (CFLAG:376 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:376 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;148,확장벌룬
;-------------------------------------------------
	CASE 148
		IF TEQUIP:22
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:377 == 0
				CFLAG:377 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:377 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:377 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:377 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:377 = 3
				;그 이외
				ENDIF
				IF (CFLAG:377 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:377 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:377 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;149,애널전극　복종, 예속만
;-------------------------------------------------
	CASE 149
		IF TEQUIP:23
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:378 == 0
				CFLAG:378 = 1
			;2번째 이후
			ENDIF
			IF (CFLAG:378 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:378 = 2
			ENDIF
		ELSE
			IF CFLAG:378 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;150,유방전극　복종, 예속만
;-------------------------------------------------
	CASE 150
		IF TEQUIP:24
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:379 == 0
				CFLAG:379 = 1
			;2번째 이후
			ENDIF
			IF (CFLAG:379 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:379 = 2
			ENDIF
		ELSE
			IF CFLAG:379 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF
;-------------------------------------------------
;151,뱀장어욕탕　복종, 예속만
;-------------------------------------------------
	CASE 151
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:380 == 0
			;처녀
			IF TALENT:0
			;그 이외
			ELSE
			ENDIF
			CFLAG:380 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:380 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:380 = 2
		ENDIF

;-------------------------------------------------
;152,벌레욕탕　복종, 예속만
;-------------------------------------------------
	CASE 152
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:381 == 0
			CFLAG:381 = 1
		;2번째 이후
		ENDIF
		IF (CFLAG:381 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:381 = 2
		ENDIF

;=================================================
;촉수계 커맨드
;=================================================
;-------------------------------------------------
;160,촉수소환
;-------------------------------------------------
	CASE 160
		IF TEQUIP:25
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:382 == 0
				CFLAG:382 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:382 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:382 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:382 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:382 = 3
				;그 이외
				ENDIF
				IF (CFLAG:382 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:382 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:382 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;161,촉수삽입
;-------------------------------------------------
	CASE 161
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:383 == 0
			;처녀
			IF TALENT:0
			;그 이외
			ELSE
			ENDIF
			CFLAG:383 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:383 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:383 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:383 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:383 = 3
			;그 이외
			ENDIF
			IF (CFLAG:383 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:383 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;162,애널촉수
;-------------------------------------------------
	CASE 162
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:384 == 0
			CFLAG:384 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:384 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:384 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:384 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:384 = 3
			;그 이외
			ENDIF
			IF (CFLAG:384 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:384 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;163,촉수클리자극
;-------------------------------------------------
	CASE 163
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:385 == 0
			CFLAG:385 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:385 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:385 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:385 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:385 = 3
			;그 이외
			ENDIF
			IF (CFLAG:385 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:385 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;164,촉수유두자극
;-------------------------------------------------
	CASE 164
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:386 == 0
			CFLAG:386 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:386 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:386 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:386 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:386 = 3
			;그 이외
			ENDIF
			IF (CFLAG:386 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:386 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;165,촉수착유
;-------------------------------------------------
	CASE 165
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:387 == 0
			CFLAG:387 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:387 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:387 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:387 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:387 = 3
			;그 이외
			ENDIF
			IF (CFLAG:387 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:387 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;166,촉수긴박
;-------------------------------------------------
	CASE 166
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:388 == 0
			CFLAG:388 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:388 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:388 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:388 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:388 = 3
			;그 이외
			ENDIF
			IF (CFLAG:388 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:388 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;167,촉수관장
;-------------------------------------------------
	CASE 167
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:389 == 0
			CFLAG:389 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:389 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:389 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:389 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:389 = 3
			;그 이외
			ENDIF
			IF (CFLAG:389 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:389 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;168,촉수구욕
;-------------------------------------------------
	CASE 168
		IF TEQUIP:36
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:390 == 0
				CFLAG:390 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:390 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:390 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:390 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:390 = 3
				;그 이외
				ENDIF
				IF (CFLAG:390 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:390 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:390 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;169,촉수페니스자극
;-------------------------------------------------
	CASE 169
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:391 == 0
			CFLAG:391 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:391 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:391 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:391 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:391 = 3
			;그 이외
			ENDIF
			IF (CFLAG:391 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:391 = 2
			ENDIF
		ENDIF

;=================================================
;특수커맨드
;=================================================
;-------------------------------------------------
;180,로션,
;-------------------------------------------------
	CASE 180
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:392 == 0
			CFLAG:392 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:392 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:392 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:392 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:392 = 3
			;그 이외
			ENDIF
			IF (CFLAG:392 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:392 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;181,미약,
;-------------------------------------------------
	CASE 181
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:393 == 0
			CFLAG:393 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:393 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:393 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:393 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:393 = 3
			;그 이외
			ENDIF
			IF (CFLAG:393 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:393 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;182,이뇨제,
;-------------------------------------------------
	CASE 182
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:394 == 0
			CFLAG:394 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:394 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:394 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:394 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:394 = 3
			;그 이외
			ENDIF
			IF (CFLAG:394 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:394 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;183,비디오카메라,
;-------------------------------------------------
	CASE 183
		IF TEQUIP:28
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:395 == 0
				CFLAG:395 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:395 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:395 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:395 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:395 = 3
				;그 이외
				ENDIF
				IF (CFLAG:395 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:395 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:395 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;184,야외플레이,
;-------------------------------------------------
	CASE 184
		IF TEQUIP:29
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:396 == 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:396 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:396 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				;그 이외
				ENDIF
				IF (CFLAG:396 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				ENDIF
			ENDIF
		ELSE
		;TEQUIP:29로 어느 장소를 선택해도 같은 구상이 나오도록  변경･장소마다 바꾸고 싶으면 == 로 하고 숫자를 만지기
			IF CFLAG:396 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
				;처음
				IF CFLAG:396 == 0
					CFLAG:396 = 1
				;2번째 이후
				ELSE
					;함락완료
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:396 < 4 || FLAG:7 == 2)
						;연모, 친애
						IF TALENT:3 || TALENT:6
						;음란, 창부
						ELSEIF TALENT:4 || TALENT:7
						;복종, 예속
						ELSEIF TALENT:5 || TALENT:8
						ENDIF
						CFLAG:396 = 4
					;굴복각인Lv3
					ENDIF
					IF MARK:2 == 3 && (CFLAG:396 < 3 || FLAG:7 == 2) && !구상유무확인(0)
						CFLAG:396 = 3
					;그 이외
					ENDIF
					IF (CFLAG:396 < 2 || FLAG:7 == 2) && !구상유무확인(0)
						CFLAG:396 = 2
					ENDIF
				ENDIF
			ENDIF
		ENDIF

;-------------------------------------------------
;185,조수를범한다,
;-------------------------------------------------
	CASE 185
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:397 == 0
			CFLAG:397 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:397 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:397 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:397 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:397 = 3
			;그 이외
			ENDIF
			IF (CFLAG:397 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:397 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;186,수치플레이,
;-------------------------------------------------
	CASE 186
		IF TEQUIP:30
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:398 == 0
				CFLAG:398 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:398 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:398 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:398 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:398 = 3
				;그 이외
				ENDIF
				IF (CFLAG:398 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:398 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:398 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;187,목욕탕플레이,
;-------------------------------------------------
	CASE 187
		IF TEQUIP:31
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:399 == 0
				CFLAG:399 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:399 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:399 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:399 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:399 = 3
				;그 이외
				ENDIF
				IF (CFLAG:399 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:399 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:399 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;188,샤워,
;-------------------------------------------------
	CASE 188
		IF TEQUIP:32
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:400 == 0
				CFLAG:400 = 1
			;2번째 이후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:400 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:400 = 4
				;굴복각인Lv3
				ENDIF
				IF MARK:2 == 3 && (CFLAG:400 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:400 = 3
				;그 이외
				ENDIF
				IF (CFLAG:400 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:400 = 2
				ENDIF
			ENDIF
		ELSE
			IF CFLAG:400 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;189,새댁플레이,　연모, 친애만
;-------------------------------------------------
	CASE 189
		IF TEQUIP:33
			K_CALL:K = TRAIN_MESSAGE
			;처음
			IF CFLAG:401 == 0
				CFLAG:401 = 1
			;2번째 이후
			ENDIF
			IF (CFLAG:401 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:401 = 2
			ENDIF
		ELSE
			IF CFLAG:401 == 1 || FLAG:7 == 2
				K_CALL:K = TRAIN_MESSAGE
			ENDIF
		ENDIF

;-------------------------------------------------
;190,사진촬영,
;-------------------------------------------------
	CASE 190
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:402 == 0
			CFLAG:402 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:402 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:402 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:402 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:402 = 3
			;그 이외
			ENDIF
			IF (CFLAG:402 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:402 = 2
			ENDIF
		ENDIF


;-------------------------------------------------
;191,강정신약,
;-------------------------------------------------
	CASE 191 
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:403 == 0
			CFLAG:403 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:403 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:403 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:403 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:403 = 3
			;그 이외
			ENDIF
			IF (CFLAG:403 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:403 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;192,콘돔정음（노예）
;-------------------------------------------------
	CASE 192
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:404 == 0
			CFLAG:404 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:404 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:404 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:404 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:404 = 3
			;그 이외
			ENDIF
			IF (CFLAG:404 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:404 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;193,콘돔정음（조수）
;-------------------------------------------------
	CASE 193
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:405 == 0
			CFLAG:405 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:405 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:405 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:405 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:405 = 3
			;그 이외
			ENDIF
			IF (CFLAG:405 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:405 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;194,정액키스
;-------------------------------------------------
	CASE 194
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:406 == 0
			CFLAG:406 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:406 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:406 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:406 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:406 = 3
			;그 이외
			ENDIF
			IF (CFLAG:406 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:406 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;195,유선개발
;-------------------------------------------------
	CASE 195
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:431 == 0
			CFLAG:431 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:431 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:368 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:431 = 3
			;그 이외
			ENDIF
			IF (CFLAG:431 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:431 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;196,미약쓴다
;-------------------------------------------------
	CASE 196
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:437 == 0
			CFLAG:437 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:437 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:368 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:437 = 3
			;그 이외
			ENDIF
			IF (CFLAG:437 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:437 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;197,팬티훑기
;-------------------------------------------------
	CASE 197
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:438 == 0
			CFLAG:438 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:438 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:368 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:438 = 3
			;그 이외
			ENDIF
			IF (CFLAG:438 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:438 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;200,사람을부른다
;-------------------------------------------------
	CASE 200
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:433 == 0
			CFLAG:433 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:433 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:433 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:433 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:433 = 3
			;그 이외
			ENDIF
			IF (CFLAG:433 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:433 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;201,윤간
;-------------------------------------------------
	CASE 201
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:434 == 0
			CFLAG:434 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:434 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:434 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:434 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:434 = 3
			;그 이외
			ENDIF
			IF (CFLAG:434 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:434 = 2
			ENDIF
		ENDIF
;=================================================
;500번대,파생커맨드
;=================================================
;-------------------------------------------------
;500,３Ｐ
;-------------------------------------------------
	CASE 500
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:407 == 0
			;처녀
			IF TALENT:0
			;그 이외
			ELSE
			ENDIF
			CFLAG:407 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:407 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:407 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:407 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:407 = 3
			;그 이외
			ENDIF
			IF (CFLAG:407 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:407 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;501,식스나인
;-------------------------------------------------
	CASE 501
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:408 == 0
			CFLAG:408 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:408 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:408 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:408 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:408 = 3
			;그 이외
			ENDIF
			IF (CFLAG:408 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:408 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;502,이와시미즈
;-------------------------------------------------
	CASE 502
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:409 == 0
			CFLAG:409 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:409 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:409 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:409 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:409 = 3
			;그 이외
			ENDIF
			IF (CFLAG:409 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:409 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;503,G스팟자극
;-------------------------------------------------
	CASE 503
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:410 == 0
			CFLAG:410 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:410 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:410 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:410 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:410 = 3
			;그 이외
			ENDIF
			IF (CFLAG:410 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:410 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;504,흐트러진모란
;-------------------------------------------------
	CASE 504
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:411 == 0
			;처녀
			IF TALENT:0
			;그 이외
			ELSE
			ENDIF
			CFLAG:411 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:411 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:411 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:411 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:411 = 3
			;그 이외
			ENDIF
			IF (CFLAG:411 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:411 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;505,기승유방자위
;-------------------------------------------------
	CASE 505
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:412 == 0
			;처녀
			IF TALENT:0
			;그 이외
			ELSE
			ENDIF
			CFLAG:412 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:412 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:412 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:412 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:412 = 3
			;그 이외
			ENDIF
			IF (CFLAG:412 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:412 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;506,삽입Ｇ스팟자극
;-------------------------------------------------
	CASE 506
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:413 == 0
			CFLAG:413 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:413 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:413 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:413 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:413 = 3
			;그 이외
			ENDIF
			IF (CFLAG:413 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:413 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;507,삽입자궁구자극
;-------------------------------------------------
	CASE 507
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:414 == 0
			CFLAG:414 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:414 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:414 = 3
			;그 이외
			ENDIF
			IF (CFLAG:414 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:414 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;508,６９파이즈리
;-------------------------------------------------
	CASE 508
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:415 == 0
			CFLAG:415 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:415 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:415 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:415 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:415 = 3
			;그 이외
			ENDIF
			IF (CFLAG:415 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:415 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;509,착유
;-------------------------------------------------
	CASE 509
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:416 == 0
			CFLAG:416 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:416 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:416 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:416 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:416 = 3
			;그 이외
			ENDIF
			IF (CFLAG:416 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:416 = 2
			ENDIF
		ENDIF

;-------------------------------------------------
;510,더블스마타
;-------------------------------------------------
	CASE 510
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:417 == 0
			CFLAG:417 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:417 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:417 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:417 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:417 = 3
			;그 이외
			ENDIF
			IF (CFLAG:417 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:417 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;511,두개펠라
;-------------------------------------------------
	CASE 511
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:418 == 0
			CFLAG:418 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:418 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:418 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:418 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:418 = 3
			;그 이외
			ENDIF
			IF (CFLAG:418 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:418 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;512,애널기승유방자위
;-------------------------------------------------
	CASE 512
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:419 == 0
			CFLAG:419 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:419 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:419 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:419 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:419 = 3
			;그 이외
			ENDIF
			IF (CFLAG:419 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:419 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;513,입과바기나를범한다(３Ｐ派生)
;-------------------------------------------------
	CASE 513
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:439 == 0
			CFLAG:439 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:439 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:439 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:439 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:439 = 3
			;그 이외
			ENDIF
			IF (CFLAG:439 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:439 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;514,입과애널을범한다(３Ｐ派生)
;-------------------------------------------------
	CASE 514
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:440 == 0
			CFLAG:440 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:440 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:440 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:440 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:440 = 3
			;그 이외
			ENDIF
			IF (CFLAG:440 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:440 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;515,애널흐트러진모란
;-------------------------------------------------
	CASE 515
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:441 == 0
			CFLAG:441 = 1
		;2번째 이후
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:441 < 4 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:441 = 4
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:441 < 3 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:441 = 3
			;그 이외
			ENDIF
			IF (CFLAG:441 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:441 = 2
			ENDIF
		ENDIF
;------------------------------------------------
;커맨드구상 여기까지
;------------------------------------------------
	CASEELSE
		SIF FLAG:6 > 0
			K_CALL:K = TRAIN_MESSAGE
ENDSELECT

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG 관련（4513을 캐릭터번호로 치환）
;파라미터 변화 등을 트리거로 한 구상　CFLAG 221～260을 사용
;-------------------------------------------------
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_A（4513을 캐릭터번호로 치환）
;조교자의 사정을 트리거로 한 구상
;-------------------------------------------------
$表示
CALL 구상변수표시
SIF RESULT < 1
	CALL TRAIN_MESSAGE
@KOJO_MESSAGE_PALAMCNG_A_K4513_1
CALL 구상변수초기화
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_A
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K4513_1
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K4513_1
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교자의 사정시의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;사정장소 변경시의 텍스트　유효시 여기서 함수 종료
;-------------------------------------------------
IF CFLAG:106
	K_CALL:K = PALAM_MESSAGE_A
	;머리카락
	IF CFLAG:106 == 1
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:221 < 3 || FLAG:7 == 2)
			CFLAG:221 = 3
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:221 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:221 = 2
		;그 이외
		ENDIF
		IF (CFLAG:221 < 1 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:221 = 1
		ENDIF
	;얼굴
	ENDIF
	IF CFLAG:106 == 2 && !구상유무확인(0)
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:222 < 3 || FLAG:7 == 2)
			CFLAG:222 = 3
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:222 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:222 = 2
		;그 이외
		ENDIF
		IF (CFLAG:222 < 1 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:222 = 1
		ENDIF
	;가슴
	ENDIF
	IF CFLAG:106 == 3 && !구상유무확인(0)
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:223 < 3 || FLAG:7 == 2)
			CFLAG:223 = 3
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:223 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:223 = 2
		;그 이외
		ENDIF
		IF (CFLAG:223 < 1 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:223 = 1
		ENDIF
	;배
	ENDIF
	IF CFLAG:106 == 4 && !구상유무확인(0)
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:224 < 3 || FLAG:7 == 2)
			CFLAG:224 = 3
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:224 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:224 = 2
		;그 이외
		ENDIF
		IF (CFLAG:224 < 1 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:224 = 1
		ENDIF
	;겨드랑이
	ENDIF
	IF CFLAG:106 == 5 && !구상유무확인(0)
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:225 < 3 || FLAG:7 == 2)
			CFLAG:225 = 3
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:225 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:225 = 2
		;그 이외
		ENDIF
		IF (CFLAG:225 < 1 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:225 = 1
		ENDIF
	;허벅지
	ENDIF
	IF CFLAG:106 == 6 && !구상유무확인(0)
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:226 < 3 || FLAG:7 == 2)
			CFLAG:226 = 3
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:226 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:226 = 2
		;그 이외
		ENDIF
		IF (CFLAG:226 < 1 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:226 = 1
		ENDIF
	;가랑이
	ENDIF
	IF CFLAG:106 == 7 && !구상유무확인(0)
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:227 < 3 || FLAG:7 == 2)
			CFLAG:227 = 3
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:227 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:227 = 2
		;그 이외
		ENDIF
		IF (CFLAG:227 < 1 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:227 = 1
		ENDIF
	;엉덩이
	ENDIF
	IF CFLAG:106 == 8 && !구상유무확인(0)
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:228 < 3 || FLAG:7 == 2)
			CFLAG:228 = 3
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:228 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:228 = 2
		;그 이외
		ENDIF
		IF (CFLAG:228 < 1 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:228 = 1
		ENDIF
	;콘돔
	ENDIF
	IF CFLAG:106 == 9 && !구상유무확인(0)
		;섹스로 사정하고, 애널섹스 이외를 실행 시
		IF GROUPMATCH(SELECTCOM , 60 , 61 , 66 , 68 , 69 , 72, 504,505,506,507)
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:229 < 3 || FLAG:7 == 2)
				CFLAG:229 = 3
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:229 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:229 = 2
			;그 이외
			ENDIF
			IF (CFLAG:229 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:229 = 1
			ENDIF
		ELSE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)
				CFLAG:230 = 3
			;굴복각인Lv3
			ENDIF
			IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:230 = 2
			;그 이외
			ENDIF
			IF (CFLAG:230 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:230 = 1
			ENDIF
		ENDIF
	ENDIF
	GOTO 表示
ENDIF


;-------------------------------------------------
;손, 입 이외로 사정
;대응 커맨드는 키스, 스마타, 풋잡, 애널강제봉사, 더블스마타, 조수를범하게한다
;-------------------------------------------------
IF TFLAG:9 == 1
	K_CALL:K = PALAM_MESSAGE_A
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:231 < 4 || FLAG:7 == 2)
		;연모, 친애
		IF TALENT:3 || TALENT:6
		;음란, 창부
		ELSEIF TALENT:4 || TALENT:7
		;복종, 예속
		ELSEIF TALENT:5 || TALENT:8
		ENDIF
		CFLAG:231 = 4
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:231 < 3 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:231 = 3
	;그 이외
	ENDIF
	IF (CFLAG:231 < 2 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:231 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;조수를범한다로 사정
;-------------------------------------------------
IF TFLAG:7 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:232 < 3 || FLAG:7 == 2)
		CFLAG:232 = 3
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:232 = 2
	;그 이외
	ENDIF
	IF (CFLAG:232 < 1 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:232 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;입으로 사정
;-------------------------------------------------
IF TFLAG:0 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;이라마치오로 사정
	IF SELECTCOM == 140
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 4 || FLAG:7 == 2)
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
				WSTR:(K++) = 。
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
			CFLAG:233 = 4
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:233 < 3 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:233 = 3
		;그 이외
		ENDIF
		IF (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:233 = 2
		ENDIF
	;그 이외의 입사정
	ELSE
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 4 || FLAG:7 == 2)
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
			CFLAG:233 = 4
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:233 < 3 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:233 = 3
		;그 이외
		ENDIF
		IF (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:233 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;손으로 사정
;-------------------------------------------------
IF TFLAG:1 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:234 < 4 || FLAG:7 == 2)
		;연모, 친애
		IF TALENT:3 || TALENT:6
		;음란, 창부
		ELSEIF TALENT:4 || TALENT:7
		;복종, 예속
		ELSEIF TALENT:5 || TALENT:8
		ENDIF
		CFLAG:234 = 4
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:234 < 3 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:234 = 3
	;그 이외
	ENDIF
	IF (CFLAG:234 < 2 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:234 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;가슴으로 사정
;-------------------------------------------------
IF TFLAG:34 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:235 < 4 || FLAG:7 == 2)
		;연모, 친애
		IF TALENT:3 || TALENT:6
		;음란, 창부
		ELSEIF TALENT:4 || TALENT:7
		;복종, 예속
		ELSEIF TALENT:5 || TALENT:8
		ENDIF
		CFLAG:235 = 4
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:235 < 3 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:235 = 3
	;그 이외
	ENDIF
	IF (CFLAG:235 < 2 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:235 = 2
	ENDIF
ENDIF
;-------------------------------------------------
;오나홀에 사정
;-------------------------------------------------
IF TFLAG:16 == 1
	K_CALL:K = PALAM_MESSAGE_A
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:231 < 4 || FLAG:7 == 2)
		;연모, 친애
		IF TALENT:3 || TALENT:6
		;음란, 창부
		ELSEIF TALENT:4 || TALENT:7
		;복종, 예속
		ELSEIF TALENT:5 || TALENT:8
		ENDIF
		CFLAG:236 = 4
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:231 < 3 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:236 = 3
	;그 이외
	ENDIF
	IF (CFLAG:231 < 2 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:236 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;섹스로 사정
;-------------------------------------------------
IF TFLAG:2 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;애널섹스계
	IF SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || SELECTCOM == 512
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:237 < 4 || FLAG:7 == 2)
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
			CFLAG:237 = 4
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:237 < 3 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:237 = 3
		;그 이외
		ENDIF
		IF (CFLAG:237 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:237 = 2
		ENDIF
	;안쪽 자극 섹스계
	ELSEIF SELECTCOM == 506 || SELECTCOM == 507
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:238 < 4 || FLAG:7 == 2)
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
			CFLAG:238 = 4
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:238 < 3 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:238 = 3
		;그 이외
		ENDIF
		IF (CFLAG:238 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:238 = 2
		ENDIF
	;그 외 섹스계
	ELSE
		;함락완료
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:239 < 4 || FLAG:7 == 2)
			;연모, 친애
			IF TALENT:3 || TALENT:6
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
			ENDIF
			CFLAG:239 = 4
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && (CFLAG:239 < 3 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:239 = 3
		;그 이외
		ENDIF
		IF (CFLAG:239 < 2 || FLAG:7 == 2) && !구상유무확인(0)
			CFLAG:239 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;역강간으로 사정시켰다
;-------------------------------------------------
IF SELECTCOM == 64 && TFLAG:10 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:240 < 3 || FLAG:7 == 2)
		CFLAG:240 = 3
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:240 = 2
	;그 이외
	ENDIF
	IF (CFLAG:240 < 1 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:240 = 1
	ENDIF
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_E（4513을 캐릭터번호로 치환）
;촉수의 사정을 트리거로 한 구상
;-------------------------------------------------
$表示
CALL 구상변수표시
SIF RESULT < 1
	CALL PALAM_MESSAGE_A
@KOJO_MESSAGE_PALAMCNG_E_K4513_1
CALL 구상변수초기화
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_E
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K4513_1
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K4513_1
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;촉수의 사정시의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;촉수가 사정
;-------------------------------------------------
IF TFLAG:15 >0
	K_CALL:K = PALAM_MESSAGE_E
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:241 < 3 || FLAG:7 == 2)
		CFLAG:241 = 3
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:241 < 2 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:241 = 2
	;그 이외
	ENDIF
	IF (CFLAG:241 < 1 || FLAG:7 == 2) && !구상유무확인(0)
		CFLAG:241 = 1
	ENDIF
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_B 관련（4513을 캐릭터번호로 치환）
;노예의 절정을 트리거로 한 구상
;-------------------------------------------------
$表示
CALL 구상변수표시
SIF RESULT < 1
	CALL PALAM_MESSAGE_E
@KOJO_MESSAGE_PALAMCNG_B_K4513_1
CALL 구상변수초기화
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_B
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K4513_1
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K4513_1
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;노예의 절정시의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;처음 C절정
;-------------------------------------------------
LOCAL = K
IF NOWEX:0 > 0 && CFLAG:242 == 0
	CFLAG:242 = 1
ENDIF
SIF 구상유무확인(LOCAL) && K > 0
	CALL WSTR기입,K - 1

;-------------------------------------------------
;처음 V절정
;-------------------------------------------------
LOCAL = K
IF NOWEX:1 > 0 && CFLAG:243 == 0
	CFLAG:243 = 1
ENDIF
SIF 구상유무확인(LOCAL) && K > 0
	CALL WSTR기입,K - 1

;-------------------------------------------------
;처음 A절정
;-------------------------------------------------
LOCAL = K
IF NOWEX:2 > 0 && CFLAG:244 == 0
	CFLAG:244 = 1
ENDIF
SIF 구상유무확인(LOCAL) && K > 0
	CALL WSTR기입,K - 1

;-------------------------------------------------
;처음 B절정
;-------------------------------------------------
LOCAL = K
IF NOWEX:3 > 0 && CFLAG:245 == 0
	CFLAG:245 = 1
ENDIF
SIF 구상유무확인(LOCAL) && K > 0
	CALL WSTR기입,K - 1

;-------------------------------------------------
;절정
;-------------------------------------------------
;강절정
LOCAL = K
IF TFLAG:29 > 1
	K_CALL:K = PALAM_MESSAGE_B
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:246 < 4 || FLAG:7 == 2)
		;연모, 친애
		IF TALENT:3 || TALENT:6
		;음란, 창부
		ELSEIF TALENT:4 || TALENT:7
		;복종, 예속
		ELSEIF TALENT:5 || TALENT:8
		ENDIF
		CFLAG:246 = 4
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:246 < 3 || FLAG:7 == 2) && !구상유무확인(LOCAL)
		CFLAG:246 = 3
	;그 이외
	ENDIF
	IF (CFLAG:246 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)
		CFLAG:246 = 2
	ENDIF
;절정
ELSEIF TFLAG:29 > 0
	K_CALL:K = PALAM_MESSAGE_B
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:246 < 4 || FLAG:7 == 2)
		;연모, 친애
		IF TALENT:3 || TALENT:6
		;음란, 창부
		ELSEIF TALENT:4 || TALENT:7
		;복종, 예속
		ELSEIF TALENT:5 || TALENT:8
		ENDIF
		CFLAG:246 = 4
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:246 < 3 || FLAG:7 == 2) && !구상유무확인(LOCAL)
		CFLAG:246 = 3
	;그 이외
	ENDIF
	IF (CFLAG:246 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)
		CFLAG:246 = 2
	ENDIF
ENDIF
SIF 구상유무확인(LOCAL) && K > 0
	CALL WSTR기입,K - 1

;-------------------------------------------------
;사정
;-------------------------------------------------
;사정하고 있다
LOCAL = K
IF TFLAG:10 > 0
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:247 < 3 || FLAG:7 == 2)
		CFLAG:247 = 3
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:247 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)
		CFLAG:247 = 2
	;그 이외
	ENDIF
	IF (CFLAG:247 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)
		CFLAG:247 = 1
	ENDIF
ENDIF
SIF 구상유무확인(LOCAL) && K > 0
	CALL WSTR기입,K - 1

;-------------------------------------------------
;분유
;-------------------------------------------------
;분유하고 있다
LOCAL = K
IF TFLAG:11 > 0
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:248 < 3 || FLAG:7 == 2)
		CFLAG:248 = 3
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:248 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)
		CFLAG:248 = 2
	;그 이외
	ENDIF
	IF (CFLAG:248 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)
		CFLAG:248 = 1
	ENDIF
ENDIF
SIF 구상유무확인(LOCAL) && K > 0
	CALL WSTR기입,K - 1

;-------------------------------------------------
;방뇨
;-------------------------------------------------
;방뇨하고 있다
LOCAL = K
IF TFLAG:29 > 0 && (TEQUIP:27 || TALENT:57)
	;함락완료
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:249 < 3 || FLAG:7 == 2)
		CFLAG:249 = 3
	;굴복각인Lv3
	ENDIF
	IF MARK:2 == 3 && (CFLAG:249 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)
		CFLAG:249 = 2
	;그 이외
	ENDIF
	IF (CFLAG:249 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)
		CFLAG:249 = 1
	ENDIF
ENDIF
SIF 구상유무확인(LOCAL) && K > 0
	CALL WSTR기입,K - 1

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_C 관련（4513을 캐릭터번호로 치환）
;조교 후의 변화를 트리거로 한 구상
;-------------------------------------------------
$表示
CALL 구상변수표시
SIF RESULT < 1
	CALL PALAM_MESSAGE_B
@KOJO_MESSAGE_PALAMCNG_C_K4513_1
CALL 구상변수초기화
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_C
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K4513_1
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K4513_1
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교 후의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;처녀상실(처녀만)
;-------------------------------------------------
IF TFLAG:3 > 0
	K_CALL:K = PALAM_MESSAGE_C
	;연모나 친애이면서 주인의 섹스에 의한 처녀상실
	IF TFLAG:20
	;그 이외의 때에 처녀상실
	ELSE
		;함락완료
		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;굴복각인Lv3
		ENDIF
		IF MARK:2 == 3 && !구상유무확인(0)
		;그 이외
		ENDIF
		IF !구상유무확인(0)
		ENDIF
	ENDIF
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_D 관련（4513을 캐릭터번호로 치환）
;파라미터 변화를 트리거로 한 구상
;-------------------------------------------------
$表示
CALL 구상변수표시
SIF RESULT < 1
	CALL PALAM_MESSAGE_C
@KOJO_MESSAGE_PALAMCNG_D_K4513_1
CALL 구상변수초기화
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_D
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K4513_1
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K4513_1
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;파라미터 변화시의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;처음 윤활이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:4 + UP:4
IF P > PALAMLV:4 && CFLAG:250 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;로션을 사용한 경우
	IF SELECTCOM == 180
	;그 이외
	ENDIF
	IF !구상유무확인(0)
	ENDIF
	CFLAG:250 = 1
ENDIF

;-------------------------------------------------
;처음 욕정이 LV2를 넘었다
;-------------------------------------------------
P = PALAM:6 + UP:6
IF P > PALAMLV:3 && CFLAG:251 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;미약을 사용한 경우
	IF SELECTCOM == 181
	;그 이외
	ENDIF
	IF !구상유무확인(0)
	ENDIF
	CFLAG:251 = 1
ENDIF

;-------------------------------------------------
;처음 굴복이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:7 + UP:7
IF P > PALAMLV:3 && CFLAG:252 == 0
	K_CALL:K = PALAM_MESSAGE_D
	CFLAG:252 = 1
ENDIF

;-------------------------------------------------
;처음 복종이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:5 + UP:5
IF P > PALAMLV:3 && CFLAG:253 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;함락완료
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
	;그 이외
	ENDIF
	IF !구상유무확인(0)
	ENDIF
	CFLAG:253 = 1
ENDIF

;-------------------------------------------------
;처음 공포가 LV3을 넘었다
;-------------------------------------------------
P = PALAM:11 + UP:11
IF P > PALAMLV:3 && CFLAG:254 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;함락완료
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
	;그 이외
	ENDIF
	IF !구상유무확인(0)
	ENDIF
	CFLAG:254 = 1
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_MARKCNG 관련（4513을 캐릭터번호로 치환）
;각인 변동을 트리거로 한 구상
;각인 변동 후에 구상을 발동합니다
;-------------------------------------------------
$表示
CALL 구상변수표시
SIF RESULT < 1
	CALL PALAM_MESSAGE_D
@KOJO_MESSAGE_MARKCNG_K4513_1
CALL 구상변수초기화
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL MARK_MESSAGE
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_MARKCNG_PY1_K4513_1
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_MARKCNG_PY1_K4513_1
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;고통각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:22 == 3
	K_CALL:K = MARK_MESSAGE_A
	;연모, 친애
	IF TALENT:3 || TALENT:6
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
	ENDIF
	IF !구상유무확인(LOCAL)
	ENDIF
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_A
ENDIF

;-------------------------------------------------
;쾌락각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:23 == 3
	K_CALL:K = MARK_MESSAGE_P
	;연모, 친애
	IF TALENT:3 || TALENT:6
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
	ENDIF
	IF !구상유무확인(LOCAL)
	ENDIF
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_P
ENDIF

;-------------------------------------------------
;굴복각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:24 == 13 || TFLAG:24 == 23 || TFLAG:24 == 33
	K_CALL:K = MARK_MESSAGE_S
	;쾌락에 의한 굴복
	IF TFLAG:24 == 13
	;공포와 고통에 의한 굴복
	ELSEIF TFLAG:24 == 23
	;굴복과 복종에 의한 굴복
	ELSEIF TFLAG:24 == 33
	ENDIF
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_S
ENDIF

;-------------------------------------------------
;반발각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:21 == 3
	K_CALL:K = MARK_MESSAGE_R
	;함락완료
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;연모, 친애
		IF TALENT:3 || TALENT:6
		;음란, 창부
		ELSEIF TALENT:4 || TALENT:7
		;복종, 예속
		ELSEIF TALENT:5 || TALENT:8
		ENDIF
	ENDIF
	IF !구상유무확인(LOCAL)
	ENDIF
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_R
ENDIF

;-------------------------------------------------
;이벤트구상 모음（4513을 캐릭터번호로 치환한다） CFLAG 261～299를 사용
;-------------------------------------------------
$表示
CALL 구상변수표시
@KOJO_MESSAGE_EVENT_K4513_1
CALL 구상변수초기화

;--------------------------------------------------
;특정 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_EVENT_PY1_K4513_1
;	RETURN 0
;ENDIF
SELECTCASE TFLAG:13
		;-------------------------------------------------
		;아침펠라
		;-------------------------------------------------
	CASE 1
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:261 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:261 = 1
			ENDIF

		;-------------------------------------------------
		;아침파이즈리
		;-------------------------------------------------
	CASE 2
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:262 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;모유체질인지 아닌지
					IF TALENT:149
					ELSE
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;모유체질인지 아닌지
					IF TALENT:149
					ELSE
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;모유체질인지 아닌지
					IF TALENT:149
					ELSE
					ENDIF
				ENDIF
				CFLAG:262 = 1
			ENDIF

		;-------------------------------------------------
		;아침기승위
		;-------------------------------------------------
	CASE 3
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침기승위A
		;-------------------------------------------------
	CASE 24
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침키스 연모, 친애 전제
		;-------------------------------------------------
	CASE 25
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침식사
		;-------------------------------------------------
	CASE 26
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;노래 부르면서인지 아닌지
					IF (TALENT:25 || TALENT:28) && ABL:14 >= 2
					ELSE
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;노래 부르면서인지 아닌지
					IF (TALENT:25 || TALENT:28) && ABL:14 >= 2
					ELSE
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;노래 부르면서인지 아닌지
					IF (TALENT:25 || TALENT:28) && ABL:14 >= 2
					ELSE
					ENDIF
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;일어나자 곁에서 자고 있다 연모, 친애 전제
		;-------------------------------------------------
	CASE 27
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침풋잡
		;-------------------------------------------------
	CASE 28
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;아침에 갓 짠 밀크
		;-------------------------------------------------
	CASE 29
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;잠자다 실례
		;-------------------------------------------------
	CASE 30
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 레즈플레이
		;-------------------------------------------------
	CASE 4
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:264 == 0
				CFLAG:264 = 1
			;그 이외
			ENDIF
			IF (CFLAG:264 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:264 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 섹스
		;-------------------------------------------------
	CASE 5
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:265 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:265 = 1
			ENDIF

		;-------------------------------------------------
		;강정신약의 반동
		;-------------------------------------------------
	CASE 6
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:266 < 2 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:266 = 2
			ENDIF
			IF (CFLAG:266 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:266 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 착유
		;-------------------------------------------------
	CASE 7
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:267 < 2 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:267 = 2
			ENDIF
			IF (CFLAG:267 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:267 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 자위
		;-------------------------------------------------
	CASE 8
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:268 < 2 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:268 = 2
			ENDIF
			IF (CFLAG:268 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:268 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인C 상대V）
		;-------------------------------------------------
	CASE 9
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인C 상대A）
		;-------------------------------------------------
	CASE 31
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인V 상대C）
		;-------------------------------------------------
	CASE 32
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인A 상대C）
		;-------------------------------------------------
	CASE 33
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（양쪽C（스마타）・양쪽C（조개맞대기））
		;-------------------------------------------------
	CASE 34
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;밤 자위
		;-------------------------------------------------
	CASE 35
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;같이잔다 연모, 친애
		;-------------------------------------------------
	CASE 36
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:269 < 1 || FLAG:7 == 2
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;황색사탕 사용시
		;-------------------------------------------------
	CASE 10
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:270 < 2 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:270 = 2
			ENDIF
			IF (CFLAG:270 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:270 = 1
			ENDIF

		;-------------------------------------------------
		;임신 발각시
		;-------------------------------------------------
	CASE 11
			SIF CFLAG:271 >= 1 && FLAG:7 != 2
				GOTO 表示
			K_CALL:K = EVENT_MESSAGE
			;임신대상을 TARGET에 격납하고, 부친은 노예는 V, 조수는 W로 한다
			;붕괴되어 버린 경우
			IF TALENT:9 == 1
			;부친이 주인이고 모친이 연모나 친애 보유
			ENDIF
			IF (TALENT:3 || TALENT:6) && CFLAG:102 == 1 && !구상유무확인(0)
			;부친이 노예나 조수고 모친이 음란이나 창부 보유
			ENDIF
			IF (TALENT:4 || TALENT:7) && (CFLAG:102 == 2 || CFLAG:102 == 3) && !구상유무확인(0)
				;부친(조수）과의 상성이 125보다 큰 경우
				IF CFLAG:102 == 2 && GET_RELATION(TARGET, W) > 125
			 	;부친(노예）과의 상성이 125보다 큰 경우
				ENDIF
				IF CFLAG:102 == 3 && GET_RELATION(TARGET, V) > 125 && !구상유무확인(0)
				ELSE
				ENDIF
			;복종이나 예속 보유
			ELSEIF (TALENT:5 || TALENT:8)
			ENDIF
			IF !구상유무확인(0)
			ENDIF
			CFLAG:271 = 1

		;-------------------------------------------------
		;출산시
		;-------------------------------------------------
	CASE 12
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:272 < 2 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:272 = 2
			ENDIF
			IF (CFLAG:272 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:272 = 1
			ENDIF

		;-------------------------------------------------
		;육아실
		;-------------------------------------------------
	CASE 13
			K_CALL:K = EVENT_MESSAGE
			;출산전
			IF TALENT:153
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 2 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:273 = 2
				ENDIF
				IF (CFLAG:273 < 1 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:273 = 1
				ENDIF
			;출산후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 4 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:273 = 4
				ENDIF
				IF (CFLAG:273 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					CFLAG:273 = 3
				ENDIF
			ENDIF

		;-------------------------------------------------
		;자식 독립시
		;-------------------------------------------------
	CASE 14
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:274 < 2 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:274 = 2
			ENDIF
			IF (CFLAG:274 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:274 = 1
			ENDIF
		;-------------------------------------------------
		;정식포장마차시
		;-------------------------------------------------
	CASE 15
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:275 == 0
				CFLAG:275 = 1
			;그 이외
			ENDIF
			IF (CFLAG:275 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:275 = 1
			ENDIF

		;-------------------------------------------------
		;창관에서 아르바이트・설정시
		;-------------------------------------------------
	CASE 16
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:276 == 0
				CFLAG:276 = 1
			;그 이외
			ENDIF
			IF (CFLAG:276 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:276 = 1
			ENDIF
		;-------------------------------------------------
		;창관에서 아르바이트・실행시
		;-------------------------------------------------
	CASE 63
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:606 == 0
				CFLAG:606 = 1
			;그 이외
			ENDIF
			IF (CFLAG:276 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:606 = 1
			ENDIF


		;-------------------------------------------------
		;렌탈시
		;-------------------------------------------------
	CASE 17
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:277 == 0
				CFLAG:277 = 1
			;그 이외
			ENDIF
			IF (CFLAG:277 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:277 = 1
			ENDIF

		;-------------------------------------------------
		;매각시
		;-------------------------------------------------
	CASE 18
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
				;연모, 친애+반발각인Lv3 미만
				IF (TALENT:3 || TALENT:6) && MARK:3 < 3
				;음란, 창부+반발각인Lv3 미만
				ELSEIF (TALENT:4 || TALENT:7) && MARK:3 < 3
				;복종, 예속+반발각인Lv3 미만
				ELSEIF (TALENT:5 || TALENT:8) && MARK:3 < 3
				ENDIF
			;반발각인Lv3
			ELSEIF MARK:3 == 3
			;그 이외
			ENDIF
			IF !구상유무확인(0)
			ENDIF

		;-------------------------------------------------
		;비디오 매각시
		;-------------------------------------------------
	CASE 19
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:278 < 2 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:278 = 2
			ENDIF
			IF (CFLAG:278 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:278 = 1
			ENDIF

		;-------------------------------------------------
		;콘서트시
		;-------------------------------------------------
	CASE 20
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:279 == 0
				CFLAG:279 = 1
			;그 이외
			ENDIF
			IF (CFLAG:279 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:279 = 1
			ENDIF

		;-------------------------------------------------
		;난교라이브시
		;-------------------------------------------------
	CASE 21
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:280 == 0
				CFLAG:280 = 1
			;그 이외
			ENDIF
			IF (CFLAG:280 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:280 = 1
			ENDIF

		;-------------------------------------------------
		;공중변소 플레이시
		;-------------------------------------------------
	CASE 22
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:281 == 0
				CFLAG:281 = 1
			;그 이외
			ENDIF
			IF (CFLAG:281 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:281 = 1
			ENDIF

		;-------------------------------------------------
		;비디오 보유시
		;-------------------------------------------------
	CASE 23
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:282 < 2 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:282 = 2
			ENDIF
			IF (CFLAG:282 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:282 = 1
			ENDIF

		;-------------------------------------------------
		;기억소거약 사용시
		;-------------------------------------------------
	CASE 30
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:283 < 2 || FLAG:7 == 2)
				;연모, 친애
				IF TALENT:3 || TALENT:6
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
				ENDIF
				CFLAG:283 = 2
			ENDIF
			IF (CFLAG:283 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				CFLAG:283 = 1
			ENDIF

		;-------------------------------------------------
		;;[아내][남편]취득시 친애 전제
		;-------------------------------------------------
	CASE 40
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:283 < 1 || FLAG:7 == 2
				CFLAG:283 = 1
			ENDIF
		;-------------------------------------------------
		;;[음마]취득시 창부 전제
		;-------------------------------------------------
	CASE 41
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:284 < 1 || FLAG:7 == 2
				CFLAG:284 = 1
			ENDIF
		;-------------------------------------------------
		;;[전속노예]취득시 예속 전제
		;-------------------------------------------------
	CASE 42
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:285 < 1 || FLAG:7 == 2
				CFLAG:285 = 1
			ENDIF
		;-------------------------------------------------
		;;오픈/클로즈・오프・가슴
		;-------------------------------------------------
	CASE 44
			K_CALL:K = EVENT_MESSAGE
			IF TEQUIP:가슴앞열림
				;가슴을 드러낸다
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:285 = 1
				ENDIF
			ENDIF
			IF !구상유무확인(0)
				;가슴을 집어넣는다
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모, 친애
					IF TALENT:3 || TALENT:6
					;음란, 창부
					ELSEIF TALENT:4 || TALENT:7
					;복종, 예속
					ELSEIF TALENT:5 || TALENT:8
					ENDIF
					CFLAG:287 = 1
				ENDIF
			ENDIF
		;-------------------------------------------------
		;;[애ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 45
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:288 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:애ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:애ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:애ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:애ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:애ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:애ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				;그 외
				ENDIF
				IF !구상유무확인(0)
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:애ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:애ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:애ＮＴＲ진행도 == 0
				;2번째（리치）
				ENDIF
				IF CFLAG:애ＮＴＲ진행도 == 1 && !구상유무확인(0)
				;3번째（함락）
				ENDIF
				IF CFLAG:애ＮＴＲ진행도 == 2 && !구상유무확인(0)
				ENDIF
			ENDIF
			CFLAG:288 = 1
		ENDIF
		;-------------------------------------------------
		;;[음ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 46
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:289 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:음ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:음ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:음ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:음ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				;그 외
				ENDIF
				IF !구상유무확인(0)
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:음ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:음ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:음ＮＴＲ진행도 == 0
				;2번째（리치）
				ENDIF
				IF CFLAG:음ＮＴＲ진행도 == 1 && !구상유무확인(0)
				;3번째（함락）
				ENDIF
				IF CFLAG:음ＮＴＲ진행도 == 2 && !구상유무확인(0)
				ENDIF
			ENDIF
			CFLAG:289 = 1
		ENDIF
		;-------------------------------------------------
		;;[예ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 47
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:290 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:예ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:예ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:예ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:예ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:예ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:예ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				;그 외
				ENDIF
				IF !구상유무확인(0)
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
					;2번째（리치）
					ENDIF
					IF CFLAG:예ＮＴＲ진행도 == 1 && !구상유무확인(0)
					;3번째（함락）
					ENDIF
					IF CFLAG:예ＮＴＲ진행도 == 2 && !구상유무확인(0)
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:예ＮＴＲ진행도 == 0
				;2번째（리치）
				ENDIF
				IF CFLAG:예ＮＴＲ진행도 == 1 && !구상유무확인(0)
				;3번째（함락）
				ENDIF
				IF CFLAG:예ＮＴＲ진행도 == 2 && !구상유무확인(0)
				ENDIF
			ENDIF
			CFLAG:290 = 1
		ENDIF
		;-------------------------------------------------
		;;애(愛)계 재함락시
		;;주인에게 연모계 함락되었거나, 한번 애(愛)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 48
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
		
		IF (CFLAG:291 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
			ENDIF
			CFLAG:291 = 1
		ENDIF
		;-------------------------------------------------
		;;음(淫)계 재함락시
		;;주인에게 음란계 함락되었거나, 한번 음(淫)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 49
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
					
		IF (CFLAG:292 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
			ENDIF
			CFLAG:292 = 1
		ENDIF
		;-------------------------------------------------
		;;예(隷)계 재함락시
		;;주인에게 복종계 함락되었거나, 한번 예(隷)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 50
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
					
		IF (CFLAG:293 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
			ENDIF
			CFLAG:293 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・난교파티
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 51
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:294 < 1 || FLAG:7 == 2)
			IF TALENT:연모 || TALENT:친애
				;이전에 연모 루트였던 경우
			ELSEIF TALENT:복종 || TALENT:예속
				;이전에 복종 루트였던 경우
			ENDIF
			IF !구상유무확인(0)
			ENDIF
			CFLAG:294 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・공원
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 52
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:295 < 1 || FLAG:7 == 2)
			IF TALENT:연모 || TALENT:친애
				;이전에 연모 루트였던 경우
			ELSEIF TALENT:복종 || TALENT:예속
				;이전에 복종 루트였던 경우
			ENDIF
			IF !구상유무확인(0)
			ENDIF
			CFLAG:295 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・변기
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 53
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)
			IF TALENT:연모 || TALENT:친애
				;이전에 연모 루트였던 경우
			ELSEIF TALENT:복종 || TALENT:예속
				;이전에 복종 루트였던 경우
			ENDIF
			IF !구상유무확인(0)
			ENDIF
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・밤의 거리
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 54
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)
			IF TALENT:연모 || TALENT:친애
				;이전에 연모 루트였던 경우
			ELSEIF TALENT:복종 || TALENT:예속
				;이전에 복종 루트였던 경우
			ENDIF
			IF !구상유무확인(0)
			ENDIF
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ전 밀회
		;;ＮＴＲ상태가 되기 전에 밀회할 때
		;-------------------------------------------------
	CASE 55
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:297 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 != 0
				;주인 이외에 함락되어 있는 경우
			ELSEIF TALENT:연모 || TALENT:친애
				;연모 루트
			ELSEIF TALENT:음란 || TALENT:창부
				;음란 루트
			ELSEIF TALENT:복종 || TALENT:예속
				;복종 루트
			ENDIF
			IF !구상유무확인(0)
			ENDIF
			CFLAG:297 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ후 첫 밀회
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)가 된 직후 하루 종료 후, 한번만 행한다
		;다른 캐릭터에 함락될 때마다 일어난다. 같은 캐릭터에 다시 함락되는 경우는 일어나지 않는다
		;-------------------------------------------------
	CASE 56
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:298 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;애ＮＴＲ
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;예ＮＴＲ
			ENDIF
			CFLAG:298 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・데이트
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;-------------------------------------------------
	CASE 57
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:299 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;애ＮＴＲ
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;예ＮＴＲ
			ENDIF
			CFLAG:299 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・본방
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;여성이라면 삽입당하는 쪽, 남성이라면 삽입하는 쪽으로 고정
		;-------------------------------------------------
	CASE 58
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:601 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;애ＮＴＲ
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;예ＮＴＲ
			ENDIF
			CFLAG:601 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・자위서로과시
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;애ＮＴＲ상태인 경우, 노출증 3이나 자위중독 1 중 하나를 충족시키는 것이 조건이 됩니다.
		;-------------------------------------------------
	CASE 59
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;애ＮＴＲ
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;예ＮＴＲ
			ENDIF
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・애널
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;CFLAG:애널횟수가 1 이상이면 삽입당하고, CFLAG:삽입횟수가 1 이상면 삽입하고 있습니다.
		;양 플래그 모두 서있는 경우, 지문에서는 삽입당하는 쪽이 우선됩니다.
		;-------------------------------------------------
	CASE 60
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;애ＮＴＲ
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;예ＮＴＲ
			ENDIF
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외플레이
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;애ＮＴＲ의 경우, 노출증이 4 이상이 아니라면 발생하지 않습니다
		;-------------------------------------------------
	CASE 61
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;애ＮＴＲ
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;예ＮＴＲ
			ENDIF
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외조교
		;;예ＮＴＲ상태(TALENT:ＮＴＲ == 3, 6)에서 CFLAG:현ＮＴＲ진행도가 6이상인 경우 하루 종료시에 발생.
		;-------------------------------------------------
	CASE 62
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			;예ＮＴＲ
			CFLAG:605 = 1
		ENDIF
	CASEELSE
		SIF FLAG:6 > 0
			K_CALL:K = EVENT_MESSAGE
ENDSELECT
;-------------------------------------------------
;@KOJO_MESSAGE_ENDING 관련(4513、4513를 캐릭터 번호로 치환한다)
;-------------------------------------------------
;-------------------------------------------------
;조건설정
;-------------------------------------------------
$表示
CALL 구상변수표시
SIF RESULT < 1
	CALL EVENT_MESSAGE
@EVENTTURNEND_K4513_1
;#LATER
;SIF TARGET < 0
;	RETURN 0
;SIF NO:TARGET != 4513
;	RETURN 0
;특수 하렘엔딩용 조건 판정 구문 샘플
;XX 안에 조건으로 하는 캐릭터의 캐릭터번호를 넣으면, 원하는 조합의 하렘엔딩을 설정할 수 있다
;하렘엔딩의 조건을 충족했을 때, 이곳의 특수 하렘엔딩의 조건도 충족한 경우, 하렘엔딩에서 파생한다
;A = 0
;B = 0
;REPEAT CHARANUM
;	IF NO:COUNT == XX || NO:COUNT == XX || NO:COUNT == XX || NO:COUNT == XX || NO:COUNT == XX
;		SIF TALENT:COUNT:6 || TALENT:COUNT:7 || TALENT:COUNT:8
;			A += 1
;		SIF TALENT:COUNT:3 || TALENT:COUNT:4 || TALENT:COUNT:5 || TALENT:COUNT:6 || TALENT:COUNT:7 || TALENT:COUNT:8
;			B += 1
;	ENDIF
;REND
;SIF A && A+B >= XXX
;	CFLAG:21 = 2
;1체 엔딩용 분기 플래그, 1체 엔딩이 있는 상위 함락소질만 주석처리를 해제한다
;SIF TALENT:6
;	CFLAG:21 = 1
;SIF TALENT:7
;	CFLAG:21 = 1
;SIF TALENT:8
;	CFLAG:21 = 1
;-------------------------------------------------
;엔딩 본문
;-------------------------------------------------
@KOJO_MESSAGE_ENDING_K4513_1
;--------------------------------------------------
;특정 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, 4513을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_ENDING_PY1_K4513_1
;	RETURN 0
;ENDIF
;--------------------------------------------------
;연모 1체 엔딩
;IF TALENT:6 && CFLAG:21 == 1
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.14513A (○○エンド)
;음란 1체 엔딩
;ELSEIF TALENT:7 && CFLAG:21 == 1
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.14513B (○○エンド)
;복종 1체 엔딩
;ELSEIF TALENT:8 && CFLAG:21 == 1
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.14513C (○○エンド)
;ENDIF

;특수 하렘엔딩
;IF CFLAG:21 == 2
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.24513 (○○エンド)
;ENDIF

;CALL GAME_CONTINUE
;RETURN 0


@ENEMY_BATTLE_EVENT_K4513_1, ARG, ARG:1, ARG:2
IF TFLAG:전투이벤트 == 1
;적에서의 데빌ＣＯ－ＯＰ 발동시의 대사
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 2
;적에서의 데밀ＣＯ－ＯＰ 참가시의 대사
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

@BATTLE_EVENT_K4513_1, ARG, ARG:1, ARG:2
IF TFLAG:전투이벤트 == 1
;아군 파티에서의 데빌ＣＯ－ＯＰ 발동시의 대사
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 2
;아군 파티에서의 데빌ＣＯ－ＯＰ 참가시의 대사
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF


@BATTLE_MESSAGE_K4513_1,ARG,ARG:1 = 0

;선택한 커맨드로 분기시키고 싶은 경우, IF문의 조건에 명령어 No를 기입할 것
;IF CFLAG:ARG:입력행동 == 
;	PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;	PRINTFORML ┃%"",72,LEFT%┃
;	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

;ELSEIF ARG:1 > 0
	;충성도가 상승하는 행동을 취했을 경우에 표시하는 구상

;	CALLFORM SKILL_NAME_{CFLAG:ARG:입력행동},ARG
;	;사용한 스킬의 이름을 LOCALS에 대입
;	LOCALS = %RESULTS%

;	SELECTCASE ARG:1
;		;물리데미지를 주는 행동으로, 영맹 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"영맹")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;마법데미지를 주는 행동으로, 교활 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"교활")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;고위력의 행동으로, 잔학 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"잔학")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;상대의 약점을 찌르는 공격 or 행동불능의 상태이상의 적에 대한 공격으로, 현명 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"현명")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;파마 상성의 행동으로, 천사 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"천사")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;주살 상성의 행동으로, 타천사 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"타천사")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;회복・상태회복 행동으로는 우애 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"우애")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;상태변화로는 교만 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"교만")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;특수로는 냉정 보유자의 충성도가 상승
;		CASE GETNUM(TALENT,"냉정")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;우둔 보유자는 주인과의 Lv차이 만큼 충성도가 상승
;		CASE GETNUM(TALENT,"우둔")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;허심 보유자는 최저 ２만큼 충성도가 상승
;		CASE GETNUM(TALENT,"허심")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;	ENDSELECT
;ELSE
;	;시스템에 의지하지 않고 충성도를 높이고 싶거나 다른 목표가 있으면 여기에 기재
;ENDIF

