;-------------------------------------------------
;マルフーシャ口上（キャラクターの名前を入れる、下にコンセプトなど一言あれば入れる）
;独り言と仲間に対してはタメ口、サマナーに呼びかける時は敬語を使う
;基本的にローテンション
;とりあえず刻印と調教開始・終了、契約周りを。コマンド周りはもう少し待ってね。
;この口上を見てかわいいなと思ったら原作の「溶鉄のマルフーシャ」を購入してプレイすべし。
;戦闘口上も近々別の人がアップすると思うから待っててね。
;-------------------------------------------------

;-------------------------------------------------
;eraMegaten전용 구상 템플릿
;X1과 X2를 캐릭터 번호로 치환하고, Y1을 조수의 캐릭터 번호로 치환해주세요
;단, 캐릭터번호가 한자리인 캐릭터의 경우, 5371, Y1은 1, X2는 01과 같이 바꿔주세요
;그리고 커맨드 내의 적당한 조건 아래의 PRINTFORMW의 뒤에 반각 공백을 두고 대사를 추가해주세요
;구상을 추가하지 않는 커맨드는 무기입하셔도 상관없습니다만, CFLAG:XXX의 앞에『;』를 붙여서 주석처리를 하시면 더욱 좋습니다
;엑스트라 구문 등, 주석처리가 되어 있는 구문을 사용할 때는,『;』를 제거해 주석처리를 해제할 필요가 있습니다
;마지막으로 read me를 건드려 보시는 것도 추천드립니다
;-------------------------------------------------

;-------------------------------------------------
;범용 구상의 작성에 사용하는 경우는, 5371→캐릭터번호가 아니라,
;K5371→PUB회화타입번호　로 변경해서 사용해주세요
;-------------------------------------------------
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	Module		:EVENT_K5371_ASSI.ERB
;	Facility	:회화타입：助手時口上
;
;	Modification Data:
;	Edit	Date			Author					Reason
;	001		20XX/XX/XX		Jガン					新規作成
;	002		2021/09/09		からす					調教口上作成&配布
;	003		
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;ライセンスについて
;	・基本項目(許可する項目を"○"に、許可しない項目を"×"に変更してください。)
;	+----+----+-------------------------------+
;	|番号|許可|ライセンス内容                 |
;	+----+----+-------------------------------+
;	|   1| ○ | 処理上のバグ修正              |
;	+----+----+-------------------------------+
;	|   2| ○ | 口上の誤字・脱字の修正        |
;	+----+----+-------------------------------+
;	|   3| ○ | 口上まとめへの収録            |
;	+----+----+-------------------------------+
;	|   4| ○ | 口上の新規追加                |
;	+----+----+-------------------------------+
;	|   5| ○ | 既存口上の改変                |
;	+----+----+-------------------------------+
;	|   6| ○ | 改変した口上の自由な再配布    |
;	+----+----+-------------------------------+
;	|   7| ○ | 口上内容の転載                |
;	+----+----+-------------------------------+

;--------------------------------------------------
;구상셀렉트 기능：구상설명
;--------------------------------------------------
;이 함수의 주석처리를 해제하면, 구상의 선택시에 선택지로 후보에 오릅니다
;이미 구상이 있는 캐릭터나 회화타입의 다른 버전의 구상을 작성할 때 이용해주세요
;이 때, 이 함수를 포함한 이 파일 내의 함수명 전체에 _임의의1-49(범용구상이면 1-9)의 숫자를 추가해주세요
;예： EVENTTRAIN_K5371 → EVENTTRAIN_K5371_12
;--------------------------------------------------
;@셀렉트구상설명_K5371,ARG = 0
;SIF ARG == 1
;	RETURN 0
;;구상명
;PRINTFORML マルフーシャ口上
;;구상설명문
;PRINTFORML    素質を見ればわかると思いますが、厄介な子なので頑張って調教してください。

;--------------------------------------------------
;EVENTTRAIN 관련（X1을 캐릭터번호로 치환）
;조교 개시시의 대화 CFLAG 201～219를 사용
;--------------------------------------------------
@EVENTTRAIN_K5371
CALL 구상변수초기화
;--------------------------------------------------
;엑스트라모드 구문　CFLAG:220을 사용
;네 를 선택시 첫조교시 구상설정용 소질로 변경 가능합니다
;아래에 처녀화할 경우의 구문을 샘플로 올려두겠습니다
;--------------------------------------------------
;IF CFLAG:220 == 0
;	PRINTL 処女モードに移行しますか？
;	PRINTL [0] はい
;	PRINTL [1] いいえ
;	$INPUT_LOOP
;	INPUT
;	IF RESULT == 0
;		PRINTFORMW ―処女モードに素質、能力を変更します―
;		TALENT:TARGET:0 = 1
;		ABL:TARGET:4 = 0
;		EXP:TARGET:0 = 0
;	ELSEIF RESULT == 1
;		PRINTFORMW ―通常モードで調教を開始します―
;	ELSE
;		GOTO INPUT_LOOP
;	ENDIF
;	CFLAG:220 = 1
;ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTTRAIN_PY1_K5371
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL EVENTTRAIN_PY1_K5371
;		RETURN 0
;	ENDIF
;ENDIF
;--------------------------------------------------
;간이 조수구상 발동시에 첫조교시 구상을 스킵（Y1을 조수의 캐릭터번호로 한다）
;여러 명이 있을 경우는, SIF NO:ASSI == Y1 || NO:ASSI == Z1으로 추가해 나간다
;--------------------------------------------------
;IF CFLAG:201 == 0 && ASSI > 0
;	SIF NO:ASSI == Y1
;		CFLAG:201 =1
;ENDIF
;-------------------------------------------------
;조교 개시시（1회만） CFLAG201
;-------------------------------------------------
DRAWLINE
;첫조교시
IF CFLAG:201 == 0 && TALENT:연모 == 0
	KSTR:(K++) = 　このマルフーシャという少女は……なんというか。戦闘員としては……戦闘員としては、だ。非常に優秀なのだが。女としては残念なことにそうではない。顔は良い。けれど目を引くような美貌ではない。ぶっちゃけ町を歩けばそこらに居そうな感じだし。スタイルも悪くはないが、そこまで良くもない平均的なもので。目につくところを強いてあげれば、戦闘員らしくよく引き締まった体をしている。
	KSTR:(K++) = 　素質的に考えて、調教はまっとうなやり方では非常に手間がかかる。が、育てれば手間に見合った価格にはなるか。しかし、彼女の戦闘員としての素質は一級品だ。このまま調教を行えば彼女の信用を失い、強力な戦力が欠けることになる。閉じた心を開かせ、再び戦いの場に立ってもらうにはかなりの時間を要すること間違いなし。
	KSTR:(K++) = 　多くの時間と手間を浪費し、その間は戦闘員としての働きをしてもらえなくなる。それでも調教する価値があるだろうか。
	
	KSTR:(K++) = 
	KSTR:(K++) = 
	KSTR:(K++) = 
	KSTR:(K++) = 
			KSTR:(K++) = 　だからどうしたというのだ。彼女を調教したい。そう思ったのなら、損得など考えずにそうするべきだ。
			KSTR:(K++) = 　あなたは彼女にこれから調教を行う旨を伝えた。
			KSTR:(K++) = 「……わかりました。それは、命令なんですよね？」
			KSTR:(K++) = 　一呼吸おいて。およそ少女に似つかわしくない、覚悟を湛えた瞳で返事をした。嫌ではないのか、と尋ねると。
			KSTR:(K++) = 「体を触られるのも。銃を持たされて殺し合いをさせられるのも。どっちも嫌なことには変わりありませんから」
			KSTR:(K++) = 　いずれ体を好き放題して、戦場にも連れ出すつもりであることは、話す必要もない。今のところは黙っておこう。
			KSTR:(K++) = 　服の裾をぎゅっと握りしめて、必死に震えを押し殺すマルフーシャの肩を押し、そっとベッドの上に押し倒した。
		CALL 구상변수초기화
	CFLAG:201 = 1
	GOTO 表示
;굴복각인Lv1
ELSEIF CFLAG:201 < 2 && MARK:2 == 1
	KSTR:(K++) = 
	CFLAG:201 = 2
	GOTO 表示
;굴복각인Lv2
ELSEIF CFLAG:201 < 3 && MARK:2 == 2
	KSTR:(K++) = 
	CFLAG:201 = 3
	GOTO 表示
;굴복각인Lv3
ELSEIF CFLAG:201 < 4 && MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	KSTR:(K++) = 
	CFLAG:201 = 4
	GOTO 表示
;함락
ELSEIF CFLAG:201 < 5 && (TALENT:3 || TALENT:4 || TALENT:5)
	;연모
	IF TALENT:3
		IF CFLAG:미조교함락 == 1
			;조교하지 않고 연모를 단 경우
			WSTR:(K++) = 
		ELSE
			WSTR:(K++) = 
		ENDIF
	;음란
	ELSEIF TALENT:4
		WSTR:(K++) = 
	;복종
	ELSEIF TALENT:5
		WSTR:(K++) = 
	ENDIF
	;공통
	IF !구상유무확인(0)
		KSTR:(K++) = 
	ENDIF
	CFLAG:201 = 5
	GOTO 表示
ENDIF

;-------------------------------------------------
;간이 조수구상（Y1을 조수의 캐릭터번호로 치환, X1을 캐릭터번호로 변경한다) CFLAG203
;말할 캐릭터를 늘릴 경우, 구문을 복사＆붙여넣기 해, CFLAG의 수를 204, 205로 늘린다
;-------------------------------------------------
;조수의 유무를 체크（없을 경우는 2번째 이후로 건너뛴다）
IF ASSI < 0 
	CALL EVENTTRAIN_SECOND_K5371
;助手
;ELSEIF NO:ASSI == Y1
	;처음
;	IF CFLAG:203 == 0
		;함락완료＋함락시 첫 구상이 발생완료
;		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && CFLAG:201 >= 5
;			WSTR:(K++) = 
		;그 이외
;		ELSE
;			KSTR:(K++) = 
;		ENDIF
;		CFLAG:203 = 1
	;2번째 이후
;	ELSEIF CFLAG:203 < 2 || FLAG:7 ==2
		;함락완료
;		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
;			WSTR:(K++) = 
		;그 이외
;		ELSE
;			KSTR:(K++) = 
;		ENDIF
;		CFLAG:203 = 2
;	ENDIF
;구상이 있는 조수가 없을 경우, 통상의 2번째 이후의 구상으로 건너뛴다
ELSE
	CALL EVENTTRAIN_SECOND_K5371
ENDIF

$表示

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM EVENTTRAIN_ZZZ
	CATCH
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;조교 개시시（2번째 이후, X1을 캐릭터번호로 변경한다）
;-------------------------------------------------
@EVENTTRAIN_SECOND_K5371
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:202 < 4 || FLAG:7 == 2)
	KSTR:(K++) = 「もういやです。何もかもいやです。あなたのことがきらいです」
	KSTR:(K++) = 　諦観に染まった目で、部屋の隅で膝を抱えて座り込む%CALLNAME:TARGET%の手を引っ張って、無理やりベッドの上に連れていく。
	KSTR:(K++) = 「今ほど銃が欲しいと思ったことはありません」
	KSTR:(K++) = 　あったらどうする。
	KSTR:(K++) = 「あなたを殺して……そのあとはなるようになるでしょう」
	KSTR:(K++) = 　聞くだけ聞いて、服を脱がして調教を始める。この問題児も、根気よく続ければそのうちしおらしくなるだろう。
	CFLAG:202 = 4
;굴복각인Lv0
ELSEIF MARK:2 == 0 && (CFLAG:202 < 1 || FLAG:7 == 2)
	KSTR:(K++) = 　%CALLNAME:TARGET%には、戦闘だけでなく労役に適性がある。そこで今日は調教が進んだ後の就職についてパネルを用意してみた。
	KSTR:(K++) = 　ﾄﾞｼﾞｬｧｰﾝ（効果音）
	KSTR:(K++) = 「そのでかいパネルと効果音は今日の為にわざわざ作ってくれたんですか」
	KSTR:(K++) =  　%CALLNAME:TARGET%は胡乱な目でこちらを見ている。何か問題でも？
	KSTR:(K++) = 「い、いえ……以前こんなことがあったような気がして」
	KSTR:(K++) = 　ここに来るまでに調教経験はついていなかったはずだが。となるとまた別の仕事で、似たようなパネルを作った上司が居るのだろう。
	KSTR:(K++) = 　まあそれはともかく。창부、肉便器、レストラン、アイドル、炭鉱夫と。売却と……いくらか進路がある。それぞれについての説明を一つ一つ行い、今現在の希望を聞いておく。
	KSTR:(K++) = 「肉便器って何ですか……」
	KSTR:(K++) = 　ほう。肉便器に興味がおありで？　なかなかハードな選択を。大量の異性同性、悪魔や人間を相手にするため肉体的にはきわめてハード。しかし客がつけば稼ぎは素晴らしい……
	KSTR:(K++) = 「意味が分かりません。という意味で聞いたのですが」
	KSTR:(K++) =  　%CALLNAME:TARGET%は肥溜めに蠢く蛆と蠅の群れを見るような目を%CALLNAME:MASTER%に向けている。そんなにひどいことか？　肉便器。少々過酷で気絶するまでマワされることはあるが、死ぬことはないし、『変わる』こともないのに。
	KSTR:(K++) = 「絶対に嫌です。娼婦も嫌です」
	KSTR:(K++) = 　そうか。無理強いはすまい。どっちも稼ぎはいいんだけどなぁ、と呟きながら、%CALLNAME:MASTER%はパネルを邪魔にならない部屋の隅に置いておく。
	KSTR:(K++) = 　ただし実際どうなるかは%CALLNAME:MASTER%の気分次第だ。蝶よ花よと育てられたお嬢様だろうと、チンポに跪いて股を濡らしながら尻を振ってよだれを垂らしながら挿入をねだる雌豚に変えるのが%CALLNAME:MASTER%の特技なのだし。
	KSTR:(K++) = 　では逆に、何がしたいのかを教えてもらおうか。
	KSTR:(K++) = 「……レストラン。できれば、パン屋がしたいです。元々そっちが本業だったので」
	KSTR:(K++) = 　結構。覚えておこう。
	CFLAG:202 = 1
;굴복각인Lv1
ELSEIF MARK:2 == 1 && (CFLAG:202 < 2 || FLAG:7 == 2)
	KSTR:(K++) = 「またですか……やめる気は……ないですよね」
	KSTR:(K++) = 　もちろんだ。ここでやめれば立派なパン屋になれないぞ。
	KSTR:(K++) = 「調教とパン屋と一体どういう関係があるんですか……」
	KSTR:(K++) = 　習得の珠がないと料理技能があげられない。
	KSTR:(K++) = 「メタな話はやめてください」
	CFLAG:202 = 2
;굴복각인Lv2
ELSEIF MARK:2 == 2 && (CFLAG:202 < 3 || FLAG:7 == 2)
	KSTR:(K++) = 　そろそろ調教にも慣れてきたか。
	KSTR:(K++) = 「慣れませんし慣れたくありませんね」
	CFLAG:202 = 3
;굴복각인Lv3＋함락소질없음
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0) && (CFLAG:202 < 4 || FLAG:7 == 2)
	KSTR:(K++) = 「私が喜んでいる？　抵抗しても無駄だから、受け入れてるだけです。協力すればその分早く終わるでしょう」
	KSTR:(K++) = 　自分から服を脱ぎ、ベッドに腰掛ける%CALLNAME:TARGET%。冷たい表情とは裏腹に、触れれば熱く、早い鼓動が伝わってくる。そのまま押し倒しても抵抗はない。調教の記憶を思い出して、興奮しているのだろう。かわいいやつだ。
	CFLAG:202 = 4
;함락완료
ELSEIF CFLAG:202 < 5 || FLAG:7 == 2
	;연모, 친애
	IF TALENT:3 || TALENT:6
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			WSTR:(K++) = 「あ……%CALLNAME:MASTER%さん。待ってました」
			WSTR:(K++) =  　部屋に入ると、%CALLNAME:TARGET%は読みかけの本を閉じて、穏やかな微笑みを浮かべてこちらを向く。以前のどこか陰のある表情もまた良いものではあったが、やはり女の子は笑顔に限る。
			
		ELSEIF RAND:2 == 0
			WSTR:(K++) =「%CSTR:일인칭%、スネジンカって妹が居るんです。%CSTR:이인칭%を紹介したら、どんな顔をするのかな」
			WSTR:(K++) = 　ベッドに座って足をぷらぷらさせて呟いた。
			WSTR:(K++) = 「でも、%CSTR:이인칭%は節操なしだからかわいい妹を取られちゃうかなぁ」
			WSTR:(K++) = 　姉妹丼もいいか。
			WSTR:(K++) = 　どすっと、体の割に大きい音を立てて、 %CALLNAME:MASTER%の脇腹に拳が突き刺さってきた。
			WSTR:(K++) = 　なかなかに鍛えられている、鋭い拳であった。日夜悪魔をぶちのめしている%CALLNAME:MASTER%も思わず悶絶するほどの……
			WSTR:(K++) = 「ダメですよ。スネジンカは%CSTR:일인칭%の小さなかわいい妹なんですから……本当節操なし。女性なら誰でもいいんですか？」
			WSTR:(K++) = 　たまに男も調教するし、悪魔も調教する。場合によっては机やピアノ、サイコロだって調教する %CALLNAME:MASTER%にとって、子供というだけでは調教しない理由にならない。
			WSTR:(K++) = 「%CSTR:이인칭%はとっても悪い人です。だから、善良な市民に被害が及ばないように、%CSTR:일인칭%がしっかり見張っていてあげますから。わかりましたか」
			WSTR:(K++) = ……裏を返せば、独占したいと。かわいいやつめ。
			ENDIF
		ELSEIF RAND:1 == 0
			WSTR:(K++) =「%CSTR:이인칭%、前にも言ったかと思うんですけど%CSTR:일인칭%の本業ってパン屋なんです。で、その……%CSTR:이인칭%と、%CSTR:일인칭%と……できれば妹も一緒に。小さくていいから、パン屋さんを開きたいなって」
			IF ITEM:식당 == 1
			WSTR:(K++) = 　パン屋ではないが食堂なら %CALLNAME:MASTER%が既に権利を所持している。
			WSTR:(K++) = 　勿論、マルフーシャがパンを作るブースを作りたいというのなら、それに応えるだけのスペースもある。あとは少しばかり投資をすれば。二人だけの、というのは少し難しいが。
			WSTR:(K++) = 「うれしいです……」
			WSTR:(K++) = 　笑顔で抱き着いてきたマルフーシャを抱き返すと、柔らかな体と石鹸の香りを間近に感じて……興奮してしまった。あなたは悪くない。マルフーシャが悪いのだ、と、ベッドに押し倒した。
			WSTR:(K++) = 「……」
			WSTR:(K++) = 　マルフーシャもまんざらではないようだ。
			ENDIF
			IF ITEM:식당 == 0
			WSTR:(K++) = 　今のところは店も持っていなければ伝手もない。しかし、マルフーシャが望むのならどうにか方法を考えよう。
			WSTR:(K++) = 「その気持ちだけで十分うれしいです。でも、いつか……お願いしますね」
			WSTR:(K++) = 　もちろんだ。
		ENDIF
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			WSTR:(K++) =
		ELSEIF RAND:2 == 0
			WSTR:(K++) =
		ELSE
			WSTR:(K++) =
		ENDIF
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			WSTR:(K++) =
		ELSEIF RAND:2 == 0
			WSTR:(K++) =
		ELSE
			WSTR:(K++) =
		ENDIF
	ENDIF
	;그 외, 공통
	IF !구상유무확인(0)
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			KSTR:(K++) = 
		ELSEIF RAND:2 == 0
			KSTR:(K++) = 
		ELSE
			KSTR:(K++) = 
		ENDIF
	ENDIF
	CFLAG:202 = 5
ENDIF


;-------------------------------------------------
;EVENTEND 관련（X1을 캐릭터번호로 치환）
;조교 종료시의 대사
;-------------------------------------------------
@EVENTEND_K5371
CALL 구상변수초기화
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTEND_PY1_K5371
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	SIF NO:ASSI == Y1
;	CALL EVENTEND_PY1_K5371
;	RETURN 0
;ENDIF
;--------------------------------------------------
;조교 종료시의 대사
;--------------------------------------------------
DRAWLINE
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:217 < 1 || FLAG:7 == 2)
	KSTR:(K++) = 「こんなところ、いつか絶対に抜け出して見せる……」
	KSTR:(K++) = 　 レベル100を超えた悪魔でも逃げられない部屋なのに、そんなもん無理に決まってるだろう。
	CFLAG:217 = 1
;굴복각인Lv1 이하
ELSEIF MARK:2 <= 1 && (CFLAG:219 < 1 || FLAG:7 == 2)
	KSTR:(K++) = 「はぁ……やっと終わりですか。%CSTR:일인칭%なんかのためにわざわざこんなに時間をかけるなんて暇なんですね」
	WSTR:(K++) = 　 %CALLNAME:TARGET%には時間をかけるだけの価値があると思ってやっていることだ。暇だからではない。
	WSTR:(K++) = 「そうですか。ありがとうございます」
	WSTR:(K++) =　 もっと気持ちを込めて言ってくれるとこっちもうれしいんだが。
	WSTR:(K++) = 「……早く出て行ってくれませんか。シャワーを浴びたいので」
	CFLAG:219 = 1
;굴복각인Lv2
ELSEIF MARK:2 == 2
	KSTR:(K++) = 「やっぱり慣れないな……でも銃を抱えて地面を這いずり回るよりはマシか」
	CFLAG:219 = 2
;굴복각인Lv3＋함락소질없음
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	KSTR:(K++) = 「この状況を受け入れている自分がいる。慣れって怖い」
	CFLAG:219 = 3
;함락완료＋체력 500미만
ELSEIF BASE:0 < 500 && (CFLAG:218 < 1 || FLAG:7 == 2)
	;연모, 친애
	IF TALENT:3 || TALENT:6
		WSTR:(K++) = 「腰がつらくて死にそうだ……気持ちよかったけど……」
		WSTR:(K++) =　 マッサージは必要かね。
		WSTR:(K++) = 「絶対いやらしいマッサージするつもりだ」
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		WSTR:(K++) =
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		WSTR:(K++) =
	ENDIF
	;공통
	IF !구상유무확인(0)
		KSTR:(K++) = 
	ENDIF
	CFLAG:218 = 1
;함락완료
ELSEIF CFLAG:219 < 4 || FLAG:7 == 2
	;연모, 친애
	IF TALENT:3 || TALENT:6
		KSTR:(K++) = 「まだできるよね。私も、%CSTR:이인칭%も……ね？」
		KSTR:(K++) =　 ……かわいらしい胸に腕を押し付けられている。引きはがそうとしたが全く動かず、これはもう望み通り突き合うしかないようだ。
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		KSTR:(K++) = 
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		KSTR:(K++) = 
	ENDIF
	;공통
	IF !구상유무확인(0)
		WSTR:(K++) = 「眠たい……シャワーだけでも、浴びて寝ないと……でもその体力もない……ぐぅ」
	ENDIF
	CFLAG:219 = 4
ENDIF

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM EVENTEND_ZZZ
	CATCH
	ENDCATCH
	
ENDIF

;--------------------------------------------------
;@KOJO_MESSAGE_COM 관련（X1을 캐릭터번호로 치환）
;커맨드 실행시에 출력됩니다
;--------------------------------------------------
@KOJO_MESSAGE_COM_K5371
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL TRAIN_MESSAGE
	RETURN 0
ENDIF
;상정외의 구상이라면 스킵(이거 지우면 버그난다)
IF SELECTCOM < 0 || SELECTCOM > 699
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_COM_PY1_K5371
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_COM_PY1_K5371
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;함수초기화
;-------------------------------------------------
CALL 구상변수초기화
 
CALLFORM COMTYPE_{SELECTCOM}
;-------------------------------------------------
;커맨드 실행시의 대사 CFLAG 300～500
;-------------------------------------------------
IF 0 == 1
;=================================================
;애무계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("애무계")
SIF TEQUIP:25 > 0 && ( SELECTCOM == 0 || SELECTCOM == 5 || SELECTCOM == 6 || SELECTCOM == 7 || SELECTCOM == 8 || SELECTCOM == 12 || SELECTCOM == 13 || SELECTCOM == 15 )
	GOTO SHOKUSHU_PLAY
SELECTCASE SELECTCOM
;-------------------------------------------------
;0,애무
;-------------------------------------------------
	CASE 0
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:300 == 0
			KSTR:(K++) =
			CFLAG:300 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:300 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:300 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:300 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:300 = 3
			ENDIF
			;그 이외
			IF (CFLAG:300 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:300 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;1,커닐링구스
;-------------------------------------------------
	CASE 1
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:301 == 0
			KSTR:(K++) =
			CFLAG:301 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:301 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:301 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:301 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:301 = 3
			ENDIF
			;그 이외
			IF (CFLAG:301 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:301 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;4,애널핥기
;-------------------------------------------------
	CASE 4
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:304 == 0
			KSTR:(K++) =
			CFLAG:304 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:304 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:304 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:304 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:304 = 3
			ENDIF
			;그 이외
			IF (CFLAG:304 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:304 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;胸系愛撫
;6胸揉み,7乳首責め,8胸愛撫
;-------------------------------------------------
	CASE 6,7,8
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:306 == 0
			KSTR:(K++) =
			CFLAG:306 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:306 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:306 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:306 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:306 = 3
			ENDIF
			;그 이외
			IF (CFLAG:306 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:306 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;9,조개젖히기
;-------------------------------------------------
	CASE 9
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:309 == 0
			KSTR:(K++) =
			CFLAG:309 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:309 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:309 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:309 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:309 = 3
			ENDIF
			;그 이외
			IF (CFLAG:309 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:309 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;自慰共通
;10胸自慰,11自慰
;-------------------------------------------------
	CASE 10,11
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:310 == 0
			KSTR:(K++) =
			CFLAG:310 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:310 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:310 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:310 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:310 = 3
			ENDIF
			;그 이외
			IF (CFLAG:310 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:310 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;애무계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;커뮤니케이션계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("커뮤계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;20,키스한다
;-------------------------------------------------
	CASE 20
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:312 == 0
			KSTR:(K++) =
			CFLAG:312 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:312 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:312 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:312 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:312 = 3
			ENDIF
			;그 이외
			IF (CFLAG:312 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:312 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;24,스킨쉽　연모, 친애만
;-------------------------------------------------
	CASE 24
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:316 == 0
			KSTR:(K++) =
			CFLAG:316 = 1
		;2번째 이후
		ELSEIF CFLAG:316 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:316 = 2
		ENDIF
;-------------------------------------------------
;28,매도
;-------------------------------------------------
	CASE 28
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:320 == 0
			KSTR:(K++) =
			CFLAG:320 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:320 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:320 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:320 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:320 = 3
			ENDIF
			;그 이외
			IF (CFLAG:320 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:320 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;커뮤니케이션계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;도구사용 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("도구계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;40,로터
;-------------------------------------------------
	CASE 40
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:322 == 0
			KSTR:(K++) =
			CFLAG:322 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:322 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:322 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:322 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:322 = 3
			ENDIF
			;그 이외
			IF (CFLAG:322 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:322 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;41,전기마사지기
;-------------------------------------------------
	CASE 41
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:323 == 0
			KSTR:(K++) =
			CFLAG:323 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:323 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:323 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:323 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:323 = 3
			ENDIF
			;그 이외
			IF (CFLAG:323 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:323 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;48,유두로터
;-------------------------------------------------
	CASE 48
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:330 == 0
			KSTR:(K++) =
			CFLAG:330 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:330 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:330 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:330 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:330 = 3
			ENDIF
			;그 이외
			IF (CFLAG:330 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:330 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;도구사용 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;조교자도구계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("대조교자도구계")
SELECTCASE SELECTCOM
	CASEELSE
ENDSELECT
;-------------------------------------------------
;조교자도구계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;섹스계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("Ｖ성교계")
SELECTCASE SELECTCOM
	CASEELSE
ENDSELECT
;-------------------------------------------------
;섹스계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						KSTR:(K++) = 
					;음란, 창부
					ELSEIF (TALENT:4 || TALENT:7)
						KSTR:(K++) = 
					;복종, 예속
					ELSEIF (TALENT:5 || TALENT:8)
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;애널섹스계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("Ａ성교계")
SELECTCASE SELECTCOM
	CASEELSE
ENDSELECT
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;봉사계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("봉사계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;80,수음
;-------------------------------------------------
	CASE 80
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:344 == 0
			KSTR:(K++) =
			CFLAG:344 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:344 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:344 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:344 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:344 = 3
			ENDIF
			;그 이외
			IF (CFLAG:344 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:344 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;フェラチオ系
;81フェラチオ,88パイズリフェラ,211亀頭攻め(受)
;-------------------------------------------------
	CASE 81,88,211
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:345 == 0
			KSTR:(K++) =
			CFLAG:345 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:345 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:345 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:345 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:345 = 3
			ENDIF
			;그 이외
			IF (CFLAG:345 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:345 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;パイズリ系
;82パイズリ,87縦パイズリ
;-------------------------------------------------
	CASE 82,87
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:346 == 0
			KSTR:(K++) =
			CFLAG:346 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:346 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:346 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:346 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:346 = 3
			ENDIF
			;그 이외
			IF (CFLAG:346 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:346 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;83,스마타
;-------------------------------------------------
	CASE 83
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:347 == 0
			KSTR:(K++) =
			CFLAG:347 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:347 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:347 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:347 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:347 = 3
			ENDIF
			;그 이외
			IF (CFLAG:347 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:347 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;84,거품춤
;-------------------------------------------------
	CASE 84
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:348 == 0
			KSTR:(K++) =
			CFLAG:348 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:348 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:348 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:348 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:348 = 3
			ENDIF
			;그 이외
			IF (CFLAG:348 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:348 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;봉사계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;조교자봉사계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("조교자봉사계")
SELECTCASE SELECTCOM
	CASEELSE
ENDSELECT
;-------------------------------------------------
;조교자봉사계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;SM계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("ＳＭ계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;鞭打系
;100スパンキング,101乳スパンキング,102鞭
;-------------------------------------------------
	CASE 100,101,102
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:354 == 0
			KSTR:(K++) =
			CFLAG:354 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:354 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:354 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:354 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:354 = 3
			ENDIF
			;그 이외
			IF (CFLAG:354 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:354 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;104,바늘
;-------------------------------------------------
	CASE 104
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:358 == 0
			KSTR:(K++) =
			CFLAG:358 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:358 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:358 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:358 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:358 = 3
			ENDIF
			;그 이외
			IF (CFLAG:358 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:358 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;SM계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;조수에 의한 커맨드, 및 레즈플레이
;=================================================
ELSEIF RESULT == COMTYPE("조수・레즈계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;124,더블펠라
;-------------------------------------------------
	CASE 124
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:366 == 0
			KSTR:(K++) =
			CFLAG:366 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:366 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:366 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:366 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:366 = 3
			ENDIF
			;그 이외
			IF (CFLAG:366 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:366 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;영향에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;평소보다 느끼고 있구나 라는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;레즈 같은 대사에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
		;둘의 펠라는 기분 좋지？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_6
 
;-------------------------------------------------
;125,더블파이즈리
;-------------------------------------------------
	CASE 125
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:367 == 0
			KSTR:(K++) =
			CFLAG:367 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:367 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:367 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:367 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:367 = 3
			ENDIF
			;그 이외
			IF (CFLAG:367 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:367 = 2
			ENDIF
		ENDIF
		
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;영향에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;평소보다 느끼고 있구나 라는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;서로 젖꼭지가 스쳐서 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
		;둘의 파이즈리로 기분 좋아져 줘 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_6
		;파이즈리로 느끼고 있어？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_7
 
	CASEELSE
ENDSELECT
;-------------------------------------------------
;조수에 의한 공통, 및 레즈플레이
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;하드한조교 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("하드계")
SELECTCASE SELECTCOM
	CASEELSE
ENDSELECT
;-------------------------------------------------
;하드한조교 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;촉수계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("촉수계")
$SHOKUSHU_PLAY
SELECTCASE SELECTCOM
;-------------------------------------------------
;触手胸系愛撫
;6胸揉み,7乳首責め,8胸愛撫
;-------------------------------------------------
	CASE 6,7,8
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:457 == 0
			KSTR:(K++) =
			CFLAG:457 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:457 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:457 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:457 = 3
			ENDIF
			;그 이외
			IF (CFLAG:457 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:457 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;160,촉수소환
;-------------------------------------------------
	CASE 160
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:25
			;처음
			IF CFLAG:382 == 0
				KSTR:(K++) =
				CFLAG:382 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:382 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:382 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:382 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:382 = 3
				ENDIF
				;그 이외
				IF (CFLAG:382 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:382 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;触手愛撫共通
;-------------------------------------------------
	IF SELECTCOM < 20 && !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
	ENDIF
;-------------------------------------------------
;촉수계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;특수커맨드
;=================================================
ELSEIF RESULT == COMTYPE("특수커맨드계") && SELECTCOM < 600
SELECTCASE SELECTCOM
;-------------------------------------------------
;187,목욕탕플레이
;-------------------------------------------------
	CASE 187
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:31
			;처음
			IF CFLAG:399 == 0
				KSTR:(K++) =
				CFLAG:399 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:399 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF (TALENT:3 || TALENT:6)
						KSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)
						KSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)
						KSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) = 
					ENDIF
					CFLAG:399 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:399 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) = 
					CFLAG:399 = 3
				ENDIF
				;그 이외
				IF (CFLAG:399 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:399 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;189,새댁플레이　연모, 친애만
;-------------------------------------------------
	CASE 189
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:33
			;처음
			IF CFLAG:401 == 0
				KSTR:(K++) =
				CFLAG:401 = 1
			;2번째 이후
			ELSEIF CFLAG:401 < 2 || FLAG:7 == 2
				KSTR:(K++) =
				CFLAG:401 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:401 == 1 || FLAG:7 == 2
				KSTR:(K++) =
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;특수 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;500번대,파생커맨드
;=================================================
ELSEIF RESULT == COMTYPE("派生系") && SELECTCOM < 600
SELECTCASE SELECTCOM
;-------------------------------------------------
;奥攻めセックス
;503Gスポット刺激,506挿入局所責め,507挿入子宮口責め
;-------------------------------------------------
	CASE 503,506,507
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:410 == 0
			KSTR:(K++) =
			CFLAG:410 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:410 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
				CFLAG:410 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:410 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) = 
				CFLAG:410 = 3
			ENDIF
			;그 이외
			IF (CFLAG:410 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:410 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;500번대,파생 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
				;연모, 친애
				IF (TALENT:3 || TALENT:6)
					KSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)
					KSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)
					KSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					KSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				KSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;600번대,비전투원 커맨드
;=================================================
ELSEIF SELECTCOM >= 600
SELECTCASE SELECTCOM
	CASEELSE
ENDSELECT
ENDIF
;-------------------------------------------------
;그 외, 어느 계통의 조교인지 몰랐던 녀셕
;-------------------------------------------------
IF !구상유무확인(0)
SELECTCASE SELECTCOM
 
ENDSELECT
ENDIF
;------------------------------------------------
;커맨드구상 여기까지
;------------------------------------------------
 
CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_COM_ZZZ
	CATCH
		CALL TRAIN_MESSAGE
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;윤간시의 구상
;CASE 0 안은 반드시 건너뛰게 되어있으며
;RETURN에서 0을 반환하면, 이후 통상적인 윤간행동을 출력합니다
;CFLAG는 사용하지 않습니다
;COMF201_윤간 내에서 LOCAL을 사용하고 있으므로 사용하지 않습니다
;-------------------------------------------------
@KOJO_MESSAGE_RINKAN_K5371, ARG:1 , ARG:2
CALL 구상변수초기화

SELECTCASE ARG:1
	CASE 0
	;CASE 0은 오리지널 윤간 구상
	;C:1회당 귀가 인수 D：흥분도 증가(및 감소)
	;C와 D는 오리지널 윤간 구상 내에서만 사용
	;C += 、D += 를 사용
		IF ARG:2 == 1
		;하드ON
		
		ELSE
		;하드OFF
		
		ENDIF
	;하드계
	;가슴을 밟힌다（거유・폭유만）
	CASE 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && FLAG:7 == 2
			;연모, 친애
			IF (TALENT:3 || TALENT:6)
				WSTR:(K++) = 
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)
				WSTR:(K++) = 
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)
				WSTR:(K++) = 
			ENDIF
			;함락완료 공통
			IF KSTR:0 == ""
				WSTR:(K++) = 
			ENDIF
		ENDIF
		;굴복각인Lv3
		IF MARK:2 == 3 && FLAG:7 == 2 && KSTR:0 == ""
			WSTR:(K++) = 
		ENDIF
		;그 이외
		IF FLAG:7 == 2 && KSTR:0 == ""
			WSTR:(K++) =
		ENDIF
	;머리를 밟힌다
	CASE 2
	
	;폭력을 당하다
	CASE 3
	
	;담배빵
	;흥분도가 낮고, 반항적, 꿋꿋함, 프라이드높음, 건방짐, 저항을 가지고 있다
	CASE 4
	
	;지면에 늘어진 정액 핥아먹인다
	;마조끼３이상
	CASE 5
	
	;몸에 낙서를 당하고 범해진다
	CASE 6
	
	;엉덩이를 치켜올려 뒤로부터 범해지면서, 머리를 짓밟혀 굴욕으로 글썽이는 상태
	CASE 7
	
	;통상계
	;음란계로 눈앞에 장대가 진열되어, 어떤 것을 원하는지 선택하게 하거나（음란・창부만）
	CASE 8
	
	;거유 이상의 캐릭터라면 「그 젖은 장식이냐？ 쉬고 있지 말라고」등등 매도당하며 파이즈리한다（거유・폭유만）
	CASE 9
	
	;삽입만은 피하려고 필사적으로 입과 손으로 몰려드는 자지에 봉사한다（연모・친애만）
	CASE 10
	
	;;양구멍을 뚫리면서 억지로 미소와 피스싸인을 취하게 시켜져 기념촬영
	CASE 11
	
	;;윤간후의 뒷처리 묘사에서, 본인 정액 투성이가 되었는데도 주위 남자들의 육봉을 혀로 청소시키고 있다
	CASE 12
	
	;안에 사정당해도 즉시 다른 사람으로 교대
	CASE 13
	
	;일제사정으로 정액투성이
	CASE 14
	
	;;양구멍 찔리며 동시 사정
	CASE 15
	
	;기승위+양겨드랑이잡
	CASE 16
	
	;;입+보지
	CASE 17
	
ENDSELECT

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_RINKAN_ZZZ
	CATCH
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG 관련（X1을 캐릭터번호로 치환）
;파라미터 변화 등을 트리거로 한 구상　CFLAG 221～260을 사용
;-------------------------------------------------
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_A（X1을 캐릭터번호로 치환）
;조교자의 사정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K5371
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_A
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K5371
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K5371
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교자의 사정시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
LOCAL = 0;사정량
;-------------------------------------------------
;사정장소 변경시의 텍스트　유효시 여기서 함수 종료
;-------------------------------------------------


IF CFLAG:106
	LOCAL = 1
	K_CALL:K = PALAM_MESSAGE_A
	IF 1 == 0
	;얼굴
	ELSEIF CFLAG:106 == 2				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:222 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				WSTR:(K++) = 
				CFLAG:222= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				WSTR:(K++) = 
				CFLAG:222= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				WSTR:(K++) = 
				CFLAG:222= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:222= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:222 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:222= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:222 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:222= 1	
		ENDIF
	;콘돔
	ELSEIF CFLAG:106 == 9
		LOCAL = 0
		;섹스로 사정하고, 애널섹스 이외를 실행 시
		IF GROUPMATCH(SELECTCOM , 60 , 61 , 66 , 68 , 69 , 72 , 504 , 505 , 506 , 507)					
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:229 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
					CFLAG:229= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
					CFLAG:229= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
					CFLAG:229= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:229= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:229 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:229= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:229 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:229= 1	
			ENDIF		
		ELSE					
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
					CFLAG:230= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
					CFLAG:230= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
					CFLAG:230= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:230= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:230= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:230 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:230= 1	
			ENDIF		
		ENDIF
	ENDIF
ENDIF


;-------------------------------------------------
;손, 입 이외로 사정
;대응 커맨드는 키스, 스마타, 풋잡, 애널강제봉사, 더블스마타, 조수를범하게한다
;-------------------------------------------------
IF TFLAG:9 > 0
	K_CALL:K = PALAM_MESSAGE_A
	IF !구상유무확인(0)
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				WSTR:(K++) = 
				CFLAG:230= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				WSTR:(K++) = 
				CFLAG:230= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				WSTR:(K++) = 
				CFLAG:230= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:230= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:230= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:230 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:230= 1	
		ENDIF		
	ENDIF
	LOCAL = TFLAG:9
ENDIF

;-------------------------------------------------
;조수를범한다로 사정
;-------------------------------------------------
IF TFLAG:7 > 0
	K_CALL:K = PALAM_MESSAGE_A
	IF !구상유무확인(0)
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:232 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				WSTR:(K++) = 
				CFLAG:232= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				WSTR:(K++) = 
				CFLAG:232= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				WSTR:(K++) = 
				CFLAG:232= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:232= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:232= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:232 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:232= 1	
		ENDIF		
	ENDIF	
	LOCAL = TFLAG:7
ENDIF

;-------------------------------------------------
;입으로 사정
;-------------------------------------------------
IF TFLAG:0 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;이라마치오로 사정
	IF SELECTCOM == 140
		IF !구상유무확인(0)
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
	;그 이외의 입사정
	ELSE
		IF !구상유무확인(0)
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
	ENDIF
	LOCAL = TFLAG:0
ENDIF

;-------------------------------------------------
;손으로 사정
;-------------------------------------------------
IF TFLAG:1 > 0
	K_CALL:K = PALAM_MESSAGE_A
	IF !구상유무확인(0)
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:234 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				WSTR:(K++) = 
				CFLAG:234= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				WSTR:(K++) = 
				CFLAG:234= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				WSTR:(K++) = 
				CFLAG:234= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:234= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:234= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:234 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:234= 1	
		ENDIF		
	ENDIF	
	LOCAL = TFLAG:1
ENDIF

;-------------------------------------------------
;가슴으로 사정
;-------------------------------------------------
IF TFLAG:34 > 0
	K_CALL:K = PALAM_MESSAGE_A
	IF !구상유무확인(0)
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:235 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				WSTR:(K++) = 
				CFLAG:235= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				WSTR:(K++) = 
				CFLAG:235= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				WSTR:(K++) = 
				CFLAG:235= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:235= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:235= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:235 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:235= 1	
		ENDIF		
	ENDIF	
	LOCAL = TFLAG:34
ENDIF
W:K = 1
;-------------------------------------------------
;오나홀에 사정
;-------------------------------------------------
IF TFLAG:16 == 1
	K_CALL:K = PALAM_MESSAGE_A
	;통상					
	IF !구상유무확인(0)
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:236 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				WSTR:(K++) = 
				CFLAG:236= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				WSTR:(K++) = 
				CFLAG:236= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				WSTR:(K++) = 
				CFLAG:236= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:236= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:236= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:236 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:236= 1	
		ENDIF		
	ENDIF	
	LOCAL = 0
ENDIF

;-------------------------------------------------
;섹스로 사정
;-------------------------------------------------
IF TFLAG:2 > 0
	LOCAL = TFLAG:2
	K_CALL:K = PALAM_MESSAGE_A
	;애널섹스계
	IF SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || SELECTCOM == 512
		IF !구상유무확인(0)
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:237 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
					CFLAG:237= 4
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
					CFLAG:237= 3
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
					CFLAG:237= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:237= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:237= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:237 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:237= 1	
			ENDIF		
		ENDIF
	;V계 섹스
	ELSE
		;안쪽 자극 섹스계
		IF SELECTCOM == 506 || SELECTCOM == 507
			IF !구상유무확인(0)
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:238 < 3 || FLAG:7 == 2)		
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
						CFLAG:238= 4
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
						CFLAG:238= 3
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
						CFLAG:238= 3
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
						CFLAG:238= 3
					ENDIF	
				ENDIF		
				;굴복각인Lv3		
				IF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:238= 2	
				ENDIF		
				;그 이외		
				IF (CFLAG:238 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:238= 1	
				ENDIF		
			ENDIF
		
		ENDIF
		;그 외 섹스계
		IF !구상유무확인(0)
			IF !구상유무확인(0)
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:239 < 3 || FLAG:7 == 2)		
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
						CFLAG:239= 4
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
						CFLAG:239= 3
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
						CFLAG:239= 3
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
						CFLAG:239= 3
					ENDIF	
				ENDIF		
				;굴복각인Lv3		
				IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:239= 2	
				ENDIF		
				;그 이외		
				IF (CFLAG:239 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) =
					CFLAG:239= 1	
				ENDIF		
			ENDIF	
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;역강간으로 사정시켰다
;-------------------------------------------------
IF SELECTCOM == 64 && TFLAG:10 > 0
	LOCAL = TFLAG:10
	K_CALL:K = PALAM_MESSAGE_A
	IF !구상유무확인(0)
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				WSTR:(K++) = 
				CFLAG:240= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				WSTR:(K++) = 
				CFLAG:240= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:240= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:240= 1	
		ENDIF		
	ENDIF	
ENDIF
;-------------------------------------------------
;사정 공통
;-------------------------------------------------
	IF !구상유무확인(0) && LOCAL > 0
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				WSTR:(K++) = 
				CFLAG:240= 4
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				WSTR:(K++) = 
				CFLAG:240= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:240= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:240= 1	
		ENDIF		
	ENDIF	

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_PALAMCNG_A_ZZZ
	CATCH
		CALL PALAM_MESSAGE_A
	ENDCATCH
	
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_E（X1을 캐릭터번호로 치환）
;촉수의 사정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_E_K5371
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_E
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K5371
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K5371
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;촉수의 사정시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
;-------------------------------------------------
;촉수가 사정
;-------------------------------------------------
IF TFLAG:15 >0
	K_CALL:K = PALAM_MESSAGE_E			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:241 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			WSTR:(K++) = 
			CFLAG:241= 4
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			WSTR:(K++) = 
			CFLAG:241= 3
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			WSTR:(K++) = 
			CFLAG:241= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(0)	
			WSTR:(K++) = 
			CFLAG:241= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:241 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
		WSTR:(K++) = 	
		CFLAG:241= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:241 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
		WSTR:(K++) =
		CFLAG:241= 1	
	ENDIF		
ENDIF

CALL 구상변수표시
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_COM_ZZZ
	CATCH
		CALL PALAM_MESSAGE_E
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_B 관련（X1을 캐릭터번호로 치환）
;노예의 절정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_B_K5371
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_B
	RETURN 0
ENDIF
;상정외의 구상이라면 스킵(이거 지우면 버그난다)
IF SELECTCOM < 0 || SELECTCOM > 700
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K5371
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K5371
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;노예의 절정시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
;-------------------------------------------------
;처음 C절정
;-------------------------------------------------
IF NOWEX:0 > 0 && CFLAG:242 == 0
	WSTR:(K++) =
	CFLAG:242 = 1
ENDIF

;-------------------------------------------------
;처음 V절정
;-------------------------------------------------
IF NOWEX:1 > 0 && CFLAG:243 == 0
	WSTR:(K++) =
	CFLAG:243 = 1
ENDIF

;-------------------------------------------------
;처음 A절정
;-------------------------------------------------
IF NOWEX:2 > 0 && CFLAG:244 == 0
	WSTR:(K++) =
	CFLAG:244 = 1
ENDIF

;-------------------------------------------------
;처음 B절정
;-------------------------------------------------
IF NOWEX:3 > 0 && CFLAG:245 == 0
	WSTR:(K++) =
	CFLAG:245 = 1
ENDIF

;-------------------------------------------------
;절정
;-------------------------------------------------
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

IF TFLAG:29 > 0
	K_CALL:K = PALAM_MESSAGE_B			
	;강절정
	IF TFLAG:29 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:246 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				WSTR:(K++) = 
				CFLAG:246= 3
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				WSTR:(K++) = 
				CFLAG:246= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(LOCAL)	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			WSTR:(K++) = 	
			CFLAG:246= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:246 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			WSTR:(K++) = 	
			CFLAG:246= 1	
		ENDIF		
	ENDIF
	;절정
	IF !구상유무확인(LOCAL)
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:246 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF (TALENT:3 || TALENT:6)	
				WSTR:(K++) = 
				CFLAG:246= 3
			;음란, 창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				WSTR:(K++) = 
				CFLAG:246= 3
			;복종, 예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(LOCAL)	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			WSTR:(K++) = 	
			CFLAG:246= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:246 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			WSTR:(K++) =
			CFLAG:246= 1	
		ENDIF		
	ENDIF
ENDIF
;-------------------------------------------------
;사정
;-------------------------------------------------
;사정하고 있다
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

IF TFLAG:10 > 0				
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:247 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			WSTR:(K++) = 
			CFLAG:247= 4
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			WSTR:(K++) = 
			CFLAG:247= 3
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			WSTR:(K++) = 
			CFLAG:247= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)	
			WSTR:(K++) = 
			CFLAG:247= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:247 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) = 	
		CFLAG:247= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:247 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) =
		CFLAG:247= 1	
	ENDIF		
ENDIF

;-------------------------------------------------
;분유
;-------------------------------------------------
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

;분유하고 있다
IF TFLAG:11 > 0			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:248 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			WSTR:(K++) = 
			CFLAG:248= 4
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			WSTR:(K++) = 
			CFLAG:248= 3
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			WSTR:(K++) = 
			CFLAG:248= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)	
			WSTR:(K++) = 
			CFLAG:248= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:248 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) = 	
		CFLAG:248= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:248 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) =
		CFLAG:248= 1	
	ENDIF		
ENDIF

;-------------------------------------------------
;방뇨
;-------------------------------------------------
;방뇨하고 있다
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

IF TFLAG:29 > 0 && (TEQUIP:27 || TALENT:57)			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:249 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			WSTR:(K++) = 
			CFLAG:249= 4
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			WSTR:(K++) = 
			CFLAG:249= 3
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			WSTR:(K++) = 
			CFLAG:249= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)	
			WSTR:(K++) = 
			CFLAG:249= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:249 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) = 	
		CFLAG:249= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:249 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) =
		CFLAG:249= 1	
	ENDIF		
ENDIF

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_PALAMCNG_B_ZZZ
	CATCH
		CALL PALAM_MESSAGE_B
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_C 관련（X1을 캐릭터번호로 치환）
;조교 후의 변화를 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_C_K5371
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_C
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K5371
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K5371
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교 후의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;처녀상실(처녀만)
;-------------------------------------------------
CALL 구상변수초기화

IF TFLAG:3 > 0
	K_CALL:K = PALAM_MESSAGE_C
	;연모나 친애이면서 주인의 섹스에 의한 처녀상실
	IF TFLAG:20
		WSTR:(K++) =
	;그 이외의 때에 처녀상실
	ELSE			
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			WSTR:(K++) = 
		ENDIF	
		;굴복각인Lv3
		IF MARK:2 == 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
ENDIF

;-------------------------------------------------
;아무것도안한다 후
;-------------------------------------------------
IF SELECTCOM == 21
	K_CALL:K = PALAM_MESSAGE_C
	;欲情LV5以上
	IF !구상유무확인(0) && PALAM:5 >= PALAMLV:5
		;함락완료
		IF 함락()
			WSTR:(K++) =
		ENDIF
		;그 이외
		IF !구상유무확인(0)
			WSTR:(K++) =「何もしないなら終わりにしてもらえませんか」
		ENDIF
	ENDIF	
	;欲情LV3以上
	IF !구상유무확인(0) && PALAM:5 >= PALAMLV:3
		;함락완료
		IF 함락()
			WSTR:(K++) =
		ENDIF
		;그 이외
		IF !구상유무확인(0)
			WSTR:(K++) =「ぼーっとするのって、幸せですね」
		ENDIF
	ENDIF	
ENDIF

;-------------------------------------------------
;청소펠라
;-------------------------------------------------
IF TFLAG:8 > 0
	;ABL:정액중독　ABL:ASSI:정액중독
	K_CALL:K = PALAM_MESSAGE_C
	;더블펠라로 조수와 청소펠라
	IF TFLAG:8 >= 2 && !구상유무확인(0)
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			WSTR:(K++) = 
		ENDIF	
		;순종Lv3
		IF ABL:0 >= 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
	;펠라 후 청소펠라
	IF !구상유무확인(0)
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			WSTR:(K++) = 
		ENDIF	
		;순종Lv3
		IF ABL:0 >= 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
ENDIF

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_COM_ZZZ
	CATCH
		CALL PALAM_MESSAGE_C
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_D 관련（X1을 캐릭터번호로 치환）
;파라미터 변화를 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_D_K5371
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_D
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K5371
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K5371
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;파라미터 변화시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
;-------------------------------------------------
;처음 윤활이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:4 + CUP:4
IF P > PALAMLV:4 && CFLAG:250 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;로션을 사용한 경우
	IF SELECTCOM == 180
		WSTR:(K++) =
	;그 이외
	ELSE
		WSTR:(K++) =「なんだこれ……%CSTR:일인칭%から出たもの？　気持ちよくなったら出てくる？　……そう、なんだ」
		WSTR:(K++) = （この行為を受け入れている？　そんな馬鹿な……）
	ENDIF
	CFLAG:250 = 1
ENDIF

;-------------------------------------------------
;처음 욕정이 LV2를 넘었다
;-------------------------------------------------
P = PALAM:6 + CUP:6
IF P > PALAMLV:3 && CFLAG:251 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;미약을 사용한 경우
	IF SELECTCOM == 181
		WSTR:(K++) =
	;그 이외
	ELSE
		WSTR:(K++) =
	ENDIF
	CFLAG:251 = 1
ENDIF

;-------------------------------------------------
;처음 굴복이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:7 + CUP:7
IF P > PALAMLV:3 && CFLAG:252 == 0
	K_CALL:K = PALAM_MESSAGE_D
	WSTR:(K++) =
	CFLAG:252 = 1
ENDIF

;-------------------------------------------------
;처음 복종이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:5 + CUP:5
IF P > PALAMLV:3 && CFLAG:253 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;함락완료
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		WSTR:(K++) =
	;그 이외
	ELSE
		WSTR:(K++) =
	ENDIF
	CFLAG:253 = 1
ENDIF

;-------------------------------------------------
;처음 공포가 LV3을 넘었다
;-------------------------------------------------
P = PALAM:11 + CUP:11
IF P > PALAMLV:3 && CFLAG:254 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;함락완료
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		WSTR:(K++) =
	;그 이외
	ELSE
		WSTR:(K++) =（このヒトは、これまで出会ったヒトの中で一番危険な人だ……）
		WSTR:(K++) =（暴力的だとか、威圧的だとかじゃない。このヒトの根は、人を作り変えることに、何の迷いもない）
		WSTR:(K++) =（この人にとって人間は商品で、この行為は商品を加工する作業なんだ……ああ、きっと私も……） 
	ENDIF
	CFLAG:254 = 1
ENDIF

CALL 구상변수표시
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_PALAMCNG_D_ZZZ
	CATCH
		CALL PALAM_MESSAGE_D
	ENDCATCH
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_MARKCNG 관련（X1을 캐릭터번호로 치환）
;각인 변동을 트리거로 한 구상
;각인 변동 후에 구상을 발동합니다
;-------------------------------------------------
@KOJO_MESSAGE_MARKCNG_K5371
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL MARK_MESSAGE
	RETURN 0
ENDIF

CALL 구상변수초기화
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_MARKCNG_PY1_K5371
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_MARKCNG_PY1_K5371
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;고통각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:22 == 3
	K_CALL:K = MARK_MESSAGE_A	
	;연모, 친애	
	IF (TALENT:3 || TALENT:6)	
		WSTR:(K++) = 
	;음란, 창부	
	ELSEIF (TALENT:4 || TALENT:7)	
		WSTR:(K++) = 
	;복종, 예속	
	ELSEIF (TALENT:5 || TALENT:8)	
		WSTR:(K++) = 
	ENDIF	
	;공통
	IF !구상유무확인(LOCAL)
		WSTR:(K++) = 「はっ、ぐぅ……痛いのには、慣れてます。けど……これは正直、つらい……」
	ENDIF		
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_A
ENDIF

;-------------------------------------------------
;쾌락각인Lv3
;-------------------------------------------------

LOCAL = K
IF TFLAG:23 == 3
	K_CALL:K = MARK_MESSAGE_P
	;연모, 친애	
	IF (TALENT:3 || TALENT:6)	
		WSTR:(K++) = 
	;복종, 예속	
	ELSEIF (TALENT:5 || TALENT:8)	
		WSTR:(K++) = 
	ENDIF	
	;공통
	IF !구상유무확인(LOCAL)
		WSTR:(K++) = 「世の中には……こんなに気持ちいいことがあったんだ……クセになりそうでちょっと怖い」
	ENDIF		
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_P
ENDIF

;-------------------------------------------------
;굴복각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:24 == 13 || TFLAG:24 == 23 || TFLAG:24 == 33
	K_CALL:K = MARK_MESSAGE_S
	;쾌락에 의한 굴복
	IF TFLAG:24 == 13
		WSTR:(K++) = （この快楽には逆らえないな……理性で嫌がっても、体が気持ちよさを思い出すと……あぁ、だめだ。垂れてきた……）
	;공포와 고통에 의한 굴복
	ELSEIF TFLAG:24 == 23
		WSTR:(K++) = （痛い……怖い………つらい。どうして%CSTR:이인칭%は私をいじめるの？　私が反抗的だから？　私がおとなしく従えば、ひどいことはしないのだろうか）
	;굴복과 복종에 의한 굴복
	ELSEIF TFLAG:24 == 33
		WSTR:(K++) = 
	ENDIF
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_S
ENDIF

;-------------------------------------------------
;반발각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:21 == 3
	K_CALL:K = MARK_MESSAGE_R
	;함락완료
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;연모, 친애	
		IF (TALENT:3 || TALENT:6)	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			WSTR:(K++) = 
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)
			WSTR:(K++) = 
		ENDIF	
	ENDIF
	;그 이외
	IF !구상유무확인(LOCAL)
		WSTR:(K++) = 「%CSTR:이인칭%がはじめてですよ。私をこんなに怒らせたのは」
		WSTR:(K++) = 　マルフーシャの瞳には憎悪の赤が灯っている。調教の方針を見直したほうがいいかもしれない。
	ENDIF
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_R
ENDIF

CALL 구상변수표시
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_MARKCNG_ZZZ
	CATCH
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;이벤트구상 모음（X1을 캐릭터번호로 치환한다） CFLAG 261～299를 사용
;-------------------------------------------------
@KOJO_MESSAGE_EVENT_K5371

;--------------------------------------------------
;특정 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_EVENT_PY1_K5371
;	RETURN 0
;ENDIF


CALL 구상변수초기화

SELECTCASE TFLAG:13
;　　1:아침펠라
;　　2:아침파이즈리
;　　3:아침기승위
;　　4:조교 후 레즈플레이
;　　5:조교 후 섹스
;　　6:강정신약의 반동
;　　7:조교 후 착유
;　　8:조교 후 자위
;　　9:조교 후 요바이
;　　10:황색사탕 사용시
;　　11:임신 발각시
;　　12:출산시
;　　13:육아실에서의 이벤트구상
;　　14:자식 독립시
;　　15:정식포장마차 실행시
;　　16:창관에서의 아르바이트시
;　　17:렌탈 실행시
;　　18:매각시
;　　19:비디오 매각시
;　　20:콘서트 실행시
;　　21:난교라이브 실행시
;　　22:공중변소 플레이 실행시
;　　23:비디오 보유시
;　　24:아침기승위A
;　　25:아침키스
;　　26:아침식사
;　　27:일어나자 곁에서 자고 있다
;　　28:아침풋잡
;　　29:아침에 갓 짠 밀크
;　　30:잠자다 실례
;　　31:요바이 C/A
;　　32:요바이 V/C
;　　33:요바이 A/C
;　　34:요바이 C/C
;　　35:밤자위
;　　36:같이잔다
;　　37:起きたら隣で寝てる
;　　38:朝の足コキ
;　　39:朝の搾りたてミルク
;　　40:아내・남편 취득
;　　41:음마 취득
;　　42:노리개 취득
;　　43:スト劇場
;　　44:갈아입기・가슴을 드러낸다
;　　45:애(愛)계 ＮＴＲ이벤트
;　　46:음(淫)계 ＮＴＲ이벤트
;　　47:예(隷)계 ＮＴＲ이벤트
;　　48:애(愛)계 재함락 이벤트
;　　49:음(淫)계 재함락 이벤트
;　　50:예(隷)계 재함락 이벤트
;　　51:ＮＴＲ요바이・난교파티
;　　52:ＮＴＲ요바이・공원
;　　53:ＮＴＲ요바이・변기
;　　54:ＮＴＲ요바이・밤의 거리
;　　55:ＮＴＲ요바이・ＮＴＲ전 밀회
;　　56:ＮＴＲ요바이・ＮＴＲ후 첫 밀회
;　　57:ＮＴＲ요바이・데이트
;　　58:ＮＴＲ요바이・본방
;　　59:ＮＴＲ요바이・자위서로과시
;　　60:ＮＴＲ요바이・애널섹스
;　　61:ＮＴＲ요바이・야외플레이
;　　62:ＮＴＲ요바이・야외조교
;　　63:창관에서 실제로 일했을 때
;　　65:교회에서 기도
;　　66:교회에서 입봉사
;　　67:교회에서 성봉사
;　　70:陰唇・胸同時露出(調教対象)
;　　71:陰唇・胸同時露出(主人)
;　　72:陰唇・胸同時露出(助手)
;　　73:陰唇出(調教対象)
;　　74:음순노출(主人)
;　　75:음순노출(助手)
;　　76:胸露出(調教対象)
;　　77:胸露出(主人)
;　　78:胸露出(助手)
;　　79:던전내 성욕처리：동료마와 섹스(당하는 측)
;　　80:던전내 성욕처리：동료마에게 봉사시킨다(봉사측)
;　　81:던전내 성욕처리：그 장소에서 자위
;　　82:던전내 성욕처리：그늘에서 자위
;　　83:던전내 성욕처리：동료마와 섹스(하는 측)
;　　84:던전내 성욕처리：동료마에게 봉사시킨다(봉사시키는 측)
;　　85:던전내 성욕처리：윤간당한다
;　　90:도장 개시
;　　91:도장 종료
;　　92:맹우
;　　93:탄광
		;-------------------------------------------------
		;아침펠라
		;-------------------------------------------------
	CASE 1
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:261 < 1 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:261 = 1
			ENDIF

		;-------------------------------------------------
		;아침파이즈리
		;-------------------------------------------------
	CASE 2
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:262 < 1 || FLAG:7 == 2)
				;모유체질
				IF TALENT:149						
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;모유체질이 아니다
				IF !구상유무확인(0)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) =
					ENDIF	
				ENDIF
				CFLAG:262 = 1
			ENDIF

		;-------------------------------------------------
		;아침기승위
		;-------------------------------------------------
	CASE 3
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침기승위A
		;-------------------------------------------------
	CASE 24
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침키스 연모, 친애 전제
		;-------------------------------------------------
	CASE 25
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침식사
		;-------------------------------------------------
	CASE 26
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;노래 부르면서
				IF (TALENT:25 || TALENT:28) && ABL:14 >= 2						
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;노래 부르지 않음
				IF !구상유무확인(0)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 「おはようございます。朝ごはんはダルニツキーと目玉焼きです。それから紅茶も淹れておきました。ジャムを入れて飲むとおいしいんですよ。一緒に食べましょう」
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) =
					ENDIF	
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;일어나자 곁에서 자고 있다 연모, 친애 전제
		;-------------------------------------------------
	CASE 27
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				WSTR:(K++) =「……あ、起きた」
				WSTR:(K++) = 　朝起きたら、目の前に美少女が居た……なんだいつものことか。%CALLNAME:MASTER%はもう一度目を閉じて、二度寝に移行した。
				WSTR:(K++) =「二度寝したら出撃の時間に間に合わないよ。評価が下がって給料も下がるからね。さあ、起きて」
				WSTR:(K++) =　 布団をはがされ、ベッドから引きずり降ろされた。
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침풋잡
		;-------------------------------------------------
	CASE 28
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;아침에 갓 짠 밀크
		;-------------------------------------------------
	CASE 29
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;잠자다 실례
		;-------------------------------------------------
	CASE 30
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 레즈플레이
		;-------------------------------------------------
	CASE 4
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:264 == 0
				WSTR:(K++) =
				CFLAG:264 = 1
			;그 이외
			ELSEIF CFLAG:264 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:264 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 섹스
		;-------------------------------------------------
	CASE 5
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:265 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:265 = 1
			ENDIF

		;-------------------------------------------------
		;강정신약의 반동
		;-------------------------------------------------
	CASE 6
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:266 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:266 = 2
			ENDIF
			IF (CFLAG:266 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:266 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 착유
		;-------------------------------------------------
	CASE 7
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:267 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:267 = 2
			ENDIF
			IF (CFLAG:267 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:267 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 자위
		;-------------------------------------------------
	CASE 8
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:268 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:268 = 2
			ENDIF
			IF (CFLAG:268 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:268 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인C 상대V）
		;-------------------------------------------------
	CASE 9
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인C 상대A）
		;-------------------------------------------------
	CASE 31
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인V 상대C）
		;-------------------------------------------------
	CASE 32
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인A 상대C）
		;-------------------------------------------------
	CASE 33
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（양쪽C（스마타）・양쪽C（조개맞대기））
		;-------------------------------------------------
	CASE 34
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;밤 자위
		;-------------------------------------------------
	CASE 35
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;같이잔다 연모, 친애
		;-------------------------------------------------
	CASE 36
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:269 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;황색사탕 사용시
		;-------------------------------------------------
	CASE 10
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:270 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:270 = 2
			ENDIF
			IF (CFLAG:270 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:270 = 1
			ENDIF

		;-------------------------------------------------
		;임신 발각시
		;-------------------------------------------------
	CASE 11
			SIF CFLAG:271 >= 1 && FLAG:7 != 2
				GOTO 表示
			K_CALL:K = EVENT_MESSAGE
			;임신대상을 TARGET에 격납하고, 부친은 노예는 V, 조수는 W로 한다
			;붕괴되어 버린 경우
			IF TALENT:9 == 1
				WSTR:(K++) =
			;부친이 주인이고 모친이 연모나 친애 보유
			ELSEIF (TALENT:3 || TALENT:6) && CFLAG:102 == 1
				WSTR:(K++) =「まさか%CSTR:일인칭%が母親になる日が来ようとは……」
				WSTR:(K++) = 　マルフーシャは大きくなりつつある腹を重そうにさすっている。
				WSTR:(K++) = 　不安なのか？
				WSTR:(K++) = 「私は孤児なので……どうせなら、よい母親になりたいとは思っていますが」
				WSTR:(K++) = 「果たして母親の愛情を知らない私が、よい母親になれるのだろうか。子供を幸せにできるのだろうか。などなど、不安が尽きません」
				WSTR:(K++) = 　大丈夫だ。@がついている。それでも不安なら、子育てが得意な悪魔を連れてこよう。いい相談相手になってくれる。
				WSTR:(K++) = 「ありがとうございます。また不安になったら、頼らせてください」
				WSTR:(K++) = 　ああ。いつでも。

			;부친이 노예나 조수고 모친이 음란이나 창부 보유
			ELSEIF (TALENT:4 || TALENT:7) && (CFLAG:102 == 2 || CFLAG:102 == 3)
				;부친(조수）과의 상성이 125보다 큰 경우
				IF CFLAG:102 == 2 && GET_RELATION(TARGET, W) > 125
					WSTR:(K++) =
			 	;부친(노예）과의 상성이 125보다 큰 경우
				ELSEIF CFLAG:102 == 3 && GET_RELATION(TARGET, V) > 125
					WSTR:(K++) =
				ELSE
					WSTR:(K++) =
				ENDIF
			;복종이나 예속 보유
			ELSEIF (TALENT:5 || TALENT:8)
				WSTR:(K++) =
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:271 = 1

		;-------------------------------------------------
		;출산시
		;-------------------------------------------------
	CASE 12
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:272 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:272 = 2
			ENDIF
			IF (CFLAG:272 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:272 = 1
			ENDIF

		;-------------------------------------------------
		;육아실
		;-------------------------------------------------
	CASE 13
			K_CALL:K = EVENT_MESSAGE
			;출산전
			IF TALENT:153
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 2 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 「あ。来てくれたんですね」
					WSTR:(K++) = 具合はどうだ。
					WSTR:(K++) = 「母子ともに良好、もうすぐ産まれる予定です……もうすぐ、母親になるんですね。私が」
					WSTR:(K++) = それは何より。欲しいものはないか、あれば買ってこよう。
					WSTR:(K++) = 「欲しいものはないですけど……抱きしめてください。やっぱり少し不安なので」
					WSTR:(K++) = %CALLNAME:MASTER%はベッドで身を起こすマルフーシャを優しく抱きしめた。
					WSTR:(K++) = 密着すると、彼女の鼓動と、もう一つ小さな鼓動を感じる。彼女の腹の中に、新たな命がいるのだと改めて実感した。
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
					CFLAG:273 = 2
				ENDIF
				IF (CFLAG:273 < 1 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) =
					CFLAG:273 = 1
				ENDIF
			;출산후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 4 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 「ほら、見てください。%CSTR:이인칭%の子供ですよ……こんなにかわいい」
						WSTR:(K++) = 「妹に姪ができたって教えてあげたらどうなるかな。『お姉ちゃんが取られた！』なんて言いそう……それとも『私がおばさんだなんて！』かな？　ふふ」
					
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
					CFLAG:273 = 4
				ENDIF
				IF (CFLAG:273 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) =
					CFLAG:273 = 3
				ENDIF
			ENDIF

		;-------------------------------------------------
		;자식 독립시
		;-------------------------------------------------
	CASE 14
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:274 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 「子供の成長って早いですねー。この前生まれたと思ったらもう親離れですか……」
					WSTR:(K++) = 「うん。やっぱり私の持ってる常識と違うような気がする。これは私がおかしいのか、この世界がおかしいのか」
					WSTR:(K++) = 「まあそれはともかく……元気でね」
					WSTR:(K++) = %CALLNAME:MASTER%とマルフーシャの子供は元気に旅立っていった。
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:274 = 2
			ENDIF
			IF (CFLAG:274 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:274 = 1
			ENDIF
		;-------------------------------------------------
		;정식포장마차시
		;-------------------------------------------------
	CASE 15
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:275 == 0
				WSTR:(K++) = 平和な仕事って、いいね……
				CFLAG:275 = 1
			;그 이외
			ELSEIF CFLAG:275 < 1 || FLAG:7 == 2
				WSTR:(K++) = 焼きたてのおいしいパンはいかがですかー。ヘルシーなダルニツキー（黒パン）に柔らかくて甘いストリーチヌイ。特別な日のためにカラヴァイの注文も承っておりますー。
				WSTR:(K++) = マルフーシャはパン屋としてよく働いてくれている。本業だからか、銃を握っている時よりも楽しそうだ。
				CFLAG:275 = 1
			ENDIF

		;-------------------------------------------------
		;창관에서 아르바이트・설정시
		;-------------------------------------------------
	CASE 16
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:276 == 0
				WSTR:(K++) = 「こんな貧相な小娘に客は来るのだろうか……ほかにも美人やスタイルのいい人はいくらでも居るのに」
				CFLAG:276 = 1
			;그 이외
			ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
				WSTR:(K++) = 「えぇ……嫌って言いましたよね？　命令？　……わかりました」
				CFLAG:276 = 1
			ENDIF
		;-------------------------------------------------
		;창관에서 아르바이트・실행시
		;-------------------------------------------------
	CASE 63
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:606 == 0
				WSTR:(K++) = 「……えーと。お代は結構なのでキャンセルして帰っていただけないでしょうか。ダメ？」
				CFLAG:606 = 1
			;그 이외
			ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
				WSTR:(K++) = 「……こんな私でも需要ってあるんだ。意外」
								CFLAG:606 = 1
			ENDIF


		;-------------------------------------------------
		;렌탈시
		;-------------------------------------------------
	CASE 17
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:277 == 0
				WSTR:(K++) =
				CFLAG:277 = 1
			;그 이외
			ELSEIF CFLAG:277 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:277 = 1
			ENDIF

		;-------------------------------------------------
		;매각시
		;-------------------------------------------------
	CASE 18
			K_CALL:K = EVENT_MESSAGE
			;반발각인Lv3
			IF MARK:3 == 3
				WSTR:(K++) = 「やっとお別れですか。清々します。せいぜい夜道に気をつけて……いつか殺しに来ますから」
				WSTR:(K++) = 売った奴隷がタダで戻ってくる。お得でしかないな。夜道に限らずいつでも歓迎だぞ。
			ENDIF
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && MARK:3 < 3 && !구상유무확인(0)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 「……………………」
					WSTR:(K++) = ショックで呆然としている。
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				WSTR:(K++) =
			ENDIF

		;-------------------------------------------------
		;비디오 매각시
		;-------------------------------------------------
	CASE 19
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:278 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 「……私にも許せることと許せないことがあります。今回は許せないことですので、折檻しますね！」
					WSTR:(K++) = マルフーシャは怒っている。シカタナイネ。
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:278 = 2
			ENDIF
			IF (CFLAG:278 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:278 = 1
			ENDIF

		;-------------------------------------------------
		;콘서트시
		;-------------------------------------------------
	CASE 20
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:279 == 0
				WSTR:(K++) =
				CFLAG:279 = 1
			;그 이외
			ELSEIF CFLAG:279 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:279 = 1
			ENDIF

		;-------------------------------------------------
		;난교라이브시
		;-------------------------------------------------
	CASE 21
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:280 == 0
				WSTR:(K++) =
				CFLAG:280 = 1
			;그 이외
			ELSEIF CFLAG:280 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:280 = 1
			ENDIF

		;-------------------------------------------------
		;공중변소 플레이시
		;-------------------------------------------------
	CASE 22
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:281 == 0
				WSTR:(K++) =
				CFLAG:281 = 1
			;그 이외
			ELSEIF CFLAG:281 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:281 = 1
			ENDIF

		;-------------------------------------------------
		;비디오 보유시
		;-------------------------------------------------
	CASE 23
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:282 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:282 = 2
			ENDIF
			IF (CFLAG:282 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:282 = 1
			ENDIF

		;-------------------------------------------------
		;기억소거약 사용시
		;-------------------------------------------------
	CASE 30
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:283 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:283 = 2
			ENDIF
			IF (CFLAG:283 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:283 = 1
			ENDIF

		;-------------------------------------------------
		;;[아내][남편]취득시 친애 전제
		;-------------------------------------------------
	CASE 40
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:283 < 1 || FLAG:7 == 2
				WSTR:(K++) =「…………なんですか、これ？　반지？　新しい装備？」
				WSTR:(K++) = 　渡された指輪をつまんで、ぐるぐると色んな角度から眺めている。……渡された意味については、まだわかっていないようだ。
				WSTR:(K++) = 　それについては仕方ない。装備品枠に指輪は何種類もあるし、属性耐性を変化させる便利なアイテムとして使うことも多々ある。
				WSTR:(K++) = 　だが、今渡したのは何の加護もない、しかしある意味では特別な、ただの装飾品としての指輪だ。
				WSTR:(K++) = 「あ、刻印だ。Marufusha……私の名前……」
				WSTR:(K++) = 　刻印を見つけて、マルフーシャの顔が青くなり、ひきつっていく。はて……付き合いを重ねて、心も開いてくれたと思っていたのだが。時期を見誤ったか？　多分そんなことはないと思うのだが。
				WSTR:(K++) = 「もしかしなくても、私のための？」
				WSTR:(K++) = 　その通りだ。
				WSTR:(K++) = 「ちょっと何言ってるかわかんないんですけど？　こういう指輪って給料三か月分って言うし私は兵士で、奴隷で……そんな高価なものを贈られる価値はないと思うんですけど」
				WSTR:(K++) = 　 お前の価値を決めるのはお前じゃない。金の卵を産む鶏に首輪もつけず放し飼いにしてたら誰に持っていかれるかわからんからな。
				WSTR:(K++) = 「えぇぇ……もう少し他に喩えはなかったんですか」
				WSTR:(K++) = 　言い換えようか。お前は他の誰にも渡したくない。大事な女だ……結婚してくれ。
				WSTR:(K++) = 「そ、それはそれで……恥ずかしいですね」
				WSTR:(K++) = 　返事は。
				WSTR:(K++) = 「…………Да（ダー）」
				WSTR:(K++) = 　照れくさそうに、顔を赤くして笑うマルフーシャ。傷跡の痛々しい手をゆっくりと取って、できるだけ優しく、その手の薬指に指輪をはめた。
				CFLAG:283 = 1
			ENDIF
		;-------------------------------------------------
		;;[음마]취득시 창부 전제
		;-------------------------------------------------
	CASE 41
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:284 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:284 = 1
			ENDIF
		;-------------------------------------------------
		;;[전속노예]취득시 예속 전제
		;-------------------------------------------------
	CASE 42
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:285 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:285 = 1
			ENDIF
		;-------------------------------------------------
		;;오픈/클로즈・오프・가슴
		;-------------------------------------------------
	CASE 44
			K_CALL:K = EVENT_MESSAGE
			IF TEQUIP:가슴앞열림
				;가슴을 드러낸다
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;그 외
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:285 = 1
			ELSE
				;가슴을 집어넣는다
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;그 외
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:287 = 1
			ENDIF
		;-------------------------------------------------
		;;[애ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 45
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:288 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;함락완료 공통	
				IF !구상유무확인(0)	
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) =
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) =
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) =
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:애ＮＴＲ진행도 == 0
					WSTR:(K++) =
				;2번째（리치）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 1
					WSTR:(K++) =
				;3번째（함락）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 2
					WSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:288 = 1
		ENDIF
		;-------------------------------------------------
		;;[음ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 46
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:289 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				ENDIF
				;함락완료 공통	
				IF !구상유무확인(0)	
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						WSTR:(K++) =
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						WSTR:(K++) =
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						WSTR:(K++) =
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:음ＮＴＲ진행도 == 0
					WSTR:(K++) =
				;2번째（리치）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 1
					WSTR:(K++) =
				;3번째（함락）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 2
					WSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:289 = 1
		ENDIF
		;-------------------------------------------------
		;;[예ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 47
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:290 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				ENDIF
				;함락완료 공통	
				IF !구상유무확인(0)	
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) =
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) =
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) =
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:예ＮＴＲ진행도 == 0
					WSTR:(K++) =
				;2번째（리치）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 1
					WSTR:(K++) =
				;3번째（함락）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 2
					WSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:290 = 1
		ENDIF
		;-------------------------------------------------
		;;애(愛)계 재함락시
		;;주인에게 연모계 함락되었거나, 한번 애(愛)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 48
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
		
		IF (CFLAG:291 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				WSTR:(K++) =
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:291 = 1
		ENDIF
		;-------------------------------------------------
		;;음(淫)계 재함락시
		;;주인에게 음란계 함락되었거나, 한번 음(淫)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 49
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
					
		IF (CFLAG:292 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				WSTR:(K++) =
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:292 = 1
		ENDIF
		;-------------------------------------------------
		;;예(隷)계 재함락시
		;;주인에게 복종계 함락되었거나, 한번 예(隷)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 50
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
					
		IF (CFLAG:293 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				WSTR:(K++) =
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:293 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・난교파티
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 51
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:294 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:294 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・공원
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 52
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:295 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:295 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・변기
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 53
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・밤의 거리
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 54
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ전 밀회
		;;ＮＴＲ상태가 되기 전에 밀회할 때
		;-------------------------------------------------
	CASE 55
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:297 < 1 || FLAG:7 == 2)				
			IF CFLAG:함락캐릭터 != 0
			;	;주인 이외에 함락되어 있는 경우
				WSTR:(K++) = 
			ELSEIF (TALENT:3 || TALENT:6)	
			;	;연모 루트
				WSTR:(K++) = 
			ELSEIF (TALENT:4 || TALENT:7)	
			;	;음란 루트
				WSTR:(K++) = 
			ELSEIF (TALENT:5 || TALENT:8)	
			;	;복종 루트
				WSTR:(K++) = 
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:297 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ후 첫 밀회
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)가 된 직후 하루 종료 후, 한번만 행한다
		;다른 캐릭터에 함락될 때마다 일어난다. 같은 캐릭터에 다시 함락되는 경우는 일어나지 않는다
		;-------------------------------------------------
	CASE 56
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:298 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:298 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・데이트
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;-------------------------------------------------
	CASE 57
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:299 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:299 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・본방
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;여성이라면 삽입당하는 쪽, 남성이라면 삽입하는 쪽으로 고정
		;-------------------------------------------------
	CASE 58
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:601 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:601 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・자위서로과시
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;애ＮＴＲ상태인 경우, 노출증 3이나 자위중독 1 중 하나를 충족시키는 것이 조건이 됩니다.
		;-------------------------------------------------
	CASE 59
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・애널
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;CFLAG:애널횟수가 1 이상이면 삽입당하고, CFLAG:삽입횟수가 1 이상면 삽입하고 있습니다.
		;양 플래그 모두 서있는 경우, 지문에서는 삽입당하는 쪽이 우선됩니다.
		;-------------------------------------------------
	CASE 60
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외플레이
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;애ＮＴＲ의 경우, 노출증이 4 이상이 아니라면 발생하지 않습니다
		;-------------------------------------------------
	CASE 61
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외조교
		;;예ＮＴＲ상태(TALENT:ＮＴＲ == 3, 6)에서 CFLAG:현ＮＴＲ진행도가 6이상인 경우 하루 종료시에 발생.
		;-------------------------------------------------
	CASE 62
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			;예ＮＴＲ
			WSTR:(K++) =
			CFLAG:605 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마와 섹스한다
		;당하는 쪽
		;PLAYER에 실행자의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 79
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마에게 봉사시킨다
		;봉사하는 쪽
		;PLAYER에 실행자의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 80
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：그 장소에서 자위
		;봉사하는 쪽
		;-------------------------------------------------
	CASE 81
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：그늘에서 자위
		;봉사하는 쪽
		;-------------------------------------------------
	CASE 82
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:605 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마와 섹스한다
		;하는 쪽
		;PLAYER에 상대의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 83
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:606 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:606 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마에게 봉사시킨다
		;봉사받는 쪽
		;PLAYER에 봉사하는 쪽의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 84
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:607 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:607 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：윤간이벤트
		;봉사하는 쪽
		;PLAYER에 실행자 = 윤간하는 악마의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 85
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:608 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:608 = 1
		ENDIF
		

		;-------------------------------------------------
		;교회에서 기도
		;-------------------------------------------------
	CASE 65
			K_CALL:K = EVENT_MESSAGE
			;교회의 종류에 따라 나눈다
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:609 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) = 「神様なんて産毛の毛先ほども信じちゃいないんですけど。祈るだけならタダですし」
						ENDIF
						CFLAG:609 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:609 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:610 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:610 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:610 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:611 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:611 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:611 = 1
					ENDIF
			ENDSELECT
		;-------------------------------------------------
		;교회에서 입봉사
		;-------------------------------------------------
	CASE 66
			K_CALL:K = EVENT_MESSAGE
			;교회의 종류에 따라 나눈다
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:612 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:612 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:612 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:613 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:613 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:613 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:614 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:614 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:614 = 1
					ENDIF
			ENDSELECT
		;-------------------------------------------------
		;교회에서 성봉사
		;-------------------------------------------------
	CASE 67
			K_CALL:K = EVENT_MESSAGE
			;교회의 종류에 따라 나눈다
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:615 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:615 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:615 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:616 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:616 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:616 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:617 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:617 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:617 = 1
					ENDIF
			ENDSELECT
	CASEELSE
ENDSELECT

$表示
CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_EVENT_ZZZ
	CATCH
		CALL EVENT_MESSAGE
	ENDCATCH
ENDIF
;-------------------------------------------------
;투기장 패배시 구상 모음（X1을 캐릭터번호로 치환한다） 기본적으로 보면 게임오버기 때문에 플래그를 사용하지 않도록
;-------------------------------------------------
@KOJO_MESSAGE_LOSE_COLOSSEUM_K5371
CALL 구상변수초기화
SELECTCASE TFLAG:투기장패배
		;-------------------------------------------------
		;오니＆덩굴
		;-------------------------------------------------
	CASE 1
			K_CALL:K = MESSAGE_LOSE_COLOSSEUM
			;연모, 친애
			IF TALENT:3 || TALENT:6
				WSTR:(K++) = 
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
				WSTR:(K++) = 
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
				WSTR:(K++) = 
			ENDIF
			;그 외
			IF !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
ENDSELECT
CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_LOSE_COLOSSEUM_ZZZ
	CATCH
		CALL MESSAGE_LOSE_COLOSSEUM
	ENDCATCH
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_ENDING 관련(X1, X2를 캐릭터 번호로 치환한다)
;-------------------------------------------------
;-------------------------------------------------
;엔딩구상의 유무를 판별하는 함수
;기술한 엔딩의 종류에 따라 주석처리를 해제
;-------------------------------------------------
@ENDING_CHECK_K5371
SELECTCASE TSTR:END종별
	;CASE "개별END"
	;	SIF TALENT:아내 || TALENT:남편
	;		RETURN 1
	;	SIF TALENT:음마
	;		RETURN 1
	;	SIF TALENT:노리개
	;		RETURN 1
	;CASE "조교사경영END"
	;	RETURN 1
	;CASE "희대의조교사END"
	;	RETURN 1
	;CASE "식당경영END"
	;	RETURN 1
	;CASE "창관경영END"
	;	RETURN 1
	;CASE "아이돌END"
	;	RETURN 1
ENDSELECT
RETURN 0

;-------------------------------------------------
;엔딩 본문
;-------------------------------------------------
@KOJO_MESSAGE_ENDING_K5371
;--------------------------------------------------
CALL 구상변수초기화
SELECTCASE TSTR:END종별
	;CASE "개별END"
		;연모 1체 엔딩
		;IF TALENT:아내 || TALENT:남편 && CFLAG:21 == 1
			;DRAWLINE
			;WSTR:(K++) = …………
			;WSTR:(K++) = ………
			;WSTR:(K++) = ……
			;WSTR:(K++) = 

			;WSTR:(K++) = 
			;WSTR:(K++) = 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;음란 1체 엔딩
		;ELSEIF TALENT:음마 && CFLAG:21 == 1
			;DRAWLINE
			;WSTR:(K++) = …………
			;WSTR:(K++) = ………
			;WSTR:(K++) = ……
			;WSTR:(K++) = 

			;WSTR:(K++) = 
			;WSTR:(K++) = 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;복종 1체 엔딩
		;ELSEIF TALENT:노리개 && CFLAG:21 == 1
			;DRAWLINE
			;WSTR:(K++) = …………
			;WSTR:(K++) = ………
			;WSTR:(K++) = ……
			;WSTR:(K++) = 

			;WSTR:(K++) = 
			;WSTR:(K++) = 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;ENDIF
	;CASE "조교사경영END"
	;CASE "희대의조교사END"
	;CASE "식당경영END"
	;CASE "창관경영END"
	;CASE "아이돌END"
ENDSELECT
CALL 구상변수표시
;개별END의 경우 여기서 RETURN 1하면, 그 후 게임은 계속되지 않고 다음 주차로 넘어갈지 게임종료할지 선택하게 됩니다
RETURN 1
