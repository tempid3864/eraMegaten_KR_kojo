;-------------------------------------------------
;誰あなたな寡黙おじさんダゴン人型口上
;作成者:490メモリー [2020.12.02]
;ダゴンさんといえば邪神、だけど影が薄いような…？あれ？というイメージから作り出した(犯行)口上です。
;魚神のダゴンさんの要素も付け足してあるのでふわっと楽しんでください。
;※恋慕と淫乱中心に埋まる予定です。
;要らない場合はゴミ箱へ。
;TS非対応です。
;マスター(@)対応は女性＞男性状態。
;地の文より台詞重視目で試作運用中。
;-------------------------------------------------
;【素質変更前】→프라이드높음,부끄럼쟁이,보수적,젖기쉬움,
;　호기심,약독내성,절륜,회복빠름,수수께끼의매력,악취둔감,
;　불결무시,거체,절벽,남자,광기,금단의지식,물고기,우둔,교만
;【素質変更後】→부끄럼쟁이,보수적,젖기쉬움,호기심,약독내성,
;　절륜,회복빠름,수수께끼의매력,악취둔감,불결무시,절벽,남자,금단의지식,우둔
;(消去される素質)프라이드높음、거체、광기、물고기、高慢の5つ。
;-------------------------------------------------
;・陥落傾向
;未陥落→マスター(@)の機嫌を損ねないようにとちょっとは従順
;屈服刻印3→機嫌を察そうとするものの空回りして寡黙さが薄れる
;연모→自我が戻ってきてもかなり従順な精力お化け
;친애→自我が完璧に戻って、やたら素直な精力お化け
;남편(아내)→未定
;음란→自我が戻ってきても好奇心と保守的持ちな精力お化け
;창부→未定
;-------------------------------------------------
;・初期設定というか初期性格モデルは寡黙かつ人見知りしがちなおじさん(人間ではない)。
;初期일인칭:わたし
;初期이인칭:서머너/(인간)
;・言葉遣いは弱弱しくて直感的な感じ。
;「～だった?」、「わたしの瘴気はあまり体によくない、よ…」みたいな。
;
;自覚があるほど感受性がかなり弱くて引きこもってるし自分は邪神だし瘴気出すし
;嫌われてるだろう(思い込み)けど人間が気になって仕方ない。
;だからサマナーの仲魔にされて(なって)からもなかなか靡かない所からスタート。
;-------------------------------------------------
;〔更新履歴〕490メモリー
;/20.12.02～
;/20.12.07～
;/21.04.23～
;-------------------------------------------------
;なんとなくぼんやりした外見
;・青っぽい髪してそう。
;・肌色は青白そう、そのせいで笑顔めっちゃ不気味そう。
;・背丈はあるけど細身？
;地の文には出ませんのでご安心ください。
;-------------------------------------------------

;-------------------------------------------------
;eraMegaten전용 구상 템플릿
;X1와 X2를 캐릭터 번호로 치환하고, Y1을 조수의 캐릭터 번호로 치환해주세요
;단, 캐릭터번호가 한자리인 캐릭터의 경우, X1, Y1은 1, X2는 01과 같이 바꿔주세요
;그리고 커맨드 내의 적당한 조건 아래의 PRINTFORMW의 뒤에 반각 공백을 두고 대사를 추가해주세요
;구상을 추가하지 않는 커맨드는 무기입하셔도 상관없습니다만, CFLAG:XXX의 앞에『;』를 붙여서 주석처리를 하시면 더욱 좋습니다
;엑스트라 구문 등, 주석처리가 되어 있는 구문을 사용할 때는,『;』를 제거해 주석처리를 해제할 필요가 있습니다
;마지막으로 read me를 건드려 보시는 것도 추천드립니다
;-------------------------------------------------

;-------------------------------------------------
;범용 구상의 작성에 사용하는 경우는, X1→캐릭터번호가 아니라,
;K1621→PUB회화타입번호　로 변경해서 사용해주세요
;-------------------------------------------------
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	Module		:EVENT_K1621_ASSI.ERB
;	Facility	:회화타입：助手時口上
;
;	Modification Data:
;	Edit	Date			Author					Reason
;	001		20XX/XX/XX		Jガン					新規作成
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;ライセンスについて
;	・基本項目(許可する項目を"○"に、許可しない項目を"×"に変更してください。)
;	+----+----+-------------------------------+
;	|番号|許可|ライセンス内容                 |
;	+----+----+-------------------------------+
;	|   1| ○ | 処理上のバグ修正              |
;	+----+----+-------------------------------+
;	|   2| ○ | 口上の誤字・脱字の修正        |
;	+----+----+-------------------------------+
;	|   3| ○ | 口上まとめへの収録            |
;	+----+----+-------------------------------+
;	|   4| ○ | 口上の新規追加                |
;	+----+----+-------------------------------+
;	|   5| ○ | 既存口上の改変                |
;	+----+----+-------------------------------+
;	|   6| ○ | 改変した口上の自由な再配布    |
;	+----+----+-------------------------------+
;	|   7| ○ | 口上内容の転載                |
;	+----+----+-------------------------------+

;--------------------------------------------------
;구상셀렉트 기능：구상설명
;--------------------------------------------------
;이 함수의 주석처리를 해제하면, 구상의 선택시에 선택지로 후보에 오릅니다
;이미 구상이 있는 캐릭터나 회화타입의 다른 버전의 구상을 작성할 때 이용해주세요
;이 때, 이 함수를 포함한 이 파일 내의 함수명 전체에 _임의의1-49(범용구상이면 1-9)의 숫자를 추가해주세요
;예： EVENTTRAIN_K1621 → EVENTTRAIN_K1621_12
;--------------------------------------------------
@셀렉트구상설명_K1621,ARG = 0
SIF ARG == 1
	RETURN 0
;구상명
PRINTFORML 寡黙おじさんダゴン人型化口上
;구상설명문
PRINTFORML    助手非推奨かつ素質変更があります
PRINTFORML    よろしいですか？

;--------------------------------------------------
;EVENTTRAIN 관련（X1을 캐릭터번호로 치환）
;조교 개시시의 대화 CFLAG 201～219를 사용
;--------------------------------------------------
@EVENTTRAIN_K1621
CALL 구상변수초기화
;--------------------------------------------------
;엑스트라모드 구문　CFLAG:220을 사용
;네 를 선택시 첫조교시 구상설정용 소질로 변경 가능합니다
;아래에 처녀화할 경우의 구문을 샘플로 올려두겠습니다
;--------------------------------------------------
IF CFLAG:220 == 0
	PRINTL この口上は素質変更が前提の口上です
	PRINTW この口上に合うように素質を変更しますか？
	PRINTL 
	PRINTL [0] 想定素質・服装に変更する
	PRINTL [1] 素質を変更しない
	;PRINTL [2] 汎用口上を適用する
	$INPUT_LOOP
	INPUT
	IF RESULT == 0
		PRINTFORMW ―素質を変更して調教を開始します―
		TALENT:프라이드높음 = 0
		TALENT:광기 = 0
		TALENT:거체 = 0
		TALENT:물고기 = 0
		TALENT:교만 = 0
		;この素質変更の感じだと邪神さまから羽やかぎづめや鉤足もぐみたいでコワイし尾っぽは残します！ヤッタネ！
		TALENT:날개 = 0
		TALENT:갈퀴 = 0
		TALENT:갈퀴발 = 0
		
		TALENT:인간화 = 1
		
		;なんか水とか雨のイメージがあって着せたけどレインコートとサンダルってなかなか怪しい組み合わせだよな…(今更)
		CFLAG:TARGET:초기옷 = [[衣装:겉옷]]
		CFLAG:TARGET:옷 = [[衣装:겉옷]]
		TEQUIP:TARGET:옷 = [[衣装:겉옷]]
		CFLAG:TARGET:초기하의 = [[衣装:카고팬츠]]
		CFLAG:TARGET:하의 = [[衣装:카고팬츠]]
		TEQUIP:TARGET:하의 = [[衣装:카고팬츠]]
		CFLAG:TARGET:초기손 = [[衣装:소매]]
		CFLAG:TARGET:손 = [[衣装:소매]]
		TEQUIP:TARGET:손 = [[衣装:소매]]
		CFLAG:TARGET:초기속옷（하） = [[衣装:트렁크스]]
		CFLAG:TARGET:속옷（하） = [[衣装:트렁크스]]
		TEQUIP:TARGET:속옷（하） = [[衣装:트렁크스]]
		CFLAG:TARGET:초기신발 = [[衣装:샌들]]
		CFLAG:TARGET:신발 = [[衣装:샌들]]
		TEQUIP:TARGET:신발 = [[衣装:샌들]]
		CFLAG:TARGET:초기겉옷 = [[衣装:우비]]
		CFLAG:TARGET:겉옷 = [[衣装:우비]]
		TEQUIP:TARGET:겉옷 = [[衣装:우비]]
		CSTR:이인칭 = 서머너
		CSTR:일인칭 = わたし
	ELSEIF RESULT == 1
		PRINTFORMW ―通常素質で調教を開始します―
	;ELSEIF RESULT == 2
		;サラマンダー口上さんからもってきました
		;これ動かないの？　わからん
		;PRINTFORMW ―汎用口上で調教を開始します―
		;CFLAG:구상셀렉트 = -1
		;TRYJUMPFORM EVENTTRAIN_PUB{ABL:회화타입}
	ELSE
		GOTO INPUT_LOOP
	ENDIF
	CFLAG:220 = 1
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTTRAIN_PY1_K1621
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL EVENTTRAIN_PY1_K1621
;		RETURN 0
;	ENDIF
;ENDIF
;--------------------------------------------------
;간이 조수구상 발동시에 첫조교시 구상을 스킵（Y1을 조수의 캐릭터번호로 한다）
;여러 명이 있을 경우는, SIF NO:ASSI == Y1 || NO:ASSI == Z1으로 추가해 나간다
;--------------------------------------------------
;IF CFLAG:201 == 0 && ASSI > 0
;	SIF NO:ASSI == Y1
;		CFLAG:201 =1
;ENDIF
;-------------------------------------------------
;조교 개시시（1회만） CFLAG201
;-------------------------------------------------
DRAWLINE
;첫조교시
IF CFLAG:201 == 0 && TALENT:연모 == 0
	WSTR:(K++) = 「……なにか用なのかい?」
	WSTR:(K++) = %CALLNAME:MASTER%へ声をかけた男は眉を下げ困ったように立ち尽くしている。
	WSTR:(K++) = ひざ下まである黒のレインコートをフードまで被っている辺りがどう見ても怪しすぎる。
	WSTR:(K++) = 調教を行うために人の姿をとらせたはいいがこれでは典型的な不審者スタイルだ。
	WSTR:(K++) = 「えっ……わ、%CSTR:일인칭%に触らないほうが、いいよ」
	WSTR:(K++) = 「%CSTR:일인칭%はこの姿でも、ダゴンなんだ、%CSTR:이인칭%なら解るだろ?」
	WSTR:(K++) = %CALLNAME:MASTER%がフードを外せそうとすれば%CALLNAME:TARGET%はわたわたと慌てて阻止する。
	WSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%の説明に無視を決め込み強引にフードをひん剥いた。
	WSTR:(K++) = 　　　
	WSTR:(K++) = 「……あっ」
	WSTR:(K++) = 次の瞬間には%CALLNAME:TARGET%の間抜けな顔が露わになり驚愕に染まる。
	WSTR:(K++) = %CALLNAME:MASTER%の思った通り%CALLNAME:TARGET%はかなりの人見知りらしい。
	WSTR:(K++) = 「…あの、%CSTR:이인칭%?　%CSTR:일인칭%は、そろそろ、COMPに…」
	WSTR:(K++) = %CALLNAME:MASTER%は%CALLNAME:TARGET%の行く手を阻みこう告げる。
	WSTR:(K++) = 『調教』の時間だと。
	WSTR:(K++) = あまりのことに呆然とする%CALLNAME:TARGET%の隙を突きベッドに組み敷いた。
	CFLAG:201 = 1
	GOTO 表示
;굴복각인Lv1
ELSEIF CFLAG:201 < 2 && MARK:2 == 1
	WSTR:(K++) = 「……また、する、のは…」
	WSTR:(K++) = %CALLNAME:TARGET%は%CALLNAME:MASTER%の姿を見るなり憂いを帯びた顔で呟いた。
	CFLAG:201 = 2
	GOTO 表示
;굴복각인Lv2
ELSEIF CFLAG:201 < 3 && MARK:2 == 2
	WSTR:(K++) = 「%CSTR:일인칭%は、%CSTR:이인칭%と、こんな事……」
	WSTR:(K++) = %CALLNAME:MASTER%が%CALLNAME:TARGET%の弱々しい否定の言葉に耳を貸すことはなかった。
	CFLAG:201 = 3
	GOTO 表示
;굴복각인Lv3
ELSEIF CFLAG:201 < 4 && MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	WSTR:(K++) = 「%CSTR:일인칭%は、%CSTR:이인칭%に、従っていれば、いい…」
	WSTR:(K++) = 「それなら、%CSTR:일인칭%は……」
	WSTR:(K++) = %CALLNAME:TARGET%は%CALLNAME:MASTER%の表情を窺うように見上げながら抵抗感を捨てた。
	CFLAG:201 = 4
	GOTO 表示
;함락
ELSEIF CFLAG:201 < 5 && (TALENT:3 || TALENT:4 || TALENT:5)
	;연모
	IF TALENT:3
		IF CFLAG:미조교함락 == 1
			;조교하지 않고 연모를 단 경우
			WSTR:(K++) = 
		ELSE
			WSTR:(K++) = 「ああ、……%CSTR:이인칭%」
			WSTR:(K++) = 
			WSTR:(K++) = 
		ENDIF
	;음란
	ELSEIF TALENT:4
		WSTR:(K++) = 
	;복종
	ELSEIF TALENT:5
		WSTR:(K++) = 
	ENDIF
	;공통
	IF !구상유무확인(0)
		WSTR:(K++) = 
	ENDIF
	CFLAG:201 = 5
	GOTO 表示
ENDIF

;-------------------------------------------------
;간이 조수구상（Y1을 조수의 캐릭터번호로 치환, X1을 캐릭터번호로 변경한다) CFLAG203
;말할 캐릭터를 늘릴 경우, 구문을 복사＆붙여넣기 해, CFLAG의 수를 204, 205로 늘린다
;-------------------------------------------------
;조수의 유무를 체크（없을 경우는 2번째 이후로 건너뛴다）
IF ASSI < 0 
	CALL EVENTTRAIN_SECOND_K1621
;助手
;ELSEIF NO:ASSI == Y1
	;처음
;	IF CFLAG:203 == 0
		;함락완료＋함락시 첫 구상이 발생완료
;		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && CFLAG:201 >= 5
;			WSTR:(K++) = 
		;그 이외
;		ELSE
;			WSTR:(K++) = 
;		ENDIF
;		CFLAG:203 = 1
	;2번째 이후
;	ELSEIF CFLAG:203 < 2 || FLAG:7 ==2
		;함락완료
;		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
;			WSTR:(K++) = 
		;그 이외
;		ELSE
;			WSTR:(K++) = 
;		ENDIF
;		CFLAG:203 = 2
;	ENDIF
;구상이 있는 조수가 없을 경우, 통상의 2번째 이후의 구상으로 건너뛴다
ELSE
	CALL EVENTTRAIN_SECOND_K1621
ENDIF

$表示

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM EVENTTRAIN_ZZZ
	CATCH
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;조교 개시시（2번째 이후, X1을 캐릭터번호로 변경한다）
;-------------------------------------------------
@EVENTTRAIN_SECOND_K1621
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:202 < 4 || FLAG:7 == 2)
	WSTR:(K++) = 
	CFLAG:202 = 4
;굴복각인Lv0
ELSEIF MARK:2 == 0 && (CFLAG:202 < 1 || FLAG:7 == 2)
	IF RAND:2 == 0
		WSTR:(K++) = 「%CSTR:일인칭%と……同じ、空間に居るだけでも危険、だから…」
		WSTR:(K++) = %CALLNAME:MASTER%が調教部屋に入ると何故か部屋がいつもより暗くじめついている。
		WSTR:(K++) = 
	ELSE
		WSTR:(K++) = 「%CSTR:이인칭%、%CSTR:일인칭%は…曰くつきの、玩具より、質が悪いんだよ…?」
		WSTR:(K++) = 「それ…なのに、どうして」
	ENDIF
	CFLAG:202 = 1
;굴복각인Lv1
ELSEIF MARK:2 == 1 && (CFLAG:202 < 2 || FLAG:7 == 2)
	IF RAND:2 == 0
		WSTR:(K++) = 「%CSTR:일인칭%が、%CSTR:이인칭%の役に、立てるとは…思えない…」
	ELSE
		WSTR:(K++) = 「%CSTR:이인칭%からは、%CSTR:일인칭%のこと、恐れる感情が…見えない」
	ENDIF
	CFLAG:202 = 2
;굴복각인Lv2
ELSEIF MARK:2 == 2 && (CFLAG:202 < 3 || FLAG:7 == 2)
	IF RAND:2 == 0
		WSTR:(K++) = 「%CSTR:이인칭%は、変わったことを好んでるんだね…」
	ELSE
		WSTR:(K++) = 「逆らわないから、%CSTR:이인칭%の気が、済んだら、すぐ出ていって…」
	ENDIF
	CFLAG:202 = 3
;굴복각인Lv3＋함락소질없음
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0) && (CFLAG:202 < 4 || FLAG:7 == 2)
	IF RAND:2 == 0
		WSTR:(K++) = 「%CSTR:일인칭%は、%CSTR:이인칭%の…奴隷らしく、なってきた、のかな…?」
	ELSE
		WSTR:(K++) = 「あ、%CSTR:이인칭%…?」
		WSTR:(K++) = 「えっと……今日も、お願い、します」
	ENDIF
	CFLAG:202 = 4
;함락완료
ELSEIF CFLAG:202 < 5 || FLAG:7 == 2
	;연모, 친애
	IF TALENT:3 || TALENT:6
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			WSTR:(K++) =
		ELSEIF RAND:2 == 0
			WSTR:(K++) =
		ELSE
			WSTR:(K++) =
		ENDIF
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			WSTR:(K++) =
		ELSEIF RAND:2 == 0
			WSTR:(K++) =
		ELSE
			WSTR:(K++) =
		ENDIF
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			WSTR:(K++) =
		ELSEIF RAND:2 == 0
			WSTR:(K++) =
		ELSE
			WSTR:(K++) =
		ENDIF
	ENDIF
	;그 외, 공통
	IF !구상유무확인(0)
		;랜덤으로 구상을 변경한다（사용하지 않을 경우는 전부 같게 하면 된다）
		IF RAND:3 == 0
			WSTR:(K++) = 
		ELSEIF RAND:2 == 0
			WSTR:(K++) = 
		ELSE
			WSTR:(K++) = 
		ENDIF
	ENDIF
	CFLAG:202 = 5
ENDIF


;-------------------------------------------------
;EVENTEND 관련（X1을 캐릭터번호로 치환）
;조교 종료시의 대사
;-------------------------------------------------
@EVENTEND_K1621
CALL 구상변수초기화
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTEND_PY1_K1621
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	SIF NO:ASSI == Y1
;	CALL EVENTEND_PY1_K1621
;	RETURN 0
;ENDIF
;--------------------------------------------------
;조교 종료시의 대사
;--------------------------------------------------
DRAWLINE
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:217 < 1 || FLAG:7 == 2)
	WSTR:(K++) = 
	CFLAG:217 = 1
;굴복각인Lv1 이하
ELSEIF MARK:2 <= 1 && (CFLAG:219 < 1 || FLAG:7 == 2)
	IF RAND:2 == 0
		WSTR:(K++) = 「%CSTR:이인칭%は、本当に、ニンゲン…なの?」
	ELSE
		WSTR:(K++) = 「この部屋も、%CSTR:일인칭%のせいで、水浸しだ…」
	ENDIF
	CFLAG:219 = 1
;굴복각인Lv2
ELSEIF MARK:2 == 2
	IF RAND:3 == 0
		WSTR:(K++) = 「%CSTR:이인칭%は、どんなアクマも手懐けたら、どこかに売り払う…」
		WSTR:(K++) = 「契約なら、仕方がない…けど」
		WSTR:(K++) = 「%CSTR:일인칭%たちは、なかなか、忘れられないんだ」
	ELSEIF RAND:2 == 0
		WSTR:(K++) = 「出来ることなら、%CSTR:일인칭%は…」
		WSTR:(K++) = 「……これじゃあ、言うだけ無駄になる、ね」
	ELSE
		WSTR:(K++) = 「後片付けなら、%CSTR:일인칭%には必要、ないよ」
		WSTR:(K++) = 「汚れは勝手に流れていく、し」
		WSTR:(K++) = 「だから、放っておいてくれればいい…」
	ENDIF
	CFLAG:219 = 2
;굴복각인Lv3＋함락소질없음
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	IF RAND:2 == 0
		WSTR:(K++) = 「%CSTR:일인칭%にも、今なら解る」
		WSTR:(K++) = 「%CSTR:이인칭%には、完全なる欲求がないから…だから、強いんだと」
	ELSE
		WSTR:(K++) = 「%CSTR:이인칭%…」
		WSTR:(K++) = 「そろそろ、『調教』が終わりなのかと思うと…不思議な感情が、湧いてくる」
	ENDIF
	CFLAG:219 = 3
;함락완료＋체력 500미만
ELSEIF BASE:0 < 500 && (CFLAG:218 < 1 || FLAG:7 == 2)
	;연모, 친애
	IF TALENT:3 || TALENT:6
		WSTR:(K++) =
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		WSTR:(K++) =
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		WSTR:(K++) =
	ENDIF
	;공통
	IF !구상유무확인(0)
		WSTR:(K++) = 
	ENDIF
	CFLAG:218 = 1
;함락완료
ELSEIF CFLAG:219 < 4 || FLAG:7 == 2
	;연모, 친애
	IF TALENT:3 || TALENT:6
		WSTR:(K++) = 
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		WSTR:(K++) = 
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		WSTR:(K++) = 
	ENDIF
	;공통
	IF !구상유무확인(0)
		WSTR:(K++) =
	ENDIF
	CFLAG:219 = 4
ENDIF

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM EVENTEND_ZZZ
	CATCH
	ENDCATCH
	
ENDIF

;--------------------------------------------------
;@KOJO_MESSAGE_COM 관련（X1을 캐릭터번호로 치환）
;커맨드 실행시에 출력됩니다
;--------------------------------------------------
@KOJO_MESSAGE_COM_K1621
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL TRAIN_MESSAGE
	RETURN 0
ENDIF
;상정외의 구상이라면 스킵(이거 지우면 버그난다)
IF SELECTCOM < 0 || SELECTCOM > 699
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_COM_PY1_K1621
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_COM_PY1_K1621
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;함수초기화
;-------------------------------------------------
CALL 구상변수초기화
 
CALLFORM COMTYPE_{SELECTCOM}
;-------------------------------------------------
;커맨드 실행시의 대사 CFLAG 300～500
;-------------------------------------------------
IF 0 == 1
;=================================================
;애무계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("애무계")
SIF TEQUIP:25 > 0 && ( SELECTCOM == 0 || SELECTCOM == 5 || SELECTCOM == 6 || SELECTCOM == 7 || SELECTCOM == 8 || SELECTCOM == 12 || SELECTCOM == 13 || SELECTCOM == 15 )
	GOTO SHOKUSHU_PLAY
SELECTCASE SELECTCOM
;-------------------------------------------------
;0,애무
;-------------------------------------------------
	CASE 0
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:300 == 0
			WSTR:(K++) = 「%CSTR:일인칭%を、触るなんて、正気じゃない…」
			CFLAG:300 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:300 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 「ん、……ぅ、ん」
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:300 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:300 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:300 = 3
			ENDIF
			;그 이외
			IF (CFLAG:300 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:300 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;1,커닐링구스
;-------------------------------------------------
	CASE 1
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:301 == 0
			KSTR:(K++) =
			CFLAG:301 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:301 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:301 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:301 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:301 = 3
			ENDIF
			;그 이외
			IF (CFLAG:301 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:301 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;3,손가락삽입
;-------------------------------------------------
	CASE 3
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:303 == 0
			KSTR:(K++) =
			CFLAG:303 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:303 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:303 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:303 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:303 = 3
			ENDIF
			;그 이외
			IF (CFLAG:303 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:303 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;4,애널핥기
;-------------------------------------------------
	CASE 4
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:304 == 0
			KSTR:(K++) =
			CFLAG:304 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:304 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:304 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:304 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:304 = 3
			ENDIF
			;그 이외
			IF (CFLAG:304 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:304 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;5,애널애무
;-------------------------------------------------
	CASE 5
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:305 == 0
			KSTR:(K++) =
			CFLAG:305 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:305 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:305 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:305 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:305 = 3
			ENDIF
			;그 이외
			IF (CFLAG:305 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:305 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;6,가슴주무르기
;-------------------------------------------------
	CASE 6
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:306 == 0
			KSTR:(K++) =
			CFLAG:306 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:306 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:306 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:306 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:306 = 3
			ENDIF
			;그 이외
			IF (CFLAG:306 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:306 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;7,유두자극
;-------------------------------------------------
	CASE 7
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:307 == 0
			KSTR:(K++) =
			CFLAG:307 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:307 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:307 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:307 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:307 = 3
			ENDIF
			;그 이외
			IF (CFLAG:307 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:307 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;8,가슴애무
;-------------------------------------------------
	CASE 8
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:308 == 0
			KSTR:(K++) =
			CFLAG:308 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:308 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:308 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:308 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:308 = 3
			ENDIF
			;그 이외
			IF (CFLAG:308 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:308 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;9,조개젖히기
;-------------------------------------------------
	CASE 9
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:309 == 0
			KSTR:(K++) =
			CFLAG:309 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:309 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:309 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:309 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:309 = 3
			ENDIF
			;그 이외
			IF (CFLAG:309 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:309 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;10,가슴자위
;-------------------------------------------------
	CASE 10
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:310 == 0
			KSTR:(K++) =
			CFLAG:310 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:310 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:310 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:310 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:310 = 3
			ENDIF
			;그 이외
			IF (CFLAG:310 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:310 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;11,자위
;-------------------------------------------------
	CASE 11
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:311 == 0
			KSTR:(K++) =
			CFLAG:311 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:311 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:311 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:311 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:311 = 3
			ENDIF
			;그 이외
			IF (CFLAG:311 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:311 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;12,날개애무
;-------------------------------------------------
	CASE 12
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:435 == 0
			KSTR:(K++) =
			CFLAG:435 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:435 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:435 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:435 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:435 = 3
			ENDIF
			;그 이외
			IF (CFLAG:435 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:435 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;13,꼬리애무
;-------------------------------------------------
	CASE 13
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:436 == 0
			KSTR:(K++) =
			CFLAG:436 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:436 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:436 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:436 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:436 = 3
			ENDIF
			;그 이외
			IF (CFLAG:436 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:436 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;14,클리애무
;-------------------------------------------------
	CASE 14
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:442 == 0
			KSTR:(K++) =
			CFLAG:442 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:442 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:442 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:442 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:442 = 3
			ENDIF
			;그 이외
			IF (CFLAG:442 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:442 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;15,엉덩이애무
;-------------------------------------------------
	CASE 15
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:444 == 0
			KSTR:(K++) =
			CFLAG:444 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:444 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:444 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:444 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:444 = 3
			ENDIF
			;그 이외
			IF (CFLAG:444 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:444 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;99,애무시킨다
;-------------------------------------------------
	CASE 99
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:462 == 0
			KSTR:(K++) =
			CFLAG:462 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:462 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:462 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:462 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:462 = 3
			ENDIF
			;그 이외
			IF (CFLAG:462 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:462 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;애무계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;커뮤니케이션계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("커뮤계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;20,키스한다
;-------------------------------------------------
	CASE 20
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:312 == 0
			KSTR:(K++) =
			CFLAG:312 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:312 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:312 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:312 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:312 = 3
			ENDIF
			;그 이외
			IF (CFLAG:312 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:312 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;21,아무것도안한다
;-------------------------------------------------
	CASE 21
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:313 == 0
			KSTR:(K++) =
			CFLAG:313 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:313 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:313 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:313 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:313 = 3
			ENDIF
			;그 이외
			IF (CFLAG:313 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:313 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;22,회화　연모, 친애만
;-------------------------------------------------
	CASE 22
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:314 == 0
			KSTR:(K++) =
			CFLAG:314 = 1
		;2번째 이후
		ELSEIF CFLAG:314 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:314 = 2
		ENDIF
;-------------------------------------------------
;23,프레젠트　연모, 친애만
;-------------------------------------------------
	CASE 23
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:315 == 0
			KSTR:(K++) =
			CFLAG:315 = 1
		;2번째 이후
		ELSEIF CFLAG:315 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:315 = 2
		ENDIF
;-------------------------------------------------
;24,스킨쉽　연모, 친애만
;-------------------------------------------------
	CASE 24
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:316 == 0
			KSTR:(K++) =
			CFLAG:316 = 1
		;2번째 이후
		ELSEIF CFLAG:316 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:316 = 2
		ENDIF
;-------------------------------------------------
;25,느긋하게있는다　연모, 친애만
;-------------------------------------------------
	CASE 25
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:317 == 0
			KSTR:(K++) =
			CFLAG:317 = 1
		;2번째 이후
		ELSEIF CFLAG:317 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:317 = 2
		ENDIF
;-------------------------------------------------
;26,가사　연모, 친애만
;-------------------------------------------------
	CASE 26
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:318 == 0
			KSTR:(K++) =
			CFLAG:318 = 1
		;2번째 이후
		ELSEIF CFLAG:318 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:318 = 2
		ENDIF
;-------------------------------------------------
;27,노래연습　연모, 친애만
;-------------------------------------------------
	CASE 27
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:319 == 0
			KSTR:(K++) =
			CFLAG:319 = 1
		;2번째 이후
		ELSEIF CFLAG:319 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:319 = 2
		ENDIF
;-------------------------------------------------
;28,매도
;-------------------------------------------------
	CASE 28
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:320 == 0
			KSTR:(K++) =
			CFLAG:320 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:320 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:320 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:320 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:320 = 3
			ENDIF
			;그 이외
			IF (CFLAG:320 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:320 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;29,비디오감상　함락완료만
;-------------------------------------------------
	CASE 29
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:321 == 0
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:321 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:321 = 2
		ENDIF
;-------------------------------------------------
;30,파후파후　함락완료만
;-------------------------------------------------
	CASE 30
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:422 == 0
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:422 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:422 = 2
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;커뮤니케이션계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;도구사용 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("도구계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;40,로터
;-------------------------------------------------
	CASE 40
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:322 == 0
			KSTR:(K++) =
			CFLAG:322 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:322 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:322 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:322 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:322 = 3
			ENDIF
			;그 이외
			IF (CFLAG:322 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:322 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;41,전기마사지기
;-------------------------------------------------
	CASE 41
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:323 == 0
			KSTR:(K++) =
			CFLAG:323 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:323 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:323 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:323 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:323 = 3
			ENDIF
			;그 이외
			IF (CFLAG:323 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:323 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;42,클리캡
;-------------------------------------------------
	CASE 42
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:11
			;처음
			IF CFLAG:324 == 0
				KSTR:(K++) =
				CFLAG:324 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:324 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:324 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:324 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:324 = 3
				ENDIF
				;그 이외
				IF (CFLAG:324 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:324 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;43,오나홀
;-------------------------------------------------
	CASE 43
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:12
			;처음
			IF CFLAG:325 == 0
				KSTR:(K++) =
				CFLAG:325 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:325 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:325 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:325 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:325 = 3
				ENDIF
				;그 이외
				IF (CFLAG:325 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:325 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;44,바이브
;-------------------------------------------------
	CASE 44
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:13
			;처음
			IF CFLAG:326 == 0
				;처녀
				IF TALENT:0
					IF 함락( ) > 0
						;연모, 친애
						IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
							WSTR:(K++) = 
						;음란, 창부
						ELSEIF 함락( ) > 0 && RESULTS == "음란"
							WSTR:(K++) = 
						;복종, 예속
						ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
							KSTR:(K++) =
						ENDIF
						;함락완료 공통
						IF !구상유무확인(0)
							KSTR:(K++) =
						ENDIF
					ENDIF
					;그 이외
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				;비처녀
				ELSE
					IF 함락( ) > 0
						;연모, 친애
						IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
							WSTR:(K++) = 
						;음란, 창부
						ELSEIF 함락( ) > 0 && RESULTS == "음란"
							WSTR:(K++) = 
						;복종, 예속
						ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
							KSTR:(K++) =
						ENDIF
						;함락완료 공통
						IF !구상유무확인(0)
							KSTR:(K++) =
						ENDIF
					ENDIF
					;그 이외
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				CFLAG:326 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:326 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:326 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:326 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:326 = 3
				ENDIF
				;그 이외
				IF (CFLAG:326 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:326 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;45,애널바이브
;-------------------------------------------------
	CASE 45
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:14
			;처음
			IF CFLAG:327 == 0
				KSTR:(K++) =
				CFLAG:327 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:327 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:327 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:327 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:327 = 3
				ENDIF
				;그 이외
				IF (CFLAG:327 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:327 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;46,애널비즈
;-------------------------------------------------
	CASE 46
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:15
			;처음
			IF CFLAG:328 == 0
				KSTR:(K++) =
				CFLAG:328 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:328 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:328 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:328 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:328 = 3
				ENDIF
				;그 이외
				IF (CFLAG:328 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:328 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;47,유두캡
;-------------------------------------------------
	CASE 47
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:16
			;처음
			IF CFLAG:329 == 0
				KSTR:(K++) =
				CFLAG:329 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:329 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:329 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:329 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:329 = 3
				ENDIF
				;그 이외
				IF (CFLAG:329 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:329 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;48,유두로터
;-------------------------------------------------
	CASE 48
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:330 == 0
			KSTR:(K++) =
			CFLAG:330 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:330 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:330 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:330 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:330 = 3
			ENDIF
			;그 이외
			IF (CFLAG:330 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:330 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;49,착유기
;-------------------------------------------------
	CASE 49
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:17
			;처음
			IF CFLAG:331 == 0
				KSTR:(K++) =
				CFLAG:331 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:331 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:331 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:331 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:331 = 3
				ENDIF
				;그 이외
				IF (CFLAG:331 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:331 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;109,사정멈추기
;-------------------------------------------------
	CASE 109
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:42
			;처음
			IF CFLAG:464 == 0
				KSTR:(K++) =
				CFLAG:464 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:464 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:464 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:464 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:464 = 3
				ENDIF
				;그 이외
				IF (CFLAG:464 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:464 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;도구사용 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;조교자도구계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("대조교자도구계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;50,클리캡쓴다
;-------------------------------------------------
	CASE 50
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:11
			;처음
			IF CFLAG:453 == 0
				KSTR:(K++) =
				CFLAG:453 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:453 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:453 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:453 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:453 = 3
				ENDIF
				;그 이외
				IF (CFLAG:453 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:453 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;51,오나홀쓴다
;-------------------------------------------------
	CASE 51
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:12
			;처음
			IF CFLAG:454 == 0
				KSTR:(K++) =
				CFLAG:454 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:454 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:454 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:454 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:454 = 3
				ENDIF
				;그 이외
				IF (CFLAG:454 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:454 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;52,바이브쓴다
;-------------------------------------------------
	CASE 52
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:13
			;처음
			IF CFLAG:455 == 0
				KSTR:(K++) =
				CFLAG:455 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:455 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:455 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:455 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:455 = 3
				ENDIF
				;그 이외
				IF (CFLAG:455 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:455 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;53,애널바이브쓴다
;-------------------------------------------------
	CASE 53
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:14
			;처음
			IF CFLAG:456 == 0
				KSTR:(K++) =
				CFLAG:456 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:456 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:456 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:456 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:456 = 3
				ENDIF
				;그 이외
				IF (CFLAG:456 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:456 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;54,애널비즈쓴다
;-------------------------------------------------
	CASE 54
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:15
			;처음
			IF CFLAG:457 == 0
				KSTR:(K++) =
				CFLAG:457 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:457 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:457 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:457 = 3
				ENDIF
				;그 이외
				IF (CFLAG:457 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:457 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;55,유두캡쓴다
;-------------------------------------------------
	CASE 55
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:16
			;처음
			IF CFLAG:458 == 0
				KSTR:(K++) =
				CFLAG:458 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:458 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:458 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:458 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:458 = 3
				ENDIF
				;그 이외
				IF (CFLAG:458 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:458 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;56,착유기쓴다
;-------------------------------------------------
	CASE 56
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:17
			;처음
			IF CFLAG:459 == 0
				KSTR:(K++) =
				CFLAG:459 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:459 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:459 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:459 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:459 = 3
				ENDIF
				;그 이외
				IF (CFLAG:459 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:459 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;196,미약쓴다
;-------------------------------------------------
	CASE 196
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:437 == 0
			KSTR:(K++) =
			CFLAG:437 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:437 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:437 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:437 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:437 = 3
			ENDIF
			;그 이외
			IF (CFLAG:437 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:437 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;조교자도구계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;섹스계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("Ｖ성교계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;60,정상위
;-------------------------------------------------
	CASE 60
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:332 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:332 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:332 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:332 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:332 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:332 = 3
			ENDIF
			;그 이외
			IF (CFLAG:332 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:332 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;61,후배위
;-------------------------------------------------
	CASE 61
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:333 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:333 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:333 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:333 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:333 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:333 = 3
			ENDIF
			;그 이외
			IF (CFLAG:333 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:333 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;66,기승위
;-------------------------------------------------
	CASE 66
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:338 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:338 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:338 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:338 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:338 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:338 = 3
			ENDIF
			;그 이외
			IF (CFLAG:338 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:338 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;68,대면좌위
;-------------------------------------------------
	CASE 68
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:340 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:340 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:340 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:340 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:340 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:340 = 3
			ENDIF
			;그 이외
			IF (CFLAG:340 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:340 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;69,배면좌위
;-------------------------------------------------
	CASE 69
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:341 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:341 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:341 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:341 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:341 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:341 = 3
			ENDIF
			;그 이외
			IF (CFLAG:341 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:341 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;72,속옷비켜삽입
;-------------------------------------------------
	CASE 72
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:420 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:420 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:420 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:420 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:420 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:420 = 3
			ENDIF
			;그 이외
			IF (CFLAG:420 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:420 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;74,역강간당한다
;-------------------------------------------------
	CASE 74
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:460 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:460 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:460 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:460 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:460 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:460 = 3
			ENDIF
			;그 이외
			IF (CFLAG:460 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:460 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;섹스계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;애널섹스계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("Ａ성교계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;62,정상위애널
;-------------------------------------------------
	CASE 62
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:334 == 0
			KSTR:(K++) =
			CFLAG:334 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:334 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:334 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:334 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:334 = 3
			ENDIF
			;그 이외
			IF (CFLAG:334 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:334 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;63,후배위애널
;-------------------------------------------------
	CASE 63
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:335 == 0
			KSTR:(K++) =
			CFLAG:335 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:335 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:335 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:335 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:335 = 3
			ENDIF
			;그 이외
			IF (CFLAG:335 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:335 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;67,기승위애널
;-------------------------------------------------
	CASE 67
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:339 == 0
			KSTR:(K++) =
			CFLAG:339 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:339 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:339 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:339 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:339 = 3
			ENDIF
			;그 이외
			IF (CFLAG:339 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:339 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;70,대면좌위애널
;-------------------------------------------------
	CASE 70
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:342 == 0
			KSTR:(K++) =
			CFLAG:342 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:342 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:342 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:342 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:342 = 3
			ENDIF
			;그 이외
			IF (CFLAG:342 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:342 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;71,배면좌위애널
;-------------------------------------------------
	CASE 71
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:343 == 0
			KSTR:(K++) =
			CFLAG:343 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:343 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:343 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:343 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:343 = 3
			ENDIF
			;그 이외
			IF (CFLAG:343 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:343 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;73,속옷비켜삽입애널
;-------------------------------------------------
	CASE 73
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:421 == 0
			KSTR:(K++) =
			CFLAG:421 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:421 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:421 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:421 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:421 = 3
			ENDIF
			;그 이외
			IF (CFLAG:421 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:421 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;봉사계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("봉사계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;80,수음
;-------------------------------------------------
	CASE 80
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:344 == 0
			KSTR:(K++) =
			CFLAG:344 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:344 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:344 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:344 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:344 = 3
			ENDIF
			;그 이외
			IF (CFLAG:344 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:344 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;81,펠라치오
;-------------------------------------------------
	CASE 81
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:345 == 0
			KSTR:(K++) =
			CFLAG:345 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:345 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:345 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:345 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:345 = 3
			ENDIF
			;그 이외
			IF (CFLAG:345 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:345 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;82,파이즈리
;-------------------------------------------------
	CASE 82
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:346 == 0
			KSTR:(K++) =
			CFLAG:346 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:346 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:346 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:346 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:346 = 3
			ENDIF
			;그 이외
			IF (CFLAG:346 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:346 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;83,스마타
;-------------------------------------------------
	CASE 83
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:347 == 0
			KSTR:(K++) =
			CFLAG:347 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:347 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:347 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:347 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:347 = 3
			ENDIF
			;그 이외
			IF (CFLAG:347 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:347 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;84,거품춤
;-------------------------------------------------
	CASE 84
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:348 == 0
			KSTR:(K++) =
			CFLAG:348 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:348 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:348 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:348 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:348 = 3
			ENDIF
			;그 이외
			IF (CFLAG:348 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:348 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;85,풋잡
;-------------------------------------------------
	CASE 85
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:349 == 0
			KSTR:(K++) =
			CFLAG:349 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:349 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:349 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:349 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:349 = 3
			ENDIF
			;그 이외
			IF (CFLAG:349 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:349 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;86,다리핥기강제
;-------------------------------------------------
	CASE 86
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:350 == 0
			KSTR:(K++) =
			CFLAG:350 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:350 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:350 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:350 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:350 = 3
			ENDIF
			;그 이외
			IF (CFLAG:350 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:350 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;87,세로파이즈리
;-------------------------------------------------
	CASE 87
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:351 == 0
			KSTR:(K++) =
			CFLAG:351 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:351 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:351 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:351 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:351 = 3
			ENDIF
			;그 이외
			IF (CFLAG:351 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:351 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;88,파이즈리펠라
;-------------------------------------------------
	CASE 88
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:352 == 0
			KSTR:(K++) =
			CFLAG:352 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:352 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:352 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:352 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:352 = 3
			ENDIF
			;그 이외
			IF (CFLAG:352 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:352 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;89,모유마시기　함락완료만
;-------------------------------------------------
	CASE 89
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:353 == 0
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:353 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:353 = 2
		ENDIF
;-------------------------------------------------
;197,팬티훑기
;-------------------------------------------------
	CASE 197
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:438 == 0
			KSTR:(K++) =
			CFLAG:438 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:438 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:438 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:438 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:438 = 3
			ENDIF
			;그 이외
			IF (CFLAG:438 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:438 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;211,귀두공격(受)
;-------------------------------------------------
	CASE 211
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:479 == 0
			KSTR:(K++) =
			CFLAG:479 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:479 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:479 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:479 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:479 = 3
			ENDIF
			;그 이외
			IF (CFLAG:479 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:479 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;봉사계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;조교자봉사계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("조교자봉사계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;2,펠라한다
;-------------------------------------------------
	CASE 2
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:302 == 0
			KSTR:(K++) =
			CFLAG:302 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:302 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:302 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:302 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:302 = 3
			ENDIF
			;그 이외
			IF (CFLAG:302 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:302 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;90,수음한다
;-------------------------------------------------
	CASE 90
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:12 == 0
			;처음
			IF CFLAG:423 == 0
				KSTR:(K++) =
				CFLAG:423 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:423 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:423 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:423 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:423 = 3
				ENDIF
				;그 이외
				IF (CFLAG:423 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:423 = 2
				ENDIF
			ENDIF
		ELSE
;-------------------------------------------------
;오나홀훑기한다
;-------------------------------------------------
			;처음
			IF CFLAG:461 == 0
				KSTR:(K++) =
				CFLAG:461 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:461 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:461 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:461 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:461 = 3
				ENDIF
				;그 이외
				IF (CFLAG:461 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:461 = 2
				ENDIF
			ENDIF
		ENDIF
;-------------------------------------------------
;91,파이즈리한다
;-------------------------------------------------
	CASE 91
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:424 == 0
			KSTR:(K++) =
			CFLAG:424 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:424 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:424 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:424 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:424 = 3
			ENDIF
			;그 이외
			IF (CFLAG:424 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:424 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;92,정상위시킨다
;-------------------------------------------------
	CASE 92
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:425 == 0
			KSTR:(K++) =
			CFLAG:425 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:425 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:425 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:425 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:425 = 3
			ENDIF
			;그 이외
			IF (CFLAG:425 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:425 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;93,후배위시킨다
;-------------------------------------------------
	CASE 93
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:426 == 0
			KSTR:(K++) =
			CFLAG:426 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:426 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:426 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:426 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:426 = 3
			ENDIF
			;그 이외
			IF (CFLAG:426 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:426 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;94,기승위한다
;-------------------------------------------------
	CASE 94
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:427 == 0
			KSTR:(K++) =
			CFLAG:427 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:427 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:427 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:427 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:427 = 3
			ENDIF
			;그 이외
			IF (CFLAG:427 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:427 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;95,애널정상위시킨다
;-------------------------------------------------
	CASE 95
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:428 == 0
			KSTR:(K++) =
			CFLAG:428 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:428 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:428 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:428 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:428 = 3
			ENDIF
			;그 이외
			IF (CFLAG:428 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:428 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;96,애널후배위시킨다
;-------------------------------------------------
	CASE 96
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:429 == 0
			KSTR:(K++) =
			CFLAG:429 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:429 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:429 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:429 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:429 = 3
			ENDIF
			;그 이외
			IF (CFLAG:429 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:429 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;97,애널기승위한다
;-------------------------------------------------
	CASE 97
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:430 == 0
			KSTR:(K++) =
			CFLAG:430 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:430 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:430 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:430 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:430 = 3
			ENDIF
			;그 이외
			IF (CFLAG:430 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:430 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;98,모유먹이기　함락완료만
;-------------------------------------------------
	CASE 98
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:443 == 0
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:443 = 1
		;2번째 이후
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모, 친애
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
				KSTR:(K++) =
			;음란, 창부
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				KSTR:(K++) =
			;복종, 예속
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
				KSTR:(K++) =
			ENDIF
			;함락완료 공통
			IF !구상유무확인(0)
				KSTR:(K++) =
				;WSTR:(K++) =
			ENDIF
			CFLAG:443 = 2
		ENDIF
;-------------------------------------------------
;210,귀두공격
;-------------------------------------------------
	CASE 210
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:480 == 0
			KSTR:(K++) =
			CFLAG:480 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:480 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:480 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:480 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:480 = 3
			ENDIF
			;그 이외
			IF (CFLAG:480 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:480 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;조교자봉사계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;SM계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("ＳＭ계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;100,스팽킹
;-------------------------------------------------
	CASE 100
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:354 == 0
			KSTR:(K++) =
			CFLAG:354 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:354 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:354 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:354 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:354 = 3
			ENDIF
			;그 이외
			IF (CFLAG:354 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:354 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;101,젖스팽킹
;-------------------------------------------------
	CASE 101
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:355 == 0
			KSTR:(K++) =
			CFLAG:355 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:355 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:355 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:355 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:355 = 3
			ENDIF
			;그 이외
			IF (CFLAG:355 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:355 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;102,채찍
;-------------------------------------------------
	CASE 102
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:356 == 0
			KSTR:(K++) =
			CFLAG:356 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:356 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:356 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:356 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:356 = 3
			ENDIF
			;그 이외
			IF (CFLAG:356 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:356 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;103,양초
;-------------------------------------------------
	CASE 103
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:357 == 0
			KSTR:(K++) =
			CFLAG:357 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:357 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:357 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:357 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:357 = 3
			ENDIF
			;그 이외
			IF (CFLAG:357 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:357 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;104,바늘
;-------------------------------------------------
	CASE 104
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:358 == 0
			KSTR:(K++) =
			CFLAG:358 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:358 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:358 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:358 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:358 = 3
			ENDIF
			;그 이외
			IF (CFLAG:358 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:358 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;105,아이마스크
;-------------------------------------------------
	CASE 105
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:18
			;처음
			IF CFLAG:359 == 0
				KSTR:(K++) =
				CFLAG:359 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:359 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:359 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:359 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:359 = 3
				ENDIF
				;그 이외
				IF (CFLAG:359 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:359 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;106,밧줄
;-------------------------------------------------
	CASE 106
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:19
			;처음
			IF CFLAG:360 == 0
				KSTR:(K++) =
				CFLAG:360 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:360 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:360 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:360 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:360 = 3
				ENDIF
				;그 이외
				IF (CFLAG:360 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:360 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;107,볼재갈
;-------------------------------------------------
	CASE 107
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:20
			;처음
			IF CFLAG:361 == 0
				KSTR:(K++) =
				CFLAG:361 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:361 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:361 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:361 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:361 = 3
				ENDIF
				;그 이외
				IF (CFLAG:361 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:361 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;SM계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;조수에 의한 커맨드, 및 레즈플레이
;=================================================
ELSEIF RESULT == COMTYPE("조수・레즈계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;120,커닐링구스강제
;-------------------------------------------------
	CASE 120
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:362 == 0
			WSTR:(K++) = 「ん、ンぶ…っ、これ、ニンゲンのあじ…?」
			WSTR:(K++) = 「……　ん、%CSTR:일인칭%の舌じゃ、解らなくて」
			CFLAG:362 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:362 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					IF TEQUIP:주도권 == 1
						WSTR:(K++) = 「」
					ELSE
						WSTR:(K++) = 
					ENDIF
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:362 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:362 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:362 = 3
			ENDIF
			;그 이외
			IF (CFLAG:362 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:362 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;121,조개맞대기
;-------------------------------------------------
	CASE 121
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:363 == 0
			KSTR:(K++) =
			CFLAG:363 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:363 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:363 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:363 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:363 = 3
			ENDIF
			;그 이외
			IF (CFLAG:363 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:363 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;122,조수를범하게한다
;-------------------------------------------------
	CASE 122
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:364 == 0
			KSTR:(K++) =
			CFLAG:364 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:364 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:364 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:364 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:364 = 3
			ENDIF
			;그 이외
			IF (CFLAG:364 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:364 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;조수가 기분 좋게 느끼고 있다 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;조수가 이리 오라고 말하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;조수가 괜찮다고 말하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;범해지며 강한 척을 하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;능숙해졌네 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
 
;-------------------------------------------------
;123,풋잡한다
;-------------------------------------------------
	CASE 123
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:365 == 0
			KSTR:(K++) =
			CFLAG:365 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:365 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:365 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:365 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:365 = 3
			ENDIF
			;그 이외
			IF (CFLAG:365 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:365 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;124,더블펠라
;-------------------------------------------------
	CASE 124
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:366 == 0
			KSTR:(K++) =
			CFLAG:366 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:366 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:366 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:366 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:366 = 3
			ENDIF
			;그 이외
			IF (CFLAG:366 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:366 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;영향에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;평소보다 느끼고 있구나 라는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;레즈 같은 대사에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
		;둘의 펠라는 기분 좋지？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_6
 
;-------------------------------------------------
;125,더블파이즈리
;-------------------------------------------------
	CASE 125
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:367 == 0
			KSTR:(K++) =
			CFLAG:367 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:367 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:367 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:367 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:367 = 3
			ENDIF
			;그 이외
			IF (CFLAG:367 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:367 = 2
			ENDIF
		ENDIF
		
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;영향에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;평소보다 느끼고 있구나 라는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;서로 젖꼭지가 스쳐서 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
		;둘의 파이즈리로 기분 좋아져 줘 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_6
		;파이즈리로 느끼고 있어？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_7
 
;-------------------------------------------------
;126,가슴맞대기
;-------------------------------------------------
	CASE 126
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:368 == 0
			KSTR:(K++) =
			CFLAG:368 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:368 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:368 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:368 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:368 = 3
			ENDIF
			;그 이외
			IF (CFLAG:368 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:368 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;127,자위과시
;-------------------------------------------------
	CASE 127
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:432 == 0
			KSTR:(K++) =
			CFLAG:432 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:432 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:432 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:432 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:432 = 3
			ENDIF
			;그 이외
			IF (CFLAG:432 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:432 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;128,조개젖히기과시
;-------------------------------------------------
	CASE 128
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:465 == 0
			KSTR:(K++) =
			CFLAG:465 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:465 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:465 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:465 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:465 = 3
			ENDIF
			;그 이외
			IF (CFLAG:465 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:465 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;129,자위구음과시
;-------------------------------------------------
	CASE 129
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:466 == 0
			KSTR:(K++) =
			CFLAG:466 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:466 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:466 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:466 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:466 = 3
			ENDIF
			;그 이외
			IF (CFLAG:466 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:466 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;조수에 의한 공통, 및 레즈플레이
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;하드한조교 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("하드계")
SELECTCASE SELECTCOM
;-------------------------------------------------
;64,역강간
;-------------------------------------------------
	CASE 64
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:336 == 0
			KSTR:(K++) =
			CFLAG:336 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:336 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:336 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:336 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:336 = 3
			ENDIF
			;그 이외
			IF (CFLAG:336 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:336 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;65,젖성교
;-------------------------------------------------
	CASE 65
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:337 == 0
			KSTR:(K++) =
			CFLAG:337 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:337 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:337 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:337 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:337 = 3
			ENDIF
			;그 이외
			IF (CFLAG:337 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:337 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;108,삼각목마
;-------------------------------------------------
	CASE 108
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:41
			;처음
			IF CFLAG:463 == 0
				KSTR:(K++) =
				CFLAG:463 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:463 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:463 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:463 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:463 = 3
				ENDIF
				;그 이외
				IF (CFLAG:463 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:463 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;140,이라마치오
;-------------------------------------------------
	CASE 140
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:369 == 0
			KSTR:(K++) =
			CFLAG:369 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:369 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:369 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:369 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:369 = 3
			ENDIF
			;그 이외
			IF (CFLAG:369 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:369 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;141,피스트퍽　복종, 예속만
;-------------------------------------------------
	CASE 141
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:370 == 0
			;처녀
			IF TALENT:0
				KSTR:(K++) =
			ELSE
				KSTR:(K++) =
			ENDIF
			CFLAG:370 = 1
		;2번째 이후
		ELSEIF CFLAG:370 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:370 = 2
		ENDIF
;-------------------------------------------------
;142,애널피스트　복종, 예속만
;-------------------------------------------------
	CASE 142
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:371 == 0
			KSTR:(K++) =
			CFLAG:371 = 1
		;2번째 이후
		ELSEIF CFLAG:371 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:371 = 2
		ENDIF
;-------------------------------------------------
;143,양구멍피스트　복종, 예속만
;-------------------------------------------------
	CASE 143
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:372 == 0
			;처녀
			IF TALENT:0
				KSTR:(K++) =
			ELSE
				KSTR:(K++) =
			ENDIF
			CFLAG:372 = 1
		;2번째 이후
		ELSEIF CFLAG:372 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:372 = 2
		ENDIF
;-------------------------------------------------
;144,방뇨
;-------------------------------------------------
	CASE 144
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:373 == 0
			KSTR:(K++) =
			CFLAG:373 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:373 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:373 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:373 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:373 = 3
			ENDIF
			;그 이외
			IF (CFLAG:373 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:373 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;145,애널봉사강제
;-------------------------------------------------
	CASE 145
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:374 == 0
			KSTR:(K++) =
			CFLAG:374 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:374 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:374 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:374 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:374 = 3
			ENDIF
			;그 이외
			IF (CFLAG:374 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:374 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;146,관장기＋플러그　해제할 때 쪽
;-------------------------------------------------
	CASE 146
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:21
			;처음
			IF CFLAG:375 == 0
				KSTR:(K++) =
				CFLAG:375 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:375 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:375 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:375 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:375 = 3
				ENDIF
				;그 이외
				IF (CFLAG:375 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:375 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;147,공기관장　방귀뀌게한다
;-------------------------------------------------
	CASE 147
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:376 == 0
			KSTR:(K++) =
			CFLAG:376 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:376 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:376 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:376 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:376 = 3
			ENDIF
			;그 이외
			IF (CFLAG:376 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:376 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;148,확장벌룬
;-------------------------------------------------
	CASE 148
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:22
			;처음
			IF CFLAG:377 == 0
				KSTR:(K++) =
				CFLAG:377 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:377 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:377 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:377 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:377 = 3
				ENDIF
				;그 이외
				IF (CFLAG:377 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:377 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;149,애널전극　복종, 예속만
;-------------------------------------------------
	CASE 149
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:23
			;처음
			IF CFLAG:378 == 0
				KSTR:(K++) =
				CFLAG:378 = 1
			;2번째 이후
			ELSEIF CFLAG:378 < 2 || FLAG:7 == 2
				KSTR:(K++) =
				CFLAG:378 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:378 == 1 || FLAG:7 == 2
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;150,유방전극　복종, 예속만
;-------------------------------------------------
	CASE 150
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:24
			;처음
			IF CFLAG:379 == 0
				KSTR:(K++) =
				CFLAG:379 = 1
			;2번째 이후
			ELSEIF CFLAG:379 < 2 || FLAG:7 == 2
				KSTR:(K++) =
				CFLAG:379 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:379 == 1 || FLAG:7 == 2
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;151,뱀장어욕탕　복종, 예속만
;-------------------------------------------------
	CASE 151
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:380 == 0
			;처녀
			IF TALENT:0
				KSTR:(K++) =
			ELSE
				KSTR:(K++) =
			ENDIF
			CFLAG:380 = 1
		;2번째 이후
		ELSEIF CFLAG:380 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:380 = 2
		ENDIF
;-------------------------------------------------
;152,벌레욕탕　복종, 예속만
;-------------------------------------------------
	CASE 152
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:381 == 0
			KSTR:(K++) =
			CFLAG:381 = 1
		;2번째 이후
		ELSEIF CFLAG:381 < 2 || FLAG:7 == 2
			KSTR:(K++) =
			CFLAG:381 = 2
		ENDIF
;-------------------------------------------------
;154,요정오나홀　소인체형계만
;-------------------------------------------------
	CASE 154
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:467 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:467 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:467 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:467 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:467 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:467 = 3
			ENDIF
			;그 이외
			IF (CFLAG:467 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:467 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;155,구슬킥
;-------------------------------------------------
	CASE 155
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:468 == 0
			KSTR:(K++) =
			CFLAG:468 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:468 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:468 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:468 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:468 = 3
			ENDIF
			;그 이외
			IF (CFLAG:468 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:468 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;156,음뇨
;-------------------------------------------------
	CASE 156
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:469 == 0
			KSTR:(K++) =
			CFLAG:469 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:469 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:469 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:469 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:469 = 3
			ENDIF
			;그 이외
			IF (CFLAG:469 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:469 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;하드한조교 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;촉수계 커맨드
;=================================================
ELSEIF RESULT == COMTYPE("촉수계")
$SHOKUSHU_PLAY
SELECTCASE SELECTCOM
;-------------------------------------------------
;0,촉수애무
;-------------------------------------------------
	CASE 0
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:455 == 0
			KSTR:(K++) =
			CFLAG:455 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:455 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:455 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:455 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:455 = 3
			ENDIF
			;그 이외
			IF (CFLAG:455 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:455 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;5,촉수애널애무
;-------------------------------------------------
	CASE 5
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:456 == 0
			KSTR:(K++) =
			CFLAG:456 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:456 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:456 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:456 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:456 = 3
			ENDIF
			;그 이외
			IF (CFLAG:456 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:456 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;6,촉수가슴주무르기
;-------------------------------------------------
	CASE 6
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:457 == 0
			KSTR:(K++) =
			CFLAG:457 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:457 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:457 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:457 = 3
			ENDIF
			;그 이외
			IF (CFLAG:457 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:457 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;7,촉수유두자극(短)
;-------------------------------------------------
	CASE 7
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:458 == 0
			KSTR:(K++) =
			CFLAG:458 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:458 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:458 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:458 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:458 = 3
			ENDIF
			;그 이외
			IF (CFLAG:458 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:458 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;8,촉수가슴애무
;-------------------------------------------------
	CASE 8
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:459 == 0
			KSTR:(K++) =
			CFLAG:459 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:459 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:459 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:459 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:459 = 3
			ENDIF
			;그 이외
			IF (CFLAG:459 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:459 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;12,촉수날개애무
;-------------------------------------------------
	CASE 12
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:460 == 0
			KSTR:(K++) =
			CFLAG:460 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:460 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:460 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:460 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:460 = 3
			ENDIF
			;그 이외
			IF (CFLAG:460 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:460 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;13,촉수꼬리애무
;-------------------------------------------------
	CASE 13
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:461 == 0
			KSTR:(K++) =
			CFLAG:461 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:461 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:461 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:461 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:461 = 3
			ENDIF
			;그 이외
			IF (CFLAG:461 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:461 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;15,촉수엉덩이애무
;-------------------------------------------------
	CASE 15
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:462 == 0
			KSTR:(K++) =
			CFLAG:462 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:462 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:462 = 4
			ENDIF
			;순종Lv3 이상
			IF ABL:0 >= 3 && (CFLAG:462 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:462 = 3
			ENDIF
			;그 이외
			IF (CFLAG:462 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:462 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;160,촉수소환
;-------------------------------------------------
	CASE 160
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:25
			;처음
			IF CFLAG:382 == 0
				KSTR:(K++) =
				CFLAG:382 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:382 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:382 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:382 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:382 = 3
				ENDIF
				;그 이외
				IF (CFLAG:382 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:382 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;161,촉수삽입
;-------------------------------------------------
	CASE 161
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:13
			;처음
			IF CFLAG:383 == 0
				;처녀
				IF TALENT:0
					IF 함락( ) > 0
						;연모, 친애
						IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
							WSTR:(K++) = 
						;음란, 창부
						ELSEIF 함락( ) > 0 && RESULTS == "음란"
							WSTR:(K++) = 
						;복종, 예속
						ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
							KSTR:(K++) =
						ENDIF
						;함락완료 공통
						IF !구상유무확인(0)
							KSTR:(K++) =
						ENDIF
					ENDIF
					;그 이외
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				;비처녀
				ELSE
					IF 함락( ) > 0
						;연모, 친애
						IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
							WSTR:(K++) = 
						;음란, 창부
						ELSEIF 함락( ) > 0 && RESULTS == "음란"
							WSTR:(K++) = 
						;복종, 예속
						ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
							KSTR:(K++) =
						ENDIF
						;함락완료 공통
						IF !구상유무확인(0)
							KSTR:(K++) =
						ENDIF
					ENDIF
					;그 이외
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				CFLAG:383 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:383 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:383 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:383 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:383 = 3
				ENDIF
				;그 이외
				IF (CFLAG:383 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:383 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;162,애널촉수
;-------------------------------------------------
	CASE 162
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:14
			;처음
			IF CFLAG:384 == 0
				KSTR:(K++) =
				CFLAG:384 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:384 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:384 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:384 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:384 = 3
				ENDIF
				;그 이외
				IF (CFLAG:384 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:384 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;163,촉수클리자극
;-------------------------------------------------
	CASE 163
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:11
			;처음
			IF CFLAG:385 == 0
				KSTR:(K++) =
				CFLAG:385 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:385 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:385 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:385 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:385 = 3
				ENDIF
				;그 이외
				IF (CFLAG:385 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:385 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;164,촉수유두자극
;-------------------------------------------------
	CASE 164
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:16
			;처음
			IF CFLAG:386 == 0
				KSTR:(K++) =
				CFLAG:386 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:386 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:386 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:386 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:386 = 3
				ENDIF
				;그 이외
				IF (CFLAG:386 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:386 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;165,촉수착유
;-------------------------------------------------
	CASE 165
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:17
			;처음
			IF CFLAG:387 == 0
				KSTR:(K++) =
				CFLAG:387 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:387 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:387 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:387 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:387 = 3
				ENDIF
				;그 이외
				IF (CFLAG:387 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:387 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;166,촉수긴박
;-------------------------------------------------
	CASE 166
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:19
			;처음
			IF CFLAG:388 == 0
				KSTR:(K++) =
				CFLAG:388 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:388 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:388 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:388 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:388 = 3
				ENDIF
				;그 이외
				IF (CFLAG:388 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:388 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;167,촉수관장　극태 촉수가 항문을 막고, 체액을 부어넣는다
;-------------------------------------------------
	CASE 167
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:21
			;처음
			IF CFLAG:389 == 0
				KSTR:(K++) =
				CFLAG:389 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:389 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:389 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:389 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:389 = 3
				ENDIF
				;그 이외
				IF (CFLAG:389 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:389 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;168,촉수구욕
;-------------------------------------------------
	CASE 168
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:36
			;처음
			IF CFLAG:390 == 0
				KSTR:(K++) =
				CFLAG:390 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:390 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:390 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:390 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:390 = 3
				ENDIF
				;그 이외
				IF (CFLAG:390 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:390 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;169,촉수페니스자극
;-------------------------------------------------
	CASE 169
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:12
			;처음
			IF CFLAG:391 == 0
				KSTR:(K++) =
				CFLAG:391 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:391 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:391 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:391 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:391 = 3
				ENDIF
				;그 이외
				IF (CFLAG:391 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:391 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;170,촉수삽입시킨다
;-------------------------------------------------
	CASE 170
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:13
			;처음
			IF CFLAG:470 == 0
				KSTR:(K++) =
				CFLAG:470 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:470 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:470 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:470 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:470 = 3
				ENDIF
				;그 이외
				IF (CFLAG:470 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:470 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;171,애널촉수시킨다
;-------------------------------------------------
	CASE 171
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:14
			;처음
			IF CFLAG:471 == 0
				KSTR:(K++) =
				CFLAG:471 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:471 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:471 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:471 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:471 = 3
				ENDIF
				;그 이외
				IF (CFLAG:471 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:471 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;172,촉수클리자극시킨다
;-------------------------------------------------
	CASE 172
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:11
			;처음
			IF CFLAG:472 == 0
				KSTR:(K++) =
				CFLAG:472 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:472 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:472 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:472 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:472 = 3
				ENDIF
				;그 이외
				IF (CFLAG:472 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:472 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;173,촉수유두자극시킨다
;-------------------------------------------------
	CASE 173
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:16
			;처음
			IF CFLAG:473 == 0
				KSTR:(K++) =
				CFLAG:473 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:473 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:473 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:473 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:473 = 3
				ENDIF
				;그 이외
				IF (CFLAG:473 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:473 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;174,촉수착유시킨다
;-------------------------------------------------
	CASE 174
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:17
			;처음
			IF CFLAG:474 == 0
				KSTR:(K++) =
				CFLAG:474 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:474 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:474 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:474 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:474 = 3
				ENDIF
				;그 이외
				IF (CFLAG:474 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:474 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;175,촉수페니스자극시킨다
;-------------------------------------------------
	CASE 175
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:PLAYER:12
			;처음
			IF CFLAG:475 == 0
				KSTR:(K++) =
				CFLAG:475 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:475 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:475 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:475 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:475 = 3
				ENDIF
				;그 이외
				IF (CFLAG:475 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:475 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;촉수계 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;특수커맨드
;=================================================
ELSEIF RESULT == COMTYPE("특수커맨드계") && SELECTCOM < 600
SELECTCASE SELECTCOM
;-------------------------------------------------
;180,로션
;-------------------------------------------------
	CASE 180
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:392 == 0
			KSTR:(K++) =
			CFLAG:392 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:392 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:392 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:392 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:392 = 3
			ENDIF
			;그 이외
			IF (CFLAG:392 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:392 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;181,미약
;-------------------------------------------------
	CASE 181
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:393 == 0
			KSTR:(K++) =
			CFLAG:393 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:393 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:393 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:393 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:393 = 3
			ENDIF
			;그 이외
			IF (CFLAG:393 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:393 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;182,이뇨제
;-------------------------------------------------
	CASE 182
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:394 == 0
			KSTR:(K++) =
			CFLAG:394 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:394 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:394 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:394 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:394 = 3
			ENDIF
			;그 이외
			IF (CFLAG:394 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:394 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;183,비디오카메라
;-------------------------------------------------
	CASE 183
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:28
			;처음
			IF CFLAG:395 == 0
				KSTR:(K++) =
				CFLAG:395 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:395 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:395 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:395 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:395 = 3
				ENDIF
				;그 이외
				IF (CFLAG:395 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:395 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;184,야외플레이
;-------------------------------------------------
	CASE 184
		K_CALL:K = TRAIN_MESSAGE
		;TEQUIP:29로 어느 장소를 선택해도 같은 구상이 나오도록
		;변경･장소마다 바꾸고 싶으면 == 로 하고 숫자를 만지기
		;폐빌딩=1　삼림=2　공원=4　냇가=8
		IF TEQUIP:29
			;처음
			IF CFLAG:396 == 0
				KSTR:(K++) =
				CFLAG:396 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:396 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:396 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:396 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:396 = 3
				ENDIF
				;그 이외
				IF (CFLAG:396 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:396 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;185,조수를범한다
;-------------------------------------------------
	CASE 185
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:397 == 0
			KSTR:(K++) =
			CFLAG:397 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:397 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:397 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:397 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:397 = 3
			ENDIF
			;그 이외
			IF (CFLAG:397 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:397 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;범해져서 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;우리들, 정말 사이 좋지？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;부럽지？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;이쪽이 신경쓰이는 것 같다 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
 
;-------------------------------------------------
;186,수치플레이
;-------------------------------------------------
	CASE 186
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:30
			;처음
			IF CFLAG:398 == 0
				KSTR:(K++) =
				CFLAG:398 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:398 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:398 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:398 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:398 = 3
				ENDIF
				;그 이외
				IF (CFLAG:398 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:398 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;187,목욕탕플레이
;-------------------------------------------------
	CASE 187
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:31
			;처음
			IF CFLAG:399 == 0
				KSTR:(K++) =
				CFLAG:399 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:399 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:399 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:399 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:399 = 3
				ENDIF
				;그 이외
				IF (CFLAG:399 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:399 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;188,샤워
;-------------------------------------------------
	CASE 188
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:32
			;처음
			IF CFLAG:400 == 0
				KSTR:(K++) =
				CFLAG:400 = 1
			ELSE
				IF 함락( ) > 0 && (CFLAG:400 < 3 || FLAG:7 == 2)
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						WSTR:(K++) = 
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						WSTR:(K++) = 
					ENDIF
					CFLAG:400 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:400 < 2 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) = 
					CFLAG:400 = 3
				ENDIF
				;그 이외
				IF (CFLAG:400 || FLAG:7 == 2) && !구상유무확인(0)
					KSTR:(K++) =
					CFLAG:400 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;굴복각인Lv3
			IF MARK:2 == 3 && !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;189,새댁플레이　연모, 친애만
;-------------------------------------------------
	CASE 189
		K_CALL:K = TRAIN_MESSAGE
		IF TEQUIP:33
			;처음
			IF CFLAG:401 == 0
				KSTR:(K++) =
				CFLAG:401 = 1
			;2번째 이후
			ELSEIF CFLAG:401 < 2 || FLAG:7 == 2
				KSTR:(K++) =
				CFLAG:401 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:401 == 1 || FLAG:7 == 2
				KSTR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;190,사진촬영
;-------------------------------------------------
	CASE 190
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:402 == 0
			KSTR:(K++) =
			CFLAG:402 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:402 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:402 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:402 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:402 = 3
			ENDIF
			;그 이외
			IF (CFLAG:402 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:402 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;191,강정신약
;-------------------------------------------------
	CASE 191
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:403 == 0
			KSTR:(K++) =
			CFLAG:403 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:403 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:403 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:403 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:403 = 3
			ENDIF
			;그 이외
			IF (CFLAG:403 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:403 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;192,콘돔정음（노예）
;-------------------------------------------------
	CASE 192
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:404 == 0
			KSTR:(K++) =
			CFLAG:404 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:404 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:404 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:404 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:404 = 3
			ENDIF
			;그 이외
			IF (CFLAG:404 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:404 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;193,콘돔정음（조수）
;-------------------------------------------------
	CASE 193
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:405 == 0
			KSTR:(K++) =
			CFLAG:405 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:405 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:405 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:405 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:405 = 3
			ENDIF
			;그 이외
			IF (CFLAG:405 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:405 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;정액이 무척 맛있어 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;이건 내꺼야！대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;부럽지？대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;먼저 받아갈께 대사（사이좋음）
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
 
;-------------------------------------------------
;194,정액키스
;-------------------------------------------------
	CASE 194
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:406 == 0
			KSTR:(K++) =
			CFLAG:406 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:406 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:406 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:406 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:406 = 3
			ENDIF
			;그 이외
			IF (CFLAG:406 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:406 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;195,유선개발
;-------------------------------------------------
	CASE 195
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:431 == 0
			KSTR:(K++) =
			CFLAG:431 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:431 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:431 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:431 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:431 = 3
			ENDIF
			;그 이외
			IF (CFLAG:431 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:431 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;200,사람을부른다
;-------------------------------------------------
	CASE 200
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:433 == 0
			KSTR:(K++) =
			CFLAG:433 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:433 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:433 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:433 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:433 = 3
			ENDIF
			;그 이외
			IF (CFLAG:433 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:433 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;201,윤간
;-------------------------------------------------
	CASE 201
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:434 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:434 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:434 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:434 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:434 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:434 = 3
			ENDIF
			;그 이외
			IF (CFLAG:434 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:434 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;202,사람을모으게한다
;-------------------------------------------------
	CASE 202
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:476 == 0
			KSTR:(K++) =
			CFLAG:476 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:476 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:476 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:476 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:476 = 3
			ENDIF
			;그 이외
			IF (CFLAG:476 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:476 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;203,원격조교
;-------------------------------------------------
	CASE 203
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:477 == 0
			KSTR:(K++) =
			CFLAG:477 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:477 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:477 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:477 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:477 = 3
			ENDIF
			;그 이외
			IF (CFLAG:477 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:477 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;204,정액먹기
;-------------------------------------------------
	CASE 204
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:478 == 0
			KSTR:(K++) =
			CFLAG:478 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:478 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:478 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:478 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:478 = 3
			ENDIF
			;그 이외
			IF (CFLAG:478 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:478 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;특수 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;500번대,파생커맨드
;=================================================
ELSEIF RESULT == COMTYPE("派生系") && SELECTCOM < 600
SELECTCASE SELECTCOM
;-------------------------------------------------
;500,３Ｐ
;-------------------------------------------------
	CASE 500
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:407 == 0
			KSTR:(K++) =
			CFLAG:407 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:407 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:407 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:407 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:407 = 3
			ENDIF
			;그 이외
			IF (CFLAG:407 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:407 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;안쪽이 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;계속 가버려라！대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;무척 기분 좋아 보여 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;여기에 넣어줘 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
 
;-------------------------------------------------
;501,식스나인
;-------------------------------------------------
	CASE 501
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:408 == 0
			KSTR:(K++) =
			CFLAG:408 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:408 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:408 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:408 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:408 = 3
			ENDIF
			;그 이외
			IF (CFLAG:408 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:408 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;502,이와시미즈
;-------------------------------------------------
	CASE 502
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:409 == 0
			KSTR:(K++) =
			CFLAG:409 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:409 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:409 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:409 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:409 = 3
			ENDIF
			;그 이외
			IF (CFLAG:409 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:409 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;503,G스팟자극
;-------------------------------------------------
	CASE 503
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:410 == 0
			KSTR:(K++) =
			CFLAG:410 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:410 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:410 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:410 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:410 = 3
			ENDIF
			;그 이외
			IF (CFLAG:410 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:410 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;504,흐트러진모란
;-------------------------------------------------
	CASE 504
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:411 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:411 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:411 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:411 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:411 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:411 = 3
			ENDIF
			;그 이외
			IF (CFLAG:411 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:411 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;505,기승유방자위
;-------------------------------------------------
	CASE 505
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:412 == 0
			;처녀
			IF TALENT:0
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			;비처녀
			ELSE
				IF 함락( ) > 0
					;연모, 친애
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
						WSTR:(K++) = 
					;음란, 창부
					ELSEIF 함락( ) > 0 && RESULTS == "음란"
						WSTR:(K++) = 
					;복종, 예속
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
						KSTR:(K++) =
					ENDIF
					;함락완료 공통
					IF !구상유무확인(0)
						KSTR:(K++) =
					ENDIF
				ENDIF
				;그 이외
				IF !구상유무확인(0)
					KSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:412 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:412 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:412 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:412 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:412 = 3
			ENDIF
			;그 이외
			IF (CFLAG:412 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:412 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;506,삽입음부자극
;-------------------------------------------------
	CASE 506
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:413 == 0
			KSTR:(K++) =
			CFLAG:413 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:413 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:413 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:413 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:413 = 3
			ENDIF
			;그 이외
			IF (CFLAG:413 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:413 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;507,삽입자궁구자극
;-------------------------------------------------
	CASE 507
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:414 == 0
			KSTR:(K++) =
			CFLAG:414 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:414 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:414 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:414 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:414 = 3
			ENDIF
			;그 이외
			IF (CFLAG:414 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:414 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;508,６９파이즈리
;-------------------------------------------------
	CASE 508
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:415 == 0
			KSTR:(K++) =
			CFLAG:415 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:415 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:415 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:415 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:415 = 3
			ENDIF
			;그 이외
			IF (CFLAG:415 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:415 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;509,착유
;-------------------------------------------------
	CASE 509
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:416 == 0
			KSTR:(K++) =
			CFLAG:416 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:416 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:416 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:416 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:416 = 3
			ENDIF
			;그 이외
			IF (CFLAG:416 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:416 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;510,더블스마타
;-------------------------------------------------
	CASE 510
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:417 == 0
			KSTR:(K++) =
			CFLAG:417 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:417 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:417 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:417 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:417 = 3
			ENDIF
			;그 이외
			IF (CFLAG:417 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:417 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;함께 기분 좋아지자는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;내쪽이 더 기분 좋게 해줄 께 하는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;영향에 대한 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;평소보다 느끼고 있구나 라는 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;서로 기분 좋네 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
 
;-------------------------------------------------
;511,두개펠라
;-------------------------------------------------
	CASE 511
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:418 == 0
			KSTR:(K++) =
			CFLAG:418 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:418 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:418 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:418 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:418 = 3
			ENDIF
			;그 이외
			IF (CFLAG:418 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:418 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;자신도 확실히 기분 좋게 해줘 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;마스터의 페니스가 닿아서 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;2인분의 정액 마시게 해줄께 대사를
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
		;능숙해졌네 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_5
 
;-------------------------------------------------
;512,애널기승유방자위
;-------------------------------------------------
	CASE 512
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:419 == 0
			KSTR:(K++) =
			CFLAG:419 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:419 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:419 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:419 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:419 = 3
			ENDIF
			;그 이외
			IF (CFLAG:419 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:419 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;513,입과바기나를범한다
;-------------------------------------------------
	CASE 513
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:439 == 0
			KSTR:(K++) =
			CFLAG:439 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:439 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:439 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:439 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:439 = 3
			ENDIF
			;그 이외
			IF (CFLAG:439 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:439 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;안쪽이 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;입이 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;계속 가버려라！대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;무척 기분 좋아 보여 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
 
;-------------------------------------------------
;514,입과애널을범한다
;-------------------------------------------------
	CASE 514
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:440 == 0
			KSTR:(K++) =
			CFLAG:440 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:440 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:440 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:440 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:440 = 3
			ENDIF
			;그 이외
			IF (CFLAG:440 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:440 = 2
			ENDIF
		ENDIF
		;조수구상 호출 함수
		;안쪽이 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_1
		;입이 무척 기분 좋아 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_2
		;계속 가버려라！대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_3
		;무척 기분 좋아 보여 대사
		;		K_CALL:K = KOJO_MESSAGE_ASSIPAIR_COM_4
 
;-------------------------------------------------
;515,애널흐트러진모란
;-------------------------------------------------
	CASE 515
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:441 == 0
			KSTR:(K++) =
			CFLAG:441 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:441 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:441 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:441 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:441 = 3
			ENDIF
			;그 이외
			IF (CFLAG:441 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:441 = 2
			ENDIF
		ENDIF
	CASEELSE
ENDSELECT
;-------------------------------------------------
;500번대,파생 공통
;-------------------------------------------------
		IF !구상유무확인(0)
			K_CALL:K = TRAIN_MESSAGE
			IF 함락( ) > 0 && FLAG:7 == 2
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && FLAG:7 == 2 && !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
			;그 이외
			IF FLAG:7 == 2 && !구상유무확인(0)
				;WSTR:(K++) =
			ENDIF
		ENDIF
;=================================================
;600번대,비전투원 커맨드
;=================================================
ELSEIF SELECTCOM >= 600
SELECTCASE SELECTCOM
;-------------------------------------------------
;600,기초훈련
;-------------------------------------------------
	CASE 600
		K_CALL:K = TRAIN_MESSAGE
		;처음
		IF CFLAG:481 == 0
			KSTR:(K++) =
			CFLAG:481 = 1
		ELSE
			IF 함락( ) > 0 && (CFLAG:481 < 3 || FLAG:7 == 2)
				;연모, 친애
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"
					WSTR:(K++) = 
				ENDIF
				;함락완료 공통
				IF !구상유무확인(0)
					WSTR:(K++) = 
				ENDIF
				CFLAG:481 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:481 < 2 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) = 
				CFLAG:481 = 3
			ENDIF
			;그 이외
			IF (CFLAG:481 || FLAG:7 == 2) && !구상유무확인(0)
				KSTR:(K++) =
				CFLAG:481 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;601,시험소환
;-------------------------------------------------
	CASE 601
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:서머너 && !TALENT:비전투원
			KSTR:(K++) =
		ENDIF
;-------------------------------------------------
;602,그림자추출
;-------------------------------------------------
	CASE 602
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:페르소나구사자 && !TALENT:비전투원
			KSTR:(K++) =
;			;초기페르소나추가
			;ABL:초기페르소나 =
			
;			;초기페르소나능력변경
			;ABL:페르소나1데이터／스킬1 =
			
			;ABL:페르소나1데이터／습득스킬1 =
			;ABL:페르소나1데이터／습득LV1 =
 
		ENDIF
;-------------------------------------------------
;603,각성원조
;-------------------------------------------------
	CASE 603
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:이능자 && !TALENT:비전투원
			KSTR:(K++) =
;			;능력변경
			;ABL:스킬1 = 
			
			;ABL:습득스킬1
			;ABL:습득LV1
 
		ENDIF
;-------------------------------------------------
;604,바이러스조사
;-------------------------------------------------
	CASE 604
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:식노 && !TALENT:비전투원
			KSTR:(K++) =
		ENDIF
;-------------------------------------------------
;605,영매실험
;-------------------------------------------------
	CASE 605
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:데빌시프터 && !TALENT:비전투원
			KSTR:(K++) =
;			;초기링크악마
			;CFLAG:초기링크악마
			
;			;초기링크악마능력변경
			;ABL:초기변신악마스킬1
			
			;ABL:초기변신악마습득스킬1
			;ABL:초기변신악마습득LV1
			
			;ABL:初期リンク悪魔공격상성
			;ABL:初期リンク悪魔최저공격횟수
			;ABL:初期リンク悪魔최대공격횟수
			;ABL:初期リンク悪魔사거리
			;ABL:初期リンク悪魔공격범위
			;ABL:初期リンク悪魔추가효과
			;ABL:初期リンク悪魔추가효과상성
			;ABL:初期リンク悪魔추가효과명중률
			;ABL:初期リンク悪魔추가효과최대명중률
		ENDIF
;-------------------------------------------------
;606,개조수술
;-------------------------------------------------
	CASE 606
		K_CALL:K = TRAIN_MESSAGE
		IF TALENT:기계의처녀 && !TALENT:비전투원
			KSTR:(K++) =
		ENDIF
	CASEELSE
ENDSELECT
ENDIF
;-------------------------------------------------
;그 외, 어느 계통의 조교인지 몰랐던 녀셕
;-------------------------------------------------
IF !구상유무확인(0)
SELECTCASE SELECTCOM
 
ENDSELECT
ENDIF
;------------------------------------------------
;커맨드구상 여기까지
;------------------------------------------------
 
CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_COM_ZZZ
	CATCH
		CALL TRAIN_MESSAGE
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;윤간시의 구상
;CASE 0 안은 반드시 건너뛰게 되어있으며
;RETURN에서 0을 반환하면, 이후 통상적인 윤간행동을 출력합니다
;CFLAG는 사용하지 않습니다
;COMF201_윤간 내에서 LOCAL을 사용하고 있으므로 사용하지 않습니다
;-------------------------------------------------
@KOJO_MESSAGE_RINKAN_K1621, ARG:1 , ARG:2
CALL 구상변수초기화

SELECTCASE ARG:1
	CASE 0
	;CASE 0은 오리지널 윤간 구상
	;C:1회당 귀가 인수 D：흥분도 증가(및 감소)
	;C와 D는 오리지널 윤간 구상 내에서만 사용
	;C += 、D += 를 사용
		IF ARG:2 == 1
		;하드ON
		
		ELSE
		;하드OFF
		
		ENDIF
	;하드계
	;가슴을 밟힌다（거유・폭유만）
	CASE 1
		IF (함락( ) > 0) && FLAG:7 == 2
			;연모, 친애
			IF 함락( ) > 0 && RESULTS == "연모"
				WSTR:(K++) = 
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"
				WSTR:(K++) = 
			;복종, 예속	
			ELSEIF 함락( ) > 0 && RESULTS == "복종"
				WSTR:(K++) = 
			ENDIF
			;함락완료 공통
			IF KSTR:0 == ""
				WSTR:(K++) = 
			ENDIF
		ENDIF
		;굴복각인Lv3
		IF MARK:2 == 3 && FLAG:7 == 2 && KSTR:0 == ""
			WSTR:(K++) = 
		ENDIF
		;그 이외
		IF FLAG:7 == 2 && KSTR:0 == ""
			WSTR:(K++) =
		ENDIF
	;머리를 밟힌다
	CASE 2
	
	;폭력을 당하다
	CASE 3
	
	;담배빵
	;흥분도가 낮고, 반항적, 꿋꿋함, 프라이드높음, 건방짐, 저항을 가지고 있다
	CASE 4
	
	;지면에 늘어진 정액 핥아먹인다
	;마조끼 ３이상
	CASE 5
	
	;몸에 낙서를 당하고 범해진다
	CASE 6
	
	;엉덩이를 치켜올려 뒤로부터 범해지면서, 머리를 짓밟혀 굴욕으로 글썽이는 상태
	CASE 7
	
	;통상계
	;음란계로 눈앞에 장대가 진열되어, 어떤 것을 원하는지 선택하게 하거나（음란・창부만）
	CASE 8
	
	;거유 이상의 캐릭터라면 「그 젖은 장식이냐？ 쉬고 있지 말라고」등등 매도당하며 파이즈리한다（거유・폭유만）
	CASE 9
	
	;삽입만은 피하려고 필사적으로 입과 손으로 몰려드는 자지에 봉사한다（연모・친애만）
	CASE 10
	
	;;양구멍을 뚫리면서 억지로 미소와 피스싸인을 취하게 시켜져 기념촬영
	CASE 11
	
	;;윤간후의 뒷처리 묘사에서, 본인 정액 투성이가 되었는데도 주위 남자들의 육봉을 혀로 청소시키고 있다
	CASE 12
	
	;안에 사정당해도 즉시 다른 사람으로 교대
	CASE 13
	
	;일제사정으로 정액투성이
	CASE 14
	
	;;양구멍 찔리며 동시 사정
	CASE 15
	
	;기승위+양겨드랑이잡
	CASE 16
	
	;;입+보지
	CASE 17
	
ENDSELECT

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_RINKAN_ZZZ
	CATCH
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG 관련（X1을 캐릭터번호로 치환）
;파라미터 변화 등을 트리거로 한 구상　CFLAG 221～260을 사용
;-------------------------------------------------
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_A（X1을 캐릭터번호로 치환）
;조교자의 사정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K1621
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_A
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K1621
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K1621
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교자의 사정시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
LOCAL = 0;射精量
;-------------------------------------------------
;사정장소 변경시의 텍스트　유효시 여기서 함수 종료
;-------------------------------------------------


IF CFLAG:106
	LOCAL = 1
	K_CALL:K = PALAM_MESSAGE_A
	IF 1 == 0
	;머리카락
	ELSEIF CFLAG:106 == 1				
		IF (함락( ) > 0) && (CFLAG:221 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:221= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:221= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:221= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:221= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:221 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:221= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:221 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:221= 1	
		ENDIF		
	;얼굴
	ELSEIF CFLAG:106 == 2				
		IF (함락( ) > 0) && (CFLAG:222 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:222= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:222= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:222= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:222= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:222 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:222= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:222 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:222= 1	
		ENDIF		
	;가슴
	ELSEIF CFLAG:106 == 3				
		IF (함락( ) > 0) && (CFLAG:223 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:223= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:223= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:223= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:223= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:223 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:223= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:223 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:223= 1	
		ENDIF		
	;배
	ELSEIF CFLAG:106 == 4				
		IF (함락( ) > 0) && (CFLAG:224 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:224= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:224= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:224= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:224= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:224 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:224= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:224 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:224= 1	
		ENDIF		
	;겨드랑이
	ELSEIF CFLAG:106 == 5				
		IF (함락( ) > 0) && (CFLAG:225 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:225= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:225= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:225= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:225= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:225 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:225= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:225 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:225= 1	
		ENDIF		
	;허벅지
	ELSEIF CFLAG:106 == 6				
		IF (함락( ) > 0) && (CFLAG:226 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:226= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:226= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:226= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:226= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:226 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:226= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:226 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:226= 1	
		ENDIF		
	;가랑이
	ELSEIF CFLAG:106 == 7				
		IF (함락( ) > 0) && (CFLAG:227 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:227= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:227= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:227= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:227= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:227 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:227= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:227 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:227= 1	
		ENDIF		
	;엉덩이
	ELSEIF CFLAG:106 == 8				
		IF (함락( ) > 0) && (CFLAG:228 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:228= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:228= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:228= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:228= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:228 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:228= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:228 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:228= 1	
		ENDIF		
	;콘돔
	ELSEIF CFLAG:106 == 9
		LOCAL = 0
		;섹스로 사정하고, 애널섹스 이외를 실행 시
		IF GROUPMATCH(SELECTCOM , 60 , 61 , 66 , 68 , 69 , 72 , 504 , 505 , 506 , 507)					
			IF (함락( ) > 0) && (CFLAG:229 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:229= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:229= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:229= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:229= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:229 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:229= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:229 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:229= 1	
			ENDIF		
		ELSE					
			IF (함락( ) > 0) && (CFLAG:230 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:230= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:230= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:230= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:230= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:230= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:230 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:230= 1	
			ENDIF		
		ENDIF
	ENDIF
ENDIF


;-------------------------------------------------
;손, 입 이외로 사정
;대응 커맨드는 키스, 스마타, 풋잡, 애널강제봉사, 더블스마타, 조수를범하게한다
;-------------------------------------------------
IF TFLAG:9 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;대량사정				
	IF TFLAG:9 > 1
		IF (함락( ) > 0) && (CFLAG:230 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:230= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:230= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:230= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:230= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:230= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:230 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:230= 1	
		ENDIF		
	ENDIF
	;통상				
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:230 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:230= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:230= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:230= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:230= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:230= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:230 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:230= 1	
		ENDIF		
	ENDIF
	LOCAL = TFLAG:9
ENDIF

;-------------------------------------------------
;조수를범한다로 사정
;-------------------------------------------------
IF TFLAG:7 > 0
	K_CALL:K = PALAM_MESSAGE_A			
	;대량사정					
	IF TFLAG:7 > 1
		IF (함락( ) > 0) && (CFLAG:232 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:232= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:232= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:232= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:232= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:232= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:232 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:232= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:232 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:232= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:232= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:232= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:232= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:232= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:232 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:232= 1	
		ENDIF		
	ENDIF	
	LOCAL = TFLAG:7
ENDIF

;-------------------------------------------------
;입으로 사정
;-------------------------------------------------
IF TFLAG:0 > 0
	K_CALL:K = PALAM_MESSAGE_A
	;이라마치오로 사정
	IF SELECTCOM == 140			
		;대량사정						
		IF TFLAG:0 > 1
			IF (함락( ) > 0) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF !구상유무확인(0)
			IF (함락( ) > 0) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
	;그 이외의 입사정
	ELSE
		;대량사정						
		IF TFLAG:0 > 1
			IF (함락( ) > 0) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF !구상유무확인(0)
			IF (함락( ) > 0) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:233= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:233= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:233= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
	ENDIF
	LOCAL = TFLAG:0
ENDIF

;-------------------------------------------------
;손으로 사정
;-------------------------------------------------
IF TFLAG:1 > 0
	K_CALL:K = PALAM_MESSAGE_A	
	;대량사정					
	IF TFLAG:1 > 1
		IF (함락( ) > 0) && (CFLAG:234 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:234= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:234= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:234= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:234= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:234= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:234 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:234= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:234 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:234= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:234= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:234= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:234= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:234= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:234 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:234= 1	
		ENDIF		
	ENDIF	
	LOCAL = TFLAG:1
ENDIF

;-------------------------------------------------
;가슴으로 사정
;-------------------------------------------------
IF TFLAG:34 > 0
	K_CALL:K = PALAM_MESSAGE_A		
	;대량사정					
	IF TFLAG:34 > 1
		IF (함락( ) > 0) && (CFLAG:235 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:235= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:235= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:235= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:235= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:235= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:235 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:235= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:235 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:235= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:235= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:235= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:235= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:235= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:235 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:235= 1	
		ENDIF		
	ENDIF	
	LOCAL = TFLAG:34
ENDIF
W:K = 1
;-------------------------------------------------
;오나홀에 사정
;-------------------------------------------------
IF TFLAG:16 == 1
	K_CALL:K = PALAM_MESSAGE_A		
	;대량사정					
	IF TFLAG:16 > 1
		IF (함락( ) > 0) && (CFLAG:236 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:236= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:236= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:236= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:236= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:236= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:236 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:236= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:236 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:236= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:236= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:236= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:236= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:236= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:236 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:236= 1	
		ENDIF		
	ENDIF	
	LOCAL = 0
ENDIF

;-------------------------------------------------
;섹스로 사정
;-------------------------------------------------
IF TFLAG:2 > 0
	LOCAL = TFLAG:2
	K_CALL:K = PALAM_MESSAGE_A
	;애널섹스계
	IF SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || SELECTCOM == 512
		;대량사정						
		IF TFLAG:2 > 1
			IF (함락( ) > 0) && (CFLAG:237 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:237= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:237= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:237= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:237= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:237= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:237 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:237= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF !구상유무확인(0)
			IF (함락( ) > 0) && (CFLAG:237 < 3 || FLAG:7 == 2)		
				;연모, 친애	
				IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
					WSTR:(K++) = 
					CFLAG:237= 4
				;음란, 창부	
				ELSEIF 함락( ) > 0 && RESULTS == "음란"	
					WSTR:(K++) = 
					CFLAG:237= 3
				;복종, 예속	
				ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
					WSTR:(K++) = 
					CFLAG:237= 3
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
					CFLAG:237= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) = 	
				CFLAG:237= 2	
			ENDIF		
			;그 이외		
			IF (CFLAG:237 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
				WSTR:(K++) =
				CFLAG:237= 1	
			ENDIF		
		ENDIF
	;V계 섹스
	ELSE
		;안쪽 자극 섹스계
		IF SELECTCOM == 506 || SELECTCOM == 507
			;대량사정						
			IF TFLAG:2 > 1
				IF (함락( ) > 0) && (CFLAG:238 < 3 || FLAG:7 == 2)		
					;연모, 친애	
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
						WSTR:(K++) = 
						CFLAG:238= 4
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"	
						WSTR:(K++) = 
						CFLAG:238= 3
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
						WSTR:(K++) = 
						CFLAG:238= 3
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
						CFLAG:238= 3
					ENDIF	
				ENDIF		
				;굴복각인Lv3		
				IF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:238= 2	
				ENDIF		
				;그 이외		
				IF (CFLAG:238 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) =
					CFLAG:238= 1	
				ENDIF		
			ENDIF	
			;통상						
			IF !구상유무확인(0)
				IF (함락( ) > 0) && (CFLAG:238 < 3 || FLAG:7 == 2)		
					;연모, 친애	
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
						WSTR:(K++) = 
						CFLAG:238= 4
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"	
						WSTR:(K++) = 
						CFLAG:238= 3
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
						WSTR:(K++) = 
						CFLAG:238= 3
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
						CFLAG:238= 3
					ENDIF	
				ENDIF		
				;굴복각인Lv3		
				IF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:238= 2	
				ENDIF		
				;그 이외		
				IF (CFLAG:238 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:238= 1	
				ENDIF		
			ENDIF
		
		ENDIF
		;그 외 섹스계
		IF !구상유무확인(0)
			;대량사정						
			IF TFLAG:2 > 1
				IF (함락( ) > 0) && (CFLAG:239 < 3 || FLAG:7 == 2)		
					;연모, 친애	
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
						WSTR:(K++) = 
						CFLAG:239= 4
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"	
						WSTR:(K++) = 
						CFLAG:239= 3
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
						WSTR:(K++) = 
						CFLAG:239= 3
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
						CFLAG:239= 3
					ENDIF	
				ENDIF		
				;굴복각인Lv3		
				IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:239= 2	
				ENDIF		
				;그 이외		
				IF (CFLAG:239 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:239= 1	
				ENDIF		
			ENDIF	
			;통상						
			IF !구상유무확인(0)
				IF (함락( ) > 0) && (CFLAG:239 < 3 || FLAG:7 == 2)		
					;연모, 친애	
					IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
						WSTR:(K++) = 
						CFLAG:239= 4
					;음란, 창부	
					ELSEIF 함락( ) > 0 && RESULTS == "음란"	
						WSTR:(K++) = 
						CFLAG:239= 3
					;복종, 예속	
					ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
						WSTR:(K++) = 
						CFLAG:239= 3
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
						CFLAG:239= 3
					ENDIF	
				ENDIF		
				;굴복각인Lv3		
				IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) = 	
					CFLAG:239= 2	
				ENDIF		
				;그 이외		
				IF (CFLAG:239 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
					WSTR:(K++) =
					CFLAG:239= 1	
				ENDIF		
			ENDIF	
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;역강간으로 사정당했다
;-------------------------------------------------
IF SELECTCOM == 64 && TFLAG:10 > 0
	LOCAL = TFLAG:10
	K_CALL:K = PALAM_MESSAGE_A		
	;대량사정					
	IF TFLAG:10 > 1
		IF (함락( ) > 0) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:240= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:240= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:240= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:240= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF !구상유무확인(0)
		IF (함락( ) > 0) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:240= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:240= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:240= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:240= 1	
		ENDIF		
	ENDIF	
ENDIF
;-------------------------------------------------
;사정 공통
;-------------------------------------------------
	IF !구상유무확인(0) && LOCAL > 0
		IF (함락( ) > 0) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:240= 4
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:240= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) = 	
			CFLAG:240= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
			WSTR:(K++) =
			CFLAG:240= 1	
		ENDIF		
	ENDIF	

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_PALAMCNG_A_ZZZ
	CATCH
		CALL PALAM_MESSAGE_A
	ENDCATCH
	
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_E（X1을 캐릭터번호로 치환）
;촉수의 사정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_E_K1621
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_E
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K1621
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K1621
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;촉수의 사정시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
;-------------------------------------------------
;촉수가 사정
;-------------------------------------------------
IF TFLAG:15 >0
	K_CALL:K = PALAM_MESSAGE_E			
	IF (함락( ) > 0) && (CFLAG:241 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
			CFLAG:241= 4
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
			CFLAG:241= 3
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
			CFLAG:241= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(0)	
			WSTR:(K++) = 
			CFLAG:241= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:241 < 2 || FLAG:7 == 2) && !구상유무확인(0)		
		WSTR:(K++) = 	
		CFLAG:241= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:241 < 1 || FLAG:7 == 2) && !구상유무확인(0)		
		WSTR:(K++) =
		CFLAG:241= 1	
	ENDIF		
ENDIF

CALL 구상변수표시
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_COM_ZZZ
	CATCH
		CALL PALAM_MESSAGE_E
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_B 관련（X1을 캐릭터번호로 치환）
;노예의 절정을 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_B_K1621
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_B
	RETURN 0
ENDIF
;상정외의 구상이라면 스킵(이거 지우면 버그난다)
IF SELECTCOM < 0 || SELECTCOM > 700
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K1621
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K1621
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;노예의 절정시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
;-------------------------------------------------
;처음 C절정
;-------------------------------------------------
IF NOWEX:0 > 0 && CFLAG:242 == 0
	WSTR:(K++) =
	CFLAG:242 = 1
ENDIF

;-------------------------------------------------
;처음 V절정
;-------------------------------------------------
IF NOWEX:1 > 0 && CFLAG:243 == 0
	WSTR:(K++) =
	CFLAG:243 = 1
ENDIF

;-------------------------------------------------
;처음 A절정
;-------------------------------------------------
IF NOWEX:2 > 0 && CFLAG:244 == 0
	WSTR:(K++) =
	CFLAG:244 = 1
ENDIF

;-------------------------------------------------
;처음 B절정
;-------------------------------------------------
IF NOWEX:3 > 0 && CFLAG:245 == 0
	WSTR:(K++) =
	CFLAG:245 = 1
ENDIF

;-------------------------------------------------
;절정
;-------------------------------------------------
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

IF TFLAG:29 > 0
	K_CALL:K = PALAM_MESSAGE_B			
	;강절정
	IF TFLAG:29 > 1
		IF (함락( ) > 0) && (CFLAG:246 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:246= 3
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:246= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(LOCAL)	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			IF RAND:2 == 0
				WSTR:(K++) = 「%CSTR:일인칭%、もう…かえって、これな…%UNICODE(0x2661)%」
			ELSE
				WSTR:(K++) = 「ッッ%UNICODE(0x2661)%　ぅっ、かは%UNICODE(0x2661)%、っ…は、はぁ……」
			ENDIF
			CFLAG:246= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:246 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			IF RAND:2 == 0
				WSTR:(K++) = 「ん、あ、ぁ゛ーーッ!!?」
			ELSE
				WSTR:(K++) = 「ひっ、こわい、こわ、ぃあ゛ッーーー%UNICODE(0x2661)%」
			ENDIF
			CFLAG:246= 1	
		ENDIF		
	ENDIF
	;절정
	IF !구상유무확인(LOCAL)
		IF (함락( ) > 0) && (CFLAG:246 < 3 || FLAG:7 == 2)		
			;연모, 친애	
			IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
				WSTR:(K++) = 
				CFLAG:246= 3
			;음란, 창부	
			ELSEIF 함락( ) > 0 && RESULTS == "음란"	
				WSTR:(K++) = 
				CFLAG:246= 3
			;복종, 예속	
			ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(LOCAL)	
				WSTR:(K++) = 
				CFLAG:246= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			IF RAND:2 == 0
				WSTR:(K++) = 「ぁ、く…体があつ、い……っ!」
			ELSE
				WSTR:(K++) = 「んぎ、あ、あ゛ッぅ……」
			ENDIF
			CFLAG:246= 2	
		ENDIF		
		;그 이외		
		IF (CFLAG:246 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
			IF RAND:2 == 0
				WSTR:(K++) = 「ぅう゛ッ…!」
			ELSE
				WSTR:(K++) = 「おかし、な、感じがっ…」
			ENDIF
			CFLAG:246= 1	
		ENDIF		
	ENDIF
ENDIF
;-------------------------------------------------
;사정
;-------------------------------------------------
;사정하고 있다
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

IF TFLAG:10 > 0				
	IF (함락( ) > 0) && (CFLAG:247 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
			CFLAG:247= 4
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
			CFLAG:247= 3
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
			CFLAG:247= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)	
			WSTR:(K++) = 
			CFLAG:247= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:247 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		IF RAND:3 == 0
			WSTR:(K++) = 「んあっ、は、ぁ……」
		ELSEIF RAND:2 == 0
			WSTR:(K++) = 「ッッ……はぁ、はー…」
		ELSE
			WSTR:(K++) = 「ぅう…ふぅ、んんっ～～…!」
		ENDIF
		CFLAG:247= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:247 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		IF RAND:2 == 0
			WSTR:(K++) = 「……っ、く」
		ELSE
			WSTR:(K++) = 「ぁ、またっ変な、かん、かくが…!」
		ENDIF
		CFLAG:247= 1	
	ENDIF		
ENDIF

;-------------------------------------------------
;분유
;-------------------------------------------------
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

;분유하고 있다
IF TFLAG:11 > 0			
	IF (함락( ) > 0) && (CFLAG:248 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
			CFLAG:248= 4
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
			CFLAG:248= 3
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
			CFLAG:248= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)	
			WSTR:(K++) = 
			CFLAG:248= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:248 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) = 	
		CFLAG:248= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:248 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) =
		CFLAG:248= 1	
	ENDIF		
ENDIF

;-------------------------------------------------
;방뇨
;-------------------------------------------------
;방뇨하고 있다
SIF 구상유무확인(0) && K > 0
	CALL WSTR기입,K - 1
LOCAL = K

IF TFLAG:29 > 0 && (TEQUIP:27 || TALENT:57)			
	IF (함락( ) > 0) && (CFLAG:249 < 3 || FLAG:7 == 2)		
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
			CFLAG:249= 4
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
			CFLAG:249= 3
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
			CFLAG:249= 3
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)	
			WSTR:(K++) = 
			CFLAG:249= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:249 < 2 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) = 	
		CFLAG:249= 2	
	ENDIF		
	;그 이외		
	IF (CFLAG:249 < 1 || FLAG:7 == 2) && !구상유무확인(LOCAL)		
		WSTR:(K++) =
		CFLAG:249= 1	
	ENDIF		
ENDIF

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_PALAMCNG_B_ZZZ
	CATCH
		CALL PALAM_MESSAGE_B
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_C 관련（X1을 캐릭터번호로 치환）
;조교 후의 변화를 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_C_K1621
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_C
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K1621
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K1621
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교 후의 텍스트
;-------------------------------------------------
;-------------------------------------------------
;처녀상실(처녀만)
;-------------------------------------------------
CALL 구상변수초기화

IF TFLAG:3 > 0
	K_CALL:K = PALAM_MESSAGE_C
	;연모나 친애이면서 주인의 섹스에 의한 처녀상실
	IF TFLAG:20
		WSTR:(K++) =
	;그 이외의 때에 처녀상실
	ELSE			
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
		ENDIF	
		;굴복각인Lv3
		IF MARK:2 == 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
ENDIF
;-------------------------------------------------
;아무것도안한다 후
;-------------------------------------------------
IF SELECTCOM == 21
	K_CALL:K = PALAM_MESSAGE_C
	;欲情LV5以上
	IF !구상유무확인(0) && PALAM:5 >= PALAMLV:5
		;함락완료
		IF 함락()
			WSTR:(K++) =
		ENDIF
		;그 이외
		IF !구상유무확인(0)
			WSTR:(K++) =
		ENDIF
	ENDIF	
	;欲情LV3以上 + 미약
	IF !구상유무확인(0) && PALAM:5 >= PALAMLV:3 && TEQUIP:26
		;함락완료
		IF 함락()
			WSTR:(K++) =
		ENDIF
		;그 이외
		IF !구상유무확인(0)
			WSTR:(K++) =
		ENDIF
	ENDIF	
	;欲情LV3以上
	IF !구상유무확인(0) && PALAM:5 >= PALAMLV:3
		;함락완료
		IF 함락()
			WSTR:(K++) =
		ENDIF
		;그 이외
		IF !구상유무확인(0)
			WSTR:(K++) =
		ENDIF
	ENDIF	
ENDIF

;-------------------------------------------------
;청소펠라
;-------------------------------------------------
IF TFLAG:8 > 0
	;ABL:정액중독　ABL:ASSI:정액중독
	K_CALL:K = PALAM_MESSAGE_C
	;더블펠라 후 조수와 정액키스　조건:조교대상이나 조수가 정액중독 보유
	IF TFLAG:8 >= 2
		;함락완료
		IF 함락()
			WSTR:(K++) =
		ENDIF
		;순종Lv3
		IF ABL:0 >= 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
	;더블펠라로 조수와 청소펠라
	IF TFLAG:8 >= 2 && !구상유무확인(0)
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
		ENDIF	
		;순종Lv3
		IF ABL:0 >= 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
	;두개펠라 후 청소펠라
	IF TFLAG:0 && TFLAG:6 && !구상유무확인(0)
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
		ENDIF	
		;순종Lv3
		IF ABL:0 >= 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
	;펠라 후 청소펠라
	IF !구상유무확인(0)
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
		ENDIF	
		;순종Lv3
		IF ABL:0 >= 3 && !구상유무확인(0)
			WSTR:(K++) = 
		ENDIF
		;공통	
		IF !구상유무확인(0)	
			WSTR:(K++) =
		ENDIF	
	ENDIF
ENDIF

CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_COM_ZZZ
	CATCH
		CALL PALAM_MESSAGE_C
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_D 관련（X1을 캐릭터번호로 치환）
;파라미터 변화를 트리거로 한 구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_D_K1621
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_D
	RETURN 0
ENDIF
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K1621
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K1621
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;파라미터 변화시의 텍스트
;-------------------------------------------------
CALL 구상변수초기화
;-------------------------------------------------
;처음 윤활이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:4 + CUP:4
IF P > PALAMLV:4 && CFLAG:250 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;로션을 사용한 경우
	IF SELECTCOM == 180
		WSTR:(K++) = 「……これ以上、ないぐらい、ぬめぬめだ…」
	;그 이외
	ELSE
		WSTR:(K++) = 「%CSTR:일인칭%でも制御できない…変な風に、濡れた…」
	ENDIF
	CFLAG:250 = 1
ENDIF

;-------------------------------------------------
;처음 욕정이 LV2를 넘었다
;-------------------------------------------------
P = PALAM:6 + CUP:6
IF P > PALAMLV:3 && CFLAG:251 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;미약을 사용한 경우
	IF SELECTCOM == 181
		WSTR:(K++) = 「%CSTR:일인칭%、ニンゲンの作る薬は、効かない…はず、なのに」
	;그 이외
	ELSE
		WSTR:(K++) = 「……?」
		WSTR:(K++) = 「あ。…色が、ついた」
		WSTR:(K++) = 「これが、%CSTR:일인칭%にも、繁殖ができる証…」
	ENDIF
	CFLAG:251 = 1
ENDIF

;-------------------------------------------------
;처음 굴복이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:7 + CUP:7
IF P > PALAMLV:3 && CFLAG:252 == 0
	K_CALL:K = PALAM_MESSAGE_D
	IF RAND:2 == 0
		WSTR:(K++) = 「%CSTR:이인칭%は、ニンゲンだけど、力がある…」
		WSTR:(K++) = 「ニンゲンを従えるニンゲン…こうも力があればおかしな話じゃあ、ない」
	ELSE
		WSTR:(K++) = 「そろそろ%CSTR:이인칭%について、理解は、した…」
	ENDIF
	CFLAG:252 = 1
ENDIF

;-------------------------------------------------
;처음 복종이 LV3을 넘었다
;-------------------------------------------------
P = PALAM:5 + CUP:5
IF P > PALAMLV:3 && CFLAG:253 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;함락완료
	IF 함락( ) > 0
		WSTR:(K++) =
	;그 이외
	ELSE
		WSTR:(K++) = 「%CSTR:이인칭%の、感情…知りたいと、思った」
		WSTR:(K++) = 「%CSTR:일인칭%には、解らないかもしれない、けど…」
	ENDIF
	CFLAG:253 = 1
ENDIF

;-------------------------------------------------
;처음 공포가 LV3을 넘었다
;-------------------------------------------------
P = PALAM:11 + CUP:11
IF P > PALAMLV:3 && CFLAG:254 == 0
	K_CALL:K = PALAM_MESSAGE_D
	;함락완료
	IF 함락( ) > 0
		WSTR:(K++) =
	;그 이외
	ELSE
		WSTR:(K++) = 「%CSTR:일인칭%、恐怖を、与えられたのは…今がはじめて、だった」
	ENDIF
	CFLAG:254 = 1
ENDIF

CALL 구상변수표시
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_PALAMCNG_D_ZZZ
	CATCH
		CALL PALAM_MESSAGE_D
	ENDCATCH
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_MARKCNG 관련（X1을 캐릭터번호로 치환）
;각인 변동을 트리거로 한 구상
;각인 변동 후에 구상을 발동합니다
;-------------------------------------------------
@KOJO_MESSAGE_MARKCNG_K1621
;조수가 조교했을시에 구상을 스킵한다（취향에 따라 사용, 행 앞의 ;를 지우면 스킵하게 된다）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈 착용시에는 지문을 표시하고 구상을 스킵한다
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL MARK_MESSAGE
	RETURN 0
ENDIF

CALL 구상변수초기화
;--------------------------------------------------
;특정 캐릭터가 조수, 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_MARKCNG_PY1_K1621
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_MARKCNG_PY1_K1621
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;고통각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:22 == 3
	K_CALL:K = MARK_MESSAGE_A	
	;연모, 친애	
	IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
		WSTR:(K++) = 
	;음란, 창부	
	ELSEIF 함락( ) > 0 && RESULTS == "음란"	
		WSTR:(K++) = 
	;복종, 예속	
	ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
		WSTR:(K++) = 
	ENDIF	
	;공통
	IF !구상유무확인(LOCAL)
		WSTR:(K++) = 
	ENDIF		
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_A
ENDIF
;-------------------------------------------------
;쾌락각인Lv3
;-------------------------------------------------

LOCAL = K
IF TFLAG:23 == 3
	K_CALL:K = MARK_MESSAGE_P
	;연모, 친애	
	IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
		WSTR:(K++) = 
	;복종, 예속	
	ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
		WSTR:(K++) = 
	ENDIF	
	;공통
	IF !구상유무확인(LOCAL)
		WSTR:(K++) = 
	ENDIF		
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_P
ENDIF

;-------------------------------------------------
;굴복각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:24 == 13 || TFLAG:24 == 23 || TFLAG:24 == 33
	K_CALL:K = MARK_MESSAGE_S
	;쾌락에 의한 굴복
	IF TFLAG:24 == 13
		WSTR:(K++) = %CALLNAME:TARGET%はいつにも増して言葉数が少なくなっている。
		WSTR:(K++) = 「%CSTR:이인칭%の、やることは…全部」
		WSTR:(K++) = 「%CSTR:일인칭%、もっと、して、欲しくなる…これで、お終い…?」
	;공포와 고통에 의한 굴복
	ELSEIF TFLAG:24 == 23
		WSTR:(K++) = %CALLNAME:TARGET%はいつにも増して言葉数が少なくなっている。
		WSTR:(K++) = 「%CSTR:이인칭%のやること、全部…」
		WSTR:(K++) = 「%CSTR:일인칭%、焼き付いて、直らない…」
	;굴복과 복종에 의한 굴복
	ELSEIF TFLAG:24 == 33
		WSTR:(K++) = %CALLNAME:TARGET%はいつにも増して言葉数が少なくなっている。
		WSTR:(K++) = 「%CSTR:이인칭%に、されたこと、全部」
		WSTR:(K++) = 「%CSTR:일인칭%、はじめて、だった…すごいどくどく、した」
	ENDIF
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_S
ENDIF

;-------------------------------------------------
;반발각인Lv3
;-------------------------------------------------
LOCAL = K
IF TFLAG:21 == 3
	K_CALL:K = MARK_MESSAGE_R
	;함락완료
	IF 함락( ) > 0
		;연모, 친애	
		IF 함락(TARGET,PLAYER) > 0 && RESULTS == "연모"	
			WSTR:(K++) = 
		;음란, 창부	
		ELSEIF 함락( ) > 0 && RESULTS == "음란"	
			WSTR:(K++) = 
		;복종, 예속	
		ELSEIF 함락(TARGET,PLAYER) > 0 && RESULTS == "복종"	
			WSTR:(K++) = 
		ENDIF	
		;함락완료 공통	
		IF !구상유무확인(LOCAL)
			WSTR:(K++) = 
		ENDIF	
	ENDIF
	;그 이외
	IF !구상유무확인(LOCAL)
		WSTR:(K++) = 
	ENDIF
	SIF !구상유무확인(LOCAL)
		CALL MARK_MESSAGE_R
ENDIF

CALL 구상변수표시
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_MARKCNG_ZZZ
	CATCH
	ENDCATCH
	
ENDIF
;-------------------------------------------------
;이벤트구상 모음（X1을 캐릭터번호로 치환한다） CFLAG 261～299를 사용
;-------------------------------------------------
@KOJO_MESSAGE_EVENT_K1621

;--------------------------------------------------
;특정 주인 시에 특수 캐릭터구상을 호출(Y1을 특정 캐릭터, X1을 노예의 캐릭터번호로 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_EVENT_PY1_K1621
;	RETURN 0
;ENDIF


CALL 구상변수초기화

SELECTCASE TFLAG:13
;　　1:아침펠라
;　　2:아침파이즈리
;　　3:아침기승위
;　　4:조교 후 레즈플레이
;　　5:조교 후 섹스
;　　6:강정신약의 반동
;　　7:조교 후 착유
;　　8:조교 후 자위
;　　9:조교 후 요바이
;　　10:황색사탕 사용시
;　　11:임신 발각시
;　　12:출산시
;　　13:육아실에서의 이벤트구상
;　　14:자식 독립시
;　　15:정식포장마차 실행시
;　　16:창관에서의 아르바이트시
;　　17:렌탈 실행시
;　　18:매각시
;　　19:비디오 매각시
;　　20:콘서트 실행시
;　　21:난교라이브 실행시
;　　22:공중변소 플레이 실행시
;　　23:비디오 보유시
;　　24:아침기승위A
;　　25:아침키스
;　　26:아침식사
;　　27:일어나자 곁에서 자고 있다
;　　28:아침풋잡
;　　29:아침에 갓 짠 밀크
;　　30:잠자다 실례
;　　31:요바이 C/A
;　　32:요바이 V/C
;　　33:요바이 A/C
;　　34:요바이 C/C
;　　35:밤자위
;　　36:같이잔다
;　　37:起きたら隣で寝てる
;　　38:朝の足コキ
;　　39:朝の搾りたてミルク
;　　40:아내・남편 취득
;　　41:음마 취득
;　　42:노리개 취득
;　　43:スト劇場
;　　44:갈아입기・가슴을 드러낸다
;　　45:애(愛)계 ＮＴＲ이벤트
;　　46:음(淫)계 ＮＴＲ이벤트
;　　47:예(隷)계 ＮＴＲ이벤트
;　　48:애(愛)계 재함락 이벤트
;　　49:음(淫)계 재함락 이벤트
;　　50:예(隷)계 재함락 이벤트
;　　51:ＮＴＲ요바이・난교파티
;　　52:ＮＴＲ요바이・공원
;　　53:ＮＴＲ요바이・변기
;　　54:ＮＴＲ요바이・밤의 거리
;　　55:ＮＴＲ요바이・ＮＴＲ전 밀회
;　　56:ＮＴＲ요바이・ＮＴＲ후 첫 밀회
;　　57:ＮＴＲ요바이・데이트
;　　58:ＮＴＲ요바이・본방
;　　59:ＮＴＲ요바이・자위서로과시
;　　60:ＮＴＲ요바이・애널섹스
;　　61:ＮＴＲ요바이・야외플레이
;　　62:ＮＴＲ요바이・야외조교
;　　63:창관에서 실제로 일했을 때
;　　65:교회에서 기도
;　　66:교회에서 입봉사
;　　67:교회에서 성봉사
;　　70:陰唇・胸同時露出(調教対象)
;　　71:陰唇・胸同時露出(主人)
;　　72:陰唇・胸同時露出(助手)
;　　73:陰唇出(調教対象)
;　　74:음순노출(主人)
;　　75:음순노출(助手)
;　　76:胸露出(調教対象)
;　　77:胸露出(主人)
;　　78:胸露出(助手)
;　　79:던전내 성욕처리：동료마와 섹스(당하는 측)
;　　80:던전내 성욕처리：동료마에게 봉사시킨다(봉사측)
;　　81:던전내 성욕처리：그 장소에서 자위
;　　82:던전내 성욕처리：그늘에서 자위
;　　83:던전내 성욕처리：동료마와 섹스(하는 측)
;　　84:던전내 성욕처리：동료마에게 봉사시킨다(봉사시키는 측)
;　　85:던전내 성욕처리：윤간당한다
;　　90:도장 개시
;　　91:도장 종료
;　　92:맹우
;　　93:탄광
		;-------------------------------------------------
		;아침펠라
		;-------------------------------------------------
	CASE 1
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:261 < 1 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:261 = 1
			ENDIF

		;-------------------------------------------------
		;아침파이즈리
		;-------------------------------------------------
	CASE 2
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:262 < 1 || FLAG:7 == 2)
				;모유체질
				IF TALENT:149						
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;모유체질이 아니다
				IF !구상유무확인(0)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) =
					ENDIF	
				ENDIF
				CFLAG:262 = 1
			ENDIF

		;-------------------------------------------------
		;아침기승위
		;-------------------------------------------------
	CASE 3
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침기승위A
		;-------------------------------------------------
	CASE 24
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침키스 연모, 친애 전제
		;-------------------------------------------------
	CASE 25
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침식사
		;-------------------------------------------------
	CASE 26
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;노래 부르면서
				IF (TALENT:25 || TALENT:28) && ABL:14 >= 2						
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;노래 부르지 않음
				IF !구상유무확인(0)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) =
					ENDIF	
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;일어나자 곁에서 자고 있다 연모, 친애 전제
		;-------------------------------------------------
	CASE 27
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;아침풋잡
		;-------------------------------------------------
	CASE 28
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;아침에 갓 짠 밀크
		;-------------------------------------------------
	CASE 29
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;잠자다 실례
		;-------------------------------------------------
	CASE 30
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 레즈플레이
		;-------------------------------------------------
	CASE 4
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:264 == 0
				WSTR:(K++) =
				CFLAG:264 = 1
			;그 이외
			ELSEIF CFLAG:264 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:264 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 섹스
		;-------------------------------------------------
	CASE 5
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:265 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:265 = 1
			ENDIF

		;-------------------------------------------------
		;강정신약의 반동
		;-------------------------------------------------
	CASE 6
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:266 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:266 = 2
			ENDIF
			IF (CFLAG:266 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:266 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 착유
		;-------------------------------------------------
	CASE 7
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:267 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:267 = 2
			ENDIF
			IF (CFLAG:267 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:267 = 1
			ENDIF

		;-------------------------------------------------
		;조교 후 자위
		;-------------------------------------------------
	CASE 8
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:268 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:268 = 2
			ENDIF
			IF (CFLAG:268 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:268 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인C 상대V）
		;-------------------------------------------------
	CASE 9
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인C 상대A）
		;-------------------------------------------------
	CASE 31
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인V 상대C）
		;-------------------------------------------------
	CASE 32
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（주인A 상대C）
		;-------------------------------------------------
	CASE 33
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（양쪽C（스마타）・양쪽C（조개맞대기））
		;-------------------------------------------------
	CASE 34
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;밤 자위
		;-------------------------------------------------
	CASE 35
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;같이잔다 연모, 친애
		;-------------------------------------------------
	CASE 36
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:269 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;황색사탕 사용시
		;-------------------------------------------------
	CASE 10
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:270 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:270 = 2
			ENDIF
			IF (CFLAG:270 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:270 = 1
			ENDIF

		;-------------------------------------------------
		;임신 발각시
		;-------------------------------------------------
	CASE 11
			SIF CFLAG:271 >= 1 && FLAG:7 != 2
				GOTO 表示
			K_CALL:K = EVENT_MESSAGE
			;임신대상을 TARGET에 격납하고, 부친은 노예는 V, 조수는 W로 한다
			;붕괴되어 버린 경우
			IF TALENT:9 == 1
				WSTR:(K++) =
			;부친이 주인이고 모친이 연모나 친애 보유
			ELSEIF (TALENT:3 || TALENT:6) && CFLAG:102 == 1
				WSTR:(K++) =
			;부친이 노예나 조수고 모친이 음란이나 창부 보유
			ELSEIF (TALENT:4 || TALENT:7) && (CFLAG:102 == 2 || CFLAG:102 == 3)
				;부친(조수）과의 상성이 125보다 큰 경우
				IF CFLAG:102 == 2 && GET_RELATION(TARGET, W) > 125
					WSTR:(K++) =
			 	;부친(노예）과의 상성이 125보다 큰 경우
				ELSEIF CFLAG:102 == 3 && GET_RELATION(TARGET, V) > 125
					WSTR:(K++) =
				ELSE
					WSTR:(K++) =
				ENDIF
			;복종이나 예속 보유
			ELSEIF (TALENT:5 || TALENT:8)
				WSTR:(K++) =
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:271 = 1

		;-------------------------------------------------
		;출산시
		;-------------------------------------------------
	CASE 12
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:272 < 2 || FLAG:7 == 2)					
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:272 = 2
			ENDIF
			IF (CFLAG:272 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:272 = 1
			ENDIF

		;-------------------------------------------------
		;육아실
		;-------------------------------------------------
	CASE 13
			K_CALL:K = EVENT_MESSAGE
			;출산전
			IF TALENT:153
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 2 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
					CFLAG:273 = 2
				ENDIF
				IF (CFLAG:273 < 1 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) =
					CFLAG:273 = 1
				ENDIF
			;출산후
			ELSE
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 4 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
					CFLAG:273 = 4
				ENDIF
				IF (CFLAG:273 < 3 || FLAG:7 == 2) && !구상유무확인(0)
					WSTR:(K++) =
					CFLAG:273 = 3
				ENDIF
			ENDIF

		;-------------------------------------------------
		;자식 독립시
		;-------------------------------------------------
	CASE 14
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:274 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:274 = 2
			ENDIF
			IF (CFLAG:274 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:274 = 1
			ENDIF
		;-------------------------------------------------
		;정식포장마차시
		;-------------------------------------------------
	CASE 15
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:275 == 0
				WSTR:(K++) =
				CFLAG:275 = 1
			;그 이외
			ELSEIF CFLAG:275 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:275 = 1
			ENDIF

		;-------------------------------------------------
		;창관에서 아르바이트・설정시
		;-------------------------------------------------
	CASE 16
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:276 == 0
				WSTR:(K++) =
				CFLAG:276 = 1
			;그 이외
			ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:276 = 1
			ENDIF
		;-------------------------------------------------
		;창관에서 아르바이트・실행시
		;-------------------------------------------------
	CASE 63
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:606 == 0
				WSTR:(K++) =
				CFLAG:606 = 1
			;그 이외
			ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:606 = 1
			ENDIF


		;-------------------------------------------------
		;렌탈시
		;-------------------------------------------------
	CASE 17
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:277 == 0
				WSTR:(K++) =
				CFLAG:277 = 1
			;그 이외
			ELSEIF CFLAG:277 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:277 = 1
			ENDIF

		;-------------------------------------------------
		;매각시
		;-------------------------------------------------
	CASE 18
			K_CALL:K = EVENT_MESSAGE
			;반발각인Lv3
			IF MARK:3 == 3
				WSTR:(K++) = 
			ENDIF
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && MARK:3 < 3 && !구상유무확인(0)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
			ENDIF
			;그 이외
			IF !구상유무확인(0)
				WSTR:(K++) =
			ENDIF

		;-------------------------------------------------
		;비디오 매각시
		;-------------------------------------------------
	CASE 19
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:278 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:278 = 2
			ENDIF
			IF (CFLAG:278 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:278 = 1
			ENDIF

		;-------------------------------------------------
		;콘서트시
		;-------------------------------------------------
	CASE 20
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:279 == 0
				WSTR:(K++) =
				CFLAG:279 = 1
			;그 이외
			ELSEIF CFLAG:279 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:279 = 1
			ENDIF

		;-------------------------------------------------
		;난교라이브시
		;-------------------------------------------------
	CASE 21
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:280 == 0
				WSTR:(K++) =
				CFLAG:280 = 1
			;그 이외
			ELSEIF CFLAG:280 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:280 = 1
			ENDIF

		;-------------------------------------------------
		;공중변소 플레이시
		;-------------------------------------------------
	CASE 22
			K_CALL:K = EVENT_MESSAGE
			;처음
			IF CFLAG:281 == 0
				WSTR:(K++) =
				CFLAG:281 = 1
			;그 이외
			ELSEIF CFLAG:281 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:281 = 1
			ENDIF

		;-------------------------------------------------
		;비디오 보유시
		;-------------------------------------------------
	CASE 23
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:282 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:282 = 2
			ENDIF
			IF (CFLAG:282 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:282 = 1
			ENDIF

		;-------------------------------------------------
		;기억소거약 사용시
		;-------------------------------------------------
	CASE 30
			K_CALL:K = EVENT_MESSAGE
			;함락완료
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:283 < 2 || FLAG:7 == 2)
				;연모, 친애	
				IF (TALENT:3 || TALENT:6)	
					WSTR:(K++) = 
				;음란, 창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					WSTR:(K++) = 
				;복종, 예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					WSTR:(K++) = 
				ENDIF	
				;함락완료 공통	
				IF !구상유무확인(0)	
					WSTR:(K++) = 
				ENDIF	
				CFLAG:283 = 2
			ENDIF
			IF (CFLAG:283 < 1 || FLAG:7 == 2) && !구상유무확인(0)
				WSTR:(K++) =
				CFLAG:283 = 1
			ENDIF

		;-------------------------------------------------
		;;[아내][남편]취득시 친애 전제
		;-------------------------------------------------
	CASE 40
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:283 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:283 = 1
			ENDIF
		;-------------------------------------------------
		;;[음마]취득시 창부 전제
		;-------------------------------------------------
	CASE 41
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:284 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:284 = 1
			ENDIF
		;-------------------------------------------------
		;;[전속노예]취득시 예속 전제
		;-------------------------------------------------
	CASE 42
			K_CALL:K = EVENT_MESSAGE
			IF CFLAG:285 < 1 || FLAG:7 == 2
				WSTR:(K++) =
				CFLAG:285 = 1
			ENDIF
		;-------------------------------------------------
		;;오픈/클로즈・오프・가슴
		;-------------------------------------------------
	CASE 44
			K_CALL:K = EVENT_MESSAGE
			IF TEQUIP:가슴앞열림
				;가슴을 드러낸다
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;그 외
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:285 = 1
			ELSE
				;가슴을 집어넣는다
				;함락완료
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모, 친애	
					IF (TALENT:3 || TALENT:6)	
						WSTR:(K++) = 
					;음란, 창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						WSTR:(K++) = 
					;복종, 예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						WSTR:(K++) = 
					ENDIF	
					;함락완료 공통	
					IF !구상유무확인(0)	
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;그 외
				IF !구상유무확인(0)	
					WSTR:(K++) =
				ENDIF	
				CFLAG:287 = 1
			ENDIF
		;-------------------------------------------------
		;;[애ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 45
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:288 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF	
				ENDIF
				;함락완료 공통	
				IF !구상유무확인(0)	
					;진행도 분기
					;초회
					IF CFLAG:애ＮＴＲ진행도 == 0
						WSTR:(K++) =
					;2번째（리치）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						WSTR:(K++) =
					;3번째（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						WSTR:(K++) =
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:애ＮＴＲ진행도 == 0
					WSTR:(K++) =
				;2번째（리치）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 1
					WSTR:(K++) =
				;3번째（함락）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 2
					WSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:288 = 1
		ENDIF
		;-------------------------------------------------
		;;[음ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 46
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:289 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				ENDIF
				;함락완료 공통	
				IF !구상유무확인(0)	
					;진행도 분기
					;초회
					IF CFLAG:음ＮＴＲ진행도 == 0
						WSTR:(K++) =
					;2번째（리치）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						WSTR:(K++) =
					;3번째（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						WSTR:(K++) =
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:음ＮＴＲ진행도 == 0
					WSTR:(K++) =
				;2번째（리치）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 1
					WSTR:(K++) =
				;3번째（함락）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 2
					WSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:289 = 1
		ENDIF
		;-------------------------------------------------
		;;[예ＮＴＲ]이벤트시
		;-------------------------------------------------
	CASE 47
		K_CALL:K = EVENT_MESSAGE
		;함락완료
		IF (CFLAG:290 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;주인이 함락되어 있다 or 누구에게도 함락되지 않은 상태에서 ＮＴＲ
				;연모, 친애
				IF TALENT:3 || TALENT:6
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;음란, 창부
				ELSEIF TALENT:4 || TALENT:7
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				;복종, 예속
				ELSEIF TALENT:5 || TALENT:8
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) = 
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) = 
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) = 
					ENDIF
				ENDIF
				;함락완료 공통	
				IF !구상유무확인(0)	
					;진행도 분기
					;초회
					IF CFLAG:예ＮＴＲ진행도 == 0
						WSTR:(K++) =
					;2번째（리치）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						WSTR:(K++) =
					;3번째（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						WSTR:(K++) =
					ENDIF
				ENDIF
			ELSE
				;주인 이외의 누군가에게 함락된 상태에서 또 다른 캐릭터에게 ＮＴＲ
				;진행도 분기
				;초회
				IF CFLAG:예ＮＴＲ진행도 == 0
					WSTR:(K++) =
				;2번째（리치）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 1
					WSTR:(K++) =
				;3번째（함락）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 2
					WSTR:(K++) =
				ENDIF
			ENDIF
			CFLAG:290 = 1
		ENDIF
		;-------------------------------------------------
		;;애(愛)계 재함락시
		;;주인에게 연모계 함락되었거나, 한번 애(愛)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 48
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
		
		IF (CFLAG:291 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				WSTR:(K++) =
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:291 = 1
		ENDIF
		;-------------------------------------------------
		;;음(淫)계 재함락시
		;;주인에게 음란계 함락되었거나, 한번 음(淫)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 49
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
					
		IF (CFLAG:292 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				WSTR:(K++) =
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:292 = 1
		ENDIF
		;-------------------------------------------------
		;;예(隷)계 재함락시
		;;주인에게 복종계 함락되었거나, 한번 예(隷)계 ＮＴＲ상태가 된 캐릭터가 다시 함락당할 때
		;-------------------------------------------------
	CASE 50
		K_CALL:K = EVENT_MESSAGE
		;재함락 전에 함락되어있던 캐릭터 → CFLAG:함락캐릭터 로 취득가능, 이름은 %NTRNAME(CFLAG:함락캐릭터)%
		;재함락 대상 캐릭터 → FAVORITE_1_ID(TARGET)　로 취득가능, 이름은 %NTRNAME(FAVORITE_1_ID(TARGET))%
		;다소 번잡해지므로 LOCAL, 혹은 #DIM 등을 사용해 알아보기 쉽게 하는 것을 권장합니다
					
		IF (CFLAG:293 < 1 || FLAG:7 == 2)
			;주인에게 다시 함락된다
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				WSTR:(K++) =
			;그 외 캐릭터・ＮＰＣ에게 다시 ＮＴＲ된다
			ELSE
				WSTR:(K++) =
			ENDIF
			CFLAG:293 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・난교파티
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 51
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:294 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:294 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・공원
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 52
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:295 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:295 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・변기
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 53
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・밤의 거리
		;;음ＮＴＲ상태（TALENT:ＮＴＲ == 2 나 TALENT:ＮＴＲ == 5）일 때만
		;-------------------------------------------------
	CASE 54
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;이전에 연모 루트였던 경우
				WSTR:(K++) =
			ELSEIF (TALENT:5 || TALENT:8)	
				;이전에 복종 루트였던 경우
				WSTR:(K++) = 
			ENDIF	
			;음란, 함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ전 밀회
		;;ＮＴＲ상태가 되기 전에 밀회할 때
		;-------------------------------------------------
	CASE 55
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:297 < 1 || FLAG:7 == 2)				
			IF CFLAG:함락캐릭터 != 0
			;	;주인 이외에 함락되어 있는 경우
				WSTR:(K++) = 
			ELSEIF (TALENT:3 || TALENT:6)	
			;	;연모 루트
				WSTR:(K++) = 
			ELSEIF (TALENT:4 || TALENT:7)	
			;	;음란 루트
				WSTR:(K++) = 
			ELSEIF (TALENT:5 || TALENT:8)	
			;	;복종 루트
				WSTR:(K++) = 
			ENDIF	
			;함락완료 공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:297 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ후 첫 밀회
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)가 된 직후 하루 종료 후, 한번만 행한다
		;다른 캐릭터에 함락될 때마다 일어난다. 같은 캐릭터에 다시 함락되는 경우는 일어나지 않는다
		;-------------------------------------------------
	CASE 56
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:298 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:298 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・데이트
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;-------------------------------------------------
	CASE 57
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:299 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:299 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・본방
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;여성이라면 삽입당하는 쪽, 남성이라면 삽입하는 쪽으로 고정
		;-------------------------------------------------
	CASE 58
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:601 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:601 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・자위서로과시
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;애ＮＴＲ상태인 경우, 노출증 3이나 자위중독 1 중 하나를 충족시키는 것이 조건이 됩니다.
		;-------------------------------------------------
	CASE 59
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・애널
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;CFLAG:애널횟수가 1 이상이면 삽입당하고, CFLAG:삽입횟수가 1 이상면 삽입하고 있습니다.
		;양 플래그 모두 서있는 경우, 지문에서는 삽입당하는 쪽이 우선됩니다.
		;-------------------------------------------------
	CASE 60
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외플레이
		;;애ＮＴＲ이나 예ＮＴＲ상태(TALENT:ＮＴＲ == 1, 3, 4, 6)에서 하루 종료시에 발생.
		;애ＮＴＲ의 경우, 노출증이 4 이상이 아니라면 발생하지 않습니다
		;-------------------------------------------------
	CASE 61
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;애ＮＴＲ
				WSTR:(K++) = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;예ＮＴＲ
				WSTR:(K++) = 
			ENDIF	
			;공통	
			IF !구상유무확인(0)	
				WSTR:(K++) =
			ENDIF	
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외조교
		;;예ＮＴＲ상태(TALENT:ＮＴＲ == 3, 6)에서 CFLAG:현ＮＴＲ진행도가 6이상인 경우 하루 종료시에 발생.
		;-------------------------------------------------
	CASE 62
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			;예ＮＴＲ
			WSTR:(K++) =
			CFLAG:605 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마와 섹스한다
		;당하는 쪽
		;PLAYER에 실행자의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 79
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마에게 봉사시킨다
		;봉사하는 쪽
		;PLAYER에 실행자의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 80
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：그 장소에서 자위
		;봉사하는 쪽
		;-------------------------------------------------
	CASE 81
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：그늘에서 자위
		;봉사하는 쪽
		;-------------------------------------------------
	CASE 82
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:605 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마와 섹스한다
		;하는 쪽
		;PLAYER에 상대의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 83
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:606 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:606 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：동료마에게 봉사시킨다
		;봉사받는 쪽
		;PLAYER에 봉사하는 쪽의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 84
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:607 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:607 = 1
		ENDIF
		;-------------------------------------------------
		;던전내 성욕처리：윤간이벤트
		;봉사하는 쪽
		;PLAYER에 실행자 = 윤간하는 악마의 번호가 들어있습니다
		;-------------------------------------------------
	CASE 85
		K_CALL:K = EVENT_MESSAGE
		IF (CFLAG:608 < 1 || FLAG:7 == 2)
			WSTR:(K++) =
			CFLAG:608 = 1
		ENDIF
		

		;-------------------------------------------------
		;교회에서 기도
		;-------------------------------------------------
	CASE 65
			K_CALL:K = EVENT_MESSAGE
			;교회의 종류에 따라 나눈다
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:609 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:609 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:609 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:610 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:610 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:610 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:611 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:611 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:611 = 1
					ENDIF
			ENDSELECT
		;-------------------------------------------------
		;교회에서 입봉사
		;-------------------------------------------------
	CASE 66
			K_CALL:K = EVENT_MESSAGE
			;교회의 종류에 따라 나눈다
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:612 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:612 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:612 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:613 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:613 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:613 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:614 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:614 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:614 = 1
					ENDIF
			ENDSELECT
		;-------------------------------------------------
		;교회에서 성봉사
		;-------------------------------------------------
	CASE 67
			K_CALL:K = EVENT_MESSAGE
			;교회의 종류에 따라 나눈다
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;메시아 교회
				CASE 13
					;처음
					IF CFLAG:615 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:615 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:615 = 1
					ENDIF
				;가이아 교회
				CASE 14
					;처음
					IF CFLAG:616 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:616 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:616 = 1
					ENDIF
				;교회
				CASE 15
					;처음
					IF CFLAG:617 == 0
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:617 = 1
					;그 이외
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;로우
						IF ABL:속성LC == 1
							WSTR:(K++) =
						;카오스
						ELSEIF ABL:속성LC == 3
							WSTR:(K++) =
						;뉴트럴
						ELSE
							WSTR:(K++) =
						ENDIF
						CFLAG:617 = 1
					ENDIF
			ENDSELECT
	CASEELSE
ENDSELECT

$表示
CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_EVENT_ZZZ
	CATCH
		CALL EVENT_MESSAGE
	ENDCATCH
ENDIF
;-------------------------------------------------
;투기장 패배시 구상 모음（X1을 캐릭터번호로 치환한다） 기본적으로 보면 게임오버기 때문에 플래그를 사용하지 않도록
;-------------------------------------------------
@KOJO_MESSAGE_LOSE_COLOSSEUM_K1621
CALL 구상변수초기화
SELECTCASE TFLAG:투기장패배
		;-------------------------------------------------
		;오니＆덩굴
		;-------------------------------------------------
	CASE 1
			K_CALL:K = MESSAGE_LOSE_COLOSSEUM
			;연모, 친애
			IF TALENT:3 || TALENT:6
				WSTR:(K++) = 
			;음란, 창부
			ELSEIF TALENT:4 || TALENT:7
				WSTR:(K++) = 
			;복종, 예속
			ELSEIF TALENT:5 || TALENT:8
				WSTR:(K++) = 
			ENDIF
			;그 외
			IF !구상유무확인(0)
				WSTR:(K++) = 
			ENDIF
ENDSELECT
CALL 구상변수표시
;구상이 없으면 다른 구상을 호출
IF RESULT == 0
	TRYCCALLFORM KOJO_MESSAGE_LOSE_COLOSSEUM_ZZZ
	CATCH
		CALL MESSAGE_LOSE_COLOSSEUM
	ENDCATCH
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_ENDING 관련(X1, X2를 캐릭터 번호로 치환한다)
;-------------------------------------------------
;-------------------------------------------------
;엔딩구상의 유무를 판별하는 함수
;기술한 엔딩의 종류에 따라 주석처리를 해제
;-------------------------------------------------
@ENDING_CHECK_K1621
SELECTCASE TSTR:END종별
	;CASE "개별END"
	;	SIF TALENT:아내 || TALENT:남편
	;		RETURN 1
	;	SIF TALENT:음마
	;		RETURN 1
	;	SIF TALENT:노리개
	;		RETURN 1
	;CASE "조교사경영END"
	;	RETURN 1
	;CASE "희대의조교사END"
	;	RETURN 1
	;CASE "식당경영END"
	;	RETURN 1
	;CASE "창관경영END"
	;	RETURN 1
	;CASE "아이돌END"
	;	RETURN 1
ENDSELECT
RETURN 0

;-------------------------------------------------
;엔딩 본문
;-------------------------------------------------
@KOJO_MESSAGE_ENDING_K1621
;--------------------------------------------------
CALL 구상변수초기화
SELECTCASE TSTR:END종별
	;CASE "개별END"
		;연모 1체 엔딩
		;IF TALENT:아내 || TALENT:남편 && CFLAG:21 == 1
			;DRAWLINE
			;WSTR:(K++) = …………
			;WSTR:(K++) = ………
			;WSTR:(K++) = ……
			;WSTR:(K++) = 

			;WSTR:(K++) = 
			;WSTR:(K++) = 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;음란 1체 엔딩
		;ELSEIF TALENT:음마 && CFLAG:21 == 1
			;DRAWLINE
			;WSTR:(K++) = …………
			;WSTR:(K++) = ………
			;WSTR:(K++) = ……
			;WSTR:(K++) = 

			;WSTR:(K++) = 
			;WSTR:(K++) = 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;복종 1체 엔딩
		;ELSEIF TALENT:노리개 && CFLAG:21 == 1
			;DRAWLINE
			;WSTR:(K++) = …………
			;WSTR:(K++) = ………
			;WSTR:(K++) = ……
			;WSTR:(K++) = 

			;WSTR:(K++) = 
			;WSTR:(K++) = 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;ENDIF
	;CASE "조교사경영END"
	;CASE "희대의조교사END"
	;CASE "식당경영END"
	;CASE "창관경영END"
	;CASE "아이돌END"
ENDSELECT
CALL 구상변수표시
;개별END의 경우 여기서 RETURN 1하면, 그 후 게임은 계속되지 않고 다음 주차로 넘어갈지 게임종료할지 선택하게 됩니다
RETURN 1


@ENEMY_BATTLE_EVENT_K1621, ARG, ARG:1, ARG:2
IF TFLAG:전투이벤트 == 1
;적에서의 데빌ＣＯ－ＯＰ 발동시의 대사
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 2
;적에서의 데밀ＣＯ－ＯＰ 참가시의 대사
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

@BATTLE_EVENT_K1621, ARG, ARG:1, ARG:2
;■대사의 행 수를 늘리고 싶을 때는
;			LOCALS:10 = 1행째
;			LOCALS:11 = 2행째
;			LOCALS:12 = 3행째
;			LOCALS:13 = 4행째
;			LOCALS:14 = 5행째
;로 늘려나간다
;SIF ARG < 0 && NO:ARG != X1
;	RETURN 0
;VARSET LOCALS

SELECTCASE TFLAG:전투이벤트
	;COOP발동 ARG는 발동자
	CASE 1
		LOCALS:10 = 
	;COOP참가
	CASE 2
		;주인이 COOP발동
		IF FLAG:행동순1 != -1 && FLAG:행동순1 == MASTER
			LOCALS:10 = 
		;그 이외
		ELSE
			LOCALS:10 = 
		ENDIF
	;피소환시
	CASE 3
			LOCALS:10 = 
	;공격　ARG가 공격하는 동료마, ARG:1이 적, ARG:2가 데미지량
	CASE 11
		CALLFORM SKILL_TYPE_{CFLAG:ARG:입력행동}
		LOCAL = MAXBASE:(ARG:1):GET_TYPE(RESULT)
		;상대가 죽었다
		IF BASE:(ARG:1):ＨＰ <= 0
			LOCALS:10 = 
		;약점을 찔렀다. 즉 COOP
		ELSEIF LOCAL > 100 && LOCAL < 999
			IF CFLAG:(ARG:1):방어플래그
				LOCALS:10 = 
			ELSE
				;이쪽은 COOP발동으로 돌리기 위해서 CUT
				RETURN 0
			ENDIF
		;대 데미지
		ELSEIF 데미지비율(ARG:1, ARG:2) > 50
			LOCALS:10 = 
		ELSEIF 데미지비율(ARG:1, ARG:2) > 25
			LOCALS:10 = 
		ELSEIF 데미지비율(ARG:1, ARG:2) > 0
			LOCALS:10 = 
		;무효되었다
		ELSEIF 데미지비율(ARG:1, ARG:2) <= 0
			LOCALS:10 = 
		ENDIF
	;피데미지
	;ARG가 공격을 받는 동료마, ARG:1이 적, ARG:2가 데미지량
	CASE 12
		;죽었다
		IF BASE:ARG:ＨＰ <= 0
			LOCALS:10 = 
		;대 데미지
		ELSEIF 데미지비율(ARG, ARG:2) > 50 || 현HP비율(ARG) < 25
			LOCALS:10 = 
		;무효
		ELSEIF ARG:2 == 0
			LOCALS:10 = 
		;흡수
		ELSEIF ARG:2 < 0
			LOCALS:10 = 
		ENDIF
	;반사당했다
	CASE 15
			LOCALS:10 = 
	;반사했다
	CASE 16
			LOCALS:10 = 
	;회피당했다
	CASE 17
			LOCALS:10 = 
	;회피했다
	CASE 18
			LOCALS:10 = 
	;반격스킬 발동했다
	CASE 20
			LOCALS:10 = 
	;배틀에서 승리했다
	CASE 90
			LOCALS:10 = 
	;레벨 업했다
	CASE 91
			LOCALS:10 = 
	;도주에 성공했다
	CASE 92
			LOCALS:10 = 
	;도주에 실패했다
	CASE 93
			LOCALS:10 = 
	;전투개시시
	CASE 94
		IF FLAG:선제공격플래그
			LOCALS:10 = 
		ELSEIF 현HP비율(ARG) < 25
			;빈사
			LOCALS:10 = 
		ELSE
			LOCALS:10 = 
		ENDIF
	;백어택을 당했다
	CASE 95
		IF 현HP비율(ARG) < 25
			;빈사
			LOCALS:10 = 
		ELSE
			LOCALS:10 = 
		ENDIF
	;적에게 선제공격을 당했다
	CASE 96
		IF 현HP비율(ARG) < 25
			;빈사
			LOCALS:10 = 
		ELSE
			LOCALS:10 = 
		ENDIF
ENDSELECT
SIF !(LOCALS:10 == "")
	CALL MESSAGE_B2(CALLNAME:ARG,0, LOCALS:10, LOCALS:11, LOCALS:12, LOCALS:13, LOCALS:14)


@BATTLE_MESSAGE_K1621,ARG,ARG:1 = 0
;■대사의 행 수를 늘리고 싶을 때는
;			LOCALS:10 = 1행째
;			LOCALS:11 = 2행째
;			LOCALS:12 = 3행째
;			LOCALS:13 = 4행째
;			LOCALS:14 = 5행째
;로 늘려나간다
VARSET LOCALS
;사용한 스킬의 이름을 LOCALS에 대입
CALLFORM SKILL_NAME_{CFLAG:ARG:입력행동},ARG
LOCALS = %RESULTS%
;FLAG:ARG:타겟

;붕괴된 경우에는 구상 표시하지 않도록 할 수 있다
;SIF TALENT:ARG:9
;	RETURN 0

CALLFORM SKILL_EFECT_{CFLAG:ARG:입력행동}
;주석처리하기 쉽도록
IF 1 == 0
;-------------------------------------------------
;상태이상시
;-------------------------------------------------
;매료시
ELSEIF CFLAG:ARG:상태이상 == GET_STATE_NUM("CHARM")
	;행동불능（헤롱헤롱 거리고 있다）
	IF CFLAG:ARG:입력행동 == 2314
		LOCALS:10 = 
	ELSE
		LOCALS:10 = 
	ENDIF
;혼란시
ELSEIF CFLAG:ARG:상태이상 == GET_STATE_NUM("PANIC")
	;금전뿌리기
	IF CFLAG:ARG:입력행동 == 2315
		LOCALS:10 = 
	;행동불능（멍하니 멈춰 있다）
	ELSEIF CFLAG:ARG:입력행동 == 2314
		LOCALS:10 = 
	ELSE
		LOCALS:10 = 
	ENDIF
;-------------------------------------------------
;특정스킬
;-------------------------------------------------
;이쪽은 통상공격
ELSEIF CFLAG:ARG:입력행동 == 0
	LOCALS:10 = 
;ELSEIF CFLAG:ARG:입력행동 == (스킬번호)
;	LOCALS:10 = 
;섹시댄스
ELSEIF CFLAG:ARG:입력행동 == 1212
	LOCALS:10 = 
;캔디보이스
ELSEIF CFLAG:ARG:입력행동 == 1213
	LOCALS:10 = 
;파이널누드
ELSEIF CFLAG:ARG:입력행동 == 1214
	LOCALS:10 = 
;-------------------------------------------------
;총공격（특수탄환 포함）
;-------------------------------------------------
ELSEIF RANGE(CFLAG:ARG:입력행동,2101, 2200)
	;GUN
	IF CFLAG:ARG:입력행동 == 2101
		LOCALS:10 = 
	;그 외（특수탄환）
	ELSE
		LOCALS:10 = 
	ENDIF
;-------------------------------------------------
;기본행동
;-------------------------------------------------
ELSEIF RANGE(CFLAG:ARG:입력행동,2301, 2400)
	;ＣＨＡＮＧＥ
	IF CFLAG:ARG:입력행동 == 2301
		LOCALS:10 = 
	ELSE
		LOCALS:10 = 
	ENDIF
;-------------------------------------------------
;아이템
;-------------------------------------------------
ELSEIF RANGE(CFLAG:ARG:입력행동,3001, 3500)
	LOCALS:10 = 
;-------------------------------------------------
;링케이지
;-------------------------------------------------
ELSEIF RANGE(CFLAG:ARG:입력행동,4000, 5000)
	;메시아라이저
	IF CFLAG:ARG:입력행동 == 4160
		LOCALS:10 = 
	;L히트라이저
	ELSEIF CFLAG:ARG:입력행동 == 4200
		LOCALS:10 = 
	;사망유희
	ELSEIF CFLAG:ARG:입력행동 == 4800
		LOCALS:10 = 
	ELSE
		LOCALS:10 = 
	ENDIF
;-------------------------------------------------
;범용스킬
;-------------------------------------------------
;검격
ELSEIF RANGE(CFLAG:ARG:입력행동,1, 99)
	LOCALS:10 = 
;비구
ELSEIF RANGE(CFLAG:ARG:입력행동,101, 199)
	LOCALS:10 = 
;타격
ELSEIF RANGE(CFLAG:ARG:입력행동,201, 299)
	LOCALS:10 = 
;전술
ELSEIF RANGE(CFLAG:ARG:입력행동,301, 400)
	;물어뜯기계
	IF RANGE(CFLAG:ARG:입력행동,321, 326)
		LOCALS:10 = 
	;그 외
	ELSE
		LOCALS:10 = 
	ENDIF
;회복
ELSEIF RANGE(CFLAG:ARG:입력행동,401, 499)
	;전체회복
	IF CFLAG:ARG:타겟 > 6
		LOCALS:10 = 
	ELSE
		LOCALS:10 = 
	ENDIF
;지원
ELSEIF RANGE(CFLAG:ARG:입력행동,501, 599)
	;강화계
	IF RANGE(CFLAG:ARG:입력행동,501, 505) || CFLAG:ARG:입력행동 == 558
		LOCALS:10 = 
	;약체계
	ELSEIF RANGE(CFLAG:ARG:입력행동,506, 509) || CFLAG:ARG:입력행동 == 541 || CFLAG:ARG:입력행동 == 556
		LOCALS:10 = 
	;도발
	ELSEIF CFLAG:ARG:입력행동 == 515
		LOCALS:10 = 
	;그 외
	ELSE
		LOCALS:10 = 
	ENDIF
;방어
ELSEIF RANGE(CFLAG:ARG:입력행동,601, 699)
	LOCALS:10 = 
;화염
ELSEIF RANGE(CFLAG:ARG:입력행동,701, 799)
	LOCALS:10 = 
;빙결
ELSEIF RANGE(CFLAG:ARG:입력행동,801, 899)
	LOCALS:10 = 
;전격
ELSEIF RANGE(CFLAG:ARG:입력행동,901, 999)
	LOCALS:10 = 
;충격
ELSEIF RANGE(CFLAG:ARG:입력행동,1001, 1099)
	LOCALS:10 = 
;신경
ELSEIF RANGE(CFLAG:ARG:입력행동,1101, 1199)
	LOCALS:10 = 
;정신
ELSEIF RANGE(CFLAG:ARG:입력행동,1201, 1299)
	LOCALS:10 = 
;파마
ELSEIF RANGE(CFLAG:ARG:입력행동,1301, 1399)
	LOCALS:10 = 
;주살
ELSEIF RANGE(CFLAG:ARG:입력행동,1401, 1499)
	LOCALS:10 = 
;지변
ELSEIF RANGE(CFLAG:ARG:입력행동,1501, 1599)
	LOCALS:10 = 
;수격
ELSEIF RANGE(CFLAG:ARG:입력행동,1601, 1699)
	LOCALS:10 = 
;질풍
ELSEIF RANGE(CFLAG:ARG:입력행동,1701, 1799)
	LOCALS:10 = 
;중력
ELSEIF RANGE(CFLAG:ARG:입력행동,1801, 1899)
	LOCALS:10 = 
;핵열
ELSEIF RANGE(CFLAG:ARG:입력행동,1901, 1999)
	LOCALS:10 = 
;만능
ELSEIF RANGE(CFLAG:ARG:입력행동,2001, 2099)
	;자폭계
	IF RANGE(CFLAG:ARG:입력행동,2005, 2007)
		LOCALS:10 = 
	ELSE
		LOCALS:10 = 
	ENDIF
;마법소녀계
ELSEIF RANGE(CFLAG:ARG:입력행동,2989, 2999)
	LOCALS:10 = 
;-------------------------------------------------
;그 외
;-------------------------------------------------
ELSE
	LOCALS:10 = 
ENDIF
;-------------------------------------------------
;진・그 외
;-------------------------------------------------
IF LOCALS:10 == ""
	;반드시 무언가 말한다
	LOCALS:10 = 
ENDIF

SIF !(LOCALS:10 == "")
	CALL MESSAGE_B2(CALLNAME:ARG,1, LOCALS:10, LOCALS:11, LOCALS:12, LOCALS:13, LOCALS:14)


;(ネタバレ？)
;本来、ダゴンさんは邪神的な立ち位置。信仰が薄れた今では邪神っていうより悪魔のダゴンさんの側面が強い。
;そんな中でマスター(@)と契約したがために自我が(ほぼ)消失。
;だけども契約が深まれば邪神としての自我を取り戻す。主にしゃべり方が変化する。
