@BATTLE_MESSAGE_K4106, ARG,ARG:1 = 0
LOCAL:1 = 1
LOCAL = CFLAG:ARG:입력행동
LOCALS = では行くぞ

;崩壊してる場合は口上表示しない
SIF TALENT:ARG:9
	RETURN 0


CALLFORM SKILL_EFECT_{LOCAL}
IF RESULT == 1 && RAND:3 == 0
	CALLFORM SKILL_TYPE_{CFLAG:ARG:입력행동}
	SELECTCASE RESULT
		;검격
		CASE 0
			IF RAND:2
				LOCALS = 裂けよ
			ELSE
				LOCALS = 綺麗な花にしてやろうぞ、飾り切りでな
			ENDIF
		;비구
		CASE 1
			IF RAND:2
				LOCALS = ほれほれ、避けぬと穴だらけじゃぞ
			ELSE
				LOCALS = 『塵モ残サヌ…』
			ENDIF
		;타격
		CASE 2
			IF RAND:2
				LOCALS = 少しは耐えておくれ…血溜まりは後が面倒だからのう
			ELSE
				LOCALS = 『挽キ肉…イヤ、搾リ汁二シテヤル』
			ENDIF
		;전술
		CASE 3
			IF RAND:2
				LOCALS = さぁ遊舞に付き合っておくれ…バラバラにならぬように耐えてみせよ…
			ELSE
				LOCALS = 『力押シノミガ魔人ノ本領デハ無イ』
			ENDIF
		;화염
		CASE 4
			IF RAND:2
				LOCALS = こんがりと焼いてやろう…
			ELSE
				LOCALS = では、燃え尽きよ
			ENDIF
		;빙결
		CASE 5
			IF RAND:2
				LOCALS = 氷の彫像というのも乙なものじゃのう…
			ELSE
				LOCALS = 凍りつかせてくれよう…意外と心地よいかもしれぬぞ
			ENDIF
		;전격
		CASE 6
			IF RAND:2
				LOCALS = 痛みによって痺れる心地を味わうがいい…
			ELSE
				LOCALS = 痺れ果ててそこでだらしのない姿を晒すがいい
			ENDIF
		;충격
		CASE 7
			IF RAND:2
				LOCALS = ホッホッホ…破裂する気分はどうじゃ
			ELSE
				LOCALS = さぁ逃げ惑え
			ENDIF
		;신경
		CASE 8	
			IF RAND:2
				LOCALS = さて、どのように嬲ってくれようか…
			ELSE
				LOCALS = 嫌だ嫌だと言っても神経には効くからのう
			ENDIF
		;정신
		CASE 9
			IF RAND:2
				LOCALS = ふん…なんと脆いこころじゃ…
			ELSE
				LOCALS = 矮小じゃのう…砕けてしまえ
			ENDIF
		;파마
		CASE 10
			IF RAND:2
				LOCALS = 大淫婦の浄化で天に登るか、好き者め
			ELSE
				LOCALS = 文字通り昇天させてやろうぞ
			ENDIF
		;주살
		CASE 11
			LOCALS = 奈落へ落ちよ、呪いは魂につけてやる
		;지변
		CASE 12
			IF RAND:2
				LOCALS = 土砂の味はどうじゃ
			ELSE
				LOCALS = 土塊に身を裂かれる気分はどうじゃ…
			ENDIF
		;수격
		CASE 13
			IF RAND:2
				LOCALS = 冷水を浴びせてやろう…目を開けられぬ程ののう
			ELSE
				LOCALS = こういう使い方もあるのでのう…
			ENDIF
		;질풍
		CASE 14
				LOCALS = 木っ端のごとく四裂するがいい…
		;중력
		CASE 15
			IF RAND:2
				LOCALS = さぁ圧縮してやろう…肉の一片までもな
			ELSE
				LOCALS = 叩き堕ちよ、重力の鎖は万有だからのう
			ENDIF
		;핵열
		CASE 16
			IF RAND:2
				LOCALS = 溶かしてやろう、其の身までな
			ELSE
				LOCALS = 蒸発して泡も残さぬようにしてやろう…
			ENDIF
		;만능
		CASE 17
			LOCALS = ホッホッホ…喰らうがいい…！
		;サイコ
		;CASE 18
		CASEELSE
			LOCALS = 狂乱の音の色はいつ聞いても甘美じゃのう
	ENDSELECT
;회복
ELSEIF RESULT == 2
	IF CFLAG:ARG:타겟 > 6
		IF RAND:2
			LOCALS = 大淫婦の癒やしじゃ、願い下げかのう？
		ELSE
			LOCALS = ほぅら少しか楽にはなったじゃろう？
		ENDIF
	ELSE
		IF FLAG:(CFLAG:ARG:타겟) == 0 && 함락(ARG)
			IF RAND:2
				LOCALS = 魔人を落としておいて先に逝くなど許さぬぞ。
			ELSE
				LOCALS = 全く…肝が冷える…これが終わったら温めておくれ
			ENDIF
		ELSE
			IF RAND:2
				LOCALS = さっさと立たぬか
			ELSE
				LOCALS = ほれ仕切りなおしじゃ。立て直せ
			ENDIF
		ENDIF
	ENDIF
;状態変化
;ELSEIF RESULT == 3
;特殊
;ELSEIF RESULT == 4
;状態回復
ELSEIF RESULT == 5
	LOCALS = ほれ、しゃんと立て。
ENDIF


;千年の呪怨
IF LOCAL == 1414
	SELECTCASE RAND:3
	CASE 0
		LOCALS = 魔人の身に満ちた死の呪詛を少しは喰ろうてみるかえ？
	CASE 1
		LOCALS = ホッホッホ…呪怨によって朽ち果てよ
	CASEELSE
		LOCALS = 呪詛とともに消え堕ちよ塵芥
	ENDSELECT
;魔界の調べ
ELSEIF LOCAL == 1132
	SELECTCASE RAND:3
	CASE 0
		LOCALS = そら、聞き惚れるがいい
	CASE 1
		LOCALS = 魔界の音の調べに容赦の二文字は存在せぬぞ
	CASEELSE
		LOCALS = さぁ、音に酔いしれよ
	ENDSELECT
;放電
ELSEIF LOCAL == 910
	SELECTCASE RAND:3
	CASE 0
		LOCALS = 痺れて果てよ…
	CASE 1
		LOCALS = 脳髄まで痺れて酔いしれよ…
	CASEELSE
		LOCALS = さぁ、電撃にて逝ってしまえ…！
	ENDSELECT
;汚れに満ちた金の杯
ELSEIF LOCAL == 2068
	;연모, 친애
	IF TALENT:3 || TALENT:6
		SELECTCASE RAND:3
		CASE 0
			LOCALS = ふふ…純然成る愛のために、削がれた汚れを受けるがいい！
		CASE 1
			LOCALS = わらわの大元の汚れじゃ。飲み下せると思うでないぞ…！
		CASEELSE
			LOCALS = 古き汚れがなんじらにはお似合いじゃ。
		ENDSELECT
	;음란, 창부
	ELSEIF TALENT:4 || TALENT:7
		SELECTCASE RAND:3
		CASE 0
			LOCALS = アハ、アハハハハ、溶け落ちよ我が汚れによって！
		CASE 1
			LOCALS = さぁ、汚れの中で淫蕩の限りを尽くそうぞ
		CASEELSE
			LOCALS = アハハハ、世の理に満ちよ汚れと快楽よ！
		ENDSELECT
	;복종, 예속
	ELSEIF TALENT:5 || TALENT:8
		SELECTCASE RAND:3
		CASE 0
			LOCALS = わらわの中身を曝け出せ、杯よ
		CASE 1
			LOCALS = 主の命だ、溶け落ちよ
		CASEELSE
			LOCALS = 塵芥が、飲まれるがいい
		ENDSELECT
	;그 이외
	ELSE
		LOCALS = ホッホッホ喰らうがいい、我が汚れを！
	ENDIF
;デクンダ
ELSEIF LOCAL == 505
	LOCALS = 口直しをせねばな。
;スクンダ
ELSEIF LOCAL == 507
	LOCALS = その速度、目障りじゃ
;デカジャ
ELSEIF LOCAL == 509
	LOCALS = 仕切りなおしじゃ…フェアに行かねばな
;気合
ELSEIF LOCAL == 511
	LOCALS = さぁて…いくかのう…！
;コンセントレイト
ELSEIF LOCAL == 512
	LOCALS = さぁ集中じゃ…
;雄たけび
ELSEIF LOCAL == 514
	LOCALS = 『グルゥゥォォォォォオオオオオオオオ！！！！』
;도발
ELSEIF LOCAL == 515
	LOCALS = つまらぬ…つまらん…児戯にもならぬぞ愚図が

;食いしばり
ELSEIF LOCAL == 2406
	LOCALS = 魔人を舐めるなよ…
;不屈の闘志
ELSEIF LOCAL == 2407
	LOCALS = この程度で…朽ちてたまるものか…！

ENDIF

SIF LOCALS != "EMPTY"
	CALL MESSAGE_B(LOCAL:1, LOCALS, LOCALS:1, LOCALS:2, LOCALS:3, LOCALS:4)

@BATTLE_EVENT_K4106, ARG, ARG:1, ARG:2
SIF ARG < 0 && NO:ARG != 4518
	RETURN 0
LOCAL:1 = 1
LOCALS = EMPTY
SELECTCASE TFLAG:전투이벤트
	;COOP발동 ARG는 발동자
	CASE 1
		IF RAND:3 == 0
			LOCALS = さて、一気に片付けるかのう
		ELSEIF !RAND:2
			LOCALS = 機はここじゃ、此処で行く
		ELSE
			LOCALS = 隙を見せたな…！
		ENDIF
	CASE 2
		IF RAND:3 == 0
			LOCALS = 重ねあわせよう、逃れると思うな
		ELSEIF !RAND:2
			LOCALS = ここじゃな…！
		ELSE
			LOCALS = さぁ、行くぞ…！
		ENDIF
	;공격　ARG가 공격하는 동료마, ARG:1이 적, ARG:2가 데미지량
	CASE 11
		CALLFORM SKILL_TYPE_{CFLAG:ARG:입력행동}
		LOCAL = MAXBASE:(ARG:1):GET_TYPE(RESULT)
		;상대가 죽었다
		IF BASE:(ARG:1):ＨＰ <= 0
			IF 함락(ARG) && RAND:2
				LOCALS = さらばじゃ、邪魔者
			ELSE
				IF RAND:2
					LOCALS = さぁ、これが死という極上の快楽じゃ
				ELSE
					LOCALS = 大淫婦の腕に抱かれて昇天するがいい
				ENDIF
			ENDIF
		;약점을 찔렀다. 즉 COOP
		ELSEIF LOCAL > 100 && LOCAL < 999
			IF CFLAG:(ARG:1):방어플래그
				LOCALS = 硬いのう…
			ELSE
				;이쪽은 COOP발동으로 돌리기 위해서 CUT
				RETURN 0
			ENDIF
		;대 데미지
		ELSEIF 데미지비율(ARG:1, ARG:2) > 50
			IF RAND:2
				LOCALS = さぁあとどれだけ耐えられるかのう…
			ELSE
				LOCALS = さぁ、もう逝ってしまえ、逝って楽になるがいい
			ENDIF
		ELSEIF 데미지비율(ARG:1, ARG:2) > 20
			IF RAND:2
				LOCALS = ほら、ほらまだまだ続けるぞ
			ELSE
				LOCALS = まだまだ狂宴は始まったばかりじゃ
			ENDIF
		ELSEIF 데미지비율(ARG:1, ARG:2) > 0
			IF RAND:2
				LOCALS = さぁまだまだ終わらぬぞ
			ELSE
				LOCALS = 心地よくしてやろう…そんなに硬くなるな
			ENDIF
		;무효化された
		ELSEIF ARG:2 == 0
			IF 함락(ARG) && RAND:2
				LOCALS = …利かぬか
			ELSE
				LOCALS = 魔人の一撃を意にも介さんか…
			ENDIF
		;흡수された
		ELSEIF ARG:2 < 0
			IF RAND:2
				LOCALS = ほう…吸収するか
			ELSE
				LOCALS = むぅ…吸い取るか…
			ENDIF
		ENDIF
	;피데미지
	;ARG가 공격을 받는 동료마, ARG:1이 적, ARG:2가 데미지량
	CASE 12
		;죽었다
		IF BASE:ARG:ＨＰ <= 0
			IF 함락(ARG) && RAND:2
				LOCALS = 許せ…
			ELSE
				IF RAND:2
					LOCALS = な…に…？
				ELSE
					LOCALS = ホッホッホ…よく、やる…のう…
				ENDIF
			ENDIF
		;대 데미지
		ELSEIF 데미지비율(ARG, ARG:2) > 50 || 현HP비율(ARG) < 25
			IF 함락(ARG) && RAND:2
				LOCALS = この程度で魔人を殺しきれると思い上がるでないぞ…！
			ELSE
				IF RAND:2
					LOCALS = …思ったよりもやるのう
				ELSE
					LOCALS = 存外やるのう…さぁ、もっと楽しませておくれ…！
				ENDIF
			ENDIF
		ELSEIF 데미지비율(ARG, ARG:2) > 20
			IF RAND:2
				LOCALS = 仔細なし。余裕じゃ
			ELSE
				LOCALS = …大したものでもないのう
			ENDIF
		ELSEIF 데미지비율(ARG, ARG:2) > 0
			IF RAND:2
				LOCALS = 蚊ほども利かぬな。舐めているのかえ？
			ELSE
				LOCALS = …この程度かえ？
			ENDIF
		;무효
		ELSEIF ARG:2 == 0
			IF RAND:2
				LOCALS = 今何かしたかの…？
			ELSE
				LOCALS = 魔人を舐めるなよ、つまらぬ事で時間を使わせるな
			ENDIF
		;흡수
		ELSEIF ARG:2 < 0
			IF RAND:2
				LOCALS = ホッホッホ…心地よいぞ
			ELSE
				LOCALS = これはとんだ贈り物だ…ありがたく頂くことにしよう
			ENDIF
		ENDIF
	CASE 15
		;被反射
		PRINTFORMW ARG:2 = {ARG:2}
		IF RAND:2
			LOCALS = …弾くか
		ELSE
			LOCALS = ふむ、これを弾くか
		ENDIF
	CASE 16
		;反射
		IF RAND:2
			LOCALS = ほれ、返してやろう
		ELSE
			LOCALS = 相手を見定めて放つことだ。さもなければ…ほら、こうなる。
		ENDIF
	CASE 17
		;敵回避
		IF RAND:2
			LOCALS = 避けたか…
		ELSE
			LOCALS = 避けたな…つれないのう
		ENDIF
	CASE 18
		;自分回避
		IF RAND:2
			LOCALS = なんじゃ今のは
		ELSE
			LOCALS = 今何かしたかのう？
		ENDIF
ENDSELECT
SIF LOCALS != "EMPTY"
	CALL MESSAGE_B(LOCAL:1, LOCALS, LOCALS:1, LOCALS:2, LOCALS:3, LOCALS:4)
